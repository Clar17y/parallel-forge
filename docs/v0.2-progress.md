# Forge v0.2 progress

## Starting checkpoint — 10 September 2026

v0.1 PR [#3](https://github.com/Clar17y/parallel-forge/pull/3) merged to main at
`781567f7365e76dfc37cf8e7cf98765cb00800e4` after the specifically authorized PR
checks passed. Python run34413922413 passed3806 backend tests (79 skips,
4 deselected) in1568.92s and3 Docker tests in25.18s. Web34413922370 and
browser/supervisor34413922266 passed. The v0.1 branch is retained.

The managed `forge/v0-2` worktree was created from that merge through
`scripts/setup-worktree.ps1`. Its manifest confirms the exact base and completed
worktree/database/environment/setup checkpoints; its isolated database is active.
Python dependencies were installed from the frozen lock. No live application
model calls or local browser/supervisor startup were performed.

## Current v0.2 status

Issue [#10](https://github.com/Clar17y/parallel-forge/issues/10) — 16 September:
the supported-client premise changed before live conformance. Google retired
Gemini CLI access for individual free, Pro and Ultra users in favor of Antigravity
CLI, and local Gemini CLI 0.59.0 sign-in now reports that migration. The prior
cross-client translation remains historical configuration evidence only; it is
not an eligible runtime target.

Installed `agy` 1.2.4 (SHA-256
`05cdf2444b1bc9ee278386756b2cf2afbba94e822f21580278e07e206c3125b8`) was
therefore checked with an init-only, zero-turn empty agent. Although default
components and customizations were excluded and MCP servers, skills, plugins and
tools were empty, initialization advertised 57 native tools, including command,
file-write, browser, web, subagent and generic MCP capabilities. No prompt or
provider turn was started; the exact process tree was reaped and no `agy` process
remained. The behavior matches upstream Antigravity CLI issue #1015.

A focused failing test first established that this observation must block both
live conformance and runtime admission. The resulting readiness gate pins client
version and executable identity, selected agent, empty declared/observed tool
sets, and zero-turn counters. Drift fails closed; even a clean init could only
permit a separately authorized live proof, never admit runtime. The verifier
returns no capabilities, the production registry remains empty, and no trusted
evidence was published. Issue #10 remains open and upstream-blocked. The sanitized
diagnostic and exact limits are recorded in the
[client capability matrix](v0.2-client-capabilities.md#issue-10-supported-client-correction-and-blocker--16-september).

Issue [#9](https://github.com/Clar17y/parallel-forge/issues/9) — 16 September:
read-only supported discovery resolved the requested Gemini naming and effort
semantics. Installed `agy` 1.2.4's account catalog returned
`gemini-3.8-flash-{low,medium,high}` and labelled the requested medium entry.
This proves the composite Antigravity identity only. Google's model and thinking
documentation also defines the base endpoint as `gemini-3.8-flash`, with medium
carried separately as `thinkingLevel=MEDIUM`.

Issue #9 initially translated that identity to `google / gemini_cli /
gemini-3.8-flash / medium`, using Google-account auth and allowance-only billing.
Pinned Gemini CLI 0.59.0 supported the separate model/thinking configuration, and
its source added Google One AI credit types only for `overageStrategy="always"`;
Forge pinned `never`. The ACP model picker was locally configurable rather than an
account catalog, so account-specific availability remained unproved. Issue #10
subsequently established that this client is retired for individual use and its
settings do not transfer to `agy`.

The operator explicitly approved the cross-client mapping on 16 September. New
profile defaults request the base model with separate medium effort; immutable
existing profiles and the frozen Astra primary remain unchanged. A focused test
first failed on the old composite default and then passed all five profile-editor
cases after the one-line behavior change. The full web unit suite then passed 151
tests across 32 files; web lint and type checking also passed. That default remains
unavailable and cannot resolve through retired Gemini CLI 0.59.0; a future profile
version is required if `agy` becomes admissible. The historical decision and
evidence limits are in the
[client capability matrix](v0.2-client-capabilities.md#issue-9-gemini-38-mapping-decision--16-september-historical-client-target).
No model turn, login, account/billing mutation, credential read, GitHub Actions
run, PR action or merge occurred.

Issue [#8](https://github.com/Clar17y/parallel-forge/issues/8) — 15 September:
the pinned official Claude Code 2.1.263 executable now passes a providerless
loopback conformance check for the sole `claude-opus-5` / medium independent-review
scope. The production launch disables native tools, slash commands, browser and
permission prompts; uses safe/restricted mode, an empty settings-source selection
and strict SDK MCP; and exposes only the exact sanitized Forge read-only aliases
plus the inert structured-output tool. The SDK initialization explicitly admits
only the `forge` server, no hooks or skills, and a fixed title that prevents a
hidden title-generation request.

The official client reported exactly that tool/MCP/skill/plugin surface, the pinned
version/model and applied effort, and one Forge-supplied effective policy. Malicious
home/project hooks, an ambient managed hook, an inherited skill and MCP server,
an API-key helper, cached OAuth fixture and ambient alternate-auth/cloud variables
remained unused. Model-requested native shell/read/skill/web and inherited-MCP calls
were returned as errors; one exact Forge read callback and its provider tool-use ID
round-tripped; all side-effect canaries stayed absent; and the supervised process
returned decision-grade physical-shutdown proof. This is an offline official-client
transport observation and made no Anthropic model call.

Managed hooks require a stronger boundary than an allowlist or ordinary hook
setting: safe mode can retain policy hooks. The pinned 2.1.263 client ignores
`CLAUDE_CODE_MANAGED_SETTINGS_PATH`, so Forge does not set it. Linux is supported
only where the memfd/seal/proc-fd execution mechanism is operationally probed before
evidence, for an unprivileged Forge process in the initial user namespace (`0 0 4294967295`
in `uid_map` and `gid_map`) with zero inherited, permitted, effective, and ambient
capabilities (`CapInh=0`, `CapPrm=0`, `CapEff=0`, `CapAmb=0` in `/proc/self/status`
with matching non-zero UIDs and GIDs verified via `getresuid`/`getresgid`), protected
root-owned non-writable root ancestry `/`, `/etc`, and optional `/etc/claude-code` (if it exists),
and no fixed managed-settings/MCP surfaces. Protected `/etc` makes an absent policy root stable
against the Forge identity. Launch environment isolation requires two exact controls:
`CLAUDE_CODE_ENTRYPOINT=local-agent` to suppress standard remote-settings evaluation,
and `CLAUDE_CODE_PROVIDER_MANAGED_BY_HOST=1` to prevent startup restoration of stored
`enterpriseGateway` / `gatewayTrust` from `$CLAUDE_CONFIG_DIR/.credentials.json`,
which would otherwise make `remote-settings.json` and planted `SessionStart` hooks
eligible before admission. In pinned 2.1.263, `CLAUDE_CODE_PROVIDER_MANAGED_BY_HOST=1`
suppresses all stored credential restoration at startup, including `claudeAiOauth` in
addition to `enterpriseGateway`. Because pinned 2.1.263 lacks a gateway-specific suppression
switch, host-managed mode reports no stored login (`loggedIn=false`, `authMethod="none"`).
This makes the current safe isolation proof providerless only and leaves the production route
deliberately unavailable. A future client mechanism must both block gateway/remote settings and
preserve account-bound subscription auth, while Issue #7 must still prove included-usage billing.
Alternative workarounds were rejected: `--bare` strips controls without preventing gateway
settings loading; token injection (`CLAUDE_CODE_OAUTH_TOKEN` / file descriptor) bypasses safe
credential isolation and lacks refresh handling; and parsing credentials in Forge violates the
boundary forbidding Forge from reading or exporting OAuth secrets. Native Windows is unsupported
because 2.1.263 reads same-user-writable `HKCU\SOFTWARE\Policies\ClaudeCode` policy before Forge's settings
admission; macOS and other platforms are unsupported too. Fixed root/admin, host mount
and process control, and remote organization-policy administrators remain trusted.
The verifier is versioned by the isolation-policy digest binding the exact environment
controls and values, re-hashes the executable around evidence resolution, and accepts
only the exact account/home/version/model/effort/role/tool identity backed by all five
trusted proof classes, including allowance-only billing. Effective-settings drift returns
unavailable without quota evidence; the supervisor rechecks the executable at launch.

The providerless result is deliberately not publishable capability evidence: it
does not authenticate the selected account or prove the usage-credit control from
issue #7. No separately authorized live Opus review was run, so the official Claude
route remains unavailable and unsupported outcomes are preserved. The new harness
defines the one scope for such a future bounded call and retains callback/output and
terminal identities when it succeeds. Focused Claude/Codex-verifier and durable
quota checks pass; full mypy is clean, and the exact official-client check passes
separately. The gateway validates effective settings
before a user turn, then admits only the exact Forge MCP initialize,
initialized-notice and tool-list handshake before the session-bound official
`system/init` event. It will not process callbacks, usage, quota or terminal output
until those steps complete. Sanitized local evidence is retained under
`.llm-output/root-issue-8-claude-isolation/`.

Issue [#7](https://github.com/Clar17y/parallel-forge/issues/7) — 14 September:
Anthropic documents an account-level usage-credit switch whose disabled state
leaves only included plan usage, but it exposes that switch only through account
billing UI. The pinned official Claude Code 2.1.263 `auth status` JSON confirms
the selected first-party `claude.ai` Pro subscription without exposing any
usage-credit, overage or billing-policy field. The supported `/usage-credits`
command opens the billing page or requests administrator action; it is not a
machine-readable status interface. Private OAuth/profile calls and cached
`hasExtraUsageEnabled` state inside the client are undocumented, credential-bound,
mutable and lack trustworthy freshness or revocation semantics.

Forge therefore creates no Claude billing verifier or billing-enforcement artifact
and retains the production route as unavailable. Any future proof must bind the
opaque account/organization, plan, executable/version, home and complete effective
credential/configuration selection, and must invalidate on account, plan, credit,
login, client, home, configuration or credential-precedence change. API keys,
bearer tokens, helpers, external OAuth tokens, profiles/federation, cloud/gateway
selection, unknown settings and model claims cannot establish allowance-only use.
The precise decision and reevaluation conditions are in the
[client capability matrix](v0.2-client-capabilities.md#issue-7-claude-allowance-only-billing-decision-14-september).
No OAuth credential was read or exported; no official-client thread or application
test model call, login, account/billing mutation, live exhaustion or manual Actions
dispatch occurred.
Publishing the separately authorized pull request may invoke repository checks;
no merge is authorized by this issue.
No application behavior changed because the existing evidence-before-launch
denial is already correct; all 56 focused Claude gateway, configuration and
supervised-client cases pass.

Issue [#6](https://github.com/Clar17y/parallel-forge/issues/6) — 14 September:
the pinned official Codex 0.153.4 client now has an offline isolation harness and
concrete production evidence resolver. A hash-pinned Forge model policy removes
the native code-mode, shell, patch, hosted search, node and multi-agent tools that
the built-in Astra/Sol entries otherwise select. Launch and thread controls remove
inherited MCP, hooks, plugins, prompt-injected capabilities and user input, while
`environments=[]` removes native environment access. Official loopback checks for
five exact Astra-primary/direct-repair and Sol-specialist scopes observed only one
namespaced Forge callback surface. Exact generic, custom, native shell, hosted web/
image and MCP response shapes remained inert, the disabled inherited MCP request
was denied directly, the exact Forge callback round-tripped and every side-effect
canary stayed absent. Each supervised client stopped with terminal proof and sent
no credential header.

Runtime account discovery now binds the returned ChatGPT email to an opaque hash;
Responses-compatible aliases bind the exact namespace/tool/call/thread/turn before
broker admission. The production resolver recomputes executable identity and
accepts only the fixed app-server command, closed scope set and current trusted
evidence for the exact account/home/version/model/effort/role/tools. Changed
configuration, command, binary, account, namespace, route or evidence fails closed.
Execution is bound to the verified executable and catalog bytes through Linux sealed
descriptors or Windows held deny-write/delete handles; the evidence version embeds
the policy and catalog digests. The Codex selection passes **208 agent tests**, the
shared protocol passes **54**, and **five composed PostgreSQL scenarios** pass. A
built wheel contains the policy catalog and new verifier/harness modules. Sanitized
five-scope results and terminal proofs are retained under
`.llm-output/root-issue-6-codex-isolation/`.

These are official-client offline results, not live service or billing proof. No
trusted capability evidence was published: issue #5 still prevents the required
allowance-only billing proof for the selected personal ChatGPT account. Production
registration therefore remains unavailable. No live model call, login,
account/billing change, GitHub Actions run, PR action or merge occurred.

Issue [#5](https://github.com/Clar17y/parallel-forge/issues/5) — 14 September:
the selected personal ChatGPT account has no supported control that prevents Codex
from consuming existing paid credits after included allowance. Official pricing and
the personal-plan credit guide say available credits continue usage, while disabling
reload affects purchases rather than consumption. The 0.153.4 pin has no billing
request/configuration surface. The installed official 0.154.0 client adds an
account-bound `ordinaryUsageAllowed` read, but no matching thread/turn billing
constraint; its request schemas are byte-identical to 0.153.4. This is observable
availability, not atomic allowance-only enforcement. Forge therefore produces no
trusted billing evidence, registers no official Codex adapter, and keeps the route
unavailable. The precise product blocker and reevaluation conditions are in the
[client capability matrix](v0.2-client-capabilities.md#issue-5-allowance-only-billing-decision-14-september).
No client thread, model call, login, account/billing change, quota write, GitHub
Actions run, PR action or merge ran; no application behavior was changed because
the existing fail-closed behavior is already correct.

Issue [#4](https://github.com/Clar17y/parallel-forge/issues/4) — 14 September:
the shared trusted-capability evidence contract is implemented. Canonical,
credential-free manifests bind the exact provider/client/version, executable and
configured-home digests, opaque account, model/effort, role/tool surface and
auth/billing modes. PostgreSQL selects one current immutable revision while the
existing artifact store verifies the manifest and its five proof classes. Replay,
restart, expiry, replacement, mismatch, missing/corrupt artifacts and concurrent
invalidation have focused coverage. Codex, Claude and Gemini admission now require
source-resolved evidence for the exact invocation scope and accept asynchronous
verifiers; fixture booleans alone cannot launch a client. Migration 0022 is
additive. Provider-specific conformance, installation loading and production
registration remain separate issues, so no official route is enabled by this
change. Review was the issue-required primary self-review; no provider or live
model call, GitHub Actions run, PR action, billing/account change or merge ran.

Issue-sized delivery — 13 September: the operator stopped the umbrella effort
and requested that all remaining work become separate GitHub issues. The pushed
checkpoint is `12332e4`; [the bounded issue queue](v0.2-issue-backlog.md) links
issues #4–#30 with dependencies, acceptance criteria and focused verification.
Future work is one selected issue per goal, without automatically resuming the
whole plan. The large goal remains inactive, not complete. No new implementation
goal, GitHub Actions run, live model call, PR or merge was started in this split.

Local verification completed — 13 September. The operator declined further
GitHub Actions runs and authorized full local repairs and verification. Both CI
approvals are consumed; no additional workflow was dispatched. Runtime checkpoint
`0764852`, following `5200217` and `45bed78`, is committed on `forge/v0-2`
in the existing managed worktree. The final checkpoint adds documentation and one
test-fixture repair only. These checkpoints are now pushed; their verification
used no live model calls, PR actions or merge.

Durable quota admission, provider classification, approved fallback and audited
operator reporting are implemented and exercised by the deterministic selection.
The same PostgreSQL path suppresses concurrent/restarted workers, admits one
recovery probe, preserves newer exhaustion and distinguishes known resets from
bounded probe cooldowns. Skipped routes spend no attempt/repair/capacity budget;
fallback retains partial work, ownership, the selected primary and billing gates.
Unknown allowance remains unknown. Forge owns this state and runtime behavior.

The reproduced CI defects are repaired: platform assumptions, schema contracts
and ORM/migration agreement, the planning Git denial category and the read-only
worktree-lock fixture. Applied constraints remain intact; the removed metadata
index never existed in PostgreSQL. Local Linux also exposed CI-REPAIR-9: approved
disjoint writers could fail on brief filesystem-lock contention. The writer now
retries only typed contention while opening its capability, before changing a
file, for at most five seconds. Existing durable effect admission and cancellation
fences remain active; failures inside a write or during release never replay it.
The held-lock test was observed failing before the repair; controlled-time and
actual parallel-writer cases pass afterward.

| Verification | Local result |
| --- | --- |
| Complete Linux deterministic backend selection at `0764852` | **6,314 passed, 2 failed, 92 skipped**, 10 live/Docker deselections; 4,809.96s pytest duration |
| Linux supervisor/process recovery with corrected init environment, same source/image | **51 passed, 1 platform skip**, including both failed full-run cases; 2.40s |
| Docker selection at `0764852` | **9 passed**, 5 host variants deselected; 153.96s |
| Chromium/API/worker restart and multi-tab scenario at `0764852` | **1 passed**; 26.88s |
| Windows affected tool/security coverage, matching runtime source | **772 passed, 69 skips**; 378.82s |
| Windows supervisor/process recovery at `0764852` | **52 passed**; 1.91s |
| Ruff and source mypy | Pass; **428 source files** |

The full run's only failures were killed grandchildren left as zombies by the
local harness's Python PID 1. Docker `--init` supplies a reaper. The targeted Linux
rerun used identical source, image, dependencies and assertions; both failures
passed without application changes. This is complete-selection evidence plus a
targeted environment correction, **not a single all-green full run or a CI pass**.
Windows workflow-equivalent evidence at `45bed78` (1,148 passed, four skips) is
retained with fresh coverage for the subsequently changed tools. Counts overlap
and are not added together.

The skip audit found LOCAL-VERIFY-1: a linked-quarantine test hid its missing
fixture directory as an unsupported-symlink skip. Narrowing that skip reproduced
the setup failure. The fixture now prepares the actual private quarantine, and
asserts retained registration and outside bytes. **Five Linux quarantine cases
pass**; Windows has **four passes and one genuine symlink-privilege skip**. This
changes no application source. The privileged Linux bind-mount case remains
skipped in the unprivileged harness; the separately configured browser case passes
as shown above.

The Linux harness uses Debian/WSL2 Docker, Python 3.14.5, Node 24.20.0, frozen
dependencies and private PostgreSQL 17.11 without outbound network. Control
UID/GID 1000 remains separate from sandbox UID 10001. Earlier harness setup
failures and incomplete runs are preserved. All owned local runs are terminal and
their containers stopped. The committed `0764852` secret scan has **zero findings
across 1,190 files**. Candidate hashes, raw logs/JUnit, primary source self-review,
resolved findings and focused follow-ups are retained under
`.llm-output/root-v02-local-verification/`; `final-checkpoint.json` records the
final test/documentation checkpoint and evidence reuse. No independent review or
separate model-specific gate is claimed for these increments.

Historical CI remains unchanged: Web **34758714002 passed** both platforms at
`700e36e`, with unchanged Web/workflow inputs; Python/runtime **34758712793 failed**.
Its PostgreSQL job reached 95% before the 60-minute timeout. CI-REPAIR-8 increases
that job's cap to 90 minutes without changing commands or triggers; this local
configuration repair grants no CI authorization. Logs remain in
`.llm-output/root-v02-ci-repairs/`. Browser/supervisor **34756512565** passed at
`66cf6fb`; dated checkpoints below preserve the earlier evidence.

Full v0.2 remains incomplete. Issues #5 and #7 establish that the chosen personal
ChatGPT route lacks an allowance-only control and the Claude route lacks supported
verification of its account-level control. Both official routes remain deliberately
unavailable pending provider capabilities. Trusted installation loading,
remaining provider-specific capability production, full live A1–A9
conformance and separately authorized release steps remain unmet; deterministic
fixtures do not supply those capabilities. Production registration stays closed
by default.

| Slice | Current state |
| --- | --- |
| 1 — capabilities | Shared canonical evidence identity, artifact verification, PostgreSQL current-revision selection and exact scoped gateway admission are implemented through migration 0022. Issues #6 and #8 add pinned official-client offline tool-isolation proof and exact Codex/Claude evidence resolvers. Issues #5 and #7 confirm the missing supported allowance-only billing proofs. No billing evidence is published for either route, and both remain unavailable. Live provider conformance and Gemini isolation remain unproved. |
| 2 — contracts/profiles | Typed tasks/decisions, frozen profiles, approvals, additive persistence through migration 0021 and honest usage/quota records implemented with focused checks. All three retained legacy states now have actual v0.1-source schema/row/artifact/recovery/control evidence, including historical test-session continuity. Deployment and mixed-version compatibility remain due. |
| 3 — scheduling | Delegation, primary continuation, scoped reservations/effect fences, budgets, candidate/review/acceptance, durable quota admission and stopped-task resume have local unit/PostgreSQL and review evidence. Production route filtering, remaining-budget fitting and source-bound primary reassignment are verified. Whole scenarios remain. |
| 4 — factory/supervisor/broker | Shared runtime factory, supervised clients, controlled tools and durable recovery are implemented. Production composition proves counter repair, catalog integration and a direct primary typo repair through actual writes, check/commit barriers, handoff recovery, selected review or explicit no-review, final validation and retained human PR approval. Real provider capability evidence and complete runtime conformance remain due. |
| 5 — Codex vertical | Concrete runtime adapter, deterministic app-server protocol, launch controls, effective-configuration preflight and exact production evidence resolver implemented. Composed Codex-primary counter delivery and quota suppression/probe cases pass. The workspace-local official 0.153.4 installation passes all five providerless isolation scopes with only namespaced Forge tools. Trusted evidence publication and production registration remain unavailable because billing cannot be proven; authorized live proof remains due. |
| 6 — other adapters | Claude and Google gateways/codecs and concrete runtime registrations have supervised fake-peer coverage. The pinned Claude client now has providerless official-client isolation proof and an exact production evidence resolver for its sole review scope, while issue #7 keeps admission unavailable. Exact factory binding, Gemini worker settlement/control and Claude durable quota recovery are implemented with focused checks. Gemini production verification and live provider conformance remain due. |
| 7 — operator flow | Versioned profile API/UI/CLI, refreshing task/attempt inspector, audited quota reporting, subscription usage assessment and worker registration diagnostics implemented with focused evidence. The task view reports observed execution capacity and fair-order constraints using scheduler accounting. Controls cover queued, active and settled histories, including quota deferral and pending scope decisions. Public-process and multi-tab profile/control variants pass; official-client login/readiness and whole browser acceptance remain. Historical independent findings are repaired; current increments use primary self-review. |
| 8 — acceptance/release | Composed deterministic A1–A9 variants retain manifests and artifacts. The complete local backend selection, targeted environment/fixture corrections, Docker and browser checks are recorded above; historical independent reviews cover earlier increments and current increments use primary self-review. Whole live A1–A9 conformance and separately authorized PR/release steps remain; further CI runs are declined. |

CI results and repairs — 13 September: the authorized batch completed exactly once
per workflow at `66cf6fb`. [Browser/supervisor 34756512565](https://github.com/Clar17y/parallel-forge/actions/runs/34756512565)
passed, including three PostgreSQL control cases, the subscription Chromium case,
three browser scenarios and supervisor startup/shutdown. [Python/runtime 34756510424](https://github.com/Clar17y/parallel-forge/actions/runs/34756510424)
passed its secret scan and Windows job (**1,113 tests**, three skips), but Linux
mypy found eight Windows-API typing errors and PostgreSQL collection stopped at
duplicate `test_subscription` module names; the Docker step did not run.
[Web 34756511558](https://github.com/Clar17y/parallel-forge/actions/runs/34756511558)
failed the same three tests on both platforms. These are failed runs, not full
verification passes. Exact dispatches, terminal jobs and logs are retained in
`.llm-output/root-v02-ci-preflight/`.

All three defects were reproduced locally. The Windows module now uses a
`sys.platform` import guard understood by mypy. The persistence test module has a
unique name with identical contents. Usage-page test doubles now distinguish API
and subscription responses and expect the existing assessment parameter. The
repaired candidate passes **120 focused Python/PostgreSQL tests** (one POSIX skip),
**150 Web tests**, both Linux/Windows mypy targets (**428 files** each), Web types
and lint, and affected Ruff checks. Whole backend collection now selects **6,360
tests** without errors; collection is not execution. Primary source self-review
resolved these findings. The later operator instruction above declines further CI.

The operator selected the **existing personal ChatGPT login**. Bounded official
Codex 0.153.4 metadata recognizes that login and lists Astra/low. Twenty-three of
24 fixed scalar configuration comparisons match; `multi_agent_v2` does not, and
one inherited MCP server is not explicitly disabled. Forge's predicate rejects
this configuration. The metadata client is confirmed stopped. Allowance-only
billing remains unproved for this personal account; the account choice does not
authorize paid credits. No login, thread/model request, billing change or quota
state write occurred. Sanitized evidence is in
`.llm-output/root-codex-personal-metadata/`. Account selection is resolved;
production admission, configuration isolation and authorized live proof remain due.

The CI repairs are committed and pushed as `700e36e`; its committed archive has
zero pinned Gitleaks findings. The requested two-run Python/runtime and Web CI
batch remains bound to that exact commit in `.llm-output/root-v02-ci-repairs/`.

A subsequent local Codex fix resolves the configuration representation mismatch.
The published `FeatureToml` type and a second metadata read show that
`multi_agent_v2` is explicitly disabled inside a table. The preflight now reads
that boolean for the two supported structured gates (`multi_agent_v2`,
`code_mode`), retaining strict checks for absent, unknown or wrongly typed values
and every other capability. It passes **116 Codex tests**, **three PostgreSQL
composition cases**, affected Ruff/format and mypy. A fresh owned-home official
metadata case also passes the corrected predicate; the client is stopped without
a model turn. Primary self-review is in `.llm-output/root-codex-table-acceptance.json`.
This local fix is additional to `700e36e` and is not covered by that pending CI
request.

The approved repair batch has now run once per requested workflow at `700e36e`.
[Web 34758714002](https://github.com/Clar17y/parallel-forge/actions/runs/34758714002)
passed both platforms. [Python/runtime 34758712793](https://github.com/Clar17y/parallel-forge/actions/runs/34758712793)
passed secret scanning and Windows; PostgreSQL later hit the 60-minute job limit,
as recorded above. Linux typing now
passes, but its unit job found one test assuming Windows path case rules on POSIX.
That assertion was reproduced locally with controlled platform semantics, repaired
without a runtime change, and **134 affected protocol/domain tests pass**.
No CI rerun is authorized or dispatched.

The subsequent Codex increment adds bounded trusted names for per-launch and
per-thread MCP disabling. Fake clients verify ignored/unknown configuration rejects
before a thread, names cannot inject configuration, and home settings remain intact.
**138 Codex tests and three PostgreSQL composition cases pass**, with source mypy
and affected Ruff checks. Official 0.153.4 metadata now passes the effective
configuration predicate for both an owned fixture and the selected personal login
using the disable override. Each baseline rejects the active entry; all clients
stop, without threads, models, login/billing changes or persisted account/server
details. Evidence is retained in `.llm-output/root-codex-mcp-metadata/` and the
`repair-mcp-*` records under `.llm-output/root-v02-ci-preflight/`.

This resolves the observed personal-home MCP configuration mismatch. It is local
work after `700e36e`, alongside the structured-feature and platform-test fixes.
Allowance-only billing, full native-tool conformance, trusted production capability
admission and installation loading remain incomplete. Review is primary self-review.
`.llm-output/root-codex-mcp-acceptance.json` binds the current source, checks and
sanitized metadata, closing CODEX-PERSONAL-1/2 and CI-REPAIR-4 locally.

CI preparation — 13 September: integrated Ruff checks pass across the orchestrator,
tests and scripts; mypy passes **428 source files**. Web lint/type checks and both
OpenAPI/generated-TypeScript consistency checks pass. One existing test import
needed formatting. The pinned Gitleaks **8.30.1** candidate scan found one public
fixture idempotency label, which now uses the same short label as the matching
legacy fixture; the scan then reports **zero findings across 1,190 candidate files**.
No secret allowance or scan exclusion was added.

The four affected actual-v0.1 PostgreSQL upgrade/control cases pass in **98.04s**.
Their fresh manifests retain **62 verified artifacts** and prove eight historical
processes completed and stopped. This changes two test-only lines/blocks, with no
application behavior, migration or acceptance-scenario change. Matching Python/web
static evidence is reused after these test-only repairs. Earlier focused runtime
evidence and resolved findings are retained; no whole-suite result is inferred.

The source checkpoint is `feat: checkpoint v0.2 subscription runtime and acceptance`.
Primary self-review and preflight evidence are in
`.llm-output/root-v02-ci-preflight/acceptance.json`. The exact checkpoint and the
prepared one-run-each Python/runtime, Web and Browser CI request are in that
directory's `request.json`/`request.md`; dispatch requires the separately scoped
human authorization. A checkpoint push to this branch has no CI trigger or PR.
Trusted official-client account/billing/isolation admission, installation loading,
whole A1–A9 conformance and authorized live/release work remain incomplete.

The Gemini home-binding increment passes **60 focused gateway/home tests in
9.01s**, **four PostgreSQL composition/control cases in 34.72s** (two Docker
variants deselected), **two actual Docker stop cases in 33.95s** (two host
variants deselected), five-file Ruff/format, one-source mypy and whitespace checks.
The focused test first proved that an otherwise complete report without a home
binding admitted a physical fake-client launch. Gemini now requires that report
to match the canonical configured home, as Codex and Claude already do. Missing,
relative or foreign-home proof rejects before launch; matching home cannot replace
the other capability gates. Installation/report representations omit the home.

The composed paths retain success/throttling classification, the selected primary,
active pause/cancel recovery and quota-blocked resume. Four fresh A7 manifests retain
20 verified artifacts. `.llm-output/root-gemini-home-acceptance.json` binds the
candidate, primary source self-review and checks. The 60-test evidence is reused
from the completed matching-input run; no source changed afterward. Earlier runner
evidence remains applicable to unchanged runner paths; the affected A7 paths have
fresh PostgreSQL/Docker checks. Capability reports remain explicit fixtures.
Trusted official-client admission/installation loading, whole A1–A9, authorized
live/CI and release are still incomplete. No official client, model call, full CI,
separate AI review/gate, production migration, commit, PR or merge ran.

The active Docker cancellation increment passes **nine Docker cases in 154.85s**
(five host variants deselected), **20 PostgreSQL recovery/composition cases in
66.29s** (two Docker variants deselected), and **249 focused unit tests in 55.40s**.
Five-file Ruff/format, one-source mypy, whitespace and workflow YAML/collection
checks pass. The Docker CI step now selects nine cases; no CI was dispatched.

A7 now pauses and cancels an actual running `slow-unit` container through the
production task-control heartbeat. The first implementation exposed a production
defect: cancellation waited for the Docker client to return before removing its
container, exceeding the subscription cleanup window. The runner now removes the
verified owned container while the client waits, then drains the client and
preserves terminal output, access repair and recovery fences. Controlled fake
process tests cover delayed container visibility, repeated cancellation and
refusal to remove a foreign container. Existing cleanup limits are unchanged.

Both real-Docker A7 variants prove that the provider and MCP processes stop, the
same observed container is absent, and its check returns cancellation/exit 137
without timeout or releasing the fixture barrier. Recreated composition uses the
existing tool/effect recovery order to reconcile the interrupted effect once.
Pause resume retains its ordinary repair charge; a later quota-blocked poll adds
no attempts, repairs or held capacity. Partial files, result evidence, selected
primary and human gates survive. Unknown reset remains an explicit probe cooldown
with unknown token telemetry. Eight fresh manifests retain **75 verified artifacts**,
including the full Codex flow's pending human PR approval archive.

Selected-primary source self-review and candidate-bound checks are in
`.llm-output/root-docker-stop-acceptance.json`. These current checks supersede
older runner evidence where this source change affects it. Provider responses
remain fixtures; the commands use real local Linux Docker hosted on Windows.
Trusted production capability verification/installation loading, whole A1–A9,
authorized live/CI and release remain incomplete. No separate AI review/gate,
live model, production migration, commit, PR, merge or deployment ran.

The Docker delivery/quota extension passes **four Docker-policy PostgreSQL cases
in 109.25s**, **four matching trusted-host cases in 101.33s**, and **four affected
catalog/review/trivial cases in 189.02s**. Four-file Ruff/format, workflow YAML,
seven-case Docker CI collection and whitespace checks pass. The preceding 77
runner/fixture unit checks and three original Docker base checks retain matching
relevant inputs and are reused; changed acceptance paths have fresh evidence.

The full fake-Codex primary flow now reaches the pending human PR gate under
Docker as well as trusted-host policy. Approved quota fallback repairs the retained
write and checks it in Docker; unapproved fallback preserves the block across
recreated concurrent pollers without attempts, repairs or checks. The selected
primary, ownership, billing boundaries and human gates remain unchanged.

Source self-review found that the shared exporter retained the pending approval
digest without its artifact. The new focused assertion failed before the fix and
passed afterward. It now seeds that pending artifact into the existing parent-chain
retention. Twelve fresh manifests retain **205 verified artifacts**, including
**five pending human PR approval archives**; both Codex variants retain all five
command results. Prior smaller archives remain historical evidence. Separate
fixture builds returned three immutable image IDs with identical runtime
configuration and layers; each scenario's commands match its configured ID.

Selected-primary source self-review, candidate and current/reused evidence are in
`.llm-output/root-docker-delivery-acceptance.json`. Application source, migration
guards and versioned fixture semantics are unchanged. The Docker CI step is ready
with seven selected cases; it has not run in CI. Trusted production capability
verification/installation loading, whole A1–A9 conformance, authorized live/CI and
release remain incomplete. No live model, separate AI review/gate, production
migration, commit, PR or merge ran in this increment.

The Docker A1 increment passes **four real-Docker checks in 38.54s**, **four
composed PostgreSQL regressions in 99.41s**, **77 focused runner/fixture checks in
26.02s**, three-file Ruff/format, workflow YAML/step assertions and whitespace
checks. The same approved counter fixture now runs its failed check, repair,
passing check and handoff recovery through production Docker composition, using
an immutable image built from the pinned Dockerfile and `trusted_project=false`.
Command artifacts bind Docker mode, the image ID, disabled network, bounded output
and sandboxed execution. The original Docker image/mount/staged-file contracts
also pass on the local Linux Docker daemon hosted on Windows.

The shared trusted-host A1, concrete fake-Codex final human gate and both A6 quota
outcomes pass current PostgreSQL checks. Five manifests retain 57 verified
artifacts and report their actual runner mode. The first focused test failed on
the missing runner parameter. Its first implemented run passed scenario assertions
but failed teardown because the new module omitted the existing disposable-row
cleanup fixture; the corrected complete rerun passes without changing migration
guards. Application source and fixture semantics are unchanged.

Selected-primary source self-review and current evidence are retained in
`.llm-output/root-docker-counter-acceptance.json`. The existing Docker CI step now
includes the new variant; no CI was triggered. Provider/launch evidence remains
scripted. Trusted production capability verification and operator installation
loading, whole A1–A9 proof, authorized live/CI and release remain incomplete. No
live model, separate AI review/gate, production migration or release action ran.

The public-worker registration increment passes **60 unit checks in 2.79s**,
**two PostgreSQL/process cases in 23.40s**, four-file Ruff/format, one-source mypy
and whitespace checks. `run_worker` now accepts the existing trusted subscription
adapter interface and supplies it to its owned composition after startup recovery
admission. It snapshots the iterable once and rejects combining registrations with
caller-owned handlers before creating a database engine. The initial focused
tests failed because that entry-point parameter was missing.

Each process case starts an empty-registry worker, two registered workers and a
fresh registered restart. The normal command lane queues planning. The blocked
case retains the exact PostgreSQL quota/reset and makes zero attempts or repairs;
the eligible case performs one supervised fake Codex read and reaches the pending
human plan gate. Both retain the selected Astra primary, release capacity and
publish stopped registration snapshots. All eight Python worker processes
acknowledge drained shutdown and have confirmed stop evidence (supervised close
code 1). The initial wrong test disposition label was corrected to the existing
`plan_approval` value; no product gate changed.

Selected-primary source self-review and current evidence are retained in
`.llm-output/root-worker-registration-acceptance.json`. The preceding Claude
250-unit/two-PostgreSQL evidence is reused for its unchanged execution inputs;
the changed worker entry point and its test are covered by this batch. The
capability report, provider peer and repository inspector are explicit fixtures.
An operator-facing installation loader and trusted account/billing/isolation
verification remain incomplete; the unconfigured CLI registry remains empty.
No official client, live model, full CI, separate AI review/gate or release action
ran in this increment. Whole v0.2 remains incomplete.

The Claude configuration-home increment passes **250 unit checks in 9.15s**
(one existing POSIX-only skip), **two PostgreSQL quota cases in 6.98s**, five-file
Ruff/format, one-source mypy and whitespace checks. Forge now requires an explicit
canonical existing Claude home, matches it to the capability report and passes
only `CLAUDE_CONFIG_DIR` plus required platform environment to the supervised
client. The fourteen new cases cover environment isolation, pre-launch rejection
and retained billing/hook/version gates. Existing three-run quota suppression and
single-probe checks pass with that binding.

The global Claude client now reports 2.1.268. A separate official **2.1.263** npm
installation with scripts disabled matches Forge's unchanged pin. Its version/help
and two owned unauthenticated `auth status` fixtures pass the bounded metadata
checks; inert home/project API-key settings are excluded. Native auth returns 1
with `loggedIn=false`; both supervised wrappers are confirmed stopped (close code
1). The initial multiline-JSON probe failure is retained. No login or model turn
ran. `.llm-output/root-claude-home-acceptance.json` binds source self-review,
candidate, package/binary hashes, observations and current checks. These results
do not establish account identity, managed-hook isolation or allowance-only
billing. Trusted production admission/default-worker registration, whole scenarios,
authorized live/CI and release remain incomplete. No separate AI review/gate or
release action occurred.

The historical-source A9 increment passes **eight composed PostgreSQL cases in
168.48s**: four actual v0.1-source cases and all four existing retained-service
cases. Nine-file Ruff/format and whitespace checks pass. The unchanged workflow
YAML/history-checkout checks retain matching inputs. The fixture exports v0.1 merge
`781567f7365e76dfc37cf8e7cf98765cb00800e4` from local Git and runs its schema
creation, registration, planning and delivery code in separate supervised Python
3.14 processes. Each process's 250 loaded Forge source files and package search
paths match that export; project metadata and lockfile are byte-identical to the
current test environment. Provider output is explicitly scripted.

The actual stored v0.1 states cover plan approval, an interrupted admitted write,
and retained validation/review with pending pause or cancel. After confirmed
shutdown, two head upgrades preserve every original column in 19 selected evidence
tables and all artifact bytes. A fresh old-source process reads each state and
validates its plan binding against schema 0021 without changing those rows or
bytes. Current recovery replays the original write receipt without another effect
and quarantines unknown provider completion once. Current authenticated APIs
accept the historical review cases' test sessions and preserve control replay,
stale-version conflicts, resume and the evidence-bound human gates.

Eight manifests retain **124 verified artifacts**. All eight historical processes
completed their requested operation and were confirmed stopped; supervised close
returned code 1, not normal exit zero. Test-session credentials cross only the
private process pipe, are removed before provenance, and are explicitly checked
absent from the final manifests. The shared controlled-tool fixture and existing
recovery/control assertions are reused. Control-event counts now derive from the
retained history, so each replay must add exactly one event regardless of earlier
plan pauses.

Selected-primary source self-review and current evidence are retained in
`.llm-output/root-historical-recovery-acceptance.json`. Initial missing-entry-point
failure and focused checks are recorded; the earlier plan-only checkpoint remains
in `.llm-output/root-historical-v01-acceptance.json`. Namespace provenance and
post-reader preservation findings are resolved. This is historical Python source
with a current test driver and a shared locked environment; deployed binaries,
mixed-version writers, rollback, actual browser and live-provider proof remain
separate requirements. Whole v0.2 remains incomplete. Application source was
unchanged. No official client, live model, full CI, production migration, separate
AI review/gate or release action was invoked in this increment.

The specialist registration increment adds `ClaudeRuntimeAdapter` and
`GeminiRuntimeAdapter` through the existing factory. Both bind a fresh gateway to
the exact frozen route, broker and lifecycle without client I/O during construction.
Capabilities remain checked on every execution. Registration cannot replace the
selected primary, guess an effort or enable paid usage. The first focused checks
failed because these production registrations did not exist; the initial optional
Gemini effort assumption was corrected to preserve the mandatory frozen-route
contract. Import-order and type-narrowing findings were repaired locally.

Selected-primary source self-review accepts the bounded registration increment:
**18 unit checks pass in 2.49s**. The initial **six PostgreSQL cases passed in
38.52s**; after the Gemini type-narrowing repair, its **four affected cases pass
again in 33.49s**, while the two Claude cases retain matching relevant inputs.
Six-file Ruff/format, two-source mypy and whitespace checks pass. The existing
231-unit/seven-PostgreSQL Codex batch also retains matching recorded inputs and
is reused. Composed Gemini settlement, throttling, pause/cancel and Claude quota
recovery use the new registrations. Both final A7 manifests retain three verified
artifacts and physical client/MCP stop evidence. Source review and current evidence
are retained in
`.llm-output/root-specialist-runtime-acceptance.json`. The capability reports and
clients remain explicit test doubles. Default-worker registration, trusted account,
billing and full tool-isolation evidence remain incomplete. No official client,
live model, full CI, separate AI review/gate or release action was invoked in this
increment.

A workspace-local official Codex **0.153.4** installation now matches Forge's
unchanged runtime pin. Installation used the exact public npm package with scripts
disabled under `.llm-output/codex-client-0.153.4`; the global 0.154.0 installation
was not selected. Version/schema generation and two fresh unauthenticated
configuration metadata checks passed. All 24 controls matched; an inherited MCP
entry remained visible and was rejected. Both clients stopped, with no login,
thread or model request. The six selected schemas match the original 0.153.4
evidence. `.llm-output/root-codex-pinned-acceptance.json` binds package integrity,
binary/source hashes, observations and the still-matching checks below. This
resolves the missing pinned executable, not account/billing/isolation admission.

The Codex effective-configuration increment is accepted by selected-primary source
self-review (13 September): **231 unit checks pass in 10.13s**, with one existing
POSIX-only skip, and **seven PostgreSQL cases pass in 64.52s**. Five-file Ruff/format,
one-source mypy and whitespace checks pass. Matching input hashes are retained.
The composed cases rerun primary planning, quota suppression/recovery and the
complete deterministic counter delivery to its pending human PR gate.

Before starting a thread, the gateway now reads effective configuration for the
exact working directory. All 24 fixed controls must match in value and type, and
every inherited MCP entry must be explicitly disabled. Missing or conflicting
configuration returns unavailable, revokes the broker and closes the client without
a model turn, tool call or invented quota evidence. Existing capability and billing
checks remain mandatory before client launch. The focused ordering test failed
before implementation; 23 later failures were missing brokers in the new test
fixtures, repaired before the affected suites passed.

Metadata-only checks found the globally installed Codex is now **0.154.0**, while
Forge still admits **0.153.4**. Two fresh owned, unauthenticated homes confirmed all
24 startup controls through the official `config/read` interface. An inherited MCP
fixture remained visible and was rejected by the new predicate. No thread, login
or model turn was started. All clients were confirmed stopped; their supervised
close returned code 1 with a completed operation, not a normal exit-zero claim.
Five selected request/config schemas match the earlier generated schemas; the
requirements response adds application-network and WebMCP fields. This is bounded
metadata evidence, not complete isolation, account binding or billing proof.

`.llm-output/root-codex-preflight-acceptance.json` binds the candidate, current
checks, counter manifest/artifacts, sanitized metadata and source self-review.
It supersedes affected Codex runtime/configuration test inputs; previous results
below remain historical. No runtime pin was upgraded and the production registry
remains empty. Trusted account/capability admission, authorized live/CI and release
steps remain incomplete. No live model, full CI, production migration or release
action ran in this increment.

The Codex-primary counter delivery increment was accepted by selected-primary source
self-review (13 September): **one composed PostgreSQL case passes in 50.63s** with
its manifest/artifacts retained. Two-file Ruff/format and whitespace checks pass.
At that checkpoint, the **206 unit and 56 PostgreSQL** runtime checks below had
matching recorded inputs and were reused. That scenario increment changed no
application source; the effective-configuration batch above now covers its affected
protocol and composition inputs.

Five fresh supervised fake Codex processes derive their decisions from the actual
request schema and durable context. The existing synthetic Gemini writer performs
real managed writes, failed `unit`, repair, passing `unit` and handoff. Recreated
composition applies that handoff once; the primary accepts it, checkpoints,
records a policy-permitted no-review decision and submits final acceptance. Both
required policy checks then pass and the evidence-bound human PR gate remains
pending. Six attempts, three provider-requested checks, zero scheduled repair
debits/capacity leaks and unknown primary quota are verified.

The early fixture failures exposed an incomplete usage frame, an incorrect diff
scope, a failed check cited as passing evidence, and a held final-check barrier.
Production guards remained intact. The last mixed batch passed the existing A1
case but failed new manifest export on expired ORM rows; copying verified launch
payloads inside their session repaired export, and the new case was rerun.
`.llm-output/root-codex-counter-acceptance.json` binds the final candidate, artifacts,
matching checks, initial failures and source self-review. The primary processes
are physical fake peers; the writer's launch proof remains synthetic. Official
capability admission, public-worker registration, live/CI and release remain due.

The Codex runtime-binding increment is accepted by selected-primary source
self-review (13 September): **206 unit checks pass in 8.30s**, with one existing
POSIX-only skip, and **56 PostgreSQL checks pass in 109.30s**. Five-file Ruff/format,
one-source mypy and whitespace checks pass. `CodexRuntimeAdapter` supplies the
existing factory with one fresh gateway per exact route/attempt, broker and durable
lifecycle. Construction performs no client/account I/O; every invocation retains
the gateway's capability checks. The first focused test failed because this
concrete registration did not exist.

The composed primary planning case now uses this adapter through the production
worker, controlled repository read and result settlement to reach the evidence-bound
human plan gate. A named-check fixture omission correctly triggered the existing
policy guard; the fixture now registers that check before freezing the run. A
mistyped telemetry assertion and exception-type lint finding were also repaired.
The two Codex quota cases use the same concrete adapter across concurrent recreated
workers, preserving suppression, one recovery probe and exact attempt accounting.

`.llm-output/root-codex-runtime-acceptance.json` binds the candidate, final checks,
initial failures and source self-review. These results supersede the affected
Codex configuration/notification test inputs; unchanged launch-source/schema
characterization is retained. The provider peer and capability source are fake.
Trusted production admission, default-worker registration and a complete Codex
delivery remain incomplete; the default registry stays empty. No live model,
full CI, production migration or release action ran.

The A7 active-client control increment is accepted by selected-primary source
self-review (13 September): **178 unit checks pass in 11.33s**, with one existing
POSIX-only skip, and **29 PostgreSQL checks pass in 107.14s**, including two new
composed cases. Two-file Ruff/format and whitespace checks pass. Production source
is unchanged. A real supervised fake Gemini client and its MCP child read/write
the managed counter fixture; audited Pause/Cancel triggers lease-driven shutdown.
Both process identities are observed alive before the control and gone afterward.
Recreated composition settles the stop once, keeps partial files and receipts,
rejects later writes and preserves the frozen Astra-low primary.

An operator quota report remains blocked through restart and resume. The later
suppressed poll adds no attempt, repair or execution capacity. Ordinary interruption
resume still requires its existing repair reservation and a frozen allowance for
another unknown measurement. The first fixture correctly failed that telemetry
guard; it now explicitly permits two unknown attempts. Later manifest export
rejected a process field ending in `token`; the fixture now records OS identity
under the existing `process_identity` name. Neither repair changes production guards.

`.llm-output/root-active-client-controls-acceptance.json` binds current checks,
both manifests, initial failures and self-review. The primary still uses synthetic
planning/delegation evidence; the specialist/MCP stop proof is physical. No named
check, complete repair, public-process/browser control or official-client billing
proof is claimed. No live model, full CI or release action ran; whole v0.2 remains
incomplete. The Codex launch-configuration source characterization below is
unchanged; the runtime-binding batch above supersedes its affected test inputs.

The Codex launch-configuration increment is accepted by selected-primary source
self-review (13 September). Two focused failing tests exposed the missing explicit
client home and startup controls. Forge now requires a canonical existing home
matched by its capability report, passes only that `CODEX_HOME` plus the supervisor's
platform environment, and applies fixed supported startup/per-thread overrides.
Those select the frozen model/effort and ChatGPT provider, disable hosted/native
integrations and automatic repository instructions, and preserve environmentless
Forge callbacks. Credentials remain owned by the official client.

Verification passes **196 unit checks in 7.97s**, with one existing POSIX-only skip,
and **five PostgreSQL composition/runner checks in 13.38s**. Five-file Ruff/format,
one-source mypy and whitespace checks pass. The initial broader run exposed a
fake-peer argument-count assumption; it now parses and validates the added TOML
arguments and matching thread controls. Both failed runs are retained. Schema/hash
characterization accepts the overrides against pinned 0.153.4 sources and the
installed generated thread schema; it does not execute official-client behavior.

`.llm-output/root-codex-configuration-acceptance.json` binds these checks, inputs,
source manifests and resolved **CODEX-CFG-1/2/3**. The explicit home and controls do
not supply trusted account identity, managed/MCP isolation or allowance-only
billing proof. The production registry remains empty pending those capabilities.
No live model call, full CI, separate AI review/gate, production migration or
release action occurred. The whole v0.2 objective remains incomplete.

The Claude quota-notification increment is accepted by selected-primary source
self-review (13 September). Two focused reds reproduced rejected warning frames
and terminal quota text being misclassified as throttling. Forge now accepts the
SDK notification shape with session checks and reads terminal `result`/`errors`.
An exact trusted allowance-window binding supplies rejection/reset authority;
warnings, unrelated windows and credit fields cannot establish or clear a block.
Confirmed evidence survives cancellation, uncertain settlement and conflicting
completion, preserving preceding receipts and rejecting later tool effects.

Self-review also found and repaired a relative-reset timing error: terminal text
uses its own first arrival time while exhaustion keeps its earlier observation.
That regression failed before repair. Final verification passes **218 unit checks
in 7.87s**, with one existing POSIX-only skip, and **two composed PostgreSQL cases
in 5.86s**. The other **34 PostgreSQL checks** retain matching relevant inputs from
the 36-case batch (77.34s); the two Claude cases were rerun after the timing repair.
Six-file Ruff/format, two-source mypy and whitespace checks pass. The composed
cases retain Astra low, a mapped Claude specialist pool, terminal launch evidence,
zero repair debits and one recovery probe across recreated executors/runners/UOWs.

`.llm-output/root-claude-notifications-acceptance.json` binds the candidate, current
inputs, explicit evidence reuse, source manifests and resolved **CLAUDE-QN-1/2/3**.
This is fake-client adapter/runner composition with test-built requests. Official
client emission, account/model identity, hook isolation and allowance-only admission
remain unproved; the default production registry stays empty. No live model call,
full CI, separate AI review/gate, production migration or release action occurred.
The entire v0.2 objective remains incomplete.

The A8 browser profile/control increment is accepted by primary source self-review
(13 September): **one Chromium process scenario passed in 23.93s**, **21 SSE
checks (including six PostgreSQL variants) in 12.34s**, and **47 affected UI checks
in 7.68s**. Three-file Ruff/format, one-source mypy, JavaScript syntax and whitespace
checks pass. The first browser run exposed idle SSE startup holding controls until
the heartbeat interval. A controlled-clock regression failed before the fix.
The first authenticated poll now flushes a comment heartbeat; cursor replay,
session expiry/revocation, disconnect and shutdown guards remain verified.

Rendered profile edits and run dialogs use real Next.js/API/worker processes and
PostgreSQL. Two tabs reject stale edits/actions; lost profile/resume responses retry
the same body/key and create one effect. Both tabs reconnect after API/worker
restarts. Original Astra low and future Sol high envelopes stay frozen; unknown
quota and zero attempts/budget reservations remain honest. Chromium closes and
all four API/worker handles plus the web process are terminal before export.
`.llm-output/root-subscription-browser-acceptance.json` binds the final evidence,
inputs, checks and finding **A8-SSE-1**. The focused case is wired into the existing
browser workflow, which has not run. Full individual-task/active-provider control,
official-client conformance and release acceptance remain incomplete. No live
model, full CI or release action occurred.

The A8 profile-freeze/resume increment is accepted by primary source self-review
(13 September): **one public-process case passed in 25.55s**, and single-file
Ruff/format plus whitespace checks pass. The existing production implementation
is unchanged. Through authenticated HTTP and the public CLI, an operator changes
the primary, specialist effort and approved fallback for future runs. A paused
run keeps its complete version-1 envelope and Astra low selection; a future run
freezes version 2. Resume queued while the worker is stopped survives API restart
and is applied once by the replacement worker. Exact replay and stale-version
checks preserve two profile versions and one pause/resume event pair. Unknown
quota stays unknown; SQL confirms zero attempts, launches, quota admissions and
budget reservations. All four child processes are terminal before evidence export.

`.llm-output/root-profile-process-acceptance.json` binds the final manifest,
candidate, inputs, checks and source self-review. Matching Codex notification
unit/PostgreSQL evidence is reused after input-hash verification. This proves
queued planning resume and frozen profiles, not active-provider drain, rendered
browser acceptance or official-client billing/isolation conformance. No live
model, full CI or release action ran; the full v0.2 objective remains incomplete.

The Codex quota-notification follow-up is accepted by primary source self-review
(13 September). Pinned
source review found that Forge rejected official terminal `error` notices before
the exhaustion classifier (**CODEX-QN-1**) and sparse account quota notifications
(**CODEX-QN-2**). Both are repaired. A trusted exact provider-limit pin can supply
absolute resets from exhausted windows; unrelated pools and credit snapshots never
establish exhaustion or recovery. Self-review also bound early errors to the
acknowledged turn and preserved first observation/relative reset through duplicate
completion notices (**CODEX-QN-3**). Confirmed exhaustion survives interruption or
uncertain completion, with no accepted decision or later tool effect.

The focused red reproduced `protocol` instead of `quota`; **183 unit cases pass
in 7.59s**, with one existing POSIX-only skip. Five-file Ruff/format, one-source mypy
and whitespace checks pass. The new two-case PostgreSQL composition test passed
in 7.27s after correcting fixture project identity and its expected
`quota_deferred` disposition. It uses three frozen Astra-primary runs, real
supervision and durable settlement with a fake Codex peer: rebuilt concurrent
pollers suppress attempts without repair/capacity debits and admit one expiry
probe. The final affected batch passes **34 PostgreSQL cases in 69.55s**, including
both composition variants, quota races/stop controls and result settlement.
`.llm-output/root-codex-notifications-acceptance.json` binds the candidate, pinned
source hashes, self-review findings, test inputs and logs. Earlier unrelated checks
are retained; superseded Codex gateway evidence is historical.

This is selected-primary source self-review, with no development assistants or
independent gate. Pinned schema/request source and current official billing
documentation still do not establish Codex allowance-only enforcement. The route
remains unavailable; no billing boundary, primary selection or live-call permission
changed. No model call, full CI or release action ran. Whole v0.2 remains incomplete.

The A8 keyless operator increment is accepted by primary source self-review
(13 September): **one process scenario passed in 35.64s**, **five affected legacy
startup/recovery/harness checks passed in 14.02s**, and two-file Ruff/format plus
whitespace checks pass. The existing process harness now has an explicit keyless
mode; the first focused test failed because that mode did not exist. Production
application behavior is unchanged. The final scenario launches the public API and
worker, uses real HTTP authentication and the public CLI, then restarts both
processes. An empty registry is reported honestly and the browser session survives.

Known-reset expiry permits an attempt without declaring recovery. Unknown-reset
reporting retains the configured 60-second probe cooldown. Exact HTTP/CLI replay
preserves two quota observations and audit events, rejects altered bodies, and
creates zero provider attempts, launches, admissions or budget reservations. All
four child handles are terminal before evidence export. The manifest records only
environment key names/presence. `.llm-output/root-keyless-operator-acceptance.json`
binds that manifest, current inputs and checks. This proves the keyless operator
flow; full profile/run resume, client-error/readiness, browser and live billing
acceptance remain. No application model, full CI or release action ran. Earlier
production evidence is reused where relevant source inputs remain unchanged.

The Gemini launch-configuration increment is accepted by primary source self-review
(13 September). Forge now materializes per-attempt settings, a trusted prompt and
empty dotenv guards, names the dedicated official-client home explicitly, and
keeps credentials under that client's ownership. Ordinary settings and supported
CLI arguments control native tools, hooks, skills, credits, main model/effort,
fallback chains, extensions and MCP names. Concurrent attempts have distinct files;
uncertain settlement or unexpected/replaced evidence prevents cleanup. The first
focused test failed on missing settings, then passed after implementation.

Verification: **185 unit checks passed in 12.50s**, with one existing POSIX-only
skip; **36 related PostgreSQL checks passed** in a batch that also exposed two
fixture failures. The composed fixture used `google-model`, rejected by the new
concrete Gemini-name validation. After changing only that placeholder to
`gemini-test`, **both composed cases passed in 8.13s**. Matching production/unit
inputs and the other 36 cases are reused; the failed batch remains recorded.
Six-file Ruff/format, two-source mypy and whitespace checks pass. Nine offline
checks exercise pinned client source snippets with fake filesystem inputs and
fixture schema/defaults; they do not run the CLI/SDK or read credentials.

`.llm-output/root-gemini-configuration-acceptance.json` binds the candidate, source
characterization, focused checks and explicit reuse. Trusted account provisioning,
home/system context and remote policy (including a `forge` MCP name collision),
auxiliary model behavior, exact 3.8 mapping and authorized live conformance remain
incomplete. The production registry remains empty. No new live provider call,
full CI, independent AI review/gate, production migration or release action ran.

The A7 stored-result control increment is accepted by primary source self-review:
**2 composed PostgreSQL cases passed in 38.69s**, plus single-file Ruff/format
and whitespace checks. After the real counter write/check/repair sequence, a
paused result stays deferred across worker recreation and exact causal resume
applies its handoff once. Cancellation retains the result without accepting it.
Both variants preserve file/result digests and all budgets, release execution
capacity, reject stale versions and replay the original control receipt after
later progress. The only code addition is the composed acceptance test; runtime
behavior is unchanged. `.llm-output/root-counter-controls-acceptance.json` binds
two manifests and their artifacts. This is deterministic fake-client lifecycle
proof, not physical process termination, all A7 hooks, full browser acceptance or
live/release conformance. The matching A5 and affected regression evidence remains
valid; no independent AI review, live provider, full CI or release action ran.

The A5 direct-primary increment is accepted by primary source self-review. Its
first composed failure exposed a production gap: final validation required a
committed candidate, while only integration workers could checkpoint. The primary
now uses the existing controlled two-phase commit and worktree barrier, with a
new authority codec binding Forge-derived approved paths. Scope checks reject
foreign edits before staging and in the exact prepared tree. Legacy/integration
codecs retain their meanings. Recovery preserves scope/attempt/tree/message
bindings after lease expiry; altered evidence requires intervention. A further
failing PostgreSQL regression caught and repaired old-codec substitution escaping
as an exception instead of an intervention result.

The composed typo case performs the exact one-line correction, rejects an
out-of-scope write, creates the checkpoint, checks it and records explicit
no-review. Real controller validation stops at human PR approval. It uses one
implementation attempt plus separate planning and acceptance calls: three provider
attempts, one worker check and zero repairs. Verification: **2 composed cases
passed in 55.84s**, including the shared-helper counter regression; **79 affected
PostgreSQL checks in 213.26s**; **134 unit checks in 35.26s** and **37 controlled-Git
checks in 47.55s**. Twelve-file Ruff/format, six-source mypy and whitespace checks
pass. `.llm-output/root-trivial-acceptance.json` binds the final manifests,
artifacts, candidate and checks. Earlier broad records remain historical after
the shared permission/commit changes. A7/A8 and official-client/live/release proof
remain incomplete. No live provider, full CI, production migration, deployment,
repository commit, PR, merge or independent development AI gate occurred.

The A4 planning/review/repair increment is accepted by primary source self-review.
The composed `review-gates` case carries a scripted Sol adversarial brief through
primary delegation, a real failed boundary check, truthful failure settlement,
selected read-only Opus review, bounded integration repair/checkpoint and a fresh
review resolving the same finding ID. A worker recreation preserves the first
stopped review before application. Final acceptance consumes bound receipts and
real controller validation stops at human PR approval. Planner/reviewer writes are
denied; the primary performs no duplicate repository investigation or checks.
The run records 12 scripted provider attempts, three worker named checks, one
primary-delegated repair task and zero automatic scheduler repair debits or repairs
within a worker attempt. The failed report receives no test credit.

Verification: **1 composed case passed in 63.75s**, **10 related PostgreSQL checks
in 32.57s**, and two-file Ruff/format plus whitespace checks pass. This increment
adds two test files and documentation; production behavior is unchanged. The
initial harness expectation was corrected after tracing truthful failure
settlement; successful settlement acknowledges that observation without claiming
the worker completed. `.llm-output/root-review-acceptance.json` binds the current
manifest, artifacts, candidate and checks. Scripted specialist decisions establish
the control/evidence path; required live specialist conformance and remaining
release scenarios are still incomplete. No live model call, full CI, production
migration, deployment, commit, PR, merge or independent development AI gate occurred.

The A3 isolation/capacity increment is accepted by primary source self-review.
Two approved runs share one registered origin and the same relative alpha path.
Across held check, checkpoint, stopped pause and unresolved write-receipt variants,
B completes its real write/unit/snapshot and a fresh worker composition applies
its handoff once before A is released. A's control/effect and budget state remain
intact. Eight retained manifests include both runs, passing B grader evidence and
the current operator snapshot. The only production addition is read-only capacity
inspection: host, frozen run and selected provider limits/counts, observation time
and the actual fair-order rule. It reuses scheduler slot accounting, excludes only
proved stopped pauses, redacts provider labels and leaves missing data unknown.
Capacity observations do not grant launches or imply quota/approval eligibility.

Verification: **4 composed cases passed in 107.95s**, **14 PostgreSQL projection
checks in 30.40s**, **15 API/application checks in 1.81s**, and **14 UI checks in
6.78s**. Seven-file Ruff/format, three-source mypy, TypeScript, scoped ESLint,
OpenAPI/type consistency and whitespace checks pass. The earlier **71 scheduler,
control and terminal-effect checks in 156.99s** are reused against unchanged
scheduling inputs; their unused task-projection input is explicitly excluded.
`.llm-output/root-capacity-acceptance.json` records the candidate, manifests,
hashes, checks and self-review. Initial harness ORM-lifetime errors were repaired;
the absent capacity projection/UI was observed failing before implementation.
This is deterministic provider/launch proof with actual PostgreSQL, managed Git
and trusted-host commands. Official route conformance and remaining release
scenarios stay incomplete. No live model call, full CI, deployment, production
migration, commit, PR, merge or independent AI gate was performed.

The composed A2 integration increment is accepted by primary self-review. It extends
the concurrent writers through a later conflicting alpha task, real check/commit
barriers, one coherent checkpoint, selected read-only review, final validation
and external-drift rejection. A2-003 exposed commit evidence comparing a raw
callback request digest with audit arguments containing only a message digest;
handoff and final acceptance now compare the retained raw request identity while
terminal proof still binds message/preparation/publication. A2-004 exposed final
acceptance assuming command results were JSON root artifacts, although production
stores the typed result under a named-check receipt. Both byte verification and
the locked persistence recheck now follow the exact verified parent closure.
The stable variant reaches human PR approval. The drift variant rejects the
candidate, preserves the human edit and reserves exactly one future repair attempt
without an active execution lease. Each run retains 12 actual provider attempts
and 13 controlled receipts; denied foreign/barrier/reviewer writes add no effect.

Verification: **6 composed A1/A2/A6 cases passed in 154.94s**, **97 affected
PostgreSQL checks in 347.21s**, **4 commit-binding checks in 20.30s**, and **96
unit checks in 0.86s**. Eight-file Ruff/format, three-source mypy and whitespace
checks pass. Manifest self-review corrected an inherited restart label: the two
new catalog cases recreate the worker before review selection, not between a
stopped handoff and application. Only those two manifests were regenerated
(**2 checks passed in 91.90s**); the other matching checks are reused with explicit
input exclusions. Six current manifests retain hash-checked artifacts and the
actual checkpoint diff. `.llm-output/root-catalog-acceptance.json` binds their
candidate, commands, source hashes, findings and self-review. Superseded outputs
are preserved. Whole A2 live proof, A3 and the remaining release scenarios remain
incomplete. No live call, full CI, deployment, production migration, PR/merge or
independent AI gate occurred; official subscription routes remain unregistered.

The shared-writer snapshot increment is accepted by primary self-review.
The composed `split-catalog` case exposed A2-001: two live,
disjoint writers could edit but neither could capture its required handoff
snapshot. The scheduler now recognizes an exact attempt/lease-bound snapshot
read. Its durable exclusive effect freezes new mutations; all pending/uncertain
effects and reconciling peer writers still prevent capture. Named checks and
commits keep their existing exclusivity. A2-002 corrects the scripted adapter's
shared-list telemetry counter so concurrent attempts count their own receipts.
Explicit test capacity and admission barriers model normal poller retry of brief
`SKIP LOCKED` contention; no production retry or capacity policy was relaxed.
Verification: **4 composed A1/A2/A6 checks passed in 60.23s** and **107 affected
PostgreSQL checks passed in 224.76s**, including 11 new snapshot boundaries.
Four-file Ruff/format and one-source mypy pass. The initial affected run passed
its assertions but failed 11 database teardowns after lint removed a test cleanup
fixture import. Restoring that import and rerunning the affected suite resolved
the failures; the migration's evidence-preservation guard remains intact. The
four composed cases' inputs are unchanged by that independent fixture repair,
so their passing evidence is reused. Four retained manifests and exact candidate,
command and source hashes are recorded in
`.llm-output/root-writer-snapshot-acceptance.json`. Whole A2/A3
check/checkpoint/drift scenarios remain incomplete. No live call, full CI,
release action or independent AI gate occurred.

The deterministic retained-service A9 increment is accepted by primary self-review.
Four composed PostgreSQL cases cover plan approval, an unfinished developer with
a completed controlled write, and reviewed validation with pending pause/cancel.
They freeze 19 evidence tables at the final v0.1 schema (`20260909_0006`), retain
actual artifact bytes and compare every original column after additive and repeated
head upgrade. Recreated authenticated API/worker components preserve plan/PR
authority, same-key control replay and stale-version conflicts. Unknown provider
completion produces one startup intervention; its terminal write receipt replays
without another mutation or replacement provider. Real local validation and a
scripted reviewer remain distinct from human PR/merge authorization.

Verification: **4 composed checks passed in 73.15s**, **41 affected PostgreSQL
checks in 111.03s**, and **7 approval-origin unit checks in 0.55s**. Six-file
Ruff/format and the workspace whitespace check pass. No production source changed
in this increment; prior matching runtime evidence is reused. Four retained
manifests bind the original/current row snapshots, safe operator responses and
hash-checked artifacts. `.llm-output/root-legacy-upgrade-acceptance.json` records
the exact candidate, commands, self-review and closed A9 coverage gaps. Fixture
repairs corrected preparation-only inspection, command lanes, finding/report
identity and summary/projection endpoint assumptions; no runtime guard was relaxed.
The fixture uses current legacy services, scripted providers, trusted-host checks
and disposable databases. Historical v0.1 binary execution, browser sessions,
mixed-version deployment, production rollback and whole A9 release acceptance
remain unproved. No live model call, full CI, production migration, deployment,
commit, PR, merge or independent AI gate occurred.

The worker-registration diagnostic increment is accepted by primary self-review.
Migration 0021 stores each worker boot's exact registry, monotone report time and
terminal stop in PostgreSQL. Production startup publishes before polling, renews
outside execution slots and reports shutdown after owned work/resources drain.
The profile page, task-inspector link, authenticated API and local CLI distinguish
current, stale, stopped and absent reports with explicit pagination. Registration
confers no provider, quota, billing or tool authority; official routes remain
unregistered until their capability prerequisites are proved.

Current verification: **20 PostgreSQL checks passed in 83.89s**, including real
composed registration without extra attempts, A1/A6 regressions, admission/budget
checks, concurrent renewal/stop and migration upgrade/downgrade/re-upgrade.
**87 affected unit/API/CLI checks passed in 4.44s** and **14 operator component
checks in 2.40s**. Eleven-source mypy, 19-file Ruff/format, TypeScript, four-file
ESLint and frozen API/type checks passed with unchanged inputs. Self-review found
and repaired stop-before-initial-report revival; startup publication was first
proved missing by a focused test. The logging test restores the standalone logger
precondition after in-process Alembic disables it. Exact commands, candidate and
RS-001/002 findings are retained in `.llm-output/root-runtime-status-acceptance.json`.
The new migration ran only on guarded disposable test databases. No live provider,
full CI, browser session, deployment, commit, PR or merge ran. This is diagnostic
setup visibility, not complete official-client readiness or whole A8 acceptance.

The composed A6 quota increment is accepted by primary self-review. A real partial
managed-worktree write survives confirmed scripted exhaustion and worker restart.
An exactly approved Luna fallback completes the same child through controlled
tools; another restart applies its stopped handoff, and the unchanged Astra primary
accepts the source-bound outcome. Without that approval, two fresh concurrent
pollers skip the blocked task, preserving its files and consuming no additional
attempt, repair or execution slot. The operator query retains the requested route,
selected fallback and blocked Google pool.

Both A6 variants and the refactored A1 regression passed: **3 PostgreSQL composed
checks in 54.13s**, with three-file Ruff/format passing and unchanged input hashes.
Production source did not change; the matching source evidence from the counter
increment below is reused. `.llm-output/root-counter-quota-acceptance.json` binds
these checks, self-review and three locally hash-checked manifests. A6's approved
worker consumes two attempts, one named check and zero scheduler repair debits;
the whole run consumes five attempts including planning, delegation and primary
acceptance. The deferred run consumes three, then none on suppressed polls. These
are deterministic provider/process proofs with explicitly trusted-host commands;
they do not establish official-client readiness, live billing/isolation, Docker
conformance or whole A6 completion. No live call, full CI or release action occurred.

The counter-service execution and handoff-history increment is accepted by primary
self-review. Real project/run creation, evidence-bound test-operator plan approval,
managed Git preparation and the composed subscription worker execute A1's wrong
patch, failed check, repair, passing check and final snapshot through controlled
tools. One worker attempt owns that sequence. Recreated worker composition applies
the stopped handoff exactly once without another provider invocation. Failed-check
history remains historical; the final successful checks must match the final
snapshot. Version-3 subscription command receipts expose verified duration and
bounded redacted output while older receipt replay stays stable. The frozen grader
recognizes the subscription receipt versions and validates every declared case.

Current evidence: **105 PostgreSQL integration checks passed in 374.28s**, **41
usage/projection checks in 124.50s**, and **238 selected unit/protocol/tool checks
in 23.83s**; six-source mypy and 13-file Ruff/format passed with unchanged inputs.
`.llm-output/root-counter-acceptance.json` binds the candidate, source self-review,
resolved A1-001/002/003 findings and exact commands. The retained A1 manifest and
13 content-addressed artifacts are hash-checked locally. They record
three provider attempts (plan, delegate, worker), two named checks, one repair
within the worker attempt, and zero scheduler repair debits because no retry was
scheduled. Provider/process proof is scripted and command execution is explicitly
trusted-host. This does not establish live capability, Docker proof or completion
of whole A1/v0.2. No live call, full CI, new migration, production registration,
commit, PR or merge occurred; no independent AI review/gate is claimed.

The bounded usage assessment is accepted by primary self-review. The existing
authenticated query and refreshing usage views now expose primary measured shares
with full coverage, actual admitted primary turns, causal recorded wait intervals,
repair debits, frozen fallback attribution and source-bound outcomes. Scalar totals
are independent of complete-group pagination; more than 100 groups remain bounded.
Missing primary measurements remain null, known zero stays zero, and quota deferral
spends no extra repair. Historical result/application/launch lineage distinguishes
completed handoffs and primary task acceptance from provider records and human
approval. The existing inspector retains access to attempts and evidence.

Current verification: 41 focused/affected PostgreSQL tests passed in 108.35s,
55 API/schema checks passed in 6.10s, and 12 operator component checks passed in
2.13s. Eight-source mypy, ten-file Ruff/format, TypeScript, four-file ESLint and
frozen OpenAPI/type checks pass, with unchanged input hashes for every final check.
New regressions covered missing primary telemetry, normal applied waits without
application payloads, causal continuation and acceptance ordering, rehashed
mismatched receipts, quota/repair accounting, bounded history and null rendering.
UA-001 through UA-007 are resolved. Exact commands, inputs and limits are retained
in `.llm-output/root-usage-assessment-acceptance.json`. This is primary self-review
and offline verification; no independent AI gate, live application call, full CI,
new migration or release action is claimed. Whole slice 7 and A8 remain incomplete.

The Google transport increment is accepted by primary self-review. The gateway
uses pinned ACP v1, a private bounded MCP proxy, the supported trusted system-file
channel and exact route/capability checks. Cancellation drains admitted callbacks
and stops the process tree before return. Unknown token measurements stay null;
generic ACP 429 is throttling, never confirmed quota exhaustion. Composed-worker
PostgreSQL tests retain the selected primary, settle actual fake-client launch
receipts and usage once, and prove that throttling creates no quota observation.

Current verification: 292 adapter/protocol/supervisor checks passed in 16.86s
(one POSIX-only skip on Windows), 38 affected PostgreSQL checks passed in 91.28s,
and six-source mypy plus 13-file Ruff/format passed. Shared supervisor regressions
cover OS-sourced Windows `SystemRoot`, valid startup-failure terminal proofs and
repeated cancellation during startup settlement. The PostgreSQL evidence is reused
after an explanatory lint comment; source AST equivalence and unchanged executed
fixtures are recorded. New tests initially exposed the lifecycle/metadata failures;
two PostgreSQL test assertions used incorrect repository names and were corrected.
`.llm-output/root-gemini-transport-acceptance.json` binds commands, inputs and
findings. This is offline verification and primary self-review, with no independent
AI gate, live model call, full CI, migration or release action. Official routes
remain unregistered until account/model/isolation/billing evidence is established.

The primary reassignment increment is accepted by primary self-review: 26 focused
PostgreSQL cases passed in 124.16s, and 176 separate affected cases passed in 617.64s
(202 distinct cases). All 402 adapter/protocol/worker/domain checks passed in
14.18s. Eleven-source mypy, 14-file Ruff/format and frozen API/type checks pass.
New decisions name the exact stopped child attempt and observed task version;
application preserves files, ownership, cumulative usage, the requested route and
the primary. The same reconciliation transaction queues only an approved specialist
binding and yields the coordinator until its child completes or requests scope.
Replay, concurrent application, rollback, stale sources, operator flags, uncertain
launches, billing/auth boundaries and quota suppression are covered. Legacy unbound
records stay readable without new authority. Primary self-review is the review
method for this increment; no independent AI gate is claimed. Matching input hashes,
commands, initial regression and fixture repairs are recorded in
`.llm-output/root-reassignment-acceptance.json`. No new migration, live call,
full CI or release action was performed. Whole A6 remains due.

The bounded quota addition is accepted: PostgreSQL shares exhaustion and probe
ownership by provider/account/pool, the scheduler suppresses ineligible launches
without spending budgets or capacity, approved fallback retains its exact frozen
route, and operator surfaces distinguish unknown telemetry, resets and cooldowns.
All material integrated-review findings were resolved. The separate Sol-high gate
passed the probe-recovery race repair; final affected PostgreSQL checks passed
87 tests. `.llm-output/root-quota-acceptance.json` records exact candidate inputs,
329 unit/operator/adapter/worker checks, five frontend checks and reuse limits.
The installed development framework is a behavioral reference only; Forge owns
its runtime implementation and PostgreSQL state.

Current recovery integration supports stopped pending decisions and final
validation before admission, after admission and after publication. Root repaired
role/phase and immutable candidate/receipt boundaries, and supplied the existing
final-acceptance dispatcher in production composition. The accepted 201-case recovery
batch passed in 594.86s. That acceptance describes the earlier candidate; individual
task-stop recovery now changes shared run, scheduling and decision guards. The fresh
affected PostgreSQL batch passed 432 tests in 1193.40s with matching inputs,
including the quota cases. The separate Sol-high correctness
gate and independent integrated Astra-low review found no material defect in the
same earlier 12-path candidate. Its source mypy and scoped Ruff/format checks passed.
Exact inputs and commands are recorded in
`.llm-output/root-resume-affected.json` and the integrated review snapshot.

The later task-control projection/quota-inspection regression passed eight
PostgreSQL tests in 19.20s. Further changes now support settled task history,
quota results arriving during task/run pause, causal scope-response versions,
Windows path normalization and evidence-bound capacity exemption. The 432-case
batch predates these changes. The new focused sets passed 15, three and eight
PostgreSQL cases respectively; these overlap the final affected rerun. The first
affected run passed 174 cases and failed two conflict-message assertions; their
wording was restored and both passed in the eight-case repair run. Final affected
verification passed: 218 passed in 646.49s (0:10:46). Current adapter/operator checks passed 122 tests,
the inspector components passed 13, and scoped Python/TypeScript/API checks passed.

The user has explicitly replaced development delegation and separate AI gates
with implementation, verification and self-review by the selected Astra primary
for the remainder of v0.2. All three active delegates were interrupted and
confirmed stopped; no delegated check process remained. Partial drafts and useful
outputs were preserved in `.llm-output/root-workflow-handover/`. The Google
transport is now accepted as the offline increment above; its official route
remains unregistered. The preserved usage draft was completed by the primary and
integrated with the bounded query and UI after new regression coverage. The
assessment acceptance above closes UA-001 through UA-007 without an agent gate.

An earlier Astra-low independent review of the task-control
batch found TC-001 (Windows path comparison) and TC-002 (unproved paused capacity
exemption). Both were reproduced, repaired and explicitly closed in the recheck,
with no new material findings. Claude's local framework check reported confirmed
exhaustion until 13 September 01:00 UTC; no Claude review call was made. The earlier
host thread-limit rejection is historical. The subsequent Sol-high quota/control
gate was interrupted without a verdict. Its separate-agent requirement is now
waived by the user's workflow override; this is not a passed independent gate.
Full browser/worker and whole v0.2 acceptance remain due.

The official Claude auth-status metadata command confirms a Pro subscription login
but exposes no usage-credit enable/disable field. Credentials and identity values
were discarded. The first production invocation-loop regression was reproduced
and repaired: startup polls composed subscription executors and drains their
settlement before shared resources close, including repeated cancellation.
Composition binds the exact registered route, controlled broker, durable client
lifecycle and existing candidate/acceptance/recovery services. Empty registries
do not open admission transactions. Missing routes and exhausted local budgets
cannot starve other runs; safe fallback preserves frozen approval and billing.
Attempt ceilings fit remaining task/run budgets atomically, including repair-slot
transfer. A fake-provider PostgreSQL test reaches the human plan gate with a real
controlled read, settled launch evidence, usage debit and late-callback denial.

Current verification: 212 worker/API/factory checks passed in 6.30s; the initial
PostgreSQL batch passed 135 and failed one outdated oversized-reservation
expectation. Its rollback test now injects a failure after repair-slot transfer
and before commit, then proves clean restart; all 11 cases in that module passed
in 20.93s. The unchanged 135 passing cases and repaired module cover 136 distinct
PostgreSQL cases, without counting overlapping reruns twice. Fourteen-source
mypy, 23-file Ruff/format, six usage-view checks, TypeScript, scoped ESLint and
frozen OpenAPI/type checks passed. Source/input identity and reuse are recorded in
`.llm-output/root-production-acceptance.json` and its referenced raw check logs.
Review for this increment was **primary self-review**, not an independent gate.

Self-review also found and repaired two regressions in the preserved usage draft:
the optional-assessment compatibility branch bypassed response validation and
failed to serialize durable UUIDs; the UI treated an omitted share as numeric.
The existing measured projection now retains validation and its two-query bound.
That earlier repair kept the assessment disconnected; the subsequent accepted
assessment above supplies the new bounded aggregation and operator coverage.

A trusted source of exact
client/account/isolation/billing capability evidence remains missing. The updated
client-capability matrix identifies those concrete gaps; offline gateways and
recovery services alone do not establish an operational production route.

Human publication gates, failed-validation repair, bounded remote PR repair and
controlled base adoption have local evidence in the later checkpoints. They do
not establish full release acceptance. No required provider is enabled from
synthetic capability claims, and the selected primary and billing/approval
boundaries remain fixed. The earlier dated checkpoints describe historical
intermediate states; this section summarizes the current implementation.

## Earlier repository and design work

- Repository hygiene: `.superpowers/` scratch reports are untracked, with all18
  local copies retained; `docs/superpowers/` specifications/plans remain tracked.
  `.llm-output/` and `.pytest-basetemp/` now have shared ignore rules rather than
  checkout-only excludes. Coverage output and OS metadata are also ignored.
  `git check-ignore`, tracked-path checks and local-copy counts passed.
- Development framework: removed the stale duplicated routing policy; use the
  installed policy, proportional review, disjoint ownership, self-review and
  credible matching evidence. Runtime agent permissions remain separate.
- PR3 P2 local startup configuration: root `.env` web settings are not passed to
  the Next child under v0.1. Default-port acceptance passed, but custom origin
  configuration is affected. Repaired after Gemini implementation and primary
  integration: resolve configuration through the provisioned Python environment,
  preserve OS-over-dotenv precedence, pass only web settings from root dotenv,
  and bind Next to the configured loopback host and port. Invalid configuration
  errors omit raw values. Tests execute the actual fixed configuration helper
  while faking server processes; 49 focused tests passed. No live server/browser
  check or full v0.2 CI was run. This bounded repair uses focused checks and
  self-review rather than a separate review round.
- [Subscription runtime design](v0.2-subscription-runtime.md) maps the existing
  gateway/tool/usage/recovery boundaries and official client capability findings.
  After primary and independent Opus 5 review of `6d87fe0`, the operator confirmed
  the product decisions: Astra low initially orchestrates Gemini Flash 3.8 workers
  and specialist review/planning; small primary edits remain allowed. Each run
  owns one worktree shared by its workers, with no attempt-duration repository
  lock and three active workers per run by default. Only the primary delegates;
  workers own ordinary repairs. Integration/checkpoint barriers protect stable
  candidates within that worktree. Review remains proportional within project
  policy, with no additional mandatory runtime review categories requested.
  The plan now specifies durable delegation, profile pinning, allowance-only
  subscription mode, attempt/tool identity, cancellation/reconciliation, migrations,
  eight delivery slices and nine acceptance scenarios. Usage/outcome visibility
  supports a possible future operator-selected Sol 5.6 high primary; no automatic
  substitution or comparison runs are authorized by that preference.
  At that checkpoint, subscription runtime adapters were not implemented.
  Subsequent implementation status is summarized above and evidenced below.
  Exact client/model/billing/tool feasibility remains a gate, and all three
  required providers need authorized live proof before full v0.2 completion.

## Continuing constraints

No further full CI dispatch or PR-triggered full run is authorized without
specific user approval; use focused checks and remote checkpoint pushes. Future
PR merges require fresh immediate authorization. Never import development CLI
bypass settings into Forge runtime agents or silently fall back to paid APIs.
Secrets remain outside source and logs; retain all managed resources unless an
explicit teardown is requested.

## v0.2 implementation continuation — 10 September 2026

- Revalidated branch `forge/v0-2`, managed worktree registration and HEAD
  `6d87fe0`. Both revised design/progress edits were present and preserved.
  Provisioned tools report Python 3.14.5 and Node.js 24.20.0.
- Capability investigation is in progress against official documentation and
  installed client help/schema, without application model calls. Support and
  allowance-only billing remain unproven until the required evidence is obtained.
- Acceptance fixtures for A1–A9 are being frozen before behavior implementation.
  The implementation map is tracing legacy roles, durable command leasing,
  operation receipts, worktree locks and production/evaluation composition.
- Current source confirms the migration targets: three closed agent roles,
  run-command lease serialization, and legacy non-null token counters. New
  subscription records must preserve legacy meanings instead of reinterpreting
  missing measurements as zero or converting historical runs.
- No v0.2 delivery slice or acceptance scenario is claimed complete. No full CI,
  PR-triggered run, live application call or merge has been performed in this
  continuation. Those scoped approvals remain required when operations are ready.

### Current bounded work and evidence

- Frozen [acceptance fixtures](v0.2-acceptance-fixtures.md) now specify A1–A9,
  fault hooks, exact check commands and deterministic versus live proof.
  Primary review corrected grader paths, checkpoint/tool classification and
  the harness protocol; live review must report actual findings, never scripted
  findings presented as real. Documentation diff checks passed.
- [Client matrix](v0.2-client-capabilities.md) records installed help/schema and
  official documentation. Exact account models and full native-tool suppression
  remain unproven. The first metadata-only Codex probe timed out; its five-second
  read loop is being repaired before drawing a capability conclusion. There
  have been no application model invocations.
- [Upgrade contract](v0.2-upgrade.md) specifies drained deployment, retained legacy
  evidence and rollback limits before migrations. Actual upgrade tests remain due.
- Gemini owns the additive domain contract foundation via
  `.llm-output/v02-domain-contract.json`. Terra owns only the new client-process
  supervisor modules/tests; neither owns legacy integration files.
- Supervisor acceptance is withheld after two incomplete handoffs. The primary
  reassessment requires an interactive transport, suspended Windows launch and
  assignment before execution, reliable identity, and discriminating lifecycle
  tests. Material findings: CP1 Windows launch race/identity; CP2 unbounded stdout;
  CP3 launch-hook and stdin-timeout cleanup gaps; CP4 closed stdin prevents dynamic
  callbacks; CP5 descendant cleanup after parent exit; CP6 mutable environment and
  nonfinite limits. The same worker retains repair ownership. Its initial five
  passing tests are evidence only for those narrow checks, not slice-4 acceptance.

### Foundation checkpoint (not delivery acceptance)

- Codex metadata discovery is now proven by the corrected Python probe:
  `.llm-output/codex-metadata/python-result.json` reports ChatGPT auth and the
  exact Astra/Sol/Terra/Luna catalog IDs and supported requested efforts. The
  earlier PowerShell failures were probe defects. No model turn was started.
- `environments=[]` in the installed thread/turn schema provides a concrete
  environmentless dynamic-tool isolation alternative. Native tool suppression,
  inherited configuration and allowance-only billing still need conformance proof.
- Domain foundation and hardened versioned codec: 71 focused and 196 domain tests
  reported passing; lint/type/format evidence is in
  `.llm-output/domain-repairs/codec-repair-results.txt`. This does not establish
  scheduling, broker admission, profile UI or legacy runtime integration.
- Frozen fixture builders/graders implemented by Gemini with 9 focused tests;
  actual logs are in `.llm-output/subscription-fixtures/`. Scenario execution
  through Forge remains outstanding.
- Primary took ownership of the incomplete supervisor after repeated partial
  worker handoffs. The native Windows helper now creates explicit inherited
  pipes, a kill-on-close Job and a suspended child, assigns it before resume,
  and obtains a creation-time identity. Interactive transport uses an absolute
  lifetime deadline and bounded streams. Fifteen focused Windows tests, Ruff
  and mypy passed; actual red/green logs are under `.llm-output/client-supervisor/`.
  Scoped gate CG-WIN-01 through CG-WIN-05 is being rechecked. Linux execution and
  durable worker/broker integration are not yet proven.
- Persistence has 7 passing real PostgreSQL checks including retained-schema0007
  data and downgrade refusal. Primary found a remaining replay-refund defect:
  a caller boolean cannot establish spent-budget settlement. Source and test
  owners are adding durable reservation identity/immutable settlement and required
  run/task authority before the scheduler can consume the repository.
- No full CI, live application model call, PR publication or merge has occurred.
  All eight delivery slices and nine scenarios remain tracked by the revised plan;
  no slice is claimed complete solely because its foundation tests pass.
- Supervisor follow-up repaired receipt/result frame equality and bounded terminal
  persistence. A lost acknowledgement now retains typed reconciliation inputs.
  Strict JSON parsing rejects duplicate keys and nonfinite constants. Twenty-one
  focused Windows tests pass (`client-supervisor/json-green.log`); source mypy passes.
  A network-disabled, read-only Docker Python 3.14.2 smoke check also passed JSONL,
  malformed/cumulative output, interactive transport and idle/deadline cleanup on
  Linux (`client-supervisor/linux-smoke.log`). Durable broker integration remains due.
- Persistence source repairs are assigned through the authorized Gemini CLI after
  the host rejected resuming the stopped native source worker (agent-thread limit).
  The independent test worker retains test ownership. Remaining acceptance gaps:
  NULL run-pool uniqueness, same-run composite foreign keys, concurrent initial
  replay serialization, prospective DAG validation and proper strict typing.
- Independent-worktree lock change: retained managed access now locks its validated
  registration rather than the common repository directory. Focused Windows evidence
  is 140 path tests and 312 affected tool/process tests (30 platform skips in the
  latter), plus lint/type checks, under `.llm-output/worktree-lock-scope/`.
  Shared metadata/ref operations retain their short repository lock. POSIX proof
  remains outstanding. The same worker now owns controlled delete/rename additions
  through the existing retained-capability and durable tool-operation boundaries.
- Latest supervisor result is 22 focused tests after adding numeric overflow
  rejection (`client-supervisor/float-green.log`). Linux descendant cleanup after
  root exit passed (`client-supervisor/linux-descendant.log`).
- Supervisor self-review added Windows startup resource-failure regressions and
  POSIX identity-query failure cleanup. Windows: 24 focused tests passed after
  attribute-list/descriptor repairs (`client-supervisor/resources-green.log`).
  Linux: 21 passed, 4 Windows-only skips using the actual test module in a
  disposable Python 3.14.2 container (`client-supervisor/linux-suite.log`), including
  observed red/green identity-failure cleanup. Container source was read-only,
  tests had no network, and output records source/test SHA-256 identities.
- Linux managed access: 7 focused tests passed for independent locks, proof/link
  validation and digest-fenced delete/rename primitives. Evidence and candidate
  hashes: `.llm-output/worktree-lock-scope/linux-managed-access.log`. This does not
  establish durable tool-service delete/rename recovery, which is still in progress.
- The Gemini persistence repair timed out after 20 minutes. Its local process tree
  was stopped; the PID has since been reused by an unrelated WebView and was not
  targeted. The unresolved provider record remains preserved. Fresh Luna fallback
  owns the source and tests now. The independent test handoff did not add the
  requested concurrent-init/composite-FK negatives, so those remain explicit
  fallback acceptance criteria; old seven-test evidence is not final acceptance.
- Shared request-time factory and production/evaluation keyless composition are
  assigned as a bounded integration task. Actual subscription adapters, scheduler,
  broker, operator flow and scenario/release evidence remain outstanding.

### Integrated foundation update (10 September 2026)

- Persistence repairs now pass 10 real PostgreSQL tests, including concurrent
  missing-pool initialization, NULL-safe uniqueness, same-run composite foreign
  keys with retained parent rows, durable reservation settlement and immutable
  preferred-route binding. Latest evidence:
  `.llm-output/persistence-repair-cli/binding-green.log`; scoped Ruff/mypy passed.
  The earlier seven-test checkpoint and outstanding repair list are superseded.
- Mutation capabilities now follow write permission for every specialist purpose:
  72 focused subscription-domain tests and 197 domain tests passed after an
  observed failing regression. Evidence: `.llm-output/domain-repairs/mutation-green.log`.
  Durable delete/rename service and recovery integration are still being completed.
- Scheduler migration 0009 and a real-PostgreSQL fake-worker vertical test pass.
  Parent yield/continuation, scope reservations, candidate barriers and expired
  lease reconciliation remain assigned acceptance criteria; this is not yet slice 3.
- Shared runtime factory focused checks passed (45 tests). Legacy ADK/delivery
  regressions resumed once scheduler imports stabilized. Subscription client
  adapters and task-attempt broker integration remain outstanding.
- Official Gemini CLI 0.59.0 was installed locally for capability inspection,
  with package lock/integrity retained under `.llm-output/gemini-client/`.
  Supported `billing.overageStrategy="never"` and explicit `tools.core=[]`
  provide concrete billing/tool controls to verify. Exact requested 3.8 model
  availability, isolated authentication and live conformance remain unproven.
  No model call was made. Codex quota metadata is a snapshot, not an enforced
  no-overage cap; its allowance-only admission boundary remains unresolved.
- Integration finding RT-TOOLS-01: the six delivery regressions were caused by
  `DevelopmentService` retaining a static developer tool list without delete/rename,
  while bound admission used the expanded capability matrix. The primary now derives
  request tools from that shared matrix; the affected ADK/delivery batch is running.
  Diagnosis: `.llm-output/runtime-factory/delivery-idle-state.json` and
  `delivery-bound-error.txt`. This was not a scheduler dispatch defect.
- File-safety finding MUT-RENAME-01: Linux rename used a separate absence check and
  overwriting rename. An observed failing race test proved a newly created destination
  could be replaced. The primary changed this to atomic `renameat2(RENAME_NOREPLACE)`;
  unavailable support fails closed. Eight actual Linux path/lock/mutation tests pass,
  with hashes in `.llm-output/file-mutations/rename-race-green.log`; Ruff/mypy passed.
  Source: https://man7.org/linux/man-pages/man2/rename.2.html .
- Neutral typed subscription gateway and deterministic Codex protocol adapter are
  assigned under `.llm-output/v02-codex-adapter-contract.json`. This work uses the
  supervisor and fake clients; it does not authorize a live model call or claim
  that current allowance-only billing/tool isolation has passed conformance.
- Open finding MUT-SOURCE-02: source digest inspection closes its handle before
  delete/rename opens the effect handle. A source change in that interval is not
  rechecked on the effect handle. Windows can verify while holding the exclusive
  mutation handle; POSIX name/content drift requires a deliberate retained-identity
  and reconciliation design. This is separate from the repaired destination race;
  whole-stack mutation acceptance remains withheld until resolved and tested.
- MUT-SOURCE-02 Windows repair: digest and byte count are now rechecked on the
  exclusive mutation handle before delete/rename; the handle includes read access.
  Both injected source-change tests failed before repair. Full Windows path module:
  143 passed, 25 platform skips (`.llm-output/file-mutations/windows-source-green.log`),
  plus scoped Ruff/mypy. POSIX source precondition races remain open; this does not
  claim atomic compare-and-delete semantics on Linux.
- Factory finding RT-PIN-02: resolved gateways previously allowed later requests
  to name another provider/model. Both negative tests reproduced this. The factory
  now wraps execution with the frozen route check; legacy evaluation resolves the
  actual request model rather than a fabricated placeholder. 47 focused tests pass
  (`runtime-factory/route-drift-green.log`); a new per-request evaluation test and
  affected integration rerun are in progress. Scoped source Ruff/mypy pass.
- Codex first handoff rejected (CX1-CX7): fake-only invented callback messages,
  incomplete official launch/tool/output fields, missing exact effort/identity and
  interruption checks, and insufficiently typed/bounded neutral result/context.
  The worker retains repair ownership against installed official JSON schemas.
  Its initial 29-test count (mostly supervisor tests) is not adapter acceptance.

### Integration and protocol checkpoint

- Candidate remains `6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db` plus the
  uncommitted scoped changes in this managed worktree. No live application calls,
  full CI, PR or release action has been performed.
- RT-TOOLS-01 and RT-PIN-02 affected integration checks now pass: 43 delivery,
  planning and per-request evaluation tests in 331.18 seconds; evidence is
  `.llm-output/runtime-factory/integration-pins-green.log`.
- After repeated incomplete Codex handoffs, the primary took production source
  ownership and an independent Sol test worker owns the two protocol test modules.
  The revised adapter uses official callback/turn identities, waits for successful
  turn completion, pins model and effort, decodes typed bounded decisions and retains
  measured interrupted usage. The current 28 deterministic tests pass; additional
  notification-order and response-status cases are under test. This is not live
  client conformance or a completed Codex vertical slice.
- Supervisor revocation runs before process termination and is bounded even if
  its acknowledgment fails. Real-process checks pass on Windows (27 passed,
  1 platform skip) and Linux (24 passed, 4 platform skips), with logs under
  `.llm-output/client-supervisor/` named `revocation-green.log` and
  `linux-revocation-green.log`. The latter records relevant source/test hashes.
- POSIX source mutation repair is assigned to a fresh worker: durable private
  staging identity, no-replace staging, source verification and retained uncertain
  state. Prior Windows repair is preserved. Full mutation acceptance remains open.
- Scheduler foundation handoff was not accepted as slice completion. A fresh
  worker owns missing capacity/control/reconciliation and multi-session PostgreSQL
  tests. Capacity is versioned deployment configuration, bound at run admission;
  provider identity comes from the frozen task route, never caller task arguments.
  Global/provider ceilings apply across runs; default global/provider/run limits
  were initially one; corrected to three to match the approved plan (see below). Waiting parents release capacity.
- Shared factory now admits typed subscription invocations through explicit
  subscription adapters, with exact invocation and result-attempt checks and no
  legacy adapter fallback. Two focused tests failed before this addition;
  50 factory/composition/evaluation tests now pass, with scoped Ruff/mypy clean.
  Evidence: `.llm-output/runtime-factory/subscription-red.log` and
  `subscription-green.log`. Production broker composition remains outstanding.
- Factory cancellation identity checks also reject a foreign attempt's attached
  result while retaining cancellation semantics. The added negative assertion
  failed first; all seven factory cases pass after repair
  (`interrupted-identity-red.log`, `interrupted-identity-green.log`).
- POSIX handoff review opened MUT-STAGE-01: deterministic staging still used a
  normal repository pathname, leaving replacement possible before final unlink or
  rename. The owner is repairing this with private retained directory authority
  and controlled-tool denial. Its 12 Linux cases and 143 Windows passes are partial
  evidence, not whole-stack acceptance. The PG mutation run's 16 test bodies passed
  but migration teardown failed while migration 0009 changed; the orphan-recovery
  run lacks a terminal summary. Both require current terminal evidence after the
  staging repair and a stable migration window.
- The Codex test follow-up was stopped after it returned no new cases. Root
  took over both test modules after confirmation that no commands remained.
  Six new assertions failed as expected: post-response turn-start notification,
  ID-bearing completion request, missing broker success status, and three foreign
  handoff identity cases. The repairs now pass 41 combined protocol/factory cases;
  scoped Ruff and strict source mypy pass. Actual retained evidence is
  `.llm-output/client-supervisor/codex-followup-red.log` and
  `codex-followup-green.log`; these supersede ambiguous worker count reports.
- Profile API implementation is assigned under
  `.llm-output/v02-profile-api-contract.md`: immutable versions, expected-version
  selection, existing operator session/CSRF protections, mutation receipts/audit,
  and real PostgreSQL concurrency checks. No configured route becomes advertised
  as verified through profile creation alone.
- Canonical tool argument schemas now live in
  `application/ports/tool_schemas.py`; both the controlled effect service and
  subscription protocol consume the same immutable definitions. All 34 actual
  Codex protocol tests pass on Linux Python 3.14.2 in an offline, read-only source
  container (`.llm-output/client-supervisor/linux-codex-green.log`, with scoped
  hashes) and on Windows (`windows-codex-green.log`). These are supervised fake
  client checks; production broker and authorized live proof remain outstanding.
- Profile API accepted for its bounded scope: 23 API/service/OpenAPI/PostgreSQL
  tests pass in `.llm-output/subscription-profiles/final-verified.log`, including
  immutable versions, concurrent append/selection and frozen run configuration.
  Operator profile UI is now delegated; client verification remains separate.
- Codex structured output now uses a closed root object with a nested decision
  union; all object properties are required and additional properties forbidden.
  This matches the pinned client strict-output path. The two new schema tests
  failed first; all 36 protocol cases pass on Windows and offline Linux
  (`schema-green.log`, `linux-schema-green.log` under client-supervisor).
- Primary route substitution is rejected even when a caller omits the primary
  binding flag. Ephemeral broker authorization is excluded from the durable
  record codec, and its token remains hidden in repr. Focused red evidence and
  the 235 passing domain/protocol tests are under `.llm-output/domain-repairs/`.
- POSIX private staging remains under final repair: retain staged inode authority
  through final mutation, and fsync new staging directory entries before moving
  a source. Same bytes at a substituted inode must not establish authority.
  Orphan recovery has terminal evidence: 34 passed in
  `.llm-output/posix-mutation-staging/orphan-green.log`.
- Independent scheduler acceptance opened SCHED-ADV-001: expired task retry was
  allowed while an admitted effect remained unresolved. The source owner is
  repairing this; scheduler slice acceptance remains open. Red evidence:
  `.llm-output/scheduler-adversarial/persistence-fourth-run.log`.
- MUT-STAGE-01 repair is accepted for its scoped implementation: retained staged
  descriptor/inode identity and durable staging-parent creation are verified.
  Final raw logs show 18 Linux cases, 143 Windows passes (35 platform skips),
  16 PostgreSQL tool-write passes, plus matching 34-pass orphan recovery evidence.
  Broker integration now owns the controlled service paths and preserves this work.
- Independent scheduler coverage now passes nine actual PostgreSQL cases after
  SCHED-ADV-001 repair, including simultaneous capacity claims and a second session
  progressing while the durable worker awaits its gateway. Evidence:
  `.llm-output/scheduler-adversarial/focused-post-format.log` and
  `candidate-hashes.txt`. Remaining integration requirements include versioned
  idempotent controls, coalesced parent continuations, bounded worker repairs and
  candidate reopen invalidation; the scheduler slice is not yet complete.
- Read-only integration inspection identified the next cohesive implementation:
  atomic typed-decision application joining attempts, budgets, children and task
  transitions, then production polling and the existing human-gate bridge.
  Plan: `.llm-output/v02-vertical-integration-plan.md`. Budget settlement will use
  distinct reservations and consumed usage: known unused capacity is released,
  unknown bounded usage retains its conservative ceiling, and every admitted
  invocation consumes an attempt unit. No missing telemetry is treated as zero.
- Profile UI Gemini route exited with provider_error (exit 1); the CLI stderr
  reports individual quota exhaustion, but the adapter did not classify it as
  fallback_required. No implementation tokens/changes were produced, and its
  pending claim is resolved. Luna medium now owns the unchanged UI contract.
  Preserved evidence: `.llm-output/agent-framework/94fa5dbe-7cb0-40db-a20c-2beb120b1aed/`.
  No quota cache or unresolved earlier pending state was modified.
- Fixture integration repair: slow-check synchronization moved from private
  `.forge/acceptance` to reserved, ignored `.forge-acceptance`. This avoids giving
  the Docker check access to mutation staging. Fixture version is now
  `v0.2-acceptance-2`; tasks/grading criteria are unchanged. The isolation assertion
  failed first, then all 10 fixture tests passed (18.39s); scoped Ruff/mypy pass.
  Evidence: `.llm-output/fixture-barrier-red.log`, `fixture-barrier-green.log`.
- Actual Linux fixture check under UID/GID 10001 initially failed to create the
  barrier in a host-owned worktree. The harness now prepares its disposable sticky
  directory before admission and fails on incomplete marker cleanup. The same
  no-network/read-only-source container check now passes with `.forge` still 0700.
  Raw evidence: `.llm-output/fixture-linux-red.log`, `fixture-linux-green.log`;
  reproducible harness: `.llm-output/fixture-linux.py`. Final Windows fixture suite:
  `.llm-output/fixture-barrier-final.log` (10 passed).
- Broker handoff was not accepted: the scaffold persists bindings/launch rows but
  still injects an arbitrary effect closure and lacks the discriminated authority
  path through ControlledToolService. The known legacy execution mismatch is the
  core implementation task, not an external prerequisite. Reassessment selects an
  explicit legacy/subscription authority union with XOR lineage persistence and
  shared controlled effect execution/recovery. A fresh bounded owner receives
  `.llm-output/v02-subscription-authority-contract.json`; decision application waits.

- Corrected scheduler defaults from one to three active slots for run/global/
  provider ceilings, matching the approved plan. Explicit configured ceilings and
  frozen run limits remain authoritative. The default assertion failed first;
  four focused domain/service/worker tests pass and all nine independent PG
  scheduler cases pass against the current migration chain (13.84s).
  Evidence: `.llm-output/scheduler-default-{red,green,pg}.log`.

- Profile UI accepted for its bounded scope after repairing mapping preservation,
  complete fallback fields, stale reload, immutable latest-only append, and real
  idempotency retry tests. An editable new-profile starter supplies the agreed
  role preferences without inserting mappings or claiming support. Nine Vitest
  tests, typecheck and lint pass; raw logs under `.llm-output/profile-ui/`.
- Local agent-browser checks with synthetic responses confirm history/append,
  starter models, project selection refresh, home navigation and390px mobile
  layout without overflow/overlays/page errors. Details/screenshots:
  `.llm-output/profile-ui-browser/verification.md`. The browser/server are stopped.
  Missing worktree-local npm dependencies were installed from the existing lock;
  Next instruction-file autogeneration is disabled via agentRules=false, generated
  files preserved in scratch, and next-env restored. Package versions unchanged.
- Profile CLI is assigned separately under `.llm-output/v02-profile-cli-contract.json`.
  It reuses the profile service with an explicit stable local-operator audit actor,
  not a fabricated browser session. Scope is local DB-privileged profile commands,
  bounded input, idempotent mutation and expected versions; no approval commands.
  Current checked Gemini account quota evidence is reused for Luna fallback.


## Broker integration regressions and current authority gap

Root took over the controlled-tool integration after repeated partial handoffs.
The real PostgreSQL broker composition exposed an incorrect UoW property
(`scheduling` versus `scheduler`) and replay UUID regeneration. Both are repaired;
operation identities now derive from the attempt and provider call key. Typed
UoW helpers replace unchecked forwarding. Mutation fences derive from validated
arguments, including both rename paths; named checks and commits request worktree
exclusivity. Existing constructor scopes can only add restrictions.

Scheduler regressions prove that child ownership does not authorize a parent
path and that an active lease can settle an effect; expiry rejects acceptance.
Initial evidence: `scheduler-authority-red.log` (2 expected failures),
`scheduler-authority-green.log` (11 passed), `broker-composition-red.log`,
`broker-composition-replay-green.log` (1 passed), `broker-scope-red.log`
(2 out-of-scope effects incorrectly admitted), and `broker-scope-green.log`
(4 passed), all under `.llm-output/`. The broker composition uses a fake effect;
it is not proof of the real controlled-write path.

The operation repository now accepts a Forge-bound UUID, preserves it on replay,
and rejects an idempotency replay with another explicit UUID. Its focused PG
check passes in `broker-operation-id-green.log`. The real controlled-write test
is now present in `test_subscription_authority_persistence.py`: it removes all
legacy execution/step rows and requires actual subscription lineage, matching
operation identity, an artifact receipt, and one effect on replay. It currently
fails at the expected denied-write assertion (`subscription-write-red.log`).
No full authority acceptance is claimed.

A worker added `SubscriptionRepository.authorize_tool`, but omitted the requested
PG tests and several required checks (latest attempt, exact route, terminal replay
proof). It remains unaccepted and is not yet used by the service. Root owns those
paths again. Its lock order also needs reconciliation with scheduler admission.
The Claude gateway is still an unavailable-only stub. The added standalone Claude
codec has only limited tests and lacks complete initialization, terminal lineage,
and usage handling; it is not an accepted adapter implementation.

Current affected verification: 105 tests pass in 51.13s in
`.llm-output/broker-scope-final-tests.log`; scoped Ruff and six-source mypy
pass in `broker-scope-ruff.log` and `broker-scope-mypy.log`. Candidate hashes
are in `broker-scope-candidate.json`. This includes the existing operation
suite and updates the exhaustive capability test for the already-implemented
developer delete/rename tools. The separate controlled subscription write
test remains intentionally red at the unimplemented service integration.


## Actual controlled subscription effects and receipt recovery

This supersedes the denied-write and untested-proof checkpoint above. Root
integrated subscription read/write/delete/rename authority into the existing
ControlledToolService and repository mutation executor. No legacy execution or
step is fabricated. Durable admission proves current task/attempt, frozen route,
policy, worktree, bound request, active lease generation, stop state and candidate
epoch. Latest-attempt, changed-route and settled-effect-without-terminal-record
regressions were observed failing before repair. Scheduler task/run lock order
now matches effect admission. Authorized calls count against the logical task
across attempts; aggregate run/decision budget accounting remains outstanding.

Mutation intents retain the broker UUID. A versioned authority payload and typed
audit lineage distinguish subscription attempts from legacy executions. Existing
artifact replay checks and writer adapters remain in use. ToolRecoveryService can
finish a subscription mutation's artifact/tool receipt after the operation has
succeeded, including delete/rename, without repeating the effect. Reads now have
the same current authority proof and emit an audit event naming the actual attempt.

Evidence under `.llm-output/`: `subscription-proof-final.log` has 8 PG scenarios;
`subscription-mutations-final.log` has 7 actual controlled-effect/replay/lost-receipt
scenarios; `subscription-controlled-recovery-suite.log` has 55 passing affected
recovery tests in 77.12s; `subscription-write-legacy.log` has 78 passing legacy
write/authorization tests in 22.65s. The latter suites predate only the added
read-event regression and equivalent test formatting. Scoped Ruff and strict
mypy of six sources pass in `subscription-controlled-*-final.log`. Candidate
hashes are in `subscription-controlled-candidate.json`.

Still required in this same slice: subscription named-check and commit admission,
startup reconciliation through the human-gate bridge, atomic broker receipt/effect
settlement, interrupted-effect reconciliation, complete read/evidence surfaces,
and adversarial cancellation/budget/late-output cases. This is substantive local
integration evidence, not completion of slice 4 or A7. No live model call, full CI,
independent integrated review, PR action or release action has been performed.


## Atomic broker receipt settlement

Root reproduced late accepted receipts after a durable stop (two PG failures in
`broker-late-result-red.log`). Scheduler settlement now returns its actual
acceptance decision, and the broker records the receipt in the same transaction
as the effect transition. A successful physical result after revocation produces
a denied provider receipt; existing tool/operation evidence remains intact.
Receipt write failure rolls back the settlement and retains the admitted fence.
The scheduler takes task before effect locks to match admission order.

Concurrent same-call completion also failed before repair (two PG failures in
`broker-concurrent-red.log`). Finalization now detects a previously committed
receipt and revalidates the active lease before returning it. Initial replay
lookup and scheduler admission share a transaction, using the subscription
run/task/binding before scheduler lock order. No UoW spans the effect; the test
now observes open scopes directly instead of relying on a fixed scope count.

Intermediate PG evidence: `broker-atomic-green.log` has 16 passing scenarios for
normal completion, stop, concurrent replay and receipt-write rollback. The final
affected suite additionally covers expiry and persisted denied receipts. This
repairs broker receipt settlement but does not implement the outstanding startup
and interrupted-effect reconciliation coordinator or named-check/commit authority.

Final atomic-settlement verification: `broker-atomic-final-tests.log` records
52 passing affected tests in 68.46s. Scoped Ruff and three-source mypy passed
in `broker-atomic-ruff-final.log` and `broker-atomic-mypy.log`.

## Idempotent effect cleanup and malformed callback tokens

Cancellation cleanup now preserves existing reconciling, settled and rejected
states for the exact effect identity. Repeated cleanup cannot reopen terminal
evidence or mask cancellation merely because another callback already settled;
a mismatched generation still rejects. Four real-PG regressions failed before
repair (`effect-cleanup-red.log`). The affected scheduler, broker and transaction
boundary suite passes 41 tests in 52.02s (`effect-cleanup-green.log`).

The broker compares encoded token bytes so malformed Unicode tokens receive the
same stable denial as other invalid tokens. The non-ASCII case failed with an
uncaught TypeError before repair (`broker-token-red.log`). All 11 callback unit
tests pass, including non-ASCII, lone surrogate and non-string inputs
(`broker-token-final.log`). Scoped Ruff and two-source mypy pass in
`broker-cleanup-ruff.log` and `broker-cleanup-mypy.log`.

Subscription named checks are under integration review. The worker reported
normal/replay, invalid-command and cancellation checks; historical lost-receipt
recovery and saved matching evidence were requested before acceptance. A separate
owner is implementing stale-effect receipt reconciliation and startup wiring.
These checkpoints do not complete the slice or the full v0.2 acceptance target.

The scheduler also now rejects a leased row whose durable expiry is missing.
Its focused PG test first failed because admission did not raise
(`scheduler-missing-expiry-red.log`). Final scheduler/broker/transaction-boundary
verification passes 48 tests in 53.32s (`scheduler-cleanup-final.log`); scoped
Ruff and mypy pass in `scheduler-cleanup-ruff.log` and
`scheduler-cleanup-mypy.log`. No schema changes were needed.

## Claude terminal identity guard

The existing Claude codec accepted foreign or missing session identity,
non-boolean error status, malformed success payloads, and tool callbacks after
its terminal frame. Seven focused regressions failed before repair
(`claude-terminal-red.log`). Terminal receipt now requires the configured session
and a typed outcome; subsequent frames cannot invoke tools. Matching failed
results retain their usage evidence. Twelve tests pass in
`claude-terminal-final.log`; scoped Ruff/mypy pass in
`claude-terminal-ruff.log` and `claude-terminal-mypy.log`.

Official SDK query/control and MCP bridge source was rechecked. The codec still
needs full initialization/MCP negotiation, bounded conversation state and a
supervised gateway. The gateway remains unavailable; this localized protocol
repair is not Claude production conformance or allowance-only billing proof.

Capability follow-up found an official Claude account-level alternative:
disabled usage credits restrict the account to included usage. The capability
report now distinguishes that supported control from the still-missing trusted
preflight verification. It also records that the separate SDK-credit proposal
was paused, avoiding stale search snippets. No account setting or production
admission was changed. See docs/v0.2-client-capabilities.md and its official links.

## Named checks accepted; read surface expanded

Root verified the named-check worker's saved logs and all scoped source hashes.
The affected suite passes 20 tests in 31.78s; its five PG cases include normal
replay, command denial, cancellation, historical receipt recovery after revoked
attempt/lease, and foreign-lineage rejection. Logs and hashes are under
`.llm-output/subscription-named-check/`. No legacy execution identity is fabricated.

Subscription invocation now routes all existing local repository/Git reads
through the shared controlled read adapters: list, read, search, instructions,
status, and diff. Five PG cases failed at admission before the routing change
(`subscription-reads-red.log`). Real controlled reads then exposed nested broker
receipt metadata serialization and tuple/list replay differences. The broker
now uses the existing recursive payload conversion. Twelve actual controlled
read/mutation/recovery cases pass in 20.20s
(`subscription-reads-replay-green.log`), including returned content, instruction
trust markers, candidate identity and one dispatch on replay. The broader
integration suite is running; planning-phase admission and evidence reads remain
part of the human-gate integration, and commit admission is still outstanding.

Root also repaired two strict-mypy issues: separate optional UUID loop binding
in SubscriptionToolAuthorizationContext and explicit legacy execution presence
before startup mutation validation. Scoped Ruff/mypy of four source files pass
in `subscription-reads-ruff.log` / `subscription-reads-mypy.log`.

## Current integrated repair findings

Effect-recovery implementation is not accepted yet. R1 requires actual terminal
OperationIntent evidence (the initial positive test omitted it); R2 requires
stale admitted effects as well as reconciling ones; R3 covers read receipts with
no operation intent; R4 bounds scans and transactions per effect; R5 isolates
malformed terminal records. The same owner is repairing these with actual
controlled-service PG evidence. Initial five synthetic PG passes do not prove
full recovery.

A fresh Claude owner added a supervised gateway, but its handoff lacked gateway
tests/raw execution logs. C1-C6 repairs were returned: supported server registration
and isolation flags; exact initialization correlation; bounded canonical replay
and mandatory tool allowlist; server-local MCP names; nested receipt conversion;
trusted system-context separation. Typed terminal decode, cancellation and
interruption usage also require gateway tests. This is implementation feedback;
no independent integrated review or production conformance is claimed.

Final affected verification for the read/receipt changes: 110 tests pass in
76.68s (`subscription-reads-final-tests.log`), covering actual subscription
reads/mutations, subscription named checks, legacy authorization, broker behavior
and scheduler acceptance. Source/test hashes are in
`.llm-output/subscription-reads-candidate.json`.

Claude verification was reassessed after a second incomplete handoff: command
and capability unit checks still did not exercise a full supervised invocation.
The implementation owner released its files. A separate Sol test engineer now
owns only a new adversarial supervised-client test file; root owns production
source repairs. Existing 308-agent counts are not evidence of complete Claude
gateway behavior. C1-C6 received partial repairs, with invocation, cancellation,
native identity and terminal evidence still subject to those tests.

## Claude capability admission checks

Capability admission now requires literal booleans for verified subscription
authentication, native-tool isolation, strict MCP configuration and allowance-only
billing. Eight malformed-value cases failed before repair
(`.llm-output/claude-capability-red.log`); the initial affected checks passed 27
tests (`claude-capability-green.log`).

Official hook documentation states that ordinary `disableAllHooks` settings do
not disable managed hooks. The report therefore now separately requires verified
`hooks_disabled=True`, defaulting to unavailable when proof is absent. The missing
proof regression failed before repair (`claude-hooks-red.log`); 32 gateway/codec
unit checks pass in 0.20s (`claude-hooks-green.log`), with scoped Ruff and mypy
passing. These checks do not establish installed-client isolation: the supervised
protocol tests are still in progress, and no production verifier or live model
call was enabled. Source: https://code.claude.com/docs/en/hooks .

A separate bounded owner is completing subscription git.commit preparation,
publication, replay and historical recovery. Effect-recovery repairs retain their
existing owner; durable decision execution awaits release of shared persistence
paths. No full CI or PR-triggered CI was run at this checkpoint.

## Supervised Claude transport verification

The independent test owner added an actual Python fake client under Forge's
ClientProcessSupervisor. Eighteen process-level cases pass in 1.44s
(`.llm-output/claude-supervised/adversarial/final-green.txt`): full SDK control/MCP
exchange and nested receipt, typed result, wrong identities, malformed/EOF,
cancellation/deadline, measured usage, and uncertain receipt settlement. The
combined gateway/codec suite passed 49 tests before the final additional
uncertain-receipt case (`init-gate-green.txt`). Scoped Ruff passes
(`scoped-ruff.txt`); source mypy passes. These are deterministic transport tests,
not installed Claude conformance or billing authorization.

Tests drove repairs for missing explicit empty skills, empty argv elements that
prevented launch, official terminal metadata, integer/string MCP ID aliasing,
interrupted assistant usage, and tool callbacks arriving before outer SDK
initialization completed. Usage reports deduplicate by assistant message ID and
retain observed totals during interruption. Injected-turn origins cannot supply a
successful requested decision. Root retains the production capability gate;
managed-hook isolation and allowance-only account proof remain prerequisites.

## Effect recovery validation reassessment

After two incomplete validation handoffs without a concrete blocker, the original
recovery owner released its paths. A fresh native test-agent spawn hit the host
thread limit, so the existing Sol test owner took the bounded PG recovery test
outcome after releasing Claude tests. Root owns recovery production repairs.

An actual ControlledToolService read recovered successfully, but an actual write
failed (`.llm-output/subscription-effect-recovery/actual-path-red.log`). Stored
write arguments intentionally contain content hashes; comparing them with the
broker's raw-argument digest was invalid. Subscription mutation request digests
now use the broker's canonical algorithm, retaining the legacy write algorithm.
Recovery binds that request digest and reuses existing full mutation intent and
outcome validation, requiring a completed operation. Dedicated actual-service,
pending/foreign-intent, rollback/replay and starvation tests are in progress;
synthetic-only positive evidence remains unaccepted.

The commit source owner added v2 subscription preparation/publication but had not
completed actual PG commit/recovery evidence. That owner retains the full task
and is completing the requested validation; source-only handoff is not acceptance.

## Durable commit-barrier finding

Inspection of commit integration found that `whole_worktree_exclusive` checks
existing writers only at admission; its exclusivity was not persisted. A later
disjoint writer could therefore be claimed during an active commit/check. A
bounded owner now owns the scheduling repository/model, additive migration 0011
and discriminating PG tests to retain the whole-worktree fence until terminal
settlement, including reconciliation and same-task concurrent callbacks. Other
managed worktrees must continue independently. Decision execution migration is
reserved as 0012 after this correction. This finding remains open until tested.

Commit authority remains restricted to the INTEGRATION specialist, as the plan
requires; routine implementation is correctly denied. Commit test fixtures are
being corrected to use an actual frozen integration route rather than widening
routine-worker permissions.

Root's final Claude self-review removed the unused inline fake-client copy and
fixed failure classification: an error containing “generate” was incorrectly
classified as quota because it contains “rate”. Two process-level regressions
failed (`classification-red.txt`); typed HTTP status now takes precedence and
text fallback uses complete terms. All 53 focused cases pass in 1.80s
(`final-root-tests.txt`); source mypy passes and Ruff's duplicate-branch style
finding was fixed (`final-root-ruff.txt`). Scoped hashes are in
`claude-supervised/adversarial/candidate.json`.

The exclusive-barrier owner produced source and complete existing scheduler logs
(35 passed in 50.64s, verified by root), but repeatedly returned without dedicated tests.
That ownership is now released. Root is completing the new PG barrier and
migration acceptance file; no source-only or partial-command result is accepted.

The exclusive barrier now has completed affected evidence: 59 tests pass in 64.88s
(`.llm-output/exclusive-effect-barrier/affected-final.log`), covering 10 new PG
cases, 35 scheduler cases and 14 broker unit cases. New checks prove later disjoint
writers queue, other worktrees/read-only tasks progress, same-task pending effects
prevent exclusive admission, races linearize, terminal settlement releases, and
expiry/reconciliation preserves the fence. Migration testing uses real DDL to
reconstruct the pre-0011 table, verifies conservative backfill, and rejects rollback
while exclusive effects remain unsettled. This last guard failed before repair.

Concurrent broker receipt finalization also needed to retain the original
exclusive scope on replay; three failures drove that repair
(`broker-replay-red.log`, `broker-replay-green.log`). Scoped Ruff and mypy on three source files
pass. Matching hashes are in `exclusive-effect-barrier/candidate.json`. The
common disposable PG fixture now removes scheduled effects before database-only
test teardown; the production downgrade guard remains strict. Independent
integrated review and complete v0.2 acceptance are still outstanding.

## Budget consumption arithmetic underway

The new `domain/subscription_budget.py` separates observed amounts from charged
amounts. Missing measurements retain a finite reservation ceiling; unbounded
unknowns remain null. Known zero is distinct from unknown. Duration uses
milliseconds, one admitted attempt remains charged when no result arrives, and
measured overruns are retained with explicit exceeded fields instead of being
clamped or discarded. Aggregation preserves unknowns and rejects unknown
reservations against finite limits.

The initial focused check failed because the module was absent
(`subscription-budget-domain-red.log`). Fourteen arithmetic/boundary cases pass
(`subscription-budget-domain-final.log`); scoped Ruff and mypy pass after two
optional-value narrowing fixes. This is domain arithmetic only: the immutable
usage ledger, atomic hierarchical reservations, repair scheduling debit and
production decision/worker integration remain outstanding. No budget migration
or UoW integration has been added yet; migration0012 remains reserved for the
next persistence work. A fresh complex-agent assignment hit the host thread
limit, so root owns this work while existing owners finish their PG outcomes.


## Actual commit and interrupted-effect recovery acceptance

Root took over commit integration after repeated incomplete source-only handoffs.
The current authority revalidation was valid; a v2 numeric authority schema was
incorrectly rejected as text by the preparation receipt codec. The claimed UUID
repair had landed in the repository-write begin call instead of Git preparation;
root removed that erroneous argument and bound preparation to the broker UUID.
Publication retains a distinct linked operation. Subscription mutations now use
the broker canonical digest; legacy mutation digest behavior remains unchanged.

Real PostgreSQL historical Git tests now lose the artifact receipt after actual
publication, expire ownership, reconcile via read-only Git inspection and settle
the tool audit with no active subscription lease. Both preparation and publication
foreign-attempt corruptions remain rejected. Seven subscription/legacy recovery
cases pass in 21.49s (`commit-root-recovery-actual.log`). Additional malformed
schema/UUID/purpose adapter tests failed before strict validation was added
(`commit-schema-red.log`, `commit-schema-green.log`). Final combined affected
verification is running; no independent integrated review is claimed yet.

Effect recovery now has accepted dedicated evidence: 16 PostgreSQL cases pass in
27.84s and three application transaction cases pass in 0.25s, with scoped Ruff
and diff checks (`.llm-output/effect-recovery-acceptance/final-*.log`). This covers
actual read/write terminals, actual predispatch mutation denial without a writer
call, stale admitted/reconciling states, live/pending/foreign fences, receipt and
effect rollback/retry, idempotency and scanning beyond malformed candidates.
Recovered rejection receipts no longer invent an OperationIntent for reads.
Actual subscription write denials retain broker identity and request/resource
metadata so a proven noninvocation can close a lost broker receipt. A denial
with any admitted intent remains fenced. An early fixture claim of distinct
ToolCall/effect IDs was corrected; the production exact-UUID binding remains.

Named-check/Git broker-fence recovery still needs its own complete terminal proof;
the above Git tests validate operation and tool audit recovery only. No broader
startup/runtime completion is implied. The proposed usage-ledger worker did not
begin implementation and released ownership. Root retains the new persistence
work; migration0012 is not yet created. Production decision application, aggregate
usage ledger, worker/gate integration and full A1–A9/review/release remain open.


The final combined commit/authority scope passed 42 tests in 59.09s
(`commit-final-scoped.log`). Scoped Ruff passed. Mypy found one tuple-inference
annotation issue after the strict UUID checks; it was fixed without behavior
change and is being rechecked. Scoped commit/effect hashes and matching evidence
are retained in `commit-effect-candidate.json`.

Usage persistence is now implemented locally by root: migration0012 adds
separate per-attempt reservation and consumption tables, with composite attempt
lineage and a downgrade guard against discarding admitted accounting. The UoW
has a new subscription_budget port/repository. First real PG settlement/replay
case passed after the expected missing-module red; further race, unknown,
release, overage, foreign/replay, rollback and migration checks are underway.
Admission uses the frozen primary contract for aggregate run ceilings and each
logical task contract for its own ceiling. Existing unresolved legacy reservation
rows block new accounting rather than being silently ignored. Repair debits and
parent allocation transfers remain decision-layer work; attempt reservations
explicitly require zero repair units to avoid double charging. No production
worker uses the new repository yet.


Usage-ledger focused acceptance now passes 41 tests in 39.77s: 17 new ledger PG
cases, 10 existing subscription persistence cases and 14 arithmetic cases
(`subscription-usage-final.log`). Scoped mypy passes on four source files; Ruff
has no remaining findings. Evidence and eight owned hashes are captured in
`subscription-usage-candidate.json`. Concurrent sibling reservations cannot exceed
run ceilings; an unrelated run commits while the first holds its admission lock.
Measured usage releases unused ceilings; known zero differs from unknown; missing
bounded/unbounded data stays honest; overruns persist and stop further admission;
retry count survives new UoWs; conflicting/foreign/stale attempts reject; rollback
and migration downgrade guards preserve accounting. Superseded-attempt and
cancelled-run regressions failed before repairs. Settlement remains valid after
cancellation. Frozen run/task pool limits are honored (two additional red/green
cases); an explicit run pool can cover several sibling task ceilings. The primary
contract is only the fallback when no explicit immutable run pool exists.

This ledger still needs hierarchical child allocation and scheduling repair debit,
explicit reconciliation adjustments for unknown settlements, and decision/worker
integration. Decision migration is now reserved as0013. No full CI/live model turn
or release action was run. The development ask-gemini route is running a disjoint
official Gemini CLI adapter contract; this is not application live verification.


Terminal broker-effect recovery now has a read-only verifier port and retained
ToolCall/OperationIntent content proofs. Named checks reuse complete receipt and
artifact reconciliation; Git verifies both completed intents and its canonical
result artifact without invoking Git. Settlement locks and rechecks the exact
proved operations and terminal record before atomically writing the revoked
broker receipt and rejecting the stale effect. Missing verifier/evidence and
changed snapshots retain the fence. Startup passes its existing tool recovery
service into this verifier. Nine real PostgreSQL cases passed in 18.53s
(`subscription-terminal-proof-green.log`); the affected combined suite is running
after operation-lock hardening. Scoped mypy passes four production files.
Combined collection exposed duplicate application/persistence test basenames;
the application files now use unique recovery_service/verifier_service names.

The development Gemini adapter run has stopped: the CLI reported individual quota
exhaustion, but framework classification was provider_error (exit1), not
fallback_required. Its pending claim is resolved and no adapter files were
created. A fresh native fallback could not spawn because the host thread limit
was reached. The original compact contract and framework logs are preserved;
no application live provider verification is claimed.

Subscription ToolCall replay now retains task/attempt/purpose in its canonical
record identity. Dedicated tests demonstrated all nine cross-lineage conflicts
across reservation/record/finalization; three parametrized PG cases and three
existing replay cases pass (`subscription-tool-lineage/final.log`,
`existing-replays.log`). This closes the observed omitted-lineage serialization
bug without changing the exact broker/tool/intent UUID binding.

Terminal recovery affected acceptance is complete locally:38 tests passed in
58.70s (`subscription-terminal-proof-affected.log`), scoped Ruff/mypy pass.
Nine scoped candidate hashes are in `subscription-terminal-proof-candidate.json`.
Decision-layer ownership is now assigned to the existing complex implementer
under the updated compact execution contract; production worker and human gate
bridge remain separate integration work. Integrated review is still outstanding.


Startup regression checks now pass24 tests (`subscription-terminal-proof-startup-green.log`,
12.09s). Initial14 failures exposed unit fixtures that mocked the DB factory but
not the newly introduced subscription recovery boundary; the fixtures now model
that boundary and assert the existing tool verifier is passed after operation/tool
recovery and before worker admission. Production startup ordering was retained.

The plan-gate worker implemented only the independent SubscriptionPlanProducer
model/decoder, not the gate consumer. Root review caught coercive schema/telemetry
validation and UUID digest serialization; repairs have10 passing domain tests
with the historical plan-evidence digest pinned. Evidence is in
`.llm-output/subscription-plan-gate/`. The full human-gate bridge remains open.

Pinned Gemini CLI0.59.0 source investigation found ACP model toolCallId is not
forwarded in MCP tools/call; only a newly random progress token is forwarded.
Pairing callback streams by order is not accepted as replay-safe identity. A
supported controlled-hook/permission alternative is being investigated; no Gemini
capability is enabled. Source map: `.llm-output/gemini-gateway-protocol-map.md`.


Primary reassessment: the complex decision worker repeatedly stopped at interface
selection and ultimately added only an append-only apply_handoff skeleton. This
was not accepted as durable decision application; root reclaimed all decision,
scheduler, ledger and UoW paths. The skeleton is preserved but has no tests and
must not be used for production completion. No decision migration/outbox exists.
The plan-gate worker likewise delivered only its producer model increment and
released ownership; root owns the unfinished bridge.

Gemini controlled-hook/permission alternatives were checked against pinned source:
BeforeTool can modify arguments but receives no stable model call identity;
AfterTool also lacks it. ACP permission responses cannot forward correlation or
updated arguments, and auto-approved calls skip that path. The callback identity
blocker remains; random hook IDs break replay and argument-derived IDs alias
concurrent identical operations. Map SHA256:
95856f79ab85a127e52d43b6818124f115a994d6450cb973778fd60e3fe14547.


Root implemented durable execution admission, replacing the unvalidated append-only
handoff skeleton. `SubscriptionDecisionExecutor.admit_next(owner, reservation)`
now atomically claims a task, creates a fresh running attempt bound to exact lease
owner/generation/candidate epoch and frozen task/envelope digests, updates task
state/version and reserves usage. Reservation rejection rolls back all three.
New UoW subscription_execution port/repository and migration0013 add nullable
historical-compatible binding columns; partial bindings reject and downgrade
refuses admitted identities. Unsettled/unaccounted historical attempts remain
fenced. No result application or production invocation is enabled by this change.

The combined admission exposed scheduler task-before-run lock inversion. Claim
now locks/skips occupied run rows before scheduler rows; expiry quarantine follows
the same order. A real two-session test failed before the repair and proves a
second worktree admits while the first run lock remains held. Empty admission
polls commit expired-task quarantine. A separate legacy terminal/no-usage case
failed before admission was tightened to require reconciliation.

Validation:57 affected admission/usage/scheduler tests passed in88.35s, then8
focused admission/constraint cases passed in12.97s after the final service/repo
repairs. Scoped mypy4 sources and Ruff pass. Logs and ten candidate hashes:
`.llm-output/subscription-execution-admission-candidate.json`. Initial missing-method,
lock wait, logical task-state, empty-poll and legacy-accounting red evidence is
retained under subscription-execution-*. Root owns all new/shared execution paths;
next additive migration is0014. Remaining work: actual typed result settlement,
child allocation/repair/continuation, gate outbox/consumer, production worker,
provider capability blockers, full A1–A9 and independent review/live/CI/release.


Result settlement now atomically records attempt usage and a replay-bound result
receipt (migration0014), fences stale task versions/leases/candidate contexts and
unresolved effects/client launches, and applies failed outcomes. Retryable failures
and FAILED handoffs queue bounded fresh attempts without a primary model call.
Repair debits reserve the promised future provider slot until admission transfers
it; a real sibling race exposed and verifies prevention of overbooking. Failed
admission rolls the transfer back. Unsupported typed decisions remain retained as
decision_pending with reconciling ownership; recording them is not acceptance.

The combined settlement/admission/usage/scheduler suites passed78 tests in123.10s
(`.llm-output/subscription-settlement-affected.log`). Follow-up inspection found
credential-bearing typed responses were rejected before usage settlement; such
responses now become bounded protocol failures with only an exact fingerprint
retained. Failure-detail fingerprints are computed before redaction to prevent
conflicting redacted replays from aliasing. The focused unsafe-response regression
passed; follow-up domain/settlement suites and typing are recorded separately.

Current official Gemini MCP/ACP/configuration documentation provides no verified
subscription-authenticated alternative carrying stable model tool-call identity
into a controlled callback. The installed source map remains decisive for0.59.0;
no Gemini capability or live account/model proof is claimed. Remaining settlement
integration includes pending repair-slot release on cancellation, remaining-budget
composition, all successful/delegating/control decisions and parent continuation.
The human-gate bridge, production worker, full A1–A9, integrated independent review,
scoped live verification, CI and release remain unfinished. No CI/live call or PR
was authorized or triggered by this checkpoint.

Final redaction/domain/settlement checks passed93 tests in33.44s; mypy passed5
sources. Scoped candidate hashes and evidence are saved in
`.llm-output/subscription-settlement-candidate.json`. Independent Opus5 review
is being requested for the integrated settlement batch; it is not yet accepted.


Opus5 high independent settlement review completed (framework run
c61e0067-b105-4f84-8a2a-e06a260ac7d7). Findings and dispositions are saved in
`.llm-output/subscription-settlement-review-findings.md` and
`subscription-settlement-review-resolution.md`. Root reproduced S01–S04 with
failing PG tests: unaccounted first candidate starved another worktree; unsafe
telemetry aborted usage settlement; nested delegation was truncated in storage;
and starting a candidate barrier invalidated the result it needed to drain.
Focused repairs pass. Receipt schema2 includes effective failure and raw telemetry
fingerprint, retains measured usage with redacted telemetry text, and preserves
schema1 replay compatibility. Candidate epoch advances at barrier close.

The affected run passed145 tests and failed20 broker fixtures because they created
an attempt before claiming a lease. Those fixtures now exercise the actual atomic
execution-admission service rather than bypassing it; their focused rerun is
pending. Scoped mypy passes3 changed source files. Lower-priority review findings
and reconciliation/unknown-count/fallback/gate integration remain open; this is
not final acceptance of the settlement layer or v0.2.

Both reused complex workers again stopped after inspection/acknowledgment without
implementation or a blocker; root reclaimed all ownership and the PG window. A
fresh complex worker could not spawn because the host thread limit was reached.
The trusted dirty-working-tree snapshot and complete plan-gate contracts/maps are
prepared, but neither outcome is implemented. Existing candidate_diff/file are
clean-HEAD legacy tools and cannot be presented as worker dirty-tree evidence.
No live provider call, full CI, PR or release action was triggered.


Broker fixture rerun passed20 tests in33.47s. The subsequent affected suite passed
68 tests and failed one admission diagnostic-order expectation; moving the logical
state check after the existing prior-attempt invariant restored that contract.
All9 focused admission/constraint tests then passed in15.24s. Final scoped Ruff
and mypy3 sources pass. Exact candidate hashes and layered evidence are in
`.llm-output/subscription-settlement-repaired-candidate.json`; unchanged domain/
usage evidence from the145-pass run is retained rather than represented as a new
single full-suite run. The final settlement suite also covers unsafe PlanOutput
classification and schema1 replay. No commands or worker writers remain active.
Next work includes remaining review findings, attempt reconciliation and bounded
unknown accounting, completed handoff proof/snapshot, full plan-gate bridge and
production worker. Neither new worker assignment produced an implementation.


The trusted dirty-working-tree snapshot producer is now implemented and connected
to the authenticated subscription broker via git.diff scope=snapshot. Legacy
working_tree/candidate scopes remain unchanged. ControlledGit reads tracked and
nonignored untracked regular files twice through CanonicalRoot, binds HEAD/base,
rejects incomplete listings, secret/reserved paths, hardlinks/symlinks, inconsistent
reads and limits (10k files,8MiB/file,64MiB aggregate). The SHA256 tree identity uses
raw bytes, paths and Git modes; the index is unchanged. This is observed evidence,
not a human approval or a claim of atomic final-candidate acceptance.

Snapshot calls reserve their ToolCall in a short transaction, read/store/verify
outside the UoW, then reauthorize and atomically finalize the receipt/artifact
lineage. The complete bounded manifest carries run/task/attempt/tool-call/worktree/
policy identity and is retained as a verified artifact; provider metadata includes
only a bounded path preview and manifest/tree digests. Caller cancellation drains
the owned reader; an operator stop can acquire the run lock during the read and
revokes acceptance. Unverified artifacts cannot produce success. No raw file
contents are persisted in the manifest.

Broader tracing found generic scheduler claim_ready also serves callers with an
already-created attempt. S01's fresh-attempt guard now belongs to the explicit
claim_execution_ready API used by SubscriptionDecisionExecutor; generic lease
claim semantics are preserved. Existing commit/tool-authority/admission checks
passed21 tests in45.45s after the compatibility repair.

Snapshot evidence:145 Git tests passed with5 platform skips in127.75s;34 broker/
commit/tool-authority/Codex protocol tests passed in43.03s; final focused tests
passed9 with1 host symlink-permission skip in20.97s after embedding manifest
identity. Scoped Ruff and mypy8 sources pass. Candidate hashes and evidence are
`.llm-output/subscription-snapshot-candidate.json`. Opus5 high independent snapshot
review is running; no final review acceptance is claimed. Successful handoff
verification/application, continuation, plan-gate bridge, worker and full A1–A9/
live/CI/release remain unfinished. No application live or CI/PR action occurred.

Read-only follow-up identified the installed Antigravity CLI1.2.0 as a distinct
official-client alternative to Gemini CLI0.59.0. Current official documentation
describes pre-tool conversation/step identity and a credit-fallback setting, but
does not establish the required MCP call correlation or fail-closed isolation.
Streaming stdin explicitly rejects control messages. Evidence and source links
are in `.llm-output/antigravity-runtime-research.md`; no application invocation,
account proof or configuration mutation occurred. This investigation does not
admit a new runtime capability.

Root also found that ToolRecoveryService.verify_terminal_effect currently holds
its own run-lock transaction across artifact verification. The outer recovery
service's separate settlement transaction does not eliminate that inner lock.
A bounded read-only refactor/test analysis is in progress; no fix is claimed yet.

Opus5 snapshot review completed (28d90ca0-e8e8-485d-a8c6-c2d3603eb366); findings and
dispositions are in `.llm-output/subscription-snapshot-review-findings.md` and
`subscription-snapshot-review-resolution.md`. Root reproduced and repaired the
interrupted-read recovery gap, absent snapshot exclusivity, CRLF checkout change
misclassification, raw exception propagation, denial identity drift and terminal
receipt replay metadata drift. Read recovery now cancels the original ToolCall
without rereading or inventing a mutation intent, then existing effect recovery
removes its fence. Admission and replay use the same exclusive flag. Git derives
changed names using safe built-in normalization; file/tree hashes remain raw bytes.
Final snapshot PG tests passed9 in25.40s; wider affected checks are running.

The terminal-effect lock regression failed with TimeoutError before repair.
Verification now preloads a bounded artifact descriptor graph in a short UoW,
verifies/reads bytes after closing it, and reuses existing validators against
immutable per-invocation bytes and current DB metadata. Existing locked call and
intent digest checks remain. All29 affected checks passed in64.36s; scoped mypy5
sources pass. This cache is neither durable state nor a production fallback.
Both complex workers again returned without implementation; root reclaimed all
file/PG ownership. The plan-gate contract remains unimplemented. No live app call,
full CI, PR or release action occurred. Lower-priority snapshot diagnostic,
orphan-artifact retention and production-worker integration findings remain open.

Final repair validation passed93 PG/recovery/broker checks in143.21s and146 Git
checks in128.93s with6 platform skips. Scoped Ruff and mypy5 sources pass.
`.llm-output/subscription-snapshot-repaired-candidate.json` records the16 scoped
hashes and evidence. A targeted Opus5 high review of crash/cancellation/fence and
artifact-lock repairs is running from `subscription-snapshot-repairs-review.json`.
No final independent acceptance or full-v0.2 completion is claimed.

The Opus repair review failed with429/session limit (framework run
88b1d824-c978-4d50-b555-d2f554c730f1), returning no findings. A fresh Astra-low
`snapshot_repair_review` native fallback is active on the same16-path candidate.
A fresh complex plan-gate worker then hit the host thread limit and did not start;
root retains gate/source/PG ownership. No tests or provider CLI processes remain
running at this checkpoint.

Astra fallback completed with no material findings in S02-01/02/03/04/05/07/08
repairs and verified HEAD/all16 hashes and recorded logs. It identified missing
adversarial coverage for recovery while the original snapshot reader remains
active, artifact graph limits and descriptor changes between loading/validation.
The existing Sol test engineer now owns those two test files and the PG window.
Root is progressing the plan bridge locally after another fresh complex spawn
hit the thread limit. A strict SubscriptionPlanApprovalEvidence schema2 and
explicit legacy/versioned decoder now bind producer plan identity and exact result
digest; all11 approval-domain tests pass, including the unchanged legacy digest.
Scoped source Ruff/mypy pass. This is groundwork only: gate repository/service,
consumer integration and full human-approval flow remain unimplemented.

The adversarial snapshot follow-up is complete: both affected PG files passed
24 tests in55.49s, with Ruff and diff whitespace checks passing. Evidence is in
`.llm-output/subscription-snapshot-adversarial/affected-final.log` and `ruff.log`.
The new tests cover recovery during a blocked original reader, late callback
rejection, descriptor count/per-item/aggregate bounds, descriptor mutation after
prefetch, and zero active verifier UoWs during both artifact-store methods.
No production changes were needed. The worker released both files and PG ownership.
A fresh Sol-high gate could not start because of the host agent-thread limit;
that separate gate has not been performed.

A fresh Terra worker `plan_gate_complete` is now implementing the full gate
contract, including frozen historical admission evidence where required. It owns
all paths in `subscription-plan-gate-complete-contract.json`, plus the execution
repository/port and subscription_results model, and the exclusive PG test window.
Root retains all other paths. No live application, full CI or PR action occurred.

Snapshot finding S02-06 now has a localized diagnostic repair: a closed port-level
SnapshotFailureReason vocabulary and SnapshotReadError preserve the controlled-Git
error boundary while reporting path rejection, index conflict, size limits,
incomplete/malformed listings and tree changes. Tool failure metadata and terminal
receipt replay expose only the code. TDD reproduced the missing reason before the
repair. The wider Git suite passed147 with6 platform skips in130.13s; scoped
Ruff/mypy3 sources pass. The final snapshot-only check and database replay evidence
are tracked in subscription-snapshot-reasons-* logs; the plan-gate worker owns PG.
No full review acceptance is inferred from this localized repair.

The existing Terra worker subscription_effect_recovery now owns only a new
read-only completed-handoff verifier port/service and application tests. It must
validate actual bound snapshot/receipt/check evidence and return immutable inputs
for locked revalidation; it does not own dispatcher or scheduler mutation. This is
a separate bounded outcome from the plan-gate worker and uses no PG test window.

Root accepted no plan-gate completion claim. The worker's64-pass log is
approval-final.log (legacy approval checks), while persistence-final.log is24
existing checks; neither exercised a new valid subscription gate. Repeated partial
handoffs required primary reassessment: root revoked all worker source/test/PG
ownership and now owns completion. No external blocker prevents new tests.
Current gate code is partial: schema3 result context, migration0015/model/ports,
service and legacy consumer branches exist, but the required full valid settled
PlanOutput -> gate -> genuine approval -> approved loader tests do not. Artifact
IO was moved to an owned service UoW factory and verified before opening it; more
adversarial proof checks remain. Current repository checks still need thorough
source binding and historical-vs-current validation repair. The new application
bridge test covers only rejected artifact verification. These changes are not
accepted for runtime use or release.

Root replaced the handoff worker's always-None stub and placeholder test with a
read-only historical evidence verifier. It validates UUID callback lineage,
accepted receipts, bounded verified manifest bytes/full closed identity, owned
changed paths (allowing disjoint sibling changes), actual named-check result
claims and terminal-effect proofs. It returns call/binding/receipt/artifact hashes
for future locked revalidation; it does not apply a decision or establish that a
check ran on the claimed snapshot. That candidate binding and current owned-output
verification remain necessary before acceptance.21 application checks pass
(subscription-handoff-final.log); scoped Ruff/mypy4 sources pass. The new
read-only operation_evidence lookup is run/task/attempt scoped and refuses
ambiguous bindings. Snapshot/replay/lookup PG + initial handoff suite passed32
in36.41s (subscription-handoff-snapshot-pg.log); later named-check app test adds1.
All commands are consumed and all native writers stopped at this checkpoint.

Root completed real PostgreSQL plan-gate flow coverage and repaired defects found
by those tests: schema3 proposal_context is retained losslessly; bounded artifact
writes use the real store's head_tail policy; migration0015 includes timestamp
columns; consumers distinguish logical-task identity from the external run task;
legacy approval fields are reconstructed and checked; producer/result/context
proofs and current pause/cancel/version/epoch/route controls are enforced. Replay
revalidates current authority; historical loading after genuine human approval
allows logical-task continuation. Rollback and artifact-I/O race tests pass.
Snapshot manifest publication also now works with the real filesystem store.

Current focused run passed53 tests in68.53s: plan-gate PG, snapshot PG and historical
handoff application tests (.llm-output/root-gate-snapshot-current.log). Scoped Ruff
and mypy4 gate sources pass. A subsequent explicit telemetry dictionary guard
passed mypy; focused runtime revalidation is pending. Candidate HEAD remains
6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db with preserved uncommitted changes.
The scoped review candidate/diff/contract are root-gate-handoff-* in .llm-output.
Opus5 high review failed provider_error before returning findings (framework run
0dcec1fb-13eb-4c42-b10c-139475db090f); fresh Astra fallback could not start because
of host agent-thread limit. Independent acceptance remains outstanding.

Root owns all source and PG work; no native writer is active. Remaining work
includes typed launch-stop/zero-launch proof, gate result application and production
dispatch, current handoff output/check candidate binding, migration retention
coverage, broader integration, provider capability prerequisites, acceptance
scenarios, UI/CLI integration and separately authorized live/CI/PR/release gates.
No full CI, live application model call, PR or release action occurred.

Final telemetry-guard revalidation passed31 tests in33.43s
(.llm-output/root-gate-final-guard.log):19 PG gate cases,1 application service case,
11 domain approval cases. Combined collection exposed duplicate test module names;
the application file is now test_subscription_plan_gate_service.py. The old saved
review manifest names that file before the rename and must be refreshed before a
future review. No review ran against or accepted this candidate. The zero-launch
proof gap is confirmed in gate _current; existing launch persistence is untyped
Mapping terminal data, so a complete launch-to-result proof needs deliberate
integration rather than treating a stop_confirmed field alone as sufficient.

Continuation classified as progress: gate retention/control tests and complete
launch-to-result proof integration changed authoritative source and test evidence.
The fresh Terra spawn failed the host thread limit. Reused idle Terra worker
subscription_commit_authority produced only an initial value type then stopped;
root rejected completion and reclaimed all files/PG. No worker remains a writer.

S06 zero-launch/typed-stop acceptance now has a concrete repair. Subscription
results schema4 carry bounded immutable SubscriptionLaunchTerminalProof from the
actual supervisor receipt. Durable lifecycle stores only process identity, outcome,
stop and byte-count evidence, excluding raw frames/stderr. Settlement validates
all terminal launches plus exact result launch/PID/creation identity/worker against
locked rows; successful decisions with missing/malformed/mismatched/uncertain or
cancelled proof remain fenced. Pre-launch failures still settle usage normally.
New launch creation requires the owning running attempt. Interactive valid-final
close is now completed, distinct from cancellation; nonzero supervisor termination
exit code alone does not contradict an already validated protocol response.

The plan gate requires schema4 result proof and verifies retained launch evidence
for historical loading as well as pending approval. Its immutable snapshot includes
the launch proof. Result schemas1-3 keep old replay shape and usage; no proof is
invented for old pending decisions. Those need forward reconciliation, still open.
The OS creation identity is named process_identity in the proof because the generic
credential-key guard correctly rejects process_start_token in canonical evidence;
the OS receipt field and credential guard remain unchanged.

TDD observed no-launch success becoming decision_pending before the fix
(root-launch-proof-red.log). Latest48 gate/launch/domain checks pass66.88s
(root-launch-final.log), including genuine operator approval, history tamper,
migration evidence retention, same real supervised child receipt persisted before
settlement, refused late launch, forged/missing/legacy/cancelled/foreign-worker
proof and schema2/3 replay. Wider earlier66 gate/result/boundary checks passed97.78s
(root-launch-integrated.log); supervisor/gateway66pass1platformskip4.51s
(root-launch-supervisor.log). Scoped11-source mypy and Ruff pass. The gate test
module is formatted; application bridge test was renamed to avoid collection clash.

HEAD stays6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db; updated hashes/scoped integrated
diff/review contract are .llm-output/root-gate-handoff-*. Claude run
0dcec1fb-13eb-4c42-b10c-139475db090f stdout confirms429 session limit resetting
2:50am Europe/London, not a code review. No independent acceptance of the new
candidate is claimed. Full production decision application/recovery, worker
composition, handoff current-output/check binding, provider prerequisites, A1-A9,
UI/CLI integration and separately authorized live/CI/PR/release remain outstanding.
All root test sessions are consumed; no fullCI/live application/PR action occurred.

Continuation progress: the plan bridge can now resume from a durable attempt ID
via SubscriptionPlanGateService.request_settled. The repository reads exact
schema4 PlanOutput/proposal context and producer; shared approval-field computation
builds evidence from the existing immutable source/policy. Publication still does
artifact IO outside a UoW and final locked source/control verification. It does
not trust the earlier read as authority. Concurrent publishers replay once.

Plan publication now atomically marks the result accepted/plan_approval, ends the
attempt, blocks the logical and scheduled primary, increments task version once,
and clears its lease. This releases active execution capacity while retaining the
human approval gate. Result bytes/digest and usage are unchanged. Current gate
verification expects the applied blocked state; historical loading still permits
continuation after actual approval. Replay of the provider result after approval
returns the applied receipt without extra usage or run-state regression.

TDD logs: root-plan-source-red.log missing request_settled; root-plan-apply-red.log
showed publication left decision_pending. Thirty PG gate tests passed54.54s
(root-plan-application-final.log), including rollback after application mutation,
concurrent publishers and stop after initial source read. A proposed external-task
mutation failed the existing append-only PostgreSQL trigger; no speculative digest
column or migration was added. The resulting immutability test passed2.32s
(root-plan-source-immutable.log), and postapproval result replay passed2.47s
(root-plan-applied-replay.log).58 legacy planning/approval/domain/application checks
passed0.80s (root-plan-application-compat.log). Final scoped Ruff/mypy4sources pass.
The test file now contains32 gate cases; the final two are independently verified.

HEAD remains6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work.
Updated hashes/diff/contract are root-gate-handoff-* in .llm-output. Fresh Astra
plan_application_review spawn again failed host agent-thread limit; Claude's
previous429 reset remains2:50am London. Independent acceptance is not claimed.
Root owns all files/PG; all root sessions consumed, no active native writer.
Next: primary continuation after approved worktree preparation, durable dispatch
of remaining typed decisions, production worker/recovery composition and current
handoff/check candidate binding. Full provider capabilities,A1-A9,UI/CLI,review,
separately authorized live/CI/PR/release remain outstanding. No live application,
fullCI, PR or release action occurred.

Continuation progress: approved subscription worktree preparation now hands off to
the durable primary instead of enqueuing legacy implement. DeliveryPreparationService
retains its command/approval/resource fences, calls resume_prepared under the final
transaction, queues the same logical primary on the exact prepared path and records
primary task/plan attempt/approval/resource identity in run.worktree_prepared. Run
transition and queue mutation commit together. The gate repository proves the
historical settled proposal, current valid approval, blocked applied task and
candidate/control state. Replay verifies prepared identity without requeueing or
provisioning again. Legacy preparation follows its existing command path.

TDD first exposed an unacknowledged approval command in the test fixture, then
proved the actual behavior gap: successful preparation left the primary blocked.
The fixture now completes that real approval command through CommandRepository
before claiming the prepare command.5 PG integration tests passed11.69s
(root-subscription-preparation-final.log): exact genuine approval/lease path,
primary attempt2 admission, replay without duplicate provision, rollback after
queueing, cancellation during provision, changed resource and revoked approval.
Provisioning itself is a controlled fake port in these tests, not real Git worktree
or model evidence.56 legacy composition/approval checks passed2.22s
(root-subscription-preparation-compat.log); scoped Ruff/mypy3sources pass.

HEAD stays6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved changes. Refreshed
review candidate/diff/contract at root-gate-handoff-* includes delivery_preparation
and new test_subscription_preparation.py. No independent review has accepted this
candidate; Claude's known reset is2:50am London and fresh native agents have hit
host thread limits. Root retains all source/PG; every command session consumed.
No fullCI/live application/PR/release action occurred. Next work is the production
provider-execution lane, remaining typed decision application and recovery, current
handoff/check candidate proof, then full A1-A9/UI/CLI/provider and release gates.

Continuation progress: fresh-attempt selection now shares its run-control fence
with budget reservation through domain/subscription_execution.py. The prepared-run
pause regression originally raised SubscriptionBudgetConflict: budget admission
already rejected the paused run, but selecting it repeatedly could starve another
ready worktree. Scheduler fresh claims now skip such rows under the existing run
lock. Generic claim_ready behavior is unchanged. The fairness test deliberately
puts the paused primary first, then proves another run is admitted with no extra
charge on the paused run.

Renewal now locks run before scheduler/logical rows, checks the same run-control
fence and both task stop flags, and rejects stopped leases. TDD proved both a run
pause and logical-task cancellation previously renewed successfully. Healthy
renewal retains owner/generation and extends the lease. This supplies the durable
renewal signal for the still-unimplemented provider revoke/interrupt worker loop;
it does not claim that loop is wired or that all admission-failure cases are solved.

29 affected scheduler/admission/usage/preparation checks passed51.82s
(root-admission-control-affected.log). After renewal changes,14 affected checks
passed28.73s (root-admission-renew-final.log). Red/green and fairness evidence is in
root-admission-pause-*,root-admission-fairness.log,root-renew-control-*.
Scoped Ruff/mypy3source checks pass. HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db
and current scoped hashes/diff/contract are recorded in root-gate-handoff-*.
Root owns all source/PG; all process handles consumed. No independent review,
fullCI, live application call, PR or release action occurred. Production invocation,
remaining typed decisions/recovery, provider prerequisites,A1-A9/UI/CLI and agreed
review/live/CI/release gates remain incomplete.

Continuation progress: the durable launch boundary now refuses a fresh intent for
paused/cancelled runs or tasks and expired/mismatched scheduler leases. It locks
run before attempt, matching admission/settlement. Existing launch identities can
still record started/terminal receipts after stop; this preserves recovery proof.
Three focused regressions first failed DID NOT RAISE (root-launch-control-red.log).
49 launch/plan-gate PG tests passed87.42s (root-launch-control-affected.log), including
postpause shutdown evidence. Scoped repository Ruff/mypy passed.

New worker/subscription_runtime.py provides SubscriptionAttemptRunner and immutable
SubscriptionAttemptOutcome. It validates request identity/timings, renews before
launch, periodically and before accepting completion, runs provider/revoke IO
outside transaction contexts, revokes before interrupting, bounds cleanup, retains
and observes noncooperative tasks, preserves measured interrupted telemetry, and
settles exactly once. External cancellation requests stop through a shielded owned
coordinator, then propagates SubscriptionInterrupted after settlement. An UNCERTAIN
provider cleanup cannot be downgraded by a lease/control failure. It returns a typed
result and settlement; production polling and decision dispatch are NOT wired.

21 unit tests passed1.37s (root-runner-unit-final.log), with red evidence in
root-runner-unit-red.log and root-runner-lifetime-red.log.2 real-PG tests passed3.98s
(root-runner-pg.log): real durable admission/usage/replay, synthetic stopped launch
proof, and run pause during blocked gateway causing revoke-before-cancel with
measured usage. No real application provider/Git operation is claimed. Scoped
runner Ruff/mypy pass. Root owns all sources and PG again; no active process.

Routing failure: Gemini CLI run d6a05d1e-c919-46c5-b66b-7b8502551cd2 exited1 after
544s with client Individual quota reached/reset61h55m38s, but adapter provider_error;
pending record is resolved and no edits were made. Fresh Luna fallback hit host
agent-thread limit. Existing Terra subscription_commit_authority returned twice
with partial code/test and no required validation; after reassessment root revoked
ownership and completed runner locally. Existing plan_gate_complete returned only
inspection for .llm-output/subscription-delegation-application-contract.json, no
edits; root reclaimed its six paths and PG window. Neither agent is an active
writer. Do not repeat these ineffective native handoffs without reassessment.

HEAD remains6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work.
No independent review has accepted the latest runner/launch changes. Claude's
known reset remains2:50am London. Full CI/live application/PR/release were not run.
DelegateDecision atomic application is designed in the saved contract but NOT
implemented. Remaining decisions, current handoff/check proof, attempt recovery,
production worker composition, provider capability gates,A1-A9,UI/CLI and agreed
review/live/CI/PR/release remain unfinished. The complete v0.2 goal remains active.

Continuation progress: DelegateDecision application is now implemented through
application/services/subscription_decisions.py, a typed UoW port and PostgreSQL
repository. Callers supply only settled attemptID. Schema4 source/identity/envelope/
route/task/budget/telemetry and matching durable stopped launches are verified.
Fresh application requires the current IMPLEMENTING run, matching prepared path,
policy identity/hash, unchanged primary/candidate/generation and no stops/effects.
It creates child contracts and scheduler rows in dependency order, records exact
attempt-linked delegation, marks the result accepted/delegated and primary attempt
terminal, blocks primary and clears lease in one transaction. Replay after child
progress or pause returns once without requeueing or recreating missing records.

Child authority now shares domain/subscription_delegation.validate_child_authority
with the model protocol decoder: frozen preferred route, bounded budget/repairs,
billing/unknown-telemetry policy, named checks and acceptance-check consistency.
DAG validation enforces scope/run/parent and dependency cycles. A child may not
wait on the yielding primary. Budgets are child ceilings; actual concurrent attempt
reservations still use existing run/task usage accounting. No legacy allocation
reservation was introduced (it would conflict with the current usage ledger).
Only DelegateDecision application is claimed; Wait/Scope/Accept/Review/Reassign
and successful handoff application remain unfinished.

Scheduler completion now wakes a blocked logical parent along with its scheduler
row and increments logical version. TDD demonstrated prior scheduler-only wake
left primary unadmittable, and that parent-dependent children deadlocked. Both
fixed. The focused delegation file contains24 cases: real approved/prepared primary,
child admission, current-source/launch/policy checks, authority violations, replay,
concurrent application once, rollback after second child fails, forward dependency,
unresolved effects and missing replay-record refusal. Provisioning/provider proof
fixtures remain synthetic; no real Git or live model evidence is claimed.

58 affected PG tests passed112.26s (root-delegation-affected.log), including21 then-
current delegation cases, scheduler acceptance and runner integration.3 additional
PG guards passed7.89s (root-delegation-final-guards.log). Initial policy-drift fixture
failed FK because version2 was absent; repaired using actual append_policy before
changing run policy.39 protocol/runner unit cases passed1.44s
(root-delegation-protocol.log). Runner later reuses shared max24h lease validation;
its current22 unit cases passed1.36s (root-runner-unit-final.log), with expected
max-lease rejection red at root-runner-lease-bound-red.log. Final scoped Ruff/mypy
10sources pass. No required check failure remains for these bounded additions.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus retained uncommitted work.
Refreshed .llm-output/root-gate-handoff-{candidate.json,review.json,review.diff}
now cover46 scoped paths (509088-character diff), including runner/delegation and
launch guards. Independent review has not accepted this candidate; known Claude
reset2:50am London, native fresh agents hit thread limit and reused implementation
workers returned incomplete work. Root owns all files/PG, no live process or writer.
No full CI/live application/PR/release action occurred. Next: remaining typed
decisions and production composition/recovery/current handoff binding, then all
provider/A1-A9/UI/CLI and review/live/CI/release gates. Complete v0.2 remains active.

Continuation progress: explicit WaitDecision now shares the same exact settled
source/current-control verifier as delegation through apply_wait. It records a
bounded set of direct children in the prepared worktree, ends the primary attempt,
clears its lease and blocks it; all-already-terminal targets immediately requeue.
Replay is once-only and source-bound. No schema migration or mutable task contract
was needed. Delegate completion now queues the primary after each terminal child;
explicit waiting consults the exact latest applied record and queues only after
its selected children terminate. Other applied dispositions, including plan
approval, are not awakened. Record key/type/run/payload/digest are checked; a broken
record keeps parent blocked without undoing the child's valid settlement.

68 affected PG cases passed140.42s (root-wait-affected.log):8 then-current waits,
24 delegation, scheduler acceptance and generic scheduler. After tighter wake
record metadata validation,12 selected cases passed33.83s
(root-wait-final-guards.log); wait file now9 cases including corrupt record type.
TDD reds: root-wait-wakeup-red.log (primary stayed blocked with siblings remaining),
root-wait-application-red.log (missing apply_wait). Final source Ruff/mypy4sources
and UoW files pass. Test helpers now optionally accept a frozen primary budget;
defaults unchanged. Wait flow uses budget20 to exercise genuine repeated primary
admission after approved preparation and child results, not fabricated state.

Known integration limit: queued primary may still wait for active overlapping
writer path reservations. Waking is not a claim of immediate concurrent primary
model execution. Preserve path isolation; full production integration must address
authority-vs-active-write reservation distinctions if needed for responsiveness.
Scope/Accept/Review/Reassign and completed handoff application remain unfinished,
as do production invocation polling/composition, attempt reconciliation and full
A1-A9/provider/UI/CLI/release gates.

ACTIVE independent review: after Claude's stated2:50am London reset, root launched
Opus5 high with concrete coupled cancellation/lease/authority/replay reason through
ask-claude. Unified exec session78288 is live; root owns monitoring (1,2,5min
backoff). Launched about01:52UTC on11September. Do not restart because no output.
Read-only candidate remains HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db with49
hashed paths and557744-character saved diff at root-gate-handoff-*; source is frozen
for review. All PG sessions consumed, no other writer/process. No independent
acceptance yet and no live application/fullCI/PR/release action. Preserve this
active review across continuation; root owns all remaining work.

Production-entry investigation during frozen review: RunService.create_run still
creates only legacy start_planning; no source path freezes project_profile into
ExecutionEnvelope. Profile selection exists, but production adapters/capability
verifiers and first primary task composition remain unimplemented. The correct
integration point must freeze selected profile under the existing project/run
creation transaction and preserve mutation replay/base/policy identity; start
planning must branch on that frozen envelope. Do not silently leave selected
subscription runs on legacy API execution.

Two scope questions need explicit implementation design, not ad hoc row changes:
initial planning precedes a managed worktree and has no approved writable scope;
postapproval primary needs concrete canonical paths for delegation. Existing
PlanOutput.affected_components is not yet a guaranteed canonical path contract.
Also LogicalTaskContract.owned_paths currently serves both delegation authority
and scheduler writer reservation; a broad primary reservation conflicts with
concurrent children when the primary only needs to make decisions. Preserve
isolation and distinguish these semantics before claiming responsive concurrent
production orchestration. No source edits were made for this investigation.


Review repair checkpoint (11 September): Opus5 high review completed successfully
at .llm-output/agent-framework/47099ebe-20f1-469b-b15c-984add434c92/stdout.log;
readable findings at root-runtime-review-findings.md. The earlier ACTIVE review
entry is superseded. All49 candidate hashes matched before root repairs.

F01/F06: scheduler terminal release now shares parent wake logic for finish,
queued cancellation and reconciliation; run locking precedes task terminalization.
Five initial focused PG tests passed. F02: decoder rejects nonchild wait IDs;
current, stopped-launch-proven semantic Wait/Delegate rejection now records the
original decision plus a typed failure receipt, debits at most one bounded repair,
releases scheduler/attempt and queues retry or terminates at exhaustion atomically.
Replay verifies both receipts without modifying provider payload/digest/usage.
Stale source, changed control, missing proof and unresolved effects remain fenced.
This does NOT implement reconciliation for other decision kinds or production
restart/dispatch. Initial affected run48pass/9fail had obsolete semantic rejection
expectations; corrected tests and receipt guard now59pass95.53s
(root-review-F02-green.log). Exhaustion and missing-receipt regression included.
Final decoder variable rename for mypy was followed by23protocol passes0.21s.

F03/F07: typed scheduling revoked/lost errors distinguish interruption from
uncertain ownership/DB failures. Runner retains same-classification provider
detail and supplies bounded diagnostic detail when overriding.25unit tests
passed1.40s including lost lease and preserved interruption detail. F04 rejected
as unsupported: installed Pydantic rejects unknown keys in real decoder test.
F05: unreleased migration0015/model use RESTRICT run/task/attempt lineage and
run index; schema regression reproduced CASCADE then33plan-gate PG tests passed
61.73s (root-review-F05-green.log). Scoped six-source mypy passes. No deployed
migration upgrade is claimed;0015 remains uncommitted/unreleased.

Root owns all paths and PG. Active affected scheduler/runner PG check is unified
session70872 (root-review-scheduler-affected.log); earlier sessions consumed. Next inspect that
check, finish scoped lint/self-review, refresh exact review candidate and request
independent repair review. HEAD remains6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db
plus preserved changes. No live app, fullCI, PR, merge or release action occurred.
Complete v0.2 remains unfinished with previously listed production/provider/
A1-A9/UI/CLI and release obligations. Do not treat bounded repair as completion.


Latest checkpoint supersedes active PG session70872 above:42scheduler/runner PG
passed75.16s (root-review-scheduler-affected.log); session consumed. Scoped lint
fixed one test import order; six-source mypy and23protocol tests already pass.
Fresh Sol-high repair_correctness spawn failed host agent thread limit; no gate
was performed, remains unresolved. No native writer active.

Repair review candidate saved separately, preserving original review evidence:
.llm-output/root-runtime-repair-{candidate.json,review.json,review.diff},52hashed
paths at HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db. ExactOpus5 high launched
through ask-claude for coupled retry/replay/cancellation/lease repair review.
Root owns monitoring, source frozen, no PG active. Continue review completion
before claiming findings resolved; no v0.2 completeness or release claim.


Correction: repair review exited before completion, no active review process.
Run4cc0bdb1-6c0d-441a-a80a-f9e499417fef returned provider_error exit1; client429
weekly limit resets September13 02:00 Europe/London. No completed repair review.
Authorized fresh Astra reviewer fallback also failed host agent thread limit.
Root owns all paths, no native writer/PG/provider process active. Review and
Sol correctness remain unresolved; source freeze may be lifted for independent
implementation, but saved52path candidate must be refreshed after relevant edits.


Production integration progress (11 September): previous goal turn was verified
progress; this turn re-read objective/current worktree. Existing Terra worker
subscription_commit_authority received bounded atomic run/envelope task, returned
inspection only without changes or blocker, and root revoked all ownership/PG.
Root owns all files and PG; no live process/native writer remains.

Found and fixed concrete integration mismatch: subscription preparation queued
absolute filesystem path as scheduler worktree_id, but broker/controlled tools
require WorktreeIdentity.worktree_name. DeliveryPreparationService now passes
canonical resource name, resume_prepared derives/verifies it against persisted
run identity while preserving approved resource/path proofs, and Wait/Delegate
application verifies same resource identity. Filesystem path remains on Run and
provisioned ManagedWorktree. Expected red at root-prepared-resource-red.log;
45preparation/delegation/wait PG cases pass118.08s root-prepared-resource-green.log.
Four-source mypy passes. No actual Git/provider roundtrip is claimed by fixtures.

RunService.create_run now freezes selected project profile into ExecutionEnvelope
inside existing project/run/mutation transaction; same policy version and exact
preferred routes, primary flag, fallback lists and billing. No model mapping or
capability verification is fabricated. No profile preserves legacy creation.
run.created gains profile/version/envelope digest only when selected. Replay
returns original run/envelope even after profile version changes. Invalid profile
without primary rolls back run, command and mutation receipt. New domain helper
subscription_envelope.freeze_profile tested exact primary/worker/fallback and
missingprimary; red ModuleNotFound then2pass. Real creation red missing envelope;
4new+legacy PG cases pass6.54s root-run-envelope-green.log, latest2selected-profile
PG cases (including rollback) pass3.53s root-run-envelope-final.log.30application/
domain cases pass0.59s after updating legacy fake UoW with absentprofile port.
Scoped Ruff/mypy pass. No source writes from worker were retained because none made.

Subscription planning dispatch remains incomplete: selected profile is frozen but
start_planning still goes through legacy PlanningService; production subscription
primary task/context/poll/adapter composition and phase-specific controlled tools
must be connected next. ControlledToolService currently allows subscription tools
only IMPLEMENTING; pre-worktree planning needs repository_resource_identity with
read-only tools and canonical reader binding. Primary phase/budget/writable-scope
and authority-vs-activewriter concerns above still need resolution. Do not claim
production planning works merely because freezing now succeeds. Saved52path repair
review candidate is now STALE after resource-ID fixes; refresh for next independent
review. Opus weekly limit and freshnative threadlimit remain review blockers.
No fullCI/live application/PR/release action; full8slices/A1-A9 goal remains active.


Subscription planning authority progress (11September): root-owned direct bounded
change in application/services/tools.py plus new application/test_subscription_
planning_tools.py and persistence/test_subscription_planning_read.py. Primary and
planning purposes may read canonical repository during PLANNING before worktree
preparation, through existing repository_resource_identity, reader root binding,
policy exclusions, schema validation, durable subscription attempt/effect proof
and budget checks. Pending human gates remain denied. No general primary write,
named-check or Git authority is granted in PLANNING. Other subscription execution
phases beyond IMPLEMENTING are still not enabled. Existing managed-worktree reads
retain their resource binding when a worktree exists.

TDD red2 cases denied valid primary/planning reads (root-planning-tools-red.log).
New14 unit cases cover successful real RepositoryReader read, actual subscription
lineage in recorded result, forbidden worker purpose, pause/gate/resource/root/
exclusion mismatch and no write/delete/rename/check/Git authority. Two test fixes:
valid AWAITING_PLAN_APPROVAL metadata instead of invalid PLANNING+gate snapshot;
primary commit denial is purpose authorization, unlike phase-denied writes/checks.
Affected104 application tests pass16.72s root-planning-tools-final.log, including
legacy planner/namedcheck/authorization. This revealed a pre-existing planner
named-check path requiring invocationID before role denial; invoke now dispatches
named checks only for legacy DEVELOPER, others use existing typed denial path.
No unauthorized check is executed. Scoped Ruff/mypy pass.

Real PG planning-read test uses actual executor admission, broker binding/effect,
ControlledToolService, filesystem RepositoryReader, durable tool record/receipt
and replay after source changes.1pass2.26s root-planning-read-pg.log. Initial fixture
reader omitted policy effective secret exclusions and correctly denied; fixed by
constructing reader from persisted policy. Fixture manually stages PLANNING and
primary schedule: this is NOT proof of production command dispatch or live client.
No model call/Git execution/fullCI/PR/release occurred. Root owns all paths/PG,
no process active;1040/60427 unit sessions consumed. Review quota/thread limits
remain as recorded. Continue production start_planning dispatch/bootstrap, frozen
primary request context and polling/client/broker composition, approved writable
scope and remaining decision/recovery/acceptance/provider/UI/CLI/release work.


Initial production planning dispatch (11September): root implemented
SubscriptionPlanningService and wired PlanningHandler in worker composition to
select it from durable ExecutionEnvelope. Missing envelope retains legacy path;
lookup failure propagates without legacy fallback. Service validates current
leased initial start_planning command (identity/payload/schema/actor/version),
locks run, verifies frozen envelope/policy and current control intent, creates one
deterministic primary logical task with zero writable paths, admits scheduler and
queues under repository_resource_identity, then CREATED->PLANNING plus exact
source event in same transaction. No provider/filesystem IO inside transaction.
Replay verifies original encoded task/event, command/envelope/resource, current
primary identity/route/budget, returns existing primary without requeue even after
attempt admission. Added typed SubscriptionRepository.get_task for durable lookup.
Pending cancellation, paused state, stale payload, expired command lease and
injected transition failure cannot publish primary work. A changed primary budget
is rejected on replay. Initial command only; resume/plan revision still need work.

Settings.subscription_primary_budget / FORGE_SUBSCRIPTION_PRIMARY_BUDGET supplies
an explicit frozen TaskBudget at initial planning. Default retains TaskBudget
limits except64 total provider attempts for the whole orchestration (default3 was
insufficient for plan/delegate/wait/accept plus workers). Environment JSON validated;
.env.example documents it. Does not authorize paid mode: task budget billing must
match frozen primary route, allowance-only remains default. Later worker settings
changes do not rewrite an already-started run budget. Production invocation/polling
still must consume this queued task; no live agent run is claimed.

Evidence: root-planning-start-red.log missing service. Initial fixture repaired to
use standalone PostgresCommandRepository for claiming, and thaw frozen RunEvent
payload before typed decode. Later guard-test fixture used incomplete PAUSED SQL
shape and an accidentally misplaced assertion block; fixed with actual runs.pause
and restored test location. Replay-budget red at root-planning-start-replay-red.log
then fixed source.7PG guards passed11.87s root-planning-start-green.log. Latest
13combined creation/start/read/legacy PG checks pass21.44s root-planning-start-
affected.log. Extra real-handler/config-change replay test passes2.22s
root-planning-start-config-replay.log.36dispatch/composition/settings unit cases
pass2.31s root-planning-dispatch-green.log (5existing ADK deprecation warnings).
Scoped Ruff/mypy5sources pass. No required check currently failing for this batch.

HEAD remains6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted
changes. Source review candidates are stale; re-review remains blocked by Opus
weekly quota and fresh native thread limit. All command/test sessions consumed,
no agent/process active, root owns all files and PG. Next: primary invocation
context and official-client/broker composition/polling, approved writable scope
for initially empty primary, phase permissions for later stages, decision dispatch/
restart reconciliation/current handoff/candidate binding, completeA1-A9/provider/
UI/CLI and authorized live/CI/PR/release. No scope reduction or completion claim.

Invocation phase boundary (11 September): root added typed optional RunState to
SubscriptionInvocationRequest and shared phase filtering in protocol decoding and
provider output schemas. A PRIMARY in PLANNING advertises/accepts only PlanOutput;
a plan is rejected in every other explicit run state. Untrusted context cannot
override this field. Existing isolated adapter callers that omit state retain
compatibility; production request assembly MUST populate it from durable state.
This is a boundary primitive, not a claim of production enforcement: no production
request constructor or subscription polling loop is wired yet. Other decisions in
later phases still require their durable application guards and future dispatch.

TDD: root-protocol-phase-red.log records six expected missing-field failures;
29 initial protocol cases passed in root-protocol-phase-green.log. Expanded tests
cover every non-planning state, a valid planning result, untyped state rejection,
and schema consistency despite contradictory untrusted context. Protocol plus
Codex/Claude gateway/protocol/supervision affected suites: 118 passed in 3.09s,
root-protocol-phase-affected.log. Scoped Ruff and mypy (two sources) pass. Root
self-reviewed the four-path increment (two sources, test, this document).
HEAD remains 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db with preserved uncommitted
work. Independent review/provider/CI/release limitations above remain unresolved.
No processes are pending. Next remains authoritative invocation assembly, approved
primary writable scope, provider/broker composition and production dispatch.

Durable invocation assembly (11 September): root completed the next bounded
integration seam. SubscriptionRequestBuilder now calls a PostgreSQL preflight,
loads the current run and pinned/current project policy plus original human task,
uses persisted RunState, validates the repository/worktree resource identity, and
builds an immutable request with a fresh secrets.token_urlsafe(32) broker token.
The trusted prompt is constant/versioned; human text, policy summary, exact named
command names and known logical-task contracts are in bounded untrusted context.
PolicySummary excludes command argv/environment/credentials; provider route,
effort, auth/billing, task budget and identity come unchanged from admission.
PLANNING primary/planner requests expose only the four repository-read tools.
IMPLEMENTING uses existing specialist tool permissions and requires a managed
worktree resource. Other phases currently fail explicitly pending their phase
permissions and decision dispatch; this is not completion of the full runtime.

PostgresSubscriptionExecutionRepository.invocation_resource holds the run lock
for short DB-only assembly, checks actionable current control intent, exact
attempt/lease/task/envelope/route/candidate bindings, current running task/version,
lease expiry/stops, scheduler admission and unresolved effects. Self-review added
scheduler ownership/provider/parent/dependency/read-only equality against the task
contract (a focused regression first reproduced missing scope validation). Frozen
attempt matching is shared with settlement, preserving historical replay behavior.
SubscriptionRepository.invocation_tasks loads at most257 other rows in this run,
rejects more than256 instead of silently truncating, validates payload identity,
and supplies typed known-task context. Launch and tool boundaries still revalidate
after the assembly transaction commits; this does not hold locks across IO.

Evidence: root-invocation-context-red.log expected missing method. Initial11/12
passed; pending-control fixture lacked required actor metadata and was corrected.
46 admission/context/result/launch settlement PG checks then passed80.13s at
root-invocation-context-affected.log. Request-builder missing-module red is in
root-request-builder-red.log; real start-planning/admission/build passed2.28s.
Policy guard fixture initially tried mutating append-only policy rows (DB correctly
refused); changed to append a new version and prove stale-run refusal. Five builder
checks passed9.56s at root-request-builder-final.log. Scope mismatch regression
failed as expected at root-invocation-scope-red.log, then guards were added.
Final combined context/builder23PG checks pass39.54s at
root-invocation-builder-final.log. Scoped Ruff and mypy5source files pass. No
required check for this increment remains failing; no provider/FS IO in these tests.
Root self-reviewed this batch. Independent integrated re-review remains outstanding
under the recorded Opus quota/native thread-limit constraints; no release waiver.

Candidate: HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted
work. Seven code/test paths for this increment are recorded with hashes in
.llm-output/root-invocation-builder-candidate.json. Root owns all paths; all command
sessions consumed, no process active. Still needed: production invocation poll and
broker/client composition, durable result/known-task outcome context, approved
primary writable scope, remaining phase permissions/decision/restart handling,
all A1-A9/provider/UI/CLI evidence and separately authorized live/CI/PR/release.

Attempt reservation enforcement before dispatch (11 September): tracing the
production invocation join found that the ledger reserved per-attempt ceilings,
but the gateways and runner still executed against the whole logical task budget.
Root fixed this prerequisite before wiring dispatch. SubscriptionBudgetRepository
now exposes reserved_budget(run, task, attempt), requiring exact outstanding
reservation lineage and one attempt/zero repair units. SubscriptionRequestBuilder
loads that persisted value under its existing short run-locked transaction.
SubscriptionInvocationRequest carries optional attempt_budget and exposes budget
as its effective execution ceiling. Production builder always supplies it;
legacy isolated request fixtures retain their task budget when omitted. Validation
rejects ceilings outside task authority, billing changes, wrong attempt/repair
units, or weakened unknown-telemetry policy. The task contract remains unchanged
for aggregate accounting and delegation authority.

Codex and Claude gateway tool/check/duration/telemetry-policy limits now use the
attempt ceiling, as does SubscriptionAttemptRunner's deadline. Both clients get
the exact attempt budget alongside the aggregate task in their context. Codex's
existing token observation guard now uses the reservation. New tests exposed
missing Claude token-limit guards; added checks on assistant usage and terminal
usage, preserving measured overages for durable settlement. Also aligned Claude's
zero-duration behavior with Codex: DEADLINE before process launch. These are
observed-usage guards, not a claim that an official client can prevent every
in-flight token overage or establish allowance-only billing proof.

TDD/evidence: root-attempt-budget-red.log failed on missing request budget.
Five builder PG checks then passed9.41s (root-attempt-budget-green.log).
root-attempt-budget-gateways-red.log reproduced two Claude token overages accepted
as success; repaired. root-attempt-budget-zero-red.log reproduced Claude's wrong
zero-duration classification; repaired. Final158 protocol/Codex/Claude/lease-runner
unit checks pass4.79s at root-attempt-budget-unit-final.log, including narrower
reserved tool/token limits and no provider invocation for zero duration. Final23
reservation-lineage/request-builder/usage PG cases pass38.73s at
root-attempt-budget-pg.log. Scoped Ruff and mypy7source files pass. Root self-reviewed
this increment; no required check for it remains failing.

No dispatch code was added in this turn: implementation order changed because the
reservation mismatch would have allowed invocation beyond its admitted ceiling.
Next: one worker operation joining admission/request/broker/client/runner/typed
decision application; production tool/client composition; pending-decision restart
handling. Still preserve full remaining scope (approved primary write authority,
later phases, handoff/current-candidate acceptance, allA1-A9, providers/UI/CLI,
independent review, authorized live/CI/PR/release). Known official capability and
review availability limitations above remain; no claim of live conformance.
HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted candidate.
Eleven code/test paths are hashed at .llm-output/root-attempt-budget-candidate.json.
Prior invocation-builder candidate hashes are stale where this turn changed them.
Root owns all paths and PostgreSQL; all process sessions consumed, none active.

Invocation coordinator and bound session composition (11 September): root added
worker/subscription_invocation.py. SubscriptionInvocationWorker.run_once owns
one atomic admission/reservation, persisted request assembly, lazy session
construction, SubscriptionAttemptRunner execution/settlement, then plan/delegate/
wait application only for decision_pending results. Plan uses the existing
SubscriptionPlanGateService; delegation/wait use their existing durable services.
Other typed decisions remain pending with application=None; no acceptance claim.
Application exceptions propagate while leaving the settled source recoverable.
The coordinator is not yet attached to worker/main.py polling.

Worker cancellation sets a shared stop event without cancelling an admission
transaction with an uncertain commit outcome. A stop during admission or request
assembly settles the admitted attempt without provider execution; session setup
failures use closed classifications and explicit measured-zero provider usage.
SubscriptionAttemptRunner now accepts the shared stop_event and retains its
existing revoke-before-cancel/settle-once ownership. Repeated outer cancellation
waits for owned settlement. Errors from decision application are not relabelled
as provider/setup failures. No locks span provider IO.

worker/subscription_session.py adds ControlledSubscriptionSessionFactory. It
requires admission-matching request identity plus explicit durable phase and
attempt budget, builds the exact SubscriptionToolAuthorizationContext and broker
with task ownership/candidate epoch, binds DurableClientProcessLifecycle to the
attempt and lease owner, and lazily initializes one ControlledToolService for
callbacks. Gateway construction is explicitly trusted and lazy: the supplied
factory must resolve the frozen route/capabilities and launch only in execute.
No official-client capability verifier or production tool/client settings factory
was fabricated here; those remain required before wiring production polling.

Evidence: root-invocation-worker-red.log expected missing module. First real
queued primary->request->fake provider->settlement->human plan gate passed2.66s.
Then real brokered repository read and durable ToolCall lineage passed2.59s
(root-invocation-worker-broker.log). Scoped mypy initially found two application
union inference errors; explicit union annotation repaired them.37 coordinator/
runner unit cases pass1.60s at root-invocation-worker-unit-final.log, covering
repeated cancellation during setup/provider, pre-admission stop, shutdown during
admission, setup failures, and pending-vs-stale/fenced decision dispatch.4PG worker/
runner integration checks pass8.06s at root-invocation-worker-pg-final.log, including
artifact publication failure leaving decision_pending with one charged invocation
and no fresh re-invocation. Session missing-module red then4unit guards pass0.47s
(root-subscription-session-green.log), including revoked callback denial before
tool setup. Final2PG planning/gate-publication-failure checks using the reusable
session factory pass4.63s at root-subscription-session-pg.log. Provider responses
and launch proof are explicitly synthetic; repository reads, DB authority, usage,
receipts, artifact store and human gate are real. Scoped Ruff and mypy3sources pass.
Root self-reviewed this bounded integration; no current required check is failing.

Candidate HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted
work; six code/test paths hashed in .llm-output/root-invocation-worker-candidate.json.
All command handles consumed, no process or other writer active; root owns paths
and PostgreSQL. Independent review remains outstanding with prior availability
constraints; no gate waived. Next: production policy-bound tool factory and exact
client/session composition, invocation polling and pending decision restart
handling. Still required: all other decision/phase applications, approved primary
scope, current handoff/candidate acceptance, fullA1-A9/provider/UI/CLI work and
separately authorized live/CI/PR/release. Full objective remains active.

Settled decision startup recovery (11 September): added
SubscriptionDecisionRecovery and a bounded keyset selector returning attempt IDs
and dispatch kinds only. Startup composes it without provider credentials, runs it
after effect reconciliation and before quarantine/normal polling, and reports
applied/deferred/unsupported counts without provider text. It resumes plans,
delegation and wait through their existing source/authority checks. Per-item
failures remain pending and do not prevent other runs progressing; cancellation
propagates and metadata scan failures still fail the startup barrier. No provider
call or new attempt is made. Applied counts completed handler calls, including
existing semantic rejection/repair outcomes, not human approval or acceptance.
Other decision types remain unsupported/pending; periodic retry is still required.

TDD found missing recovery/startup integration, then the PostgreSQL cases exposed
a dispatch encoding mismatch: PlanOutput has decision.type while delegation/wait
use the encoded decision.record.$record. Corrected the metadata selector without
weakening application guards. The two-repository pagination fixture also exposed
a shared fake resource ID; proposal_case now uses repository_resource_identity
for its project. No production scheduler exclusion was removed.

Production-composed restart after artifact publication failure passes with one
provider call and one charged attempt, producing the human plan gate on recovery.
root-decision-recovery-restart.log:9 PostgreSQL invocation/startup checks pass17.50s
with5 existing ADK warnings. root-decision-recovery-unit-final.log:52 application/
startup checks pass2.83s with5 existing ADK warnings. Scoped Ruff formatting/lint
and mypy5 source files pass. Recovery tests cover expired original lease, paused
source deferral, pagination beyond failures, replay, and cancellation. Final
affected PostgreSQL verification is recorded below after completion.

Self-review: database page transactions close before artifact/application IO;
existing handlers retain run/source locking and idempotency. New rows sorting
before the current cursor wait until the next scan. Production invocation polling,
policy-bound tool/client construction, periodic retry and other typed applications
are not added by this increment. Existing delivery tool construction can be reused
for managed worktree adapters, but must resolve subscription authority rather than
legacy AgentRole when wiring the next factory. Full v0.2 scope and outstanding
independent review/live/CI/PR/release gates remain active and unchanged.

Final affected persistence evidence: root-decision-recovery-affected.log reports
73 passed167.30s (recovery, plan gate, delegation and wait); the shared fixture's
remaining preparation consumers pass9 checks21.53s in
root-decision-recovery-preparation.log. No failing check remains for this increment.
Candidate is HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted
work, with11 code/test path hashes and scoped status in
.llm-output/root-decision-recovery-candidate.json. Earlier candidate hashes for
changed paths are stale. Root owns all paths and PostgreSQL; all process handles
are consumed, no process or other writer is active. No live provider calls, full
CI runs, PR or merge were performed. Next implementation boundary is production
policy-bound tool and exact client composition, followed by invocation polling
and periodic pending-decision retry; all other full-plan obligations remain.

Production subscription tool factory (11 September): root assigned the retained
complex implementer a bounded three-path outcome through validation. It returned
inspection only, then a17-line identity-check/unavailable stub despite an explicit
completion follow-up. Root revoked all ownership after the second partial handoff
and completed the preserved module locally; no worker process or writer remains.
This is not successful delegated implementation evidence. Repeated partial
handoffs confirm that this retained worker is not a reliable execution route.

worker/subscription_tools.py now resolves run and versioned project policy in a
short transaction, validates request/admission identity, explicit phase/budget,
current policy/schema/identity and canonical resource, then closes the transaction
before constructing filesystem/Git/environment adapters. It never uses untrusted
context for paths or policy. Lazy construction occurs after broker effect admission,
so it deliberately does not call invocation_resource (which rejects outstanding
effects before launch). Broker and ControlledToolService retain their exact
per-call authority checks. PLANNING without a worktree constructs only canonical
repository reads with policy exclusions. Managed worktrees reuse DeliveryRuntime
validation and adapters; mutations alone enable a writer, while named-check
permission independently enables runner/environment for verification/exploration.
No legacy AgentRole mapping or extra approved scope is introduced. Production
WorkerHandlers.subscription_tools is composed without provider credentials and
available for the forthcoming exact client/session factory.

TDD: replacing the inline PG tool factory with this production factory reproduced
two failures at root-subscription-tools-red.log while the worker stub was still
unavailable. Root implementation repaired them. The missing production handler
binding failed root-subscription-tools-composition-red.log, then was wired. Final
root-subscription-tools-pg-final.log:2 PostgreSQL queued planning/broker read/gate
and publication-failure/restart cases pass6.33s with5 existing ADK warnings. Actual
policy, repository reads, durable effects/usage and plan gate are exercised; model
response and launch proof remain synthetic. root-subscription-tools-affected.log:
57 session/factory/composition/entrypoint cases pass2.45s with5 existing ADK warnings.
Three added guards bring the focused factory suite to15 passing0.61s at
root-subscription-tools-unit-final.log. Tests verify adapter construction outside
transactions, reviewer isolation, verification checks without writes, planning
read-only mode, missing phase/budget and stale/foreign binding denial. Scoped Ruff
format/lint and mypy2sources pass. Root self-reviewed adapter selection, transaction
boundaries and preserved per-call authorization; independent review remains due.

Capability documentation follow-up: opened official Codex app-server documentation
at https://learn.chatgpt.com/docs/app-server (redirect from developers.openai.com).
Its account/rateLimits/read fields report usage windows and optional workspace
credits. This does not establish an enforced allowance-only boundary for the
installed account/client. No capability flag was relaxed and no live application
model call was made. Exact client admission/configuration remains unresolved.

Candidate HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work;
five code/test path hashes and scoped status are recorded in
.llm-output/root-subscription-tools-candidate.json. All command handles consumed;
root owns all files and PostgreSQL. Next: exact client/session composition and
invocation polling, periodic pending decision retry, plus all other phase/decision
applications, approved scope/current handoff acceptance, A1-A9/provider/UI/CLI
obligations and separately authorized live/CI/PR/release. Full goal stays active.

Periodic settled-decision retry (11 September): root inspected the exact runtime
factory and installed-adapter boundaries. They still lack a production capability
verifier proving the required client/model/billing controls; provider polling was
not enabled with fabricated capability reports. Independent actionable gap was
startup-only decision recovery: temporarily unavailable artifact storage or a
paused source could remain pending until another process restart. Root directly
implemented this localized worker orchestration change; retained implementation
routes remain unreliable/unavailable as documented, and no ownership was delegated.

worker/main.py now starts a separate sequential decision retry poll after startup
recovery, alongside normal/control lanes when WorkerHandlers has decision recovery.
Default delay is5 seconds after each completed scan, configurable by the trusted
run_worker decision_retry_interval argument (finite numeric,0<interval<=60; bool,
NaN and infinity rejected before engine construction). Startup already performs
its scan, so the periodic lane waits before retrying. Existing application guards,
per-item deferral and keyset pagination remain unchanged. No provider invocation
or new attempt is performed by this lane. Successful applications log bounded
counts; repeated unsupported/deferred-only scans do not flood INFO logs.

TDD root-decision-poll-red.log failed10 cases on missing poll function. Added
sequential retries, idle-stop, error propagation and cancellation. A subsequent
stop-during-scan red test exposed that stop_event alone left an active scan running;
poll now owns a scan task and stop waiter, cancels on shutdown, and awaits cleanup.
The repeated-cancellation red test exposed a second cancellation interrupting that
cleanup; shielded owned cleanup now preserves the scan until it exits. Database
scan errors still propagate to the worker's existing failure/drain/dispose path;
per-source application deferrals remain retryable. No transaction spans an idle
wait or an overlapping scan in this worker.

Final root-decision-poll-final.log:35 focused polling/entrypoint/startup-barrier
tests pass2.10s with5 existing ADK warnings. Production entrypoint test asserts the
new poll receives the configured recovery service only after startup succeeds.
root-decision-poll-pg.log:6 PostgreSQL recovery cases pass15.29s. After the final
cleanup repair, reran the affected storage-restoration case at
root-decision-poll-pg-final.log:1passed3.71s,5deselected,5existing ADK warnings.
This uses actual pending source, failed/restored artifact store and human gate;
provider attempt rows and consumed attempts stay1. Scoped Ruff format/lint and
mypy worker/main.py pass. Root self-review completed; independent integrated review
and distinct correctness questions remain open under recorded provider/host limits.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted candidate;
four code/test hashes/status at .llm-output/root-decision-poll-candidate.json.
All process handles consumed, root owns all paths and PostgreSQL. Full objective
remains active: exact production client admission/invocation polling, other typed
applications and phases, approved primary write scope, current handoff/candidate
acceptance, full A1-A9/provider/UI/CLI evidence, independent review and separately
authorized live/CI/PR/release remain required. No live application call, CI dispatch,
PR or merge was performed in this turn.

Resumed invocation task outcomes (11 September): previous turn was verified
progress on periodic recovery. Root inspected durable context and found the
request builder supplied contracts but no current task state or recorded worker
handoffs. Added InvocationTaskOutcome to the subscription persistence port and
invocation_outcomes(run_id, task_ids), then integrated it into the existing
run-locked SubscriptionRequestBuilder transaction. It includes the current task
and known tasks, so a fresh primary sees child outcomes and a repair invocation
can see its own prior handoff. Context includes task state/version/pause/cancel
flags and latest recorded handoff; no provider conversation is inherited.

The query accepts1..257 distinct task IDs, requires every task to belong to the
requested run, and selects one latest handoff by attempt number followed by
created_at/id tie-break. Attempt/task/run lineage is checked both relationally and
against decoded payload. Each handoff transfer is capped at64KiB in SQL before
decoding; invalid or oversized latest evidence is rejected, not silently replaced
with older evidence or truncated. The existing request constructor independently
bounds aggregate untrusted context. This can reject an overly large invocation;
no claim of arbitrary-history support is made. No schema migration was needed.

Trusted prompt now explicitly treats recorded outcomes as historical context,
not current candidate validation/acceptance, and its version is
forge-subscription-v2. State and handoff payloads remain in untrusted context and
flow through existing gateway serialization. The change grants no tool, write,
review or acceptance authority. Root implemented this bounded read/context path
locally under the previously recorded development-provider constraints and
self-reviewed lineage, source selection, bounds and transaction scope.

TDD root-task-outcomes-red.log reproduced missing task_outcomes for a real resumed
primary after a child failed. The first green attempt exposed a test decoding
frozen tuple context directly; fixed the test to use the gateways' JSON projection.
Builder6 checks then passed12.30s. Final root-task-outcomes-final.log:14 outcome,
request-builder and real queued planning-worker checks pass30.42s with5 existing
ADK deprecation warnings. Coverage includes real child failure handoff delivery,
latest attempt ordering despite older timestamp, current pause flag, unknown/cross-
run IDs, mismatched payload lineage, oversized latest handoff and task-count bound.
Scoped Ruff format/lint and mypy3source files pass. No live provider call or full
CI run occurred. Provider/launch responses in worker tests remain synthetic.

Candidate HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work;
five code/test hashes/status at .llm-output/root-task-outcomes-candidate.json.
All process handles consumed; root owns all files and PostgreSQL. Full goal stays
active. Current completed-handoff verification/application, approved primary write
scope, acceptance/other decisions and phases, exact client admission and production
invocation polling remain implementation work, alongside all remaining A1-A9,
provider/UI/CLI evidence, independent review and authorized live/CI/PR/release.

Subscription check candidate evidence (11 September): root traced completed
handoff application and found a prerequisite: command receipts proved successful
execution but did not bind uncommitted input files to the handoff snapshot.
Implemented that evidence path before permitting completed-handoff application.
NamedCheckOperationAdapter captures working-tree digests before and after a
subscription command through ControlledGit. Capture runs via asyncio.to_thread
so the event loop can continue lease/stop processing. Existing subscription broker
check effects retain their global effect serialization. The observations do not
claim to detect every transient mutation inside a trusted command or external
editor; current candidate/owned-output fences still belong to the dispatcher.

Subscription terminal receipts now use receipt_version2 with nullable
candidate_tree_digest_before/after, persist those fields content-addressably and
reconstruct them unchanged during reconciliation/replay. Stable, changed and
unavailable snapshots are distinguishable. Snapshot failure or mismatched head/base
leaves an explicit unknown digest and preserves the terminal command result; it
never invents candidate proof or reruns the command. Legacy receipts/version1 and
cancel-before-launch receipts remain supported. Version2 schema/authority/digest
validation rejects missing, malformed, boolean or wrong-version fields.

VerifiedSubscriptionHandoff now reports checks_match_snapshot. It is true only
when every verified named check's before and after digests match the handoff's
verified snapshot (vacuously true with no named checks). Historical check evidence
without candidate fields remains historically verifiable but has false binding;
changed inputs cannot be labelled matching. This does not itself approve the
current candidate or terminalize the worker. The actual completed-handoff
transaction, current owned-output comparison and primary acceptance remain due.

TDD root-check-candidate-red.log reproduced absent before/after snapshots in2PG
cases, then both passed in root-check-candidate-green.log. Handoff missing-proof-
field cases failed root-check-handoff-red.log before implementation. Broad affected
suite root-check-candidate-affected.log:123 checks pass54.48s across named adapter,
receipt codecs, controlled named-check service, historical handoff, subscription
named checks and PG lineage. After offloading snapshots, reran affected subscription
checks (final result below). Added both before- and after-mismatch coverage; final
historical handoff suite24 checks pass0.38s at root-check-handoff-final.log. Scoped
Ruff formatting/lint and mypy3source files pass. Tests use synthetic snapshots and
runner results with real PostgreSQL/artifact/broker/recovery paths; no live provider
conformance claim is made. Root self-reviewed version compatibility, command receipt
preservation, worker responsiveness and metadata propagation. Integrated independent
review remains outstanding under recorded availability limits.

Final offloaded-capture evidence: root-check-candidate-final.log9 subscription
named-check cases pass17.11s. No required check for this increment is failing.
Candidate HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work;
six code/test hashes and scoped status are in
.llm-output/root-check-candidate-candidate.json. All command handles consumed;
root owns all paths and PostgreSQL, no other writer or process is active. Full
v0.2 objective remains unchanged and active, including completed handoff/current
candidate application, approved primary scope, other decisions/phases, exact
client admission and invocation polling, all remaining A1-A9/provider/UI/CLI
work, independent review and separately authorized live/CI/PR/release. No live
application model call, full CI, PR or merge was performed.

Current handoff output comparison (11 September): previous turn was verified
progress on named-check candidate evidence. Root added the missing scoped current-
output comparison to SubscriptionHandoffVerifier. Callers can supply a trusted
GitWorkingTreeSnapshot as current_snapshot; verification compares complete owned
file records (path, mode, content digest, byte count), scoped changed paths and
base identity with the recorded, artifact-verified snapshot. Disjoint sibling
changes are permitted; additions/deletions/content/mode changes within ownership
are rejected. A task with no owned paths requires the whole tree to match.
Aliased file/change paths are rejected using repository path-key semantics.
A claimed candidate commit additionally requires the current head to match it.

VerifiedSubscriptionHandoff now carries output_digest plus optional
current_tree_digest. Historical-only verification leaves current_tree_digest=None;
it never invents a fresh observation. The existing task/handoff/artifact/call
proofs and checks_match_snapshot remain intact. This is observation, not authority:
the forthcoming completed-handoff dispatcher must capture the current snapshot
while retaining task ownership, recheck durable source/epoch and all proof bindings,
then atomically record completion and wake the primary. No terminal application,
current-candidate acceptance, or human approval was added in this increment.

TDD root-current-handoff-red.log:8 current-snapshot tests failed on the missing
parameter. Root implementation produced32 passing handoff cases. Additional checks
cover whole-tree comparison with no owned paths, adjacent-directory scope prefixes,
and absence of invented current evidence. root-current-handoff-final.log:35 pass
0.43s. Scoped Ruff format/lint and mypy2source files pass. Tests exercise real
artifact-byte/receipt validation fixtures with synthetic snapshots; no live client
or whole-runtime acceptance claim. Root self-reviewed comparison scope and historical
compatibility. Integrated independent review remains outstanding.

Candidate HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work;
three source/test path hashes/status at .llm-output/root-current-handoff-candidate.json.
All command handles consumed, root owns all paths and PostgreSQL. Next required
integration is the completed-handoff proposal/current-snapshot/application path,
including durable proof rechecks and idempotent terminalization/primary wake.
Full remaining v0.2 scope and separately authorized live/CI/PR/release gates are
unchanged; no live model call, full CI, PR or merge was performed.

### Completed handoff source proposal (2026-09-11)

Root added SettledSubscriptionHandoff and the decision repository/application
handoff_proposal boundary. It returns the exact retained child contract/handoff,
result digest, current policy/managed worktree, candidate epoch and task version.
The application rolls back its short transaction before returning for external
artifact/Git verification. The task stays reconciling with its original lease
owner/generation and the parent stays blocked; repeated reads do not apply a
decision. Historical receipt references are intentionally not verified by this
source-loading operation.

The repository shares the existing delegation/wait source checks rather than
duplicating their stopped-launch, telemetry/consumption, immutable result/context,
run/policy, task version, candidate epoch and retained lease authority checks.
The handoff branch additionally requires a completed non-primary child with exact
attempt identity, and checks scheduler parent/owned paths/read-only authority
against the frozen contract. It returns before any decision mutation. Expiry of
the original lease alone does not defeat recovery of a stopped settled source;
changed owner/generation, pause/cancel, epoch/version, worktree or proof does.

TDD: root-handoff-proposal-red.log failed on missing handoff_proposal after fixing
the fixture's required evidence ID; initial green passed. Self-review identified
missing scheduler authority comparisons. root-handoff-scheduler-binding-red.log
observed 3 expected failures for changed parent/scope/read-only; implementation
closed all three. root-handoff-proposal-affected.log:47 PostgreSQL cases passed
124.82s (proposal/delegation/wait, before final handoff-only scheduler guards).
root-handoff-proposal-final.log:51 cases passed42.45s (16 PostgreSQL proposal
cases plus35 artifact-verifier tests) on final source. Scoped Ruff lint/format
5paths and mypy4source paths passed. Real PostgreSQL source/launch fixtures were
used, with synthetic provider results and unverified receipt IDs; no live model
call or completed-handoff application claim.

Candidate remains HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved work.
Five path hashes/status/check references: .llm-output/root-handoff-proposal-candidate.json.
Fresh Terra spawn again failed with host agent-thread limit; no worker acquired
ownership. Root performed implementation, focused validation and self-review;
independent integrated review remains outstanding. All process handles consumed;
root retains all path and PostgreSQL ownership.

Next: connect proposal to a current snapshot and verified evidence, then locked
proof rechecks and atomic terminalization/parent wake with idempotent replay.
Read-only/no-owned-path tasks require a durable whole-tree observation fence;
retained task ownership alone does not prevent sibling changes. This source
proposal is not that fence and is not acceptance authority. Do not hold a run
transaction through artifact/Git/provider IO or bypass the pending completion
boundary. Full v0.2 remaining scope and separately authorized live/CI/PR/release
gates remain unchanged; no goal completion, commit, PR, CI dispatch or merge.

### Completed handoff observation, application and recovery (2026-09-11)

Root connected the settled handoff proposal to current output observation,
artifact/receipt verification, locked durable proof rechecks and atomic child
completion/parent wake. SubscriptionHandoffApplication is composed in production
WorkerHandlers and passed to startup/periodic SubscriptionDecisionRecovery.
Pending TaskHandoff decisions now have an explicit dispatch kind. The observer
uses DeliveryRuntime controlled Git on a thread; no artifact/Git IO holds the
source transaction. This completes the valid completed-handoff success path,
not primary candidate acceptance or any human approval/release gate.

Migration0016 adds one bounded observation fence per managed worktree, binding
run/attempt/result digest and a fresh UUID to a PostgreSQL-clock 60-second expiry.
Acquisition requires no outstanding controlled effect on that worktree. Existing
leased siblings may reason/read; new mutating effects and whole-tree commands
are denied while the fence is active. Other worktrees remain independent. Expiry
permits recovery without trusting an old reader; final application requires the
exact unexpired fence and retained source. Release cannot remove a replacement.
No repository-wide agent-duration lock was introduced. Repeated cancellation
waits for the owned release task; interrupted acquisition remains TTL-bounded.

PostgresSubscriptionHandoffEvidence now locks/refetches the run, calls, operation
bindings/accepted receipts, both commit operation phases where present, artifact
content/lineage and parent edges. It compares the existing immutable proof
digests and requires the complete bounded artifact closure. It never reads blob
bytes or performs external IO. A retained worker returned only a scaffold despite
the full outcome contract; root revoked ownership and completed implementation,
tests and self-review. No worker remains active or owns any path.

Final application revalidates stopped source/usage/launch proof, current task and
scheduler authority, policy/epoch, evidence identities, required check snapshot
binding and current output proof. It records the typed handoff, sets child/task
attempt terminal, releases ownership, wakes the primary and removes the fence
in one transaction. Migration0017 stores an independently hashed application
receipt beside the unchanged provider result, including verified evidence hash,
manifest/current/scoped-output digests and observation identity. Downgrade refuses
retained application evidence or an active observation. Exact repository replay
does not repeat the transition; the pipeline also looks up completed durable
receipts before starting another observation.

Evidence: root-handoff-fence-red.log observed missing boundary; initial green1.
root-handoff-evidence-red.log exposed the scaffold accepting a changed receipt;
root completion rejected it. root-handoff-evidence-expanded.log:22 passed75.02s,
including real broker/Git snapshot/commit receipts, operation/publication tampering,
artifact/lineage changes, missing/duplicate proof and fence exclusion/replacement.
The scheduler read-only/global-effect test explicitly seeds that scheduler flag
because its fixture profile lacks a verification route; it tests the scheduler
branch, not provider role admission. root-handoff-application-affected.log:71
PostgreSQL cases passed194.30s (handoff/proposal/fence/delegation/wait). Transaction
tests isolate evidence with a stub; separate digest tests use real evidence.

root-handoff-end-to-end.log: real snapshot handoff recovery passed5.77s with no
mocked verifier, receipts or application. It seeds implementation output and a
synthetic provider result/stopped-client fixture, then uses actual Git, broker,
filesystem artifacts, source settlement, observer/verifier, transaction and
recovery. It verifies terminal child/queued parent and no second recovery action.
No live provider execution or whole-v0.2 acceptance claim is made.

root-handoff-final.log:89 passed160.19s, covering application/fence/evidence,
recovery, existing exclusive effects, pipeline cancellation, worker composition
and populated migration refusal guards. A test-module basename collision surfaced
when application and persistence suites were collected together: the application
file is now test_subscription_decision_recovery_dispatch.py. Production composition
explicitly verifies its configured handoff processor (additional focused1pass).
Ruff lint/format and mypy13source paths pass. root-handoff-replay-final.log:22
passed47.06s on final source, including pipeline replay, concurrent completion,
transaction rollback, stale proof rejection and real end-to-end handoff replay.
All command handles are consumed. Candidate HEAD remains
6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted changes;
21 scoped path hashes/status/check references are recorded in
.llm-output/root-handoff-completion-candidate.json. Root owns all paths and PG.

Remaining: invalid completed handoffs currently defer while retaining ownership;
permanent evidence/output failures need bounded repair/rejection handling, distinct
from temporarily unavailable artifact storage. Primary approved scope, acceptance,
reassignment/scope/review decisions, remaining phases, exact official-client
capability/composition, invocation scheduling/restart, UI/CLI/A1-A9, independent
integrated review and separately authorized live/CI/PR/release remain outstanding.
Existing Opus weekly-limit/native-thread-limit review constraints have not been
waived. No commit, model call, full CI, PR, merge or goal completion occurred.

### 2026-09-11: Definite completed-handoff rejection and bounded repair

Recovery now distinguishes verified historical evidence with a definite current
mismatch from evidence it cannot verify. Changed owned outputs, a changed claimed
commit, or named checks without matching snapshot bindings yield a rejection
observation. Application rechecks the stopped source, current task/candidate,
observation fence and locked historical evidence before persisting a source-bound
application receipt. It queues one budgeted repair or terminals the child and
wakes its primary when repair capacity is exhausted. Replays neither charge again
nor disturb a subsequently admitted repair attempt. Temporary artifact storage
failure remains deferred without consuming repair budget; recovery succeeds after
storage returns. No migration was added for this change.

Validation: root-handoff-rejection-affected.log records 105 passed in 175.14s;
root-handoff-rejection-final.log records 7 passed in 20.04s, including the final
repair-admission replay assertion. root-handoff-assessment-final.log records 38
passed in 0.47s, including named-check assessment. The real Git/broker/artifact
recovery scenarios (same output, changed output, temporary storage failure) passed
3 tests in 16.63s in root-handoff-rejection-end-to-end.log. Focused Ruff lint and
format checks on 11 paths, mypy on 6 source paths, and git diff --check pass.
These tests use synthetic stopped-provider results, not live model calls.

HEAD remains 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db with preserved uncommitted
work. Scoped hashes and validation references are recorded in
.llm-output/root-handoff-rejection-candidate.json. Root owns all paths; no test
process remains active. Independent integrated review remains outstanding under
the previously recorded provider quota and native thread constraints.

Remaining handoff limitation: malformed or otherwise unverifiable historical
claims still defer. Rejection of an intrinsically invalid source claim needs a
separate source-proven predicate; a generic verification failure must not charge
repair budget. Approved primary scope and the broader runtime, UI/CLI, A1-A9,
review and separately authorized live/CI/PR/release work listed above remain
outstanding. No commit, live model call, full CI, PR, merge or goal completion.

### 2026-09-11: Source-proven invalid handoff claims

Completed handoffs with malformed, noncanonical or duplicate receipt IDs, missing
required named-check claims (including typed acceptance), unlisted check receipts,
duplicate checks or claimed failing checks now take a source-proven rejection
path. One shared predicate serves recovery and historical verification. Recovery
does not read Git or artifacts for these contradictions. Application recomputes
the predicate from the exact stopped source under existing current-task, candidate,
policy, usage and observation-fence checks. It persists a distinct canonical
application receipt, then reuses bounded repair/exhaustion and primary wake logic.
The generated failure handoff includes the claim error for the next worker.
Historical replay rechecks the reason against the frozen source, and replay after
repair admission preserves the new lease and does not debit again. External
evidence that cannot be verified still defers without a repair charge.

TDD evidence: root-handoff-claims-red.log failed for the absent pure predicate;
root-handoff-claim-application-red.log failed because invalid claims reached Git.
root-handoff-claim-application-green.log records 8 passed in 6.10s;
root-handoff-claims-guards.log records 19 passed in 20.01s, including cancellation,
expired fence, altered source, rollback and replay after repair admission.
root-handoff-claims-affected.log records 140 passed in 244.77s across handoff,
delegation and wait application. Subsequent shared-predicate reuse and receipt
message validation are covered by focused final checks: unit evidence is
root-handoff-claims-unit-final.log (54 passed in 0.52s). Final PostgreSQL validation
is root-handoff-claims-final.log: 36 passed in 108.67s, including altered observation,
recomputed-but-false receipt reason, completion/rejection replay and real Git/artifact
recovery. Ruff lint/format on 10 paths, mypy on 6 source paths and scoped
git diff --check pass. All process handles are consumed. Candidate HEAD remains
6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work; 11 scoped
path hashes/status and check references are in
.llm-output/root-handoff-claims-candidate.json. Root owns all files and PG.

A fresh complex-worker spawn was rejected by the host's agent thread limit;
no writer started. Root implemented and self-reviewed this bounded change.
Independent integrated review remains outstanding under previously recorded
Opus quota/native thread constraints; these checks do not replace that review.
No migration, commit, live provider call, full CI, PR or merge was performed.

Next integration gap confirmed in current code: resume_prepared changes the
primary's scheduling resource/state but leaves the original empty owned_paths
contract. PlanOutput.affected_components is display prose, not canonical path
authority. The next change must bind an explicit structured scope to the approved
plan, show it in plan evidence, and install it atomically at prepared resume while
preserving legacy plan bytes/digests and all human approval checks. Broader runtime,
remaining phases, official-client capabilities, UI/CLI, A1-A9 and authorized
review/live/CI/PR/release requirements remain unfinished; the full goal is active.

### 2026-09-11: Approved subscription primary scope and named checks

New subscription protocol plans require ScopedPlanOutput, an explicit owned_paths
extension of legacy PlanOutput. Canonical paths are bounded to 64; empty means no
writable paths. Retained legacy maps/JSON still decode as PlanOutput with identical
serialization/schema. Subscription evidence loaders read either form; the plan
digest binds scoped fields through the existing human approval chain. At prepared
resume, the repository recomputes the approved contract from the verified stopped
source, installs paths and named checks atomically into task/scheduler state, and
preserves primary identity, route and cumulative budget. Initial replay checks the
installed scope; replay after a newer invocation does not overwrite later state.
No scope is inferred from affected_components. New scoped plans cannot approve
unregistered checks or check names that a subscription task cannot represent.
The planning prompt is forge-subscription-v3 and explicitly requests canonical paths.

The plan panel displays writable paths (including explicit empty scope), preserves
legacy rendering and rejects malformed scope data. Python 3.14.5 and Node24.20.0
were used. TDD: root-approved-scope-domain-red.log and
root-approved-scope-protocol-red.log show the missing contract/protocol behavior;
root-approved-scope-application-red.log proves the previous resume omitted both
paths and named checks. root-approved-scope-check-name-red.log proves the additional
pre-approval identifier validation. Frontend behavioral red evidence is
root-approved-scope-ui-red-npm.log. An initial mistaken pnpm invocation installed
dependencies instead of testing; its accidental lockfile is preserved only under
.llm-output/root-approved-scope-accidental-pnpm-lock.yaml. npm ci restored the
repository's locked dependencies; package manifests/lockfile are unchanged.

Validation: root-approved-scope-unit.log:174 passed1.16s (including legacy contracts,
fake gateway, planning and approval); root-approved-scope-affected.log:86 passed
187.23s (PostgreSQL approval/preparation/delegation/requests/planning). Final scoped
checks after self-review: root-approved-scope-unit-final.log:60 passed0.32s;
root-approved-scope-final.log:8 passed19.04s. They cover exact scope/check installation,
empty/outside-scope effect denial, real stopped-result delegation and child admission,
cancellation, rollback, source mismatch, replay scope mismatch and unregistered checks.
root-approved-scope-ui-final.log:6 rendered component tests passed1.15s. Ruff on
12 Python paths, mypy on 6 sources, focused ESLint, web TypeScript and scoped
git diff --check pass. No live provider execution or complete vertical-slice claim.

All process handles are consumed. Fresh complex-worker spawning again failed at
the native thread limit, so root implemented/self-reviewed; independent integrated
review remains outstanding under the recorded Opus quota/native thread constraints.
HEAD remains 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work;
scoped hashes/status/checks are in .llm-output/root-approved-scope-candidate.json.
No migration, commit, live model call, full CI, PR or merge occurred.

Material next finding from source inspection: scheduling._has_worktree_conflict
treats the primary's approved logical scope like a worker's active path reservation.
A queued primary therefore cannot coordinate while overlapping child scopes remain
leased/reconciling. Address primary coordination admission and actual effect fences
together; do not simply exempt primary writes or disable child ownership. This is
an outstanding A1/A2 scheduling limitation, not a waived acceptance condition.
Invalid plan proposals that fail policy validation still need bounded replan/repair
recovery. Acceptance/reassignment/scope/review decisions, remaining phases, official
client capability/composition, invocation scheduling/restart, UI/CLI/A1-A9,
independent review and authorized live/CI/PR/release remain incomplete.

## 2026-09-11 primary coordination and effect fences

The scheduler now derives a coordinating primary from the exact persisted logical
contract, so its approved scope no longer reserves child paths for an entire
provider turn. Primary and child leases can start in either order. Global, run
and provider capacity limits remain enforced. Primary mutation/unknown effects
conservatively acquire the existing durable whole-worktree fence; active child
writers must drain first. Only plain repository reads with exact persisted
operation/attempt/lease bindings bypass that fence. Snapshot/commit/check effects
remain exclusive. Legacy nonexclusive primary effects retain replay identity but
also fence new writers unless verified as bound reads. Direct effect admission
now locks the run first, serializing it against claims. No migration was needed.

Root owns this batch following the recorded failed complex-worker spawn (host
thread limit). The concurrency and authority changes warrant independent review;
that remains outstanding under the recorded Opus quota/native-thread constraints.
Self-review and tests do not waive it. Owned code/test paths are scheduling.py and
new test_subscription_primary_coordination.py; the runtime plan documents behavior.

TDD evidence: .llm-output/root-primary-coordination-red.log shows the two expected
failed second-lease admissions; root-primary-coordination-green.log shows 2 passed.
Intermediate guard runs exposed test harness issues (missing cleanup fixture,
malformed-contract setup ordering and missing select import), which were fixed.
root-primary-coordination-current.log:21 passed36.01s. A real broker write test
then demonstrated exclusive admission, child blocking, exact replay without a
second effect, and child progress after settlement.

Affected PostgreSQL suites in root-primary-coordination-affected.log:155 passed
333.36s across scheduler, scheduler acceptance, exclusive effects, effect recovery,
terminal effect recovery, handoff fences/application, delegation and approved scope.
Final root-primary-coordination-final.log:23 passed40.79s, additionally proving a
completed child's failure wakes the approved primary while its sibling keeps
working, with primary writes still denied. Tests also cover exact read-binding
mismatches/aliases, legacy admitted/reconciling effects, independent worktrees,
capacity, malformed primary identity and three claim/effect races. Real broker
and durable PostgreSQL paths use fake tool/provider results; no live model proof.
Ruff on both Python paths, mypy on scheduling.py, and scoped diff checks pass.
Python3.14.5 was used. All process handles are consumed.

HEAD remains 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work.
.llm-output/root-primary-coordination-candidate.json records scoped hashes/status.
No commit, live model call, full CI, PR or merge occurred. This resolves the
previous checkpoint's primary path-reservation limitation, not all of A1/A2.

Next: invalid plan proposals with unregistered checks currently remain deferred.
Separate semantic proposal rejection from stale policy/source failures; prove the
current stopped primary result before charging bounded repair, preserve original
result bytes and make rejection replayable. Existing plan-gate _proof combines
producer proof and gate creation, so factor that authority check rather than
bypassing it or classifying every PlanEvidenceValidationError as a bad proposal.
Acceptance/reassignment/scope/review decisions, remaining phases, official-client
capabilities/composition, invocation scheduling/restart, UI/CLI/A1-A9, independent
review and authorized live/CI/PR/release remain incomplete. The full goal is active.

## 2026-09-11 bounded invalid-plan recovery

Resolved the prior checkpoint's unregistered-check plan deferral. The evidence
builder now validates current task/project/policy identity before distinguishing
an invalid scoped plan from source failure. request_settled applies that semantic
rejection through the plan-gate repository in the same transaction. The repository
reuses its exact producer/context/route/launch/current-control proof without
creating approval authority, checks settled usage and the policy contradiction,
and uses the existing scheduler and repair-budget debit. It records an explanatory
TaskHandoff plus an application receipt; original result bytes/digest are preserved.
Available repair queues the primary once; exhaustion terminalizes it without a
new debit. Exact replay verifies retained receipt, producer, decision and debit
and does not overwrite a newer invocation or plan approval. Stale source/policy,
cancellation, unknown launch, pending effect and usage mismatch defer without debit.
Legacy plans retain their existing compatibility behavior. No migration was needed.

Root implemented after a fresh complex-worker spawn again failed at the native
thread limit. Ownership covered application/ports/subscription_plan_gate.py,
application/services/subscription_plan_gate.py and plan_evidence.py, and
persistence/repositories/subscription_plan_gate.py. New test_subscription_plan_rejection.py
covers rejection; the prior approved-scope test now expects bounded rejection
instead of indefinite deferral. docs/v0.2-subscription-runtime.md documents behavior.
This is an authority/recovery change: independent integrated review remains
outstanding under the recorded Opus/native-thread constraints, not waived.

TDD: .llm-output/root-plan-rejection-red.log:1 expected failure (old generic approval
source error instead of requeue); root-plan-rejection-green.log:1 passed2.37s.
Intermediate guards:10 passed,2 test-harness failures. PostgreSQL correctly refused
an immutable policy-row edit; the corrected test installs version2 and advances
current policy. The other assertion used leased instead of logical state running.
root-plan-rejection-final.log:24 passed48.67s across rejection and approved scope.
A first affected command had a nonexistent test filename and collected no tests;
its log is root-plan-rejection-affected.log. Corrected command evidence:
root-plan-rejection-affected-current.log:98 passed166.09s,5 dependency deprecation
warnings, across rejection, approval/preparation, approved scope, decision recovery,
request builder, real invocation worker and dispatch/worker tests. This includes
corrected-plan admission -> stopped result -> existing human plan gate, plus old
rejection replay without disturbing that gate. Final added adversarial receipt
plan-binding case passes1 in2.38s (root-plan-rejection-binding-red.log was a red
probe that proved the existing evidence validator already blocks the attack;
no extra source change). No relevant source changed after the 98-test run.

Coverage includes atomic concurrent rejection, one debit, original result identity,
repair/provider-attempt exhaustion, rollback, stale policy/source, cancellation,
missing launch, unsettled effects, usage mismatch, corrupt receipt/decision/debit,
valid-plan rejection denial, replay after a new invocation, and periodic recovery
removal from pending applications. Ruff six Python paths and mypy five source
paths (including the invocation caller) pass. Python3.14.5; all handles consumed.

HEAD remains 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db with preserved uncommitted work;
scoped hashes/status/checks: .llm-output/root-plan-rejection-candidate.json.
No commit, live application model call, full CI, PR or merge occurred. The full
v0.2 goal remains active. Acceptance/reassignment/scope/review decisions, later
lifecycle phases and run-level terminal outcomes, official-client capabilities and
composition, invocation scheduling/restart, UI/CLI/A1-A9, independent review and
authorized live/CI/PR/release remain incomplete. Continue from the actual revised
plan and current code; these two scheduling/replan fixes do not prove all slices.

## 2026-09-11 scope-request application and bound response contract

Previous turn classified as verified progress (bounded plan recovery). Current
work adds the missing ScopeRequestDecision application path using the existing
stopped-source/current-control/usage/effect/epoch/worktree proof. Accepted requests
record the original decision, terminalize the provider attempt, block the worker
and release its slot, preserving original result and owned paths. Periodic decision
recovery and immediate invocation dispatch now apply scope requests. Read-only or
oversized requests use existing bounded decision rejection. No paths are granted.

Scheduler wake-up handles both delegated primaries and primaries waiting on the
requesting child. A primary trying to wait after a request already exists stays
runnable. Shared pending-request lookup validates latest terminal attempt, accepted
scope_requested result, canonical source digest and exact request record. That
lookup also supplies InvocationTaskOutcome scope_request_attempt_id/scope_request
as untrusted request context. Prompt version forge-subscription-v4 explains the
request is not authority and names the response correlation field.

The model-facing scope_response previously accepted no request identity. New
BoundScopeResponseDecision inherits the legacy contract and requires a non-nil
request_attempt_id, an answer, and disjoint normalized grant/deny keys. New protocol
output uses this type; domain serialization registers it. Legacy ScopeResponseDecision
encoding is unchanged. Durable response application is NOT implemented yet; new
responses remain unsupported/pending, just as old responses were. The persistence
_RECORDS closed tuple still needs the new type with that application change.
Do not describe scope negotiation as complete or grant from metadata alone.

Root owns this batch after the fresh scope_negotiation complex-worker spawn failed
at the host thread limit. No worker started. Ownership includes subscription and
subscription_decisions ports/repositories, decision/recovery/request services,
scheduling, invocation worker, domain/protocol, focused tests and runtime plan docs.
Independent integrated review remains outstanding under the recorded Opus quota
and native-thread constraints; no authority or release gate was waived.

TDD: .llm-output/root-scope-request-red.log:1 expected missing apply_scope_request
failure; root-scope-request-green.log:1 passed3.41s. root-scope-request-guards.log:
8 passed21.23s. root-scope-request-affected.log:125 passed244.53s with5 dependency
deprecation warnings, covering scope, delegation, primary coordination, handoff,
request/context, recovery and immediate worker dispatch. Added timing coverage
proves an already-waiting primary wakes when its selected child later requests
scope. Other coverage: existing request prevents sleeping, exact concurrent replay,
rollback, changed source, cancellation, missing launch, pending effects and recovery.

Response-contract TDD: root-scope-response-contract-red.log shows old decoding did
not reject a missing request identity. root-scope-response-contract-green.log:
129 domain/protocol tests passed0.37s. Additional legacy/bound roundtrip and Windows
case grant/deny contradiction checks pass in root-scope-response-contract-final.log:
50 passed0.28s. After all source/prompt changes, root-scope-request-final.log:
163 passed68.67s across scope application, request builder, invocation context,
protocol and domain tests. No source changed afterward. Ruff14 Python paths and
mypy11 sources pass. Python3.14.5; all process handles consumed. Fake providers and
real PostgreSQL prove these paths; no live official-client capability claim.

HEAD remains 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work.
Scoped hashes/status/checks: .llm-output/root-scope-request-candidate.json.
No commit, live model call, full CI, PR or merge. Full v0.2 goal remains active.

Next concrete outcome is recorded in .llm-output/root-scope-response-next.md:
apply BoundScopeResponseDecision against the exact latest stopped child request,
partition requested paths, keep grants inside approved primary scope, preserve
child identity/budget/route, queue through sibling ownership checks, resume denial
with context, and bind exact historical replay without overwriting later state.
The request consumes an attempt; continuation tests need a child budget above1,
not a reset. Re-prove child source before granting. Then acceptance/reassignment/
review, remaining phases/run terminal outcomes, official-client capabilities and
composition, invocation scheduling/restart, UI/CLI/A1-A9, independent review and
authorized live/CI/PR/release remain outstanding.

## 2026-09-11 bound scope-response application

Previous turn was verified progress (scope requests/bound response contract).
This turn completes durable grant/deny application for BoundScopeResponseDecision.
The handler proves the stopped primary and referenced worker request using the
existing decision source/usage/launch guards, requires the exact latest pending
request and current blocked child version/generation/contract/effect/control state,
and partitions requested path keys into grants and denials. All resulting owned
paths must remain within the frozen primary scope. Only owned_paths changes in
the child contract; route, identity, budget, checks, dependencies and acceptance
stay unchanged. Both grant and denial queue the child with its old lease released.
Normal scheduler claims preserve active sibling ownership and capacity limits.

An application receipt binds response/request result digests, request attempt,
child prior version/digest and updated contract. Both original provider results
remain unchanged. Replay re-proves historical sources and exact receipt/record
without overwriting later child state. A new response cannot answer an already
resumed request or a different later request from the same child. Invalid target,
request identity, partition or outside-scope grant uses bounded primary rejection;
stale/uncertain current source defers without repair debit. New typed response
records are attached to the proven primary producer, not the target child; this
also allows recording rejection of a nonexistent target. Legacy unbound responses
are not given inferred authority and remain unsupported pending records.

Periodic recovery and immediate invocation dispatch now apply bound responses.
InvocationTaskOutcome exposes the latest applied answer and source attempt as
untrusted context to the worker. Querying version-one record fields uses their
observed ordered name/value-pair encoding; source/result/receipt identities are
checked before exposing the answer. Prompt forge-subscription-v5 tells workers
the current contract is authoritative, not a historical response. No migration.

Root implemented after a fresh scope_response_application complex-worker spawn
failed at the host thread limit. Scope: subscription/subscription_decisions ports,
decision/recovery/request services, subscription/subscription_decisions repositories,
invocation worker and focused scope/dispatch tests. The authority/concurrency change
still needs independent integrated review under the recorded Opus quota/native
thread constraints. Self-review and tests are not a waiver.

TDD: .llm-output/root-scope-response-red.log:2 expected missing-handler failures;
root-scope-response-green.log:2 passed6.37s. Context tests initially exposed an
incorrect assumption that serialized dataclass fields were a JSON object; the
query now follows their actual ordered pairs. root-scope-response-context-final.log:
2 passed6.58s, including worker context and replay after a new child invocation.
root-scope-response-target-red.log proves the former generic recorder rejected a
nonexistent target before bounded repair; producer association is now correct.
Guard tests caught a real missing path-containment check: shared child-authority
validation covers budgets/routes, while delegation checks paths separately in
its DAG validator. The response path now explicitly enforces containment. Test
harness corrections restored misplaced assertions and used scheduler.finish.

root-scope-response-guards-final.log had38 passed and1 harness method-name failure,
then corrected affected tests passed. root-scope-response-affected.log:151 passed
295.61s,5 dependency deprecation warnings, across response/request, delegation,
primary coordination, handoff, request/context, recovery and immediate dispatch.
Final root-scope-response-final.log:24 passed70.94s. It includes grant/deny and mixed
answers, unchanged budget/route/results, new child admission and answer context,
outside authority/unknown target/request/partial partition rejection, cancellation,
pause/version/generation/path/effect/source/launch guards, rollback/concurrent
replay, active sibling conflict/release, receipt/request/decision corruption,
stale responses after resumption and after a second request, and periodic recovery.
Ruff11 Python paths and mypy8 sources pass; scoped diff checks pass. Python3.14.5.
No source changed after matching test evidence. All process handles are consumed.

HEAD remains 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work.
Scoped hashes/status/checks: .llm-output/root-scope-response-candidate.json.
No commit, live model call, full CI, PR or merge. Full v0.2 remains active; these
checks prove this request/response path, not all A1-A9 or production capability.

Next prioritize the successful delivery path: AcceptDecision and ReviewSelection
are still only recorded types and remain unsupported by application dispatch.
Current DeliveryService always advances successful validation to REVIEWING; its
validation/review evidence flow is legacy-oriented. Scheduler begin_candidate/
close_candidate have no application-service callers. Read the revised lifecycle
plan and integrate stable candidate closure, evidence-bound primary acceptance and
policy-permitted review/no-review into existing human approval gates. Do not force
legacy runs through new authority or fabricate reviewer approval. Reassignment,
later lifecycle/run-terminal outcomes, official-client capabilities/composition,
invocation scheduling/restart, UI/CLI/A1-A9, independent review and authorized
live/CI/PR/release also remain incomplete. The prior scope-response-next contract
is now implemented and is historical context, not a new pending assignment.

### 2026-09-11 closed-candidate scheduler admission

Root implemented the scheduler prerequisite for stable-candidate inspection after
fresh native workers were host thread-capped and the retained worker explicitly
reported inability to execute. No worker retains ownership. The previous turn's
investigation supplied the owning boundaries; no additional provider call was made.

Scheduler claims now consider open or closed candidates, then authenticate the
locked persisted task: only an exact primary coordinator or matching read-only
logical contract may claim closed. Draining still blocks admission; queued writers
stay queued. Primary plain repository reads require the existing exact durable
operation/attempt/lease binding to pass the closed effect barrier. Unknown effects,
writes, named checks and exclusive effects remain denied for the primary closed.
This does not loosen snapshot exclusivity or reopen edits. Capacity and existing
lease/source guards remain in the claim path. No migration or human-gate change.

TDD root-closed-candidate-red.log: expected missing closed primary claim failed,
draining passed. First green run exposed an incorrect test epoch expectation:
close_candidate increments epoch, now asserted correctly. Matching affected run
root-closed-candidate-affected.log:67 passed115.37s. Final run after import sorting
root-closed-candidate-final.log:36 passed62.65s, including five read-only contract
cases, closed/open exact read binding cases, denied closed writes, draining,
capacity, primary/worker exclusion and existing coordination cases. Ruff3 paths,
mypy scheduler and scoped diff checks passed. Python3.14; disposable PostgreSQL
fixtures. All command handles terminal and consumed. Independent integrated review
remains required and unavailable under the previously recorded routing constraints;
self-review/tests do not waive it.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved changes. Scoped identity:
.llm-output/root-closed-candidate-identity.json. Changed this turn: repository
scheduling.py, test_subscription_primary_coordination.py, new
 test_subscription_closed_candidate.py and this checkpoint. No commit/live/CI/PR.

Remaining immediate integration: subscription request assembly must obtain durable
candidate state (epoch alone does not distinguish open/closed) and restrict tool
advertisement; execution admission/context must explicitly guard candidate state.
Then implement stable closure/observation and ReviewSelection/Accept dispatch,
review/no-review evidence and existing delivery/human gates. Git diff snapshots are
still conservatively denied closed for primary and need deliberate read evidence
integration. Enqueue remains open-only; later review-task creation must be
candidate-bound. Full v0.2/A1-A9, official clients, remaining lifecycle, independent
review, authorized live/CI and PR/release are incomplete. Goal remains active.

### 2026-09-11 candidate-aware invocation context

Previous turn classified progress: closed-candidate scheduling was implemented and
verified. Root continued the invocation authorization integration directly under
recorded provider/native worker unavailability; no worker owns files or processes.

Replaced invocation_resource with a typed invocation_context returning a resource
identity and validated candidate_closed state under the existing run transaction.
It preserves all existing attempt/task/lease/epoch/envelope/policy checks and uses
the scheduler's persisted primary/read-only contract proof for closed admission.
Both execution admission and context assembly reject draining, unadmitted runs and
closed writers, including state changes without epoch changes. RequestBuilder now
intersects closed tools with the four currently supported plain repository reads;
prompt v6 explicitly describes CLOSED and supplies candidate_epoch as context,
without representing the epoch as validation or approval evidence. Updated all
callers/tests and one worker comment; no remaining invocation_resource callers.
This remains a prerequisite: Git/status/diff and evidence-read integration still
need deliberate candidate-bound effect support; this four-read surface is not the
intended final acceptance tool surface. Later phases are not silently enabled.

TDD root-candidate-context-red.log:1 expected failure showed write/check tools still
advertised closed. root-candidate-context-green.log:1 passed3.27s after repair.
root-candidate-context-affected.log:65 passed110.43s across context, request builder,
primary coordination and closed-candidate scheduling. Tests include closed primary
request, unchanged-epoch draining/closed writer context rejection and direct
execution admission recheck after a lease. Existing execution admission/constraint
suites root-candidate-context-execution.log:9 passed15.65s. Ruff5 paths, mypy3 source
paths and scoped diff checks passed. Python3.14 and disposable PostgreSQL fixtures.
No source changes after matching evidence; all handles terminal and consumed.
Independent integrated review remains outstanding under recorded route limits;
self-review/tests are not a waiver. No live call/full CI/commit/PR/merge.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted candidate.
Scoped identity .llm-output/root-candidate-context-identity.json covers port,
request service, execution repository, worker comment, two tests and this document.

Next prioritize actual ReviewSelection/Accept application and durable stable
candidate intent/observation, rather than further generic guard expansion. _finish
already releases the stopped primary slot and terminalizes its producer, but a
candidate intent needs recoverable receipt identity and historical source proof;
Git observation must occur outside the DB transaction and be revalidated. Preserve
legacy delivery gates and record policy-permitted no-review without fabricating
reviewer approval. Candidate-bound review-task creation/reopen remediation, final
evidence validation and acceptance dispatch remain absent. Full eight slices,
A1-A9, client conformance, remaining lifecycle, independent review and authorized
live/CI/PR/release remain incomplete; active goal is unchanged.

### 2026-09-11 durable review-selection candidate intent

Previous turn was verified progress on candidate-aware invocation context. Root
implemented the first actual ReviewSelection preparation transaction directly;
provider/native worker limitations remain as recorded, no active worker ownership.

New prepare_review_selection port/service/repository operation reuses _apply's
stopped primary result, launch, usage, envelope, policy, version, epoch and control
proofs. It requires every other logical task terminal, then atomically records the
selection intent, blocks primary/releases lease, terminalizes producer, closes the
scheduler barrier and stores candidate_intent receipt. Receipt binds original result
digest, proposed commit/tree and incremented epoch; it does NOT contain verified
identity or reviewer approval. candidate_prepared is preparation only. Replays
re-prove original source plus receipt/record identity and never advance epoch again.
Rollback preserves original pending producer/open barrier. Provider result bytes
remain unchanged. No migration or policy/human-gate change.

TDD root-review-intent-red.log:1 expected missing-method failure;
root-review-intent-green.log:1 passed2.87s. root-review-intent-affected.log:68 passed
188.86s across intent, delegation, scope response and wait application. Static
checks found nested-if style and optional receipt typing; merged the condition
with explicit absent-receipt rejection. Final root-review-intent-final.log:8 passed
20.79s. Includes source/usage/pause/unfinished task refusal, receipt/digest corruption,
rollback and concurrent replay. Ruff4paths, mypy3sources and scoped diff pass.
Python3.14, disposable PostgreSQL. No source changes after final evidence; all
process handles consumed. Independent integrated review remains outstanding under
recorded route limitations, not waived by these checks.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work. Scoped
identity .llm-output/root-review-intent-identity.json. No live call/CI/commit/PR/merge.

IMPORTANT remaining lifecycle: this preparation API is not wired to worker dispatch.
Doing so before observation/recovery/reopen exists would strand a run frozen.
Next build recoverable observation of candidate_prepared: reload/re-prove intent,
construct ManagedWorktree/policy context, capture trusted GitWorkingTreeSnapshot
outside transaction, and finalize only after current run/task/candidate epoch/control
recheck. Validate proposed identity, reviewer route/task, current policy and evidence;
on mismatch explicitly reopen for bounded remediation. Pending application discovery
currently only finds decision_pending/unaccepted and needs a prepared-stage branch
once the pipeline exists. Only then wire ReviewSelection immediate/periodic dispatch.
Primary AcceptDecision still lacks application, as do review/no-review delivery
integration and candidate-bound final evidence. Receipt preparation alone is not
selection acceptance, A1-A9 or full v0.2 completion. Other lifecycle/client/UI/CLI,
independent review and authorized live/CI/PR/release requirements remain active.

### 2026-09-11 prepared candidate inspection

Previous turn classified verified progress on durable review-selection preparation.
Root continued under recorded worker/provider unavailability; no new provider call
or worker ownership. Added CandidateInspection/PreparedReviewSelection application
contracts and SubscriptionCandidateInspection service. It commits preparation,
loads/re-proves the prepared source under a short transaction, performs injected
trusted GitWorkingTreeSnapshot IO outside transactions, and records observation
only after comparing current prepared context again. Source guards cover run/task
versions, phase/control, policy/envelope, task/lease/barrier/epoch, worktree identity,
other unfinished tasks and pending effects. A real second transaction in the test
locks the run during snapshot IO (5s timeout), proving the IO boundary.

Observation stores actual head/base/tree/manifest digests in the intent application
receipt and recomputes its digest. Proposed identity remains distinct and is NOT
trusted or replaced; a mismatch is retained as evidence, not converted to approval.
Typed receipt validation extends intent replay; repeated inspect avoids Git IO.
Cancellation leaves candidate_prepared without observation and a new service can
retry. No schema migration, result-byte mutation, reviewer approval or human-gate
change. Production composition/dispatch remain unwired pending complete finalization
and reopening; this callable application stage alone does not finish ReviewSelection.

TDD root-candidate-inspection-red.log:expected missing-service failure. Initial green
caught incorrect positional command-repository construction, repaired to session=.
root-candidate-inspection-green-final.log:1 passed3.16s. Affected log
root-candidate-inspection-affected.log:35 passed1 fixture failure105.99s; assigning
PAUSED directly violated ck_runs_suspension_state_shape. Fixed test to use real
runs.pause. Self-review found proposal loading implicitly prepared pending state;
root-candidate-inspection-loader-red.log proves it, and loader now requires already
candidate_prepared before source replay. Final root-candidate-inspection-final.log:
19 passed46.55s. Includes post-IO epoch/phase/task/run version/pause/barrier/base
changes, cancellation/retry, malformed rehashed receipt, read-only loading, original
intent rollback/source/replay tests. The35 passing affected cases also included
existing handoff application; subsequent edits touched only candidate loader and
test fixture/formatting. Ruff5paths/mypy4sources/scoped diff pass. No changes after
matching final evidence; all handles terminal/consumed. Independent integrated
review remains outstanding under recorded route constraints, not waived.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted candidate.
Scoped identity .llm-output/root-candidate-inspection-identity.json. No live call,
fullCI, commit, PR or merge. Full goal remains active.

Next: finalization must compare observed identity with ReviewSelection and validate
reviewer route/task plus project policy; explicitly reopen edits for bounded
remediation on mismatch. Successful selection must create a candidate-bound review
task or queue primary with recorded policy-permitted no-review context. Keep review
approval distinct from observation and primary acceptance. Wire durable prepared
stage discovery and immediate/periodic execution only when all continuations exist.
Current pending_applications only selects decision_pending/unaccepted. Broker closed
read surface still needs candidate-bound Git/evidence reads. AcceptDecision and final
evidence/legacy validation-to-human-approval integration remain absent; other full
v0.2 lifecycle/client/UI/CLI/A1-A9, review and authorized live/CI/PR/release incomplete.

### 2026-09-11 bounded recovery of candidate identity mismatch

Previous turn classified verified progress on recoverable inspection. Root continued
under recorded provider/native worker limits. Implemented explicit
reject_candidate_mismatch port/service/repository operation for the mismatch branch
of selection finalization. It requires the current prepared source and an observed
commit/tree contradiction. Under that same run transaction it consumes at most one
repair allowance, records a specific TaskHandoff failure, reopens candidate with one
epoch increment, and queues the primary or terminalizes its task on exhaustion.
Provider result bytes and prior inspection stay intact; an application rejection
receipt records reason, repair choice and reopened epoch. Durable disposition is
candidate_repair_queued or candidate_rejected with accepted=False.

Historical replay reuses stopped-source/usage/launch proofs, validates observation,
actual mismatch, rejection receipt/digest and exact failure handoff, then returns
without overwriting a later primary invocation or spending another repair. Matching
trees/commits and absent observation cannot enter recovery. This method performs no
repository mutation, approval or publication. Matching candidate selection is still
unfinished; no-review/review-task success paths and worker dispatch remain unwired.

TDD root-candidate-rejection-red.log:expected missing-method failure;
root-candidate-rejection-green.log:1 passed3.08s. Affected
root-candidate-rejection-affected.log:51 passed152.44s across rejection, inspection,
preparation and scope response. Mypy requested explicit optional-epoch narrowing in
historical receipt validation; added it, then final
root-candidate-rejection-final.log:19 passed48.32s. Includes matching/absent observation
refusal, commit mismatch with equal tree, exhaustion, rollback/concurrent replay,
replay after new invocation, pause/epoch guards and corrupted receipt/digest. Test
selection_case now accepts optional primary budget/tree digest/commit so claims are
settled normally instead of modifying immutable result bytes. Ruff5paths and
mypy3sources pass; scoped diff checks pass. No source edits after matching final
checks. All handles terminal/consumed. Independent review remains outstanding and
is not waived by self-review/test evidence.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work. Scoped
identity .llm-output/root-candidate-rejection-identity.json. No live/CI/commit/PR/merge.
No active worker owns files or processes. Goal remains active.

Next must implement successful selection finalization, not additional generic
rejection guards: compare matching observation to selection, bind an approved
reviewer route and new read-only task when requested, or queue primary with explicit
candidate-bound no-review record. Scheduler enqueue currently rejects all closed
candidates; any new review admission must authenticate the persisted read-only
contract under the run lock. Review child completion/evidence and closed Git/evidence
reads still require integration. Keep legacy reviewer/human approval behavior intact;
project reviewer_model is configuration, not an explicit new mandatory-review flag.
Only wire immediate/periodic ReviewSelection dispatch when matching and mismatch
continuations plus prepared-stage discovery are complete. AcceptDecision, final
validation/approval evidence, run-terminal outcomes and the rest of full v0.2/A1-A9,
client/UI/CLI/review/authorized live/CI/PR/release remain incomplete.

### 2026-09-11 successful candidate review selection

Previous turn classified verified progress on mismatch recovery. Root implemented
matching-candidate finalization directly under the recorded worker/provider limits.
finalize_review_selection re-proves prepared/observed current authority, requires
matching proposed tree/optional commit, and records review_selected. No-review queues
the primary while preserving the closed barrier. Review selection creates exactly
one read-only INDEPENDENT_REVIEW child on the frozen approved effective route, with
budget bounds inherited from primary, no writable paths/named checks, and no repair
allocation. Optional review_task_id is respected; otherwise its ID derives from the
producer attempt. Child construction uses shared validate_child_authority. This is
selection, not reviewer approval or primary/human acceptance.

Selection receipt binds the child contract (or explicit no-review choice) alongside
original intent/observation. Historical replay checks that receipt, matching identity
and persisted review child without requeueing or changing a later invocation. Both
roles receive current-epoch selection/inspection context in prompt v7; it explicitly
grants no approval. Scheduler enqueue now permits authenticated read-only contracts
closed, retaining write denial, and locks the run first. Self-review found begin/
close needed the same ordering; both now lock run before scheduler rows. Context
lookup filters current closed epoch before loading historical source, so superseded
selection does not block or describe a later candidate.

TDD root-review-finalization-red.log:2 expected missing-method failures.
root-review-finalization-green.log:2 passed5.92s; context log2 passed6.52s. Mypy caught
reuse of local 'created' with the older dependency set; renamed review-local binding.
root-review-finalization-affected.log:95 passed180.42s across finalization, mismatch,
request, primary coordination and scheduler suites. Self-review boundary tests:
root-review-finalization-boundaries-red.log:4 expected failures (begin/close did not
wait for held run lock; old selection was re-proven for superseded candidate).
Fixed both. Final root-review-finalization-final.log:56 passed108.73s across selection,
lock order, scheduler acceptance and request assembly. Coverage includes review/
no-review role admission and context, rollback/concurrent exact-once creation,
source/replay corruption/missing child, unapproved route refusal, stale context,
and serialized candidate transitions. Ruff11paths/mypy5sources/scoped diff pass.
Fixture helpers now optionally seed an approved review route before real plan
approval; default existing fixtures retain their prior routes. No source edits after
matching final evidence. All handles terminal/consumed. Independent integrated review
and any required correctness review remain outstanding, not waived.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted candidate.
Scoped hashes/status: .llm-output/root-review-finalization-identity.json. No live model
call, full CI, commit, PR or merge. No other writer owns paths/processes.

Remaining before enabling this flow: matching unapproved review routes/task identity
conflicts currently fail closed while prepared; wire bounded invalid-selection
recovery rather than retrying forever. Review child completed-handoff handling still
requires closed-candidate support, trusted Git/evidence reads and an evidence/report
contract sufficient for existing policy gates. Neither new inspection/finalization
nor mismatch recovery is wired into immediate/periodic dispatch yet. Build one
orchestration service and prepared-stage discovery only with all continuations.
AcceptDecision, final evidence and legacy validation-to-human-approval integration,
run-terminal/default budget behavior, official clients/UI/CLI/A1-A9 and independent
review/authorized live/CI/PR/release remain incomplete. Full goal stays active.


## 2026-09-11 closed candidate controlled reads

The request builder now advertises the shared eight named candidate reads to a
closed-candidate primary/reviewer (prompt v8); planning retains its four repository
reads. Scheduler admission requires an exact attempt/lease-bound named read,
including for read-only tasks, and retains exclusive snapshot fences and legacy
exclusive read replay identity. Durable tool authorization uses the same named-read
set plus the persisted coordinator/read-only contract proof. It no longer rejects
all closed primary calls or trusts a bare read-only flag for closed authorization.
No closed writes/checks/commits or draining-phase effects are enabled.

TDD: root-candidate-reads-red.log had 8 expected missing-tool failures; green had
8 passed23.20s. root-candidate-reads-affected.log:153 passed123.58s. Extending the
broker callback to check durable authority reproduced four primary-only failures
(root-candidate-authority-red.log); after the guard fix, green:8 passed25.35s.
Real controlled Git snapshots for both roles then passed2 in7.02s, retaining
artifact lineage, unchanged closed epoch and exact receipt replay. The real Git
fixture optionally creates a closed reader and binds its attempt to the lease;
initial fixture repairs supplied the closure epoch and complete execution binding.
The final fixture uses real envelope/task digests, not placeholder digests.
Ruff8paths/mypy4sources/scoped diff checks pass. Final combined persistence suite:
root-candidate-reads-final.log, 85 passed204.63s, covering named reads, durable
authority, snapshot recovery/cancellation, commit, primary coordination and request
assembly. Process27580 consumed terminal exit0; no PG process remains. Candidate
HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted changes;
scoped hashes/status in .llm-output/root-candidate-reads-identity.json. No source
changes after the matching final evidence. No live model call, full CI, commit,
PR or merge. Root is the only writer.

Source scope: domain/subscription.py, application/services/subscription_requests.py,
persistence/repositories/{scheduling,subscription}.py. Tests: candidate_reads,
request_builder, git_commit fixture and snapshot_tool. No independent review has
been substituted by these checks; integrated review and required correctness gate
remain outstanding under previously recorded provider/thread limits.

Next investigation: stopped reviewer TaskHandoff is still rejected by the generic
open-candidate guard in subscription_decisions._apply. New
test_subscription_review_handoff.py contains a focused regression using a
real selected reviewer and stopped launch. root-review-handoff-red.log reproduced
the expected open-only source guard failure:1 failed3.83s; repaired in the next
checkpoint below. Existing handoff_output_digest already
covers the whole tree for a task without owned paths; preserve that behavior.
Need exact current selection binding, bounded invalid-claim handling and typed
review output compatible with policy gates before enabling review dispatch. This
is not reviewer approval, acceptance, or completion of the full v0.2 goal.


## 2026-09-11 selected reviewer handoff completion

Previous goal turn was progress (closed controlled reads verified). Root continued
under the already recorded unavailable provider/native-thread routes; no concurrent
writer or delegated process. Implemented closed-candidate handoff loading only for
the exact selected independent reviewer at its invocation epoch. Historical review
selection is re-proven, including original source and immutable child contract.
SettledSubscriptionHandoff carries optional selected CandidateInspection; ordinary
open writer handoffs retain their existing behavior. Completion requires the claimed
and verified current tree to match that selection; false tree/commit claims use the
existing bounded claim-rejection path without Git IO. Application receipts retain
selected candidate identity, with historical replay after reopening. Original
provider results remain unchanged. Completed or rejected selected review wakes its
blocked primary under the same transaction; the candidate stays closed. Existing
pause/cancel flags and claim guards continue to govern subsequent admission.

TDD: prior root-review-handoff-red.log reproduced open-only guard (1 failed3.83s).
Initial green:1 passed3.51s. Added completion/rejection cases reproduced missing
parent wake (3 failed1 passed12.46s, root-review-handoff-flow.log); fixed targeted
wake, flow-fixed:4 passed12.97s. Added stale epoch, draining, cancel, missing/corrupt
selection, and verified-but-wrong-current-tree refusals. Affected suite:
root-review-handoff-affected.log:116 passed,5 failed206.29s. All five failures came
from two application-pipeline SimpleNamespace fixtures lacking selected_candidate;
one cancellation test timed out before reaching the intended snapshot. Updated
those test doubles with selected_candidate=None; no production changes after this
affected run. Final root-review-handoff-final.log:16 passed31.76s covers all new
review boundaries plus every pipeline/repeated-cancellation case. Matching 116
passes remain credible for unchanged source/test inputs. Mypy3sources, Ruff scoped
checks and diff checks pass. All process handles consumed; no active PG run.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted candidate;
.llm-output/root-review-handoff-identity.json records scoped hashes/status. Root is
sole writer. No live model call, full CI, commit, PR or merge. Independent integrated
review and required correctness scrutiny remain outstanding, not replaced by these
checks. Full v0.2 objective remains incomplete and active.

Next: typed review verdict/findings and candidate-bound review evidence compatible
with policy gates. Do not treat handoff_completed as reviewer approval. Proposed
reuse: existing domain.agent.ReviewOutput; preserve strict historical TaskHandoff
encoding by introducing a separate typed reviewed-handoff record rather than
adding a mandatory field to old serialized records. Implemented in the checkpoint below.
Check codec, protocol parse/schema, settlement/decision dispatch, pending discovery,
invocation outcome queries and evidence consumers together. Ordinary completed
handoffs remain useful but cannot satisfy review gates. Also still needed: bounded
invalid review-selection recovery, immediate/periodic candidate orchestration,
AcceptDecision/final validation/human approval, remaining runtime/client/UI/CLI,
A1-A9, independent review, authorized live/CI/PR/release.


## 2026-09-11 typed reviewer report handoff

Previous goal turn was progress (selected reviewer completion verified). Root owns
all writes; previously recorded provider/native thread limits remain, with no
active delegate. Added ReviewedTaskHandoff as a separate immutable TaskHandoff
subtype carrying the existing domain.agent.ReviewOutput. Its separate codec record
preserves the exact legacy TaskHandoff field set and encoding. ReviewOutput uses a
closed explicit serialization tag, retains verdict/findings/tested claims/missing
evidence, and revalidates on construction and decode. Existing durable payload
bounds/redaction validation remain in force. No schema/database migration needed
for the new closed JSON record type; old records are unchanged.

Provider protocol selects/requires ReviewedTaskHandoff for independent reviewers;
other roles cannot submit the extra review_output field. Output JSON schema exposes
the required typed report. Prompt v9 explains completed work versus approving
verdicts. Durable pending discovery treats both record types as handoffs; source
proof allows only the exact new subtype in addition to legacy TaskHandoff, verifies
actual stored type on replay, and record persistence/outcome queries retain both.
Completion/rejection/evidence paths reuse the prior selected-candidate bindings.
No approving verdict is inferred from handoff_completed. This is report retention,
not yet policy approval or final evidence publication.

TDD root-review-report-red.log:1 expected protocol rejection,3 passed after fixing
an initially missing review route in the test fixture. Green4 passed0.23s. Durable
red:1 expected UNSUPPORTED discovery failure3.69s; green1 passed3.56s. Completion
red:typed persistence whitelist rejected new record; another test assertion used
wrong outcome attribute (.handoff instead of .recorded_handoff). Fixed whitelist
and assertion. root-review-report-flow.log:17 passed26.06s across protocol, codec,
proposal, completion/replay/outcomes. Added invalid verdict, missing-evidence
approval, extra field, corrupted encoding and legacy roundtrip coverage.

root-review-report-affected.log:221 passed134.63s across domain subscription,
protocol, new typed reports, selected/ordinary handoff application and claim
rejection, request assembly, and evidence verification. No source changes after
this evidence. Self-review extended completion tests to all three real verdicts
(approve/request_changes/blocked), alongside legacy handoff and wrong-current-tree
refusal: root-review-report-verdicts.log:12 passed38.38s. Reports remain exact in
primary outcome queries and replay after reopening. Mypy5sources, Ruff8paths and
scoped diff checks pass. All process handles consumed; no PG process remains.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted changes.
Scoped hashes/status: .llm-output/root-review-report-identity.json. No live model
call, full CI, commit, PR or merge. Independent integrated review/correctness checks
remain outstanding under the recorded route limits. Full v0.2 goal remains active.

Next integration: legacy ReviewEvidenceManifest still requires step_id and
producer_execution_id plus validation_evidence_set_id (domain/evidence.py:313).
ReviewDecisionService and PrApprovalEvidence bind review_digest to validation and
publication. Add an explicit subscription evidence/acceptance path with genuine
subscription producer and candidate identities; never invent legacy execution IDs.
Preserve explicit no-review-required decisions without fabricating an approving
reviewer. Typed report storage now exists; gate consumption is not implemented.
Also still required: bounded invalid review selection recovery, orchestrated
immediate/periodic candidate stages, AcceptDecision/final validation/human approval,
remaining runtime/default budgets/client/UI/CLI/A1-A9, independent review and
separately authorized live/CI/PR/release work.


## 2026-09-11 current candidate review evidence sources

Previous turn was progress (typed report persistence verified). Root continues with
sole write/PG ownership under recorded unavailable provider/native routes. Added
CandidateReviewEvidence and candidate_review_evidence(run_id, primary_task_id) to
the decision repository port, implemented in a focused subscription_review module.
The loader locks the run before scheduler state, requires current IMPLEMENTING/no
pending human gate/frozen policy and an admitted closed candidate, finds exactly
one selection source for that primary and previous epoch, and re-proves its
historical stopped-source application. Returns original selection/inspection and
result/application digests with true subscription source IDs.

No-review selections retain their explicit reason and absent review fields; no
approving reviewer is fabricated. Required reviews need the exact selected child,
its latest terminal attempt at the current epoch, successful handoff replay, and a
ReviewedTaskHandoff matching the retained selected candidate. Plain legacy handoff,
pending/rejected/missing review, corrupted selection/application receipts, foreign
primary and stale/open epochs cannot provide usable source evidence. All three
real verdicts remain available unchanged; the loader is not an acceptance policy.
This is a read-only transactional source descriptor, not published evidence, fresh
Git/validation proof, primary acceptance or human approval. Callers must reload it
inside the acceptance transaction and verify current external evidence separately.

TDD root-review-evidence-red.log:1 expected missing-method failure3.55s; green1
passed3.30s. root-review-evidence-flow.log:17 passed51.54s across explicit no-review,
selected report/legacy refusal, stale/missing/pending source cases and handoff flow.
Mypy identified two un-narrowed JSON decision values; added explicit Mapping guards.
Added corruption of an otherwise completed review's application digest to each
report verdict case, confirming refusal before restoring the fixture and replaying.
Final root-review-evidence-affected.log:53 passed160.83s across source loader,
review handoff/report, selection finalization, review boundaries and ordinary
handoff application. Mypy4sources, scoped Ruff/diff checks pass. No source edits
after final evidence. All handles consumed; no PG run remains.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted candidate.
Scoped identity: .llm-output/root-review-evidence-identity.json. No live model call,
full CI, commit, PR or merge. Independent integrated review/correctness scrutiny
remain outstanding, not waived. Full v0.2 goal stays active.

Next: acceptance needs to consume these source bindings, validate current Git and
receipt claims, retain a real subscription evidence record, then hand off to final
validation and human gates. Existing DeliveryDecisionService/ValidationService
still route successful validation into legacy review; ReviewEvidenceManifest still
requires legacy execution/step IDs. Preserve source identities and explicit
no-review selection when adapting that path. GitWorkingTreeSnapshot tree digest is
content/mode based, separate from head/base identity; any commit transition must
prove the exact selected tree and retain the original review identity. No such
transition is implemented here. Bounded invalid selection recovery and immediate/
periodic orchestration still need implementation, followed by remaining runtime,
client/UI/CLI/A1-A9, independent review and authorized live/CI/PR/release work.


## 2026-09-11 closed candidate repair delegation

Previous turn was progress (current review evidence source loader verified).
Tracing acceptance found a required continuation gap: a primary awakened after
review could only read the closed candidate; DelegateDecision was rejected by the
open-only application guard, so review findings could not drive bounded repairs.
Root fixed the owning decision application layer before enabling acceptance.

Valid delegation from the exact primary at a selected closed epoch now starts a
new candidate cycle. The historical selection is re-proven; its attempt/application
digest and source/reopened epochs form a durable candidate_reopened receipt. The
same transaction validates the child graph/scope/policy, requires all other logical
and scheduled tasks terminal, no pending effects or active observation/exclusive
barrier, advances the worktree epoch, queues children and releases the primary.
Invalid semantic delegation retains the closed barrier. Enqueue failure rolls back
the whole transition. Historical replay re-proves the selection and exact reopening
receipt (including detecting complete receipt deletion), without advancing again.
Old candidate selection context is absent for the new open epoch. Ordinary open
primary delegation retains its existing behavior. The change applies to any new
delegation, initiating a new candidate cycle rather than reusing old review proof.

TDD root-redelegation-red.log:1 expected closed-source failure3.76s. First green
reached writer admission but exhausted the fixture's aggregate provider budget.
reader_case/matching_case now accept optional primary_budget; defaults unchanged.
The explicit multi-stage repair fixture uses8 attempts; green1 passed3.88s.
root-redelegation-affected.log:61 passed,1 failed174.84s across new repair flow,
ordinary delegation, candidate reads and selection finalization. The failure was
fixture JSON null versus SQL NULL when removing a receipt pair. Corrected only
that fixture. Final root-redelegation-final.log:9 passed27.81s: happy writer/replay,
invalid scope stays closed, enqueue rollback, missing/corrupt receipt/source,
active snapshot fence, concurrent exact-once application and real selected review
rejection returning control to primary then repair worker. Mypy source/scoped Ruff
and diff checks pass. No production changes after matching affected evidence.
All handles consumed; no active PG run or other writer.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted changes.
Scoped identity: .llm-output/root-redelegation-identity.json. No live model call,
full CI, commit, PR or merge. Independent integrated review/correctness scrutiny
remain outstanding under recorded provider/native limits; not waived. Full goal
remains active.

Next return to AcceptDecision: source validation, current Git/receipt verification,
explicit approving-review versus no-review selection, subscription evidence and
final-validation/human-gate integration. Acceptance must never consume proof from
a pre-repair epoch. Candidate and review dispatch/recovery are still not enabled;
bounded invalid selection recovery, immediate/periodic orchestration, remaining
runtime/client/UI/CLI/A1-A9 and independent review/authorized live/CI/PR/release
remain incomplete.


## 2026-09-11 acceptance intent preparation

Root implemented a bounded acceptance preparation path in the decision application
layer. It binds an exact closed candidate epoch and historical selection/review
sources to a durable acceptance_intent receipt. Required review must contain a
typed APPROVE report; no-review selection remains explicit. Tree/optional commit
claims and canonical, unique, nonnil receipt UUID claims are checked intrinsically.
Receipt IDs are not yet externally verified: fixtures deliberately use random IDs.
Preparation blocks the primary and releases its lease, preserving IMPLEMENTING,
the closed candidate and all human gates. It is not final acceptance.

Historical review-source loading reuses selection and completed review source
proof without requiring the current epoch to remain closed. Intent replay proves
that history and the exact application receipt/digest, surviving a later reopen
without mutating it. Fresh preparation retains current scheduler/control/epoch
checks, requires other tasks terminal and no pending effects or active barrier.
Semantic claim failure takes bounded decision repair without opening the candidate.
Self-review exposed two shared fresh-application guard gaps: revoked scheduler
admission and unexpected pre-existing application receipts. Focused red tests
reproduced both; application now refuses either rather than overwriting evidence.

Evidence in .llm-output/: root-acceptance-preparation-red.log (missing method),
root-acceptance-preparation-green.log (1 passed 3.48s),
root-acceptance-preparation-flow.log (20 passed 61.96s),
root-acceptance-receipt-red.log and root-acceptance-admission-red.log (expected
DID NOT RAISE failures before guard repairs). Final affected check:
root-acceptance-preparation-affected.log: 88 passed in 247.82s across acceptance
preparation, review handoff/evidence/finalization, delegation/handoff application
and candidate redelegation. Mypy six production files, scoped Ruff and diff checks
passed. Session 91236 is consumed, exit 0; no active test process or other writer.
HEAD 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted changes;
scoped hashes: .llm-output/root-acceptance-preparation-identity.json.

Known interface gap: subscription_protocol.decode_final allows AcceptDecision to
target another known task, while this preparation path binds it to the producing
primary task. The plan calls for both stored task acceptance/reassignment decisions
and final primary acceptance of an integrated candidate. Resolve task versus final
acceptance semantics consistently; do not silently narrow the protocol contract or
claim this path completes all acceptance. No dispatch is enabled for AcceptDecision.
Next: resolve that target distinction, verify real receipt provenance/artifact bytes
and fresh Git outside transactions with post-I/O authority/source revalidation,
retain subscription evidence, and integrate final validation/human gates. Legacy
review manifests require execution/step identities and must not receive fabricated
subscription identities. Remaining runtime/client/UI/CLI/A1-A9 work, independent
integrated review/correctness scrutiny and separately authorized live/CI/PR/release
remain outstanding. No live model call, full CI, commit, PR or merge in this step.
Full v0.2 goal remains active; provider/native routing limits are not waived gates.


## 2026-09-11 task acceptance and continuation

Previous goal turn was progress: the 88-test acceptance-preparation result was
collected and checkpointed. This turn resolved the known accept target mismatch
without removing the protocol's child target. Root owns this bounded change;
recorded unavailable provider/native routing was not retried without changed
conditions. Independent integrated review remains outstanding, not waived.

AcceptDecision.task_id now consistently identifies the accepted subject. An owned
child target acknowledges its completed verified handoff, with matching candidate
and complete, nonduplicate receipt claims. Fresh application proves terminal
logical/scheduled child state, no active child lease or pause/cancel, latest stopped
attempt and unchanged contract, then replays the completed handoff proof. It stores
the exact child attempt, result/application digests and candidate epoch in a
schema-one task_acceptance receipt, queues the primary and leaves the child/run/
candidate barrier unchanged. Unknown, unverified or mismatched targets receive
bounded decision rejection. Source corruption is refused, not converted into
approval. Primary/self acceptance retains the separate final-intent path.

Historical task-acceptance replay re-proves its retained handoff and primary source,
including record/digest/target bindings. It survives later epochs or changed child
contracts without treating new work as accepted. Fresh primary invocations receive
acceptance_attempt_id, accepted_handoff_attempt_id and the typed acceptance under
untrusted task_outcomes. The loader re-proves the acknowledgment before supplying
it. Prompt v10, the domain docstring and implementation plan explain the task versus
integrated-candidate distinction. This acknowledgment reuses a verified historical
handoff; it does not claim those checks cover the current whole worktree.

Immediate worker dispatch and periodic recovery now apply child-targeted accept
results after current decision_pending settlement. Discovery classifies the exact
target against the producer through a bounded SQL metadata query; the application
still independently validates the full source. Recovery creates no new provider
attempt and repeats idempotently. Final/self accept remains explicitly unsupported
by dispatch until its external verification/final-validation service is complete.

Self-review also reproduced a shared application control gap: queued current cancel
commands did not prevent fresh child acceptance or final-intent preparation. Both
focused red tests failed DID NOT RAISE. The shared fresh _apply guard now queries
pending_current_control_stop under the held run lock; historical replay stays
historical. Existing applied pause/cancel, source, epoch and lease guards remain.

Evidence (.llm-output/): root-task-acceptance-red.log: 1 expected source-identity
failure 3.62s; green: 1 passed 3.35s. Context red reproduced the absent acceptance
context. root-task-acceptance-flow.log: 16 passed, 1 fixture failure 52.99s; the
fixture was decoding the frozen invocation mapping without json_value, corrected
without changing codec behavior. root-task-acceptance-control-red.log: 2 expected
pending-stop failures 6.85s. root-task-acceptance-affected.log: 256 passed 340.10s
across child/final-intent acceptance, outcomes/context, handoff/delegation/wait,
review finalization, protocol and domain contracts. These inputs are preserved in
the identity record. Subsequent edits only added pending discovery/recovery wiring.
root-task-acceptance-dispatch-red.log: 1 failed, 5 passed 0.69s; worker green: 23
passed 0.54s including stale/fenced and shutdown boundaries. Recovery red: expected
unsupported versus applied mismatch 3.68s; root-task-acceptance-recovery-green.log:
21 passed, 5 existing BaseAgentConfig deprecation warnings, 21.18s. This final check
covers new/old recovery, keyset discovery, self-acceptance remaining pending and
worker dispatch. Scoped Ruff, mypy eight production files and diff checks pass.
All handles (90422, 8111, 91458) are consumed; no active test process or other writer.

HEAD 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted changes.
Scoped final and affected-test identities: .llm-output/root-task-acceptance-identity.json.
No live application model call, full CI, commit, PR or merge. Full v0.2 remains active.

Next: complete final/self acceptance. Load the prepared intent under current run,
control, policy, epoch and task authority; verify real receipt provenance and artifact
bytes plus fresh Git outside DB transactions; revalidate and retain those observations
before final validation. The exact selected candidate/review sources must survive
that transition. Existing PrEvidenceService._manifests, published resume, validation
and review evidence codecs require legacy ReviewEvidenceManifest execution/step
identities and an actual review; adapt them to real subscription sources and explicit
no-review policy rather than fabricating IDs or an approving report. Candidate review
selection/orchestration and bounded invalid-selection recovery are not yet fully
wired. Reassign, other remaining runtime/client/UI/CLI/A1-A9, independent review and
separately authorized live/CI/PR/release requirements remain unfinished.


## 2026-09-11 current final-acceptance inspection

Previous goal turn was progress: child acceptance and recovery were implemented
and verified. Root continued the final/self acceptance path under the recorded
provider/native limits, without treating those limits as waived review gates.

PreparedSubscriptionAcceptance and acceptance_proposal now re-prove the stopped
primary intent, exact selected candidate/review sources, current closed epoch,
primary task/version and control state, quiescence, managed worktree/base and
current project policy. The getter only loads an already prepared final intent.
SubscriptionAcceptanceInspection releases the DB transaction before its trusted
Git snapshot callback, then record_acceptance_inspection reloads that authority
and compares the full proposal before storing a canonical observation. Retry
inspects Git again and refuses a different observation while retaining the first.
Historical intent replay validates observation shape, digest and base binding.
This is source/Git observation, not receipt-content verification or final approval;
the run stays IMPLEMENTING and the candidate remains closed.

TDD logs in .llm-output/: root-acceptance-inspection-red.log (missing proposal),
root-acceptance-proposal-green.log (1 passed 3.80s),
root-acceptance-observation-red.log (missing inspection service),
root-acceptance-observation-green.log (2 passed 6.80s). The initial adversarial
flow had 20 passed and one lease-owner fixture error in65.97s; corrected owner
field, focused effect case passed in3.75s. A local replay variable collided with
another _apply branch's inferred type; renamed it without behavior change.
Mypy five production files, scoped Ruff and diff checks now pass.

Final affected evidence: .llm-output/root-acceptance-inspection-affected.log,
89 passed in255.10s across inspection, actual selected review sources, preparation,
child acceptance, candidate inspection and review handoff/finalization. Then
.llm-output/root-acceptance-inspection-race.log:3 passed in10.10s, covering concurrent
equal/different snapshots and cancellation. No source edits after the affected run
started. review_handoff_case gained optional primary_budget with unchanged default
for approve/request_changes/blocked coverage. Those review cases use the existing
verified-handoff fixture; this stage still does not verify real receipt bytes.
Session48605 is consumed (exit0); no active test process or other writer.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted changes.
Scoped identities and active-handle continuity are in
.llm-output/root-acceptance-inspection-identity.json. No live model call, full CI,
commit, PR or merge. Full v0.2 goal remains active.

Next: bounded recovery of candidate mismatch, real receipt/artifact verification,
final validation and evidence/publication integration. EvidenceSet's shape and
producer FKs require a legacy reviewer execution for every review row; this needs
a schema migration plus matching codec/repository/publication/resume changes to
retain actual subscription attempts and explicit no-review selection. Do not
fabricate execution IDs or an approving report. Candidate/review orchestration,
Reassign and the other runtime/client/UI/CLI/A1-A9 requirements, independent review
and separately authorized live/CI/PR/release remain incomplete.


## 2026-09-11 final-acceptance candidate mismatch recovery

Previous goal turn was progress: current source/Git inspection was implemented
and verified. Root added bounded recovery in the owning acceptance service and
repository, preserving the existing routing limitations and outstanding independent
review requirements. No other writer or PostgreSQL fixture owner is active.

SubscriptionAcceptanceInspection.reject_mismatch first prepares/loads the exact
intent in one transaction. A stored contradiction already invalidates its candidate
epoch; otherwise it obtains a fresh Git snapshot outside the transaction, including
changes after an earlier matching inspection. The repository revalidates current
source/control/policy/quiescence and the full proposal, rejects matching or foreign-
base observations, and atomically records a mismatch observation and failure handoff,
debits a bounded repair, reopens the worktree at epoch+1 and queues the primary (or
makes its task terminal if repairs are exhausted). The original observation and
selected candidate/review sources remain in the receipt. This does not approve the
run. Replays prove the rejection and preserve later candidate/task progress without
another snapshot, epoch advance or debit.

Two self-review concerns were reproduced and fixed with focused red tests. A deleted
repair debit previously went unnoticed on replay; the new path checks debit presence
and any transfer's run/task/next-attempt lineage. Another application could finish
rejection between the service's separate preparation and proposal transactions;
a forced interleaving reproduced that failure. Preparation and proposal loading
now share the first transaction, allowing the later application to return historical
replay after a concurrent rejection. Rollback coverage proves the receipt, epoch and
budget debit roll back together. No behavior changes outside these acceptance paths
are intended; affected legacy candidate rejection remains in the test batch.

Evidence in .llm-output/: root-acceptance-rejection-red.log: expected missing method
3.95s; green:1 passed4.20s; root-acceptance-rejection-debit-red.log: expected DID NOT
RAISE4.08s; root-acceptance-rejection-flow.log:10 passed33.72s; forced race red:4.11s;
root-acceptance-rejection-atomic-green.log:2 passed7.57s. Scoped Ruff, mypy four
production files and diff checks pass. Final affected evidence:
.llm-output/root-acceptance-rejection-affected.log:90 passed in273.76s across mismatch
recovery/rollback/races, current inspection, preparation, selected review acceptance,
child acceptance and existing candidate rejection/review finalization. Session14375
is consumed, exit0; all current handles are terminal. No active PostgreSQL test
process or other writer. Source hashes remained unchanged during the affected run.

HEAD6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work; scoped
hashes/process state: .llm-output/root-acceptance-rejection-identity.json. No live
model call, full CI, commit, PR or merge. Full v0.2 goal remains active. Receipt
provenance/artifact verification and successful final acceptance/validation still
need implementation and dispatch. Candidate/review recovery and orchestration,
Reassign, legacy evidence schema/publication/resume adaptation, remaining runtime/
client/UI/CLI/A1-A9, independent review and authorized live/CI/PR/release remain
outstanding. Distinguish definite missing/foreign receipt claims after a stopped,
quiescent producer from transient artifact-storage failure when implementing receipt
verification; historical worker checks do not automatically certify the integrated
candidate. Preserve actual subscription identities and explicit no-review decisions.


## 2026-09-11 final-acceptance receipt verification

Root implemented receipt provenance and artifact verification in the owning
acceptance ports/service/repository. The prior goal turn completed bounded
candidate mismatch recovery. Existing provider/native routing limits remain;
root is the sole writer and PostgreSQL fixture owner. Integrated independent
review and correctness scrutiny remain outstanding, not waived.

AcceptanceReceiptSource proves the actual primary or direct-child producer from
its retained attempt result, frozen task/envelope/route/budget, settled telemetry,
stopped launch and broker callback. It checks exact tool/run/task/attempt/purpose,
policy/worktree, arguments, successful terminal status and result correlation.
It does not infer historical authority from a revised current task contract or
fabricate legacy execution IDs. Missing/foreign/incompatible receipt claims have
a separate AcceptanceReceiptClaimError; unavailable or damaged artifact evidence
returns no proof and never consumes repair budget. Bounded rejection of that
new claim-error class still needs orchestration/application integration.

SubscriptionAcceptanceReceiptVerification verifies the complete bounded artifact
closure and actual SHA-256 bytes outside database transactions, using the same
byte loader and locked artifact-lineage checker as handoffs. Snapshots bind their
canonical manifest and real producer. Named checks require terminal effect proof
and decoded passing command results; only equal before/after tree digests mark
them as covering the selected candidate. Commit receipts retain their terminally
verified SHA without claiming that HEAD alone proves a possibly dirty working
tree. A focused red test caught and repaired that distinction during self-review.

Before retaining receipt_verification in the acceptance application receipt, the
repository rechecks current source/control/policy/quiescence, every producer and
callback, terminal intent digests and artifact descriptors/parent closure under
locks. Equal concurrent calls retain one proof; rollback is atomic. Retries read
and verify bytes again. The proof remains historical evidence: it does not approve
the run or replace fresh Git inspection, required final validation, evidence-set
publication integration or human gates. Historical mismatch-rejection replay
preserves the retained receipt proof.

Focused evidence in .llm-output/: source red failed at the missing repository
method (3.87s), source green 1 passed (3.74s); verification red failed at the missing
service import, verification green 1 passed (4.30s); flow 67 passed (61.73s), real
broker integration 1 passed (8.81s), commit-binding red 1 failed as expected (0.44s),
replay/codec/commit green 18 passed (4.70s). The real broker test spans worker
snapshot/completion, primary snapshot/selection, final acceptance citation across
those producer attempts, and verification after a worker task contract revision.
Scoped Ruff, mypy eight production files and diff checks pass.

Candidate HEAD 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted
work; twelve scoped source/test hashes and environment identity are in
.llm-output/root-acceptance-receipts-identity.json. Final affected verification is
running (root-owned session 63730; .llm-output/root-acceptance-receipts-affected.log).
Do not treat that pending run as passing evidence. No live provider call, full CI,
commit, PR or merge. Full v0.2 implementation/release goal remains active.

Receipt verification final affected check completed: 180 passed in401.68s
(.llm-output/root-acceptance-receipts-affected.log), session63730 consumed/exit0.
All twelve scoped source/test hashes remained unchanged. The batch covers new
receipt checks and real broker flow, existing handoff verifier/application/locked
evidence, final acceptance preparation/inspection/review/rejection/races and child
acceptance. No active PostgreSQL process. Next bounded work is invalid receipt
claim repair; its new integration test is prepared but not yet implemented.


## 2026-09-11 invalid final-receipt claim recovery (in progress)

Following the 180-test receipt-verification batch, root added bounded recovery
for definite receipt-claim errors. ReceiptClaimErrorKind and the exact cited UUID
form the durable diagnostic. The service re-proves the stopped/current acceptance
source and bad claim within one transaction, then uses the same atomic repair
mutation as candidate mismatch recovery. Bad citations preserve the closed
candidate and epoch; known candidate drift must use the existing reopen/repair
path. Missing bytes alone do not authorize repair. A failed handoff identifies
the bad receipt for the next primary attempt, and replay rechecks that diagnostic,
the original source and repair debit/transfer without another mutation.

Focused evidence: missing-method red3.72s; first four recovery cases pass13.78s.
Self-review found canonical-type aliasing in rejection replay (true versus epoch1).
The first test did not persist the boolean because SQLAlchemy compared it equal
to the prior integer; flag_modified made the corruption explicit, and the forced
red failed as expected3.93s. Canonical payload-digest comparison now rejects it.
All16 recovery/boundary cases pass51.30s, including rollback/concurrency, next
primary admission, exhausted repairs, current pause/mismatch refusal, bad fields,
removed debit and altered diagnostics. Scoped Ruff/mypy pass.

A follow-up focused test is running for entry points whose preparation itself
rejects malformed claims: they must retain that rejection rather than roll it
back when trying to load a prepared proposal. Session63834 is root-owned;
.llm-output/root-acceptance-receipt-claim-preparation-red.log. The final affected
check for this recovery extension has not yet run. The earlier180-test candidate
identity remains recorded separately; later recovery edits change its inputs.
Root remains the sole writer and PostgreSQL owner. Full goal, independent review,
final validation/publication integration, runtime dispatch and release work remain
outstanding; no live model call, full CI, commit, PR or merge.


Receipt-claim recovery follow-up: all three preparation/entry-point cases
reproduced the not-prepared failure (10.40s). verify, reject_claims and
reject_mismatch now preserve a rejected preparation; verification returns no
proof, and repair entry points return the retained settlement. Focused green:
3 passed9.79s. Current scoped Ruff/mypy/diff checks pass. The combined affected
suite is now running, root-owned session58206, with seventeen explicit test
modules, exact argv and sixteen source/test hashes recorded in
.llm-output/root-acceptance-receipt-claim-identity.json. Log:
.llm-output/root-acceptance-receipt-claim-affected.log. Source inputs remain frozen
for this check. Sessions63834 (red),65930 (green) and70146 (16-case boundaries)
are consumed. Do not report the current combined check as passed until it ends.


Final combined evidence for receipt verification and bounded claim recovery:
199 passed in478.97s (.llm-output/root-acceptance-receipt-claim-affected.log),
session58206 consumed/exit0. All sixteen scoped source/test hashes remained
unchanged across the run; current identity/argv/environment/check evidence is in
.llm-output/root-acceptance-receipt-claim-identity.json. No active PostgreSQL
process, other writer or unconsumed current command handle. All earlier red/green
handles this turn are terminal. Scoped Ruff, mypy and diff checks pass.

The receipt-verification and claim-recovery components are implemented. Next
required work includes successful final acceptance/final validation and evidence
publication/schema integration, plus candidate/final-acceptance runtime dispatch
and recovery. The existing evidence-set schema requires a legacy reviewer
execution for review evidence; preserve actual subscription attempt identities and
explicit no-review selection when adapting it. Reassign, remaining runtime/client/
UI/CLI/A1-A9 requirements, current independent review and separately authorized
live verification/CI/PR/release remain outstanding. HEAD remains
6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db with preserved uncommitted work; no live
model call, full CI, commit, PR or merge. Full v0.2 goal remains active.


## 2026-09-11 candidate application, dispatch and restart recovery

Root traced the dispatch gap to pending_applications: pending ReviewSelection
results were unsupported and candidate_prepared results were not scanned. The new
SubscriptionCandidateApplication uses the existing guarded repository operations
to prepare and load one proposal atomically, read Git outside the transaction,
then record its observation and finalize selection or bounded mismatch repair in
one transaction. It reuses a retained frozen observation, re-proves current
control/source authority before mutation, and returns completed historical results
without another provider invocation. This is candidate selection, not final run
acceptance; final acceptance still needs its separate fresh Git evidence.

Recovery now discovers pending and prepared selections, including those with a
retained observation. Both restart recovery and configured immediate invocation
dispatch use the same application service. Production composition supplies the
trusted Git reader shared with handoffs, respecting the worktree and secret-path
policy. Missing configuration stays unsupported; Git or authority failures stay
deferred. The production subscription provider-invocation loop is still unwired.
The initial batch preserved deferral of an unapproved reviewer route. The
follow-up below rejects new invalid selections before freezing the candidate,
using the existing bounded decision-repair path. A retained invalid prepared source
still cannot bypass its frozen route checks.

Focused evidence in .llm-output/: initial service import red; application green
6 passed in 17.15s; dispatch red: 17 expected failures in 14.64s; green: 17 passed in 13.35s.
Boundary run: 10 passed in 31.09s; its additional mismatched-run fixture was rejected
by the typed result before application. The corrected frozen-route boundary
passed in 3.42s. Covered cancellation/resume, pause/epoch changes during Git reads,
atomic rollback, concurrent single application, tree/commit mismatch and exhausted
repair budget. Root self-review also found canonical-type aliasing in candidate
receipt replay. Six forced-persistence probes reproduced boolean/float aliases
for schema/epoch, selected task schema and retained child schema (red: 17.20s).
Canonical comparisons now reject those aliases; focused green: 6 passed in 16.85s.
Scoped Ruff and mypy on six production files pass after the nullable proof guard.

Candidate HEAD 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved uncommitted work.
Thirteen scoped source/test hashes, exact affected argv for 27 modules, Python 3.14.5 and
disposable PostgreSQL port 5435 context are recorded in
.llm-output/root-candidate-application-identity.json. The affected check passed:
216 tests in 491.53s, with five upstream deprecation warnings; log
root-candidate-application-affected.log. All thirteen scoped hashes were unchanged.
Session 20983 is consumed with exit 0. Scoped Ruff, mypy and diff checks pass. Earlier focused sessions 80577, 73678, 3945, 57004, 28931 and 92502 are
terminal. Independent integrated review and correctness scrutiny remain outstanding
under the recorded provider/thread limits. Full v0.2 goal remains active; final
acceptance/validation/evidence schema and publication, remaining runtime/client/
UI/CLI/A1-A9 work, and separately authorized live verification/CI/PR/release remain.
No live application model call, full CI, commit, PR or merge in this work.


Read-only trace for the next final-acceptance integration: evidence_sets in
persistence/models/execution.py currently admits validation/review only; review
requires a real legacy reviewer execution. A subscription acceptance evidence
variant must preserve actual run/task/attempt lineage and an explicit no-review
record, while leaving legacy rows and human approval semantics intact. Existing
subscription_attempts already has a unique (run_id, task_row_id, id) key suitable
for a composite producer foreign key. ValidationService and
ControllerCheckOperationAdapter currently check HEAD before/after commands but
have no working-tree-content binding. Final subscription validation must therefore
bind the selected content before and after commands; matching HEAD alone is not
sufficient. These are inspected integration requirements, not implemented or
verified release capabilities.


Candidate route correction follow-up: the initial 216-test batch exposed a complete
flow issue in the previously deferred unapproved-route case. Preparation now uses
the same pure selected-review-task/approved-route validation as finalization,
after the current stopped-source and control guards, before closing the candidate.
Invalid or absent frozen reviewer routes consume at most one allowed repair and
queue the primary, or terminate it when repair is exhausted. The candidate stays
open at the same epoch; no Git observation or unapproved review task is created.
Replay retains the original rejected source and repair transfer after the next
primary admission. Finalization still checks the selected task before creation.

TDD: root-candidate-selection-route-red.log failed as expected at the forbidden
Git observation (3.79s); green: 2 passed in 6.97s. Route-boundary coverage passes 4 cases
in 13.53s: unapproved/missing routes, allowed/exhausted repairs, simultaneous apply,
replay and next-primary admission. Scoped Ruff/mypy pass. Added a recovery test to
prove the rejection leaves the pending scan instead of being deferred indefinitely.
The focused affected follow-up passed: 73 tests in 300.17s, with all fourteen
scoped hashes unchanged. Session 65840 is consumed with exit 0; no current command
or PostgreSQL test process remains active. Exact eight-module command, current
candidate identity and reuse scope for the preceding 216-test batch are in
.llm-output/root-candidate-selection-route-identity.json. Log:
.llm-output/root-candidate-selection-route-affected.log. Scoped Ruff, mypy and diff
checks pass. HEAD remains 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db on forge/v0-2
with preserved uncommitted work. No live model call, full CI, commit, PR or merge. Current independent review,
full runtime/final-acceptance implementation and authorized release gates remain
outstanding; full v0.2 goal stays active.


## 2026-09-11 acceptance evidence and validation content metadata

Root is continuing the full v0.2 goal under the previously recorded provider and
thread limits; no writer was delegated and no independent review is claimed.
Validation evidence schema 2 retains the candidate tree digest while canonical
schema-1 bytes remain unchanged. A new acceptance manifest retains actual primary,
selection and optional reviewer task/attempt sources, their result/application
digests, candidate identity, verified receipt artifact and exact validation parent.
No-review retains its policy-permitted selection and reason; it creates no reviewer
execution or approve report. Human gates are not opened by recording evidence.

Migration 0018 adds content and subscription-producer metadata, the composite
(run, task, attempt) foreign key and shape constraints. Downgrade refuses retained
acceptance or content-bound validation rows. Persistence reuses current stopped
source, candidate, control, receipt/callback, terminal-intent and artifact-closure
checks during VALIDATING. The existing receipt verifier was extracted without
changing its checks. Exact immutable replay remains historical evidence and does
not establish current acceptance authority. The legacy execution-bound reader now
accepts validation schema 2 and verifies its content metadata; it does not treat
acceptance records as legacy reviewer inputs.

The real reviewed-handoff path exposed a commit-proof mismatch: an independent
reviewer cannot create a commit, but its typed report identifies the HEAD it
observed. Its verified bound snapshot can now establish that HEAD; locked handoff
application still checks the selected closed candidate. Implementation commit
claims still require actual terminal commit receipts. The reviewed acceptance test
uses the real handoff verifier and database application, with persisted test
snapshot/callback artifacts; it is not live provider or command-runner evidence.

Focused logs are retained in .llm-output/: codec red/green and boundaries,
validation-content persistence red/green, acceptance-evidence persistence, reader
schema-2 red, acceptance boundaries and repairs, and reviewed-head red/green.
Initial boundary batch: 24 passed, two fixtures rejected by the existing immutable
row trigger before their intended assertions; corrected validation-parent/FK and
retained-downgrade cases: 4 passed in 17.03s. Reviewed persistence with the real
handoff verifier passed in 6.28s after the observed-HEAD correction. Its focused
unit/validation-codec follow-up passed 52 tests in 0.54s. Scoped Ruff passes and
mypy passes all 363 production files. The 28-module affected run passed 328 tests in 593.70s, with all 19 scoped hashes
unchanged. Session 44360 is consumed with exit 0; log
root-acceptance-evidence-affected.log. The scoped hashes, HEAD/status, exact argv,
Python 3.14.5 and disposable PostgreSQL port 5435 context are in
root-acceptance-evidence-identity.json.

Controller integration tracing found and corrected the new guard's step-name
mismatch (`validation` versus the controller's `validate`). Its fixture now uses
actual controller admission/finalization. Acceptance reuses the existing controller
repository to prove completion and absence of linked agent executions, and requires
the controller output artifact to be the exact validation parent. The focused real
controller step failed as expected before the correction, then passed in 4.47s.
Two discriminating output-artifact probes initially accepted missing/substituted
output (red: 8.39s); the guard correction and agent-link check pass 3 cases in
12.95s. A retained schema-1 evidence row and its artifact bytes survive the actual
0018 downgrade/upgrade path (1 passed in 2.40s). These logs use the
root-acceptance-controller-* and root-acceptance-evidence-legacy-migration prefixes.
The six-module controller evidence follow-up passed 55 tests in 128.74s, with all
19 scoped hashes unchanged; session 96131 is consumed with exit 0. Its log and
identity use the root-acceptance-controller-evidence prefix. Final source review
found that the validation parent's artifact provenance also needed checking: two
probes of changed producer kind/ID initially accepted the substitution (red:
10.04s). Acceptance now checks the exact validation artifact run/ID, producer,
media type, schema and truncation state. Those two cases pass in 8.84s.
The final three-module persistence follow-up passed 28 tests in 137.20s, with all
19 scoped hashes unchanged. Session 3077 is consumed with exit 0. Log and candidate
identity use the root-acceptance-provenance prefix. All command/test sessions from
this checkpoint are terminal; none retains a PostgreSQL test process. Ruff and
scoped mypy pass. Reuse of the preceding 328-test and 55-test batches is limited to
matching unchanged inputs. HEAD remains 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db on
forge/v0-2 with preserved uncommitted work. The evidence persistence/codec/migration
checkpoint is complete; independent integrated review remains outstanding under
the recorded provider/thread limits.
Final controller validation still needs before/after working-tree binding in
ControllerCheckOperationAdapter and recovery receipts. ValidationService must emit
schema 2 from that verified execution; metadata-only fixture coverage does not prove
runner stability. Final acceptance transition/dispatch/recovery, validation-to-PR
publication consuming actual acceptance/no-review evidence, remaining full runtime,
client/UI/CLI/A1-A9 work, current independent review and separately authorized live
verification/CI/PR/release are still outstanding. No live application model call,
full CI, commit, PR or merge. Full v0.2 goal remains active.

## 2026-09-11 controller validation bound to candidate contents

The controller now observes the selected working-tree contents before and after
each required command. A version-2 controller payload retains the expected tree;
its receipt retains both observations. The outer operation envelope stays at
version 1. Recovery verifies the recorded content bindings and artifact closure
without another command launch or a new snapshot standing in for past evidence.
Legacy request/receipt and validation canonical bytes retain version-1 behavior.
Cancellation before launch keeps a versioned no-effect receipt and cannot certify
a passed check.

ValidationService accepts a trusted CandidateInspection, binds it in admission,
and checks it before admission, checks/recovery and publication, including after
manifest storage. It publishes schema 2 only after verifying each exact completed
controller intent and receipt again. Members retain their controller receipt
digests; manifest parent links and repository provenance checks include them.
An omitted/changed replay target, legacy receipt, missing receipt or incompatible
receipt metadata cannot authorize content-bound publication. The startup adapter
restores the candidate binding from the persisted request. The upgrade notes
describe these wire versions and recovery meaning.

TDD logs in .llm-output/ record the missing request argument, required receipt
member and missing validation candidate argument before implementation. The
domain codec follow-up passed 49 tests. The first real PostgreSQL controller
validation integration passed in 4.30s, then 12 boundary cases passed in 34.14s:
same-HEAD drift before admission, during the runner and manifest publication;
crash before settlement with direct and startup recovery; altered replay
authority; refused legacy-result upgrade; and publication receipt-graph checks.
The runner and Git ports are controlled test doubles in these integration cases;
this is not Docker/live-provider or full A1-A9 evidence. Existing Git snapshot
implementation remains unchanged. Added cancellation coverage for both protocol
versions and exact receipt-parent checks in the evidence reader.

Scoped Ruff passes, mypy passes six production files, and diff checks pass.
The 27-module affected validation/evidence check passed 269 tests in 440.35s,
with all 33 scoped source/test hashes unchanged. Its exact argv, Python 3.14.5,
guarded disposable PostgreSQL port 5435 context, HEAD/status and before/after hashes
are in .llm-output/root-controller-content-affected.json; the matching log is
root-controller-content-affected.log. Session 27692 is consumed with exit 0.
Earlier boundary session 50920 is also terminal; no command or PostgreSQL test
process from this checkpoint remains active. HEAD remains
6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db on forge/v0-2 with preserved uncommitted work.

Root remains the sole writer under the previously recorded unavailable provider
and thread routes. Candidate identity/recovery integrity still needs independent
integrated review and correctness scrutiny; those requirements are not waived.
The new ValidationService argument is a controller component boundary, not yet a
complete subscription acceptance dispatcher. Final acceptance must bind its
durable primary decision to the queued validation command and carry actual
acceptance/no-review evidence through PR approval and recovery. Remaining runtime,
client/UI/CLI/A1-A9 and separately authorized live/CI/PR/release work is still in
scope. No live application model call, full CI, commit, PR or merge in this work.
The full v0.2 goal remains active.

## 2026-09-11 final acceptance dispatch and controller authority

Root remains the sole writer under the recorded provider/thread limits. Final
acceptance can now atomically transition IMPLEMENTING to VALIDATING and queue one
controller command after fresh candidate observations, receipt verification and
current approved-plan/source checks. The version-1 dispatch event binds the actual
primary result/application digests, candidate, proof artifact, approval and exact
command. The command retains schema 1 and adds the canonical acceptance attempt
ID. The original acceptance application receipt remains unchanged after dispatch.
Historical replay verifies retained bindings; it does not grant current effect
authority or launch another command.

ValidationService resolves the selected candidate from this durable source without
caller-supplied candidate metadata. It requires the acceptance identity for a
subscription plan, re-proves receipt/callback/source authority and candidate
contents before/after checks, recovery and publication, and emits the existing
content-bound validation schema 2. Pending decision recovery and the invocation
worker have optional final-acceptance dispatcher hooks. These hooks are not yet
composed into the production runtime. A focused test showed the legacy
DeliveryService otherwise sends this evidence to legacy REVIEWING; an explicit
guard now refuses that route pending the subscription publication controller.
That guard is an incomplete integration boundary, not the intended final behavior.

TDD and focused evidence in .llm-output/ cover missing dispatch/validation support,
the two-dispatcher phase race, legacy misrouting and recovery discovery. Dispatch
replay after the race now verifies the committed event before reporting success.
Boundary checks cover atomic queue/transition rollback and retry, candidate drift
at both observation points, changed epoch/pause/task cancellation/callback, and
altered queued command or retained proof. The first boundary run passed 10 cases
with one fixture lookup failure; the corrected callback case passed. The actual
required-check integration passes with a controlled runner/Git port, including
replay without a second command and rejection of changed receipt authority during
execution with no evidence publication (2 passed in 11.81s). Its initial final
assertion used an incorrect RunSnapshot field and was corrected. These checks do
not establish Docker, live-provider or full A1-A9 acceptance.

Scoped Ruff and mypy pass all nine production files; scoped diff checks pass.
The 38-module affected run passed 327 tests in 757.52s, with all 60 scoped hashes
unchanged. Its exact command, Python 3.14.5, candidate HEAD/status and disposable
PostgreSQL port 5435 context are retained by
.llm-output/root-acceptance-dispatch-run.py in root-acceptance-dispatch-affected.json
and .log. Session 86077 is consumed with exit 0; all command/test sessions from
this checkpoint are terminal and no PostgreSQL test process remains owned here.

Next integration must publish the actual acceptance manifest, freeze versioned PR
evidence retaining the review selection/no-review reason, and teach publication
and recovery to consume that evidence without a fabricated reviewer. Trace found
legacy assumptions in PrEvidenceValidator, ReviewDecisionService, candidate
revision, release monitoring and merge evidence. Current clean managed candidate,
exact validation/controller receipts, stopped primary/reviewer sources and human
PR/merge gates remain mandatory. Failed final validation also needs its actual
subscription repair/recovery route before production wiring. Remaining full
runtime, clients, UI/CLI/A1-A9 work, independent integrated review/correctness
scrutiny and separately authorized live/CI/PR/release work remain outstanding.
The read-only publication trace and next bounded outcome are retained in
.llm-output/root-acceptance-publication-next.md for continuity; no writer is
delegated by that note.
No live model call, full CI, commit, PR or merge. HEAD remains
6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db on forge/v0-2 with preserved uncommitted
work. The full v0.2 goal stays active.

## 2026-09-11 acceptance publication and human release gates

This continuation made concrete progress on the full v0.2 objective. Root remains
the sole writer under the previously recorded unavailable Gemini/Opus and native
thread routes. Independent integrated review and correctness scrutiny remain
outstanding; local checks do not replace them.

The subscription publication controller now takes actual dispatched acceptance
through final validation to the existing human PR gate. It retains the real
primary decision, review selection, optional reviewer handoff, receipt graph and
content-bound validation. A no-review path creates no legacy reviewer/report.
It rechecks current source authority, clean managed candidate contents and pending
control stops around artifact storage, then atomically records the gate and its
closed source-command event. The old temporary DeliveryService guard is replaced
by this controller route. Production final-acceptance dispatcher composition is
still pending the failure and revised-candidate paths described below.

PR and merge approval JSON now have explicit subscription schema-2 models with
acceptance and candidate-tree digests. Legacy canonical approval bytes retain
their existing decoders and fields. The downstream publication, monitoring,
merge and queue interfaces carry either explicit evidence kind. Read-only PR
preflight and publication recovery rebuild the actual retained sources and artifact
closure, including after human PR approval consumption. New effect authority
still requires a current clean candidate and remote preflight.

The run projection now exposes the candidate from linked acceptance/validation
evidence and adds nullable acceptance/tree digests. PR and merge screens display
and match those values against the refreshed projection before requesting the
existing approval challenge. Mixed or unsupported evidence versions are refused;
legacy screens and protection checks retain their behavior. The generated API
contract and TypeScript client are updated together.

TDD and focused logs in .llm-output/ cover the missing controller, legacy PR
validator, versioned merge model, DeliveryService routing, missing projection and
UI rejection before implementation. The runner-outcome test found that a false
upstream passed summary could hide an actual failed terminal result; publication
now independently checks the terminal result and exact controller receipt. The
corrected outcome/truncated-output cases passed 2 tests in 11.94s. The reviewed
handoff case passed in 9.07s; the no-review path runs an actual controlled required
check. The reviewed fixture has no policy-required command, which is distinct
from inventing successful check evidence.

The initial publication boundary/recovery run passed 8 tests and failed one final
test assertion comparing an empty list with an empty tuple. The actual simulated
PR crash had already recovered correctly. After correcting the assertion, the
extended PR/merge integration passed 2 tests in 17.63s: durable intent precedes
push, a crash after PR creation reconciles one PR/one push, a separate test-only
human merge approval binds the same acceptance/tree, and a crash after merge
reconciles one merge effect. These use real PostgreSQL and controlled Git, runner
and GitHub ports; they are not live-provider, Docker, full A1-A9 or release evidence.
Boundary coverage also checks gate rollback/retry without another command,
same-HEAD content drift during storage, and corrupted acceptance, validation,
runner and body artifact bytes. The projection test passed in 6.74s.

The operator checks passed 31 approval-control tests and 11 evidence/panel tests.
TypeScript checking, scoped ESLint, generated API consistency and scoped Ruff
pass. Mypy passes all 368 production source files. React self-review preserved
existing effect cleanup, busy/abort handling, exact challenge bindings and native
dialog semantics; decision fields are derived without new effects or state.
The 46-module affected Python suite passed 455 tests in 1326.06s, with all scoped
source/test hashes unchanged. Its candidate identity, Python 3.14.5, guarded
disposable PostgreSQL port 5435 context and exact command are retained by
.llm-output/root-subscription-publication-run.py in the matching -affected.json
and .log. Session 73705 is consumed with exit 0. Five Google library deprecation
warnings were reported; there were no test failures.

While that batch ran, root prepared and linted the separate focused
test_subscription_validation_repair.py. It expects one bounded repair/requeue,
candidate reopening, unchanged original acceptance bytes and idempotent replay
after a legitimate failed terminal check. It is outside the passing batch and now
fails as expected at the current all-passed publication guard (6.55s; retained in
root-subscription-validation-repair-red.log). The following repair checkpoint
supersedes that initial missing implementation.

Remaining work includes subscription handling for candidate drift at PR approval,
revised candidate/remote remediation
publication, and production composition of final acceptance. Current integrated
independent review and correctness scrutiny are still required. The remaining
full runtime, client/UI/CLI/A1-A9 and separately authorized live/CI/PR/release work
remain in scope. No live model call, full CI, commit, PR or merge was performed.
HEAD remains 6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db on forge/v0-2 with preserved
uncommitted work; the full goal remains active.

## 2026-09-11 failed final-validation repair

Root implemented the actual failed-check path. Publication now re-proves terminal
controller outcomes and receipt/artifact closure before choosing a PR gate or
bounded repair. A separate version-1 `run.subscription_validation_rejected` event
and controller TaskHandoff retain the exact dispatch, failed checks, validation
digest, repair receipt and target. The dispatched primary result/application
bytes and disposition remain unchanged. Repair debit, task requeue/version,
candidate epoch reopening and the real REMEDIATING transition commit atomically.
Exhausted approved run/task/cumulative budgets require human intervention.
Historical rejection replay verifies the event, retained handoff and debit without
another check or debit.

Subscription request building, controlled tools, candidate/review/acceptance
application and final dispatch now support IMPLEMENTING and REMEDIATING through
one shared work-phase set. Planning remains restricted to its existing read tools.
The next primary retains its original owned paths, route and cumulative budget,
receives the actual controller failure context, and must obtain fresh selection,
acceptance and final checks. No legacy developer command is substituted.

Focused TDD exposed the absent failure path and REMEDIATING stage guards. The
failure/next-primary/full-return modules passed 6 tests in 46.22s after correcting
a test-only immutable-context decoding assertion. Actual controlled named-check
and Git-commit repair tests passed 3 tests in 9.38s, preserving policy denial and
one-effect replay. Corrupted decision event/handoff, missing debit and cancelled
check boundaries passed 4 tests in 24.23s. A separate test reproduced expired
lease publication in both success and failure paths (2 expected failures); final
lease checks now fence both decisions, with 2 tests passing in 13.46s. Mypy passes
all 369 production source files. A later scoped Ruff check found two C408
test-only `dict()` construction warnings in test_subscription_remediation_tools.py.
After the suite completed, the literal replacements passed Ruff and the affected
three tests again (9.57s); no production behavior changed in that repair.

The 43-module affected repair suite passed 400 tests with five Google-library
deprecation warnings in 963.35s (16m03s), under root's sole PostgreSQL port 5435
ownership. All 72 scoped input hashes matched. Session 53293 is terminal and
consumed with exit 0. Its exact argv, HEAD/status, Python 3.14.5 and scoped
before/after hashes are recorded by
`.llm-output/root-subscription-validation-repair-run.py` in the matching
`-affected.json` and `.log`. The earlier 455-test publication result is historical
for source changed by this repair; it is not claimed as current repair evidence.

During that run, root prepared the separate
`test_subscription_candidate_revision.py` for same-HEAD drift at PR approval and
operator-requested revision, including full feedback beyond the summary limit.
Its red run then failed all three cases in 20.53s: drift queued legacy validation,
and operator revision used a legacy-only approval decoder. This file was outside
the completed repair batch.
Production dispatch, full A1-A9, current independent review/correctness scrutiny
and separately authorized live/CI/PR/release work remain outstanding. No live
model call, full CI, commit, PR or merge was performed. HEAD and the active full
goal are unchanged.

The next revision implementation is now in progress in the 19 paths listed by
`.llm-output/root-subscription-candidate-revision-scope.json`. It shares the exact
read-only publication proof, adds a named current PR-gate acceptance source, and
reuses atomic repair/debit/requeue and replay verification. The controller records
a separate closed revision event, and splits full bounded operator feedback into
handoff context without dropping instructions. ApprovePrHandler receives the
controller through explicit production composition; an unconfigured subscription
drift route fails closed. CandidateRevisionService uses the local controller
without needing GitHub credentials. The initial focused run passed three tests
in 23.79s (root-subscription-candidate-revision-first.log); session 51611 is terminal
and consumed. Ruff and mypy (372 production files) pass.
The 400-test result is historical for sources changed by this new revision scope;
matching affected evidence must be obtained after integration and self-review.

Revision boundary/legacy checks passed 25 tests with two teardown errors in
104.63s. The two exhaustion cases had left disposable PR interventions that the
existing downgrade guard correctly refuses. Their test cleanup now resolves those
interventions, following the legacy PR test pattern; the two cases passed again
in 15.93s. The migration guard was not changed. Worker composition and delivery
runtime checks passed 28 tests with five library deprecation warnings in 2.85s.

The full operator-revision round trip first failed during its second final check
(one failed, one passed in 20.36s). A test-only traceback located an artifact
producer-lineage conflict: the one-shot fake runner returned exactly its first
successful terminal result, including the start timestamp, for a new command.
The second simulated execution now has a fresh timestamp. Production result
identity and artifact guards remain unchanged. The shared actual
selection/acceptance test helper avoids duplicating that pipeline.

After those test repairs and a Boolean readability cleanup, all 14 revision and
round-trip tests passed in 104.37s. They cover drift requeue, full bounded operator
feedback in the next primary context, both automatic and human counter semantics,
stale epoch/task pause, final lease expiry, atomic approval-consumption rollback,
exhaustion, altered retained feedback/event and fresh acceptance/final validation
before a new PR gate. Both the operator revision and transient-check repair return
through that gate with the old acceptance binding preserved. Logs are the
root-subscription-candidate-revision-{boundaries,boundary-repair,composition,
round-trip,round-trip-diagnostic,green}.log files in .llm-output/.
The final mypy check passes all 372 production files; scoped Ruff and diff checks
pass. Root's self-review retained the shared frozen-source proof and bounded
repair mechanism, explicit handler composition, local-only feedback processing,
current source/lease checks and one transaction for requeue plus gate consumption.
Current independent integrated review/correctness scrutiny is still outstanding.

The 18-module affected publication/recovery regression suite passed 134 tests
with five Google-library deprecation warnings in 552.76s (9m12s), under root's
sole PostgreSQL port 5435 ownership. Session 26473 is terminal and consumed with
exit 0; all recorded scoped inputs matched. The
root-subscription-candidate-revision-run.py record retains the exact command,
candidate identity, reused matching focused evidence and scoped before/after
hashes in the matching -affected.json/log. Remote PR repair, revised
push/base-adoption integration,
production final-acceptance dispatch, remaining full A1-A9/client/UI/CLI work and
separately authorized live/CI/PR/release steps remain in the active goal. No live
model call, full CI, commit, PR, remote write or merge was performed.

### 11 September 2026 — subscription remote repair and revised publication

Root's 23-path remote scope reuses the current closed acceptance/receipt proof
and cumulative debit/requeue mechanism. The monitor remains the owner of the
remote-attempt counter. A new closed controller event and TaskHandoff bind the
actual remote failure, previous publication and original accepted primary.
History is preserved; fresh selection/acceptance/final checks return through an
explicit subscription acceptance event to a queued reviewed push under the
original human PR approval. Publication preflight, effect recovery, subsequent
monitoring and a second repair use the actual accepted source graph. The merge
gate remains separately human-authorized. Historical observations use the
succeeded publication/update snapshot preceding their command. Worker composition
selects the frozen execution mode and fails closed when subscription release
dependencies are absent. Remote context is a bounded, explicitly marked excerpt
with a link to the complete retained observation.

Focused red evidence: the real monitor/PR fixture reached the absent controller
(1 failed in 8.96s, after correcting one test import); fresh final validation
then incorrectly reopened a PR gate (1 failed in 13.70s); revised push expected
legacy remediation completion (2 failed in 28.59s). During implementation an
incorrect GitHubPullRequest field was repaired, preserving explicit base/head
repository and ref binding. The repair/return tests then passed 2 in 24.68s.
The first push/boundary batch passed 5 and failed 3 in 85.72s: two tests assumed
first admission would skip an unchanged push, and one test cleanup omitted an
argument. The revised tests use a lost push response and verify one effect,
replay, a second remote failure and the separate merge gate. All 8 passed in
91.04s. Five more publication boundaries passed in 71.95s, covering revoked
approval, altered acceptance/repair events, candidate drift and final lease
expiry. These checks use guarded disposable PostgreSQL port 5435 and fake remote
ports; no real remote write was performed.

A dedicated routing test passed 3 in 0.61s after its expected missing-handler
red. Worker composition initially retained a legacy-only test assumption
(1 failed, 30 passed in 3.50s). Its replacement verifies the shared publication
validator and matching mode-specific controller. All 31 composition/delivery
checks passed with five Google-library deprecation warnings in 2.61s. Mypy passes
all 375 production files; scoped Ruff and diff checks pass. These logs use the
root-subscription-remote-* prefix in .llm-output/.

Self-review found that an otherwise valid subscription event could be relabeled
as a legacy review event with a null actor. The adversarial test reproduced this
(1 failed in 15.44s). The subscription verifier now requires its exact event
variant and actor. Its focused green check passed in 14.43s; session 9188 is
terminal and consumed. The 28-module affected batch finished with 803 passing
tests and three state-contract failures in 1004.41s, with five Google-library
deprecation warnings. All scoped input hashes matched. Session 50979 is terminal
and consumed. The independent transition table had omitted the subscription
VALIDATING -> AWAITING_PR_APPROVAL and MONITORING_PR edges. After updating that
test-only contract, all 606 state-engine tests passed in 0.70s. The successful
controller/legacy checks from the affected batch are retained as matching evidence;
no production source changed for this test repair. root-subscription-remote-run.py
records the exact argv and candidate/scoped identity in the matching
-affected.json/log; the repair is root-subscription-remote-state-contract.log.
Current integrated independent review/correctness scrutiny remains
outstanding because the previously recorded provider/thread blockers have not
changed. They are not waived. Controlled base adoption and complete subscription
resume remain the next integration boundaries; final acceptance dispatch is
still deliberately uncomposed until those paths are complete. The active full
v0.2 goal, HEAD and branch are unchanged. No live model call, full CI, commit,
PR, real remote write or merge occurred.

### 11 September 2026 — subscription resume and durable quiescence

Root's next scope addresses run-control coordination: an already queued primary
has no legacy stage command to resume, an unstarted remote controller needs the
existing receipt-bound continuation, and a committed remote repair must be
acknowledged without requeue/debit repetition. Controlled base adoption remains
a separate pending path. The new resume and base tests were prepared outside the
preceding affected batch. The resume module's first collection run exposed a
test-only CommandLane import, corrected to the commands port before behavioral
execution.

Tracing found that RunRepository.prove_quiescent omitted subscription attempts,
scheduled leases/effects and client launches. Its focused red test proved that
an actual admitted subscription attempt was incorrectly reported quiescent
(1 failed in 2.52s, root-subscription-quiescence-red.log). The shared proof now
counts those durable resources; branch teardown also checks the added field.
Queued work remains exempt until admission, while expired leases and uncertain
launches remain blockers. All five PostgreSQL cases passed in 9.48s
(root-subscription-quiescence-green.log); session 78706 is terminal and consumed.
The actual teardown/branch-handler tests now include this blocker and all 53
passed in 0.90s (root-subscription-quiescence-teardown.log). The resume behavioral
red run failed all four cases in 31.54s: queued prepared/repair primaries had no
legacy failed-delivery source, the resumed remote envelope was rejected, and a
committed repair was erroneously queued again. Session 76392 is terminal and
consumed (root-subscription-resume-red.log).

The implementation now records a closed subscription-scheduler continuation that
preserves primary identity, route envelope, task controls and cumulative budget.
It acknowledges an expired remote command only after replay proves the committed
repair and the paused phase/version. Unstarted remote controllers retain exact
resume_origin authority; later push proof follows resumed_successor. Production
composition injects the same controller into resume, remote repair and final
validation. Final resume lease checks fence transition and commit. The first
four-case run passed all four in 32.32s (root-subscription-resume-first.log);
session 54935 is terminal and consumed. An optional-binding type narrowing issue
was then repaired, and mypy passes all 376 production files. All 28 worker
composition/delivery runtime tests pass with five library warnings in 2.76s.
Those logs are root-subscription-resume-mypy-green.log and -composition.log.

An additional failed-before-admission case reproduced the scheduler continuation
skipping a failed remote command (1 failed in 9.22s). A failed command at the
paused version now takes priority and must pass the existing failed-delivery
receipt proof before redelivery. All 17 resume boundary/PR-return cases passed in
160.55s (root-subscription-resume-boundaries.log); session 70533 is terminal and
consumed. This includes revised push recovery and a second remote failure after
an actual resumed repair. This scope is root-subscription-resume-scope.json.

The actual stopped-primary test then failed at quiescence (1 failed in 3.56s):
settlement retained the result/usage as stale but left the task in reconciliation.
The new subscription_resumption repository re-proves the original task, route,
envelope, stopped launch, usage and candidate epoch under the run lock. It spends
one bounded repair, retains the original result/disposition, stores a separate
closed application receipt and controlled failure handoff, and requeues the same
logical task. The run-resume event binds those receipt digests. Historical replay
rechecks them without requeue or debit. Active/uncertain work still blocks resume,
and insufficient remaining budget keeps the run paused without partial mutations.

The first implementation failed replay (1 failed in 3.71s): event freezing turns
receipt arrays into tuples. Control replay now compares canonical payloads,
preserving strict JSON distinctions instead of Python list/tuple representation.
After that repair and explicit decoder narrowing, the round trip passed in 3.68s.
Mypy passes all 377 production files. Logs: root-subscription-stopped-resume-
{red,first,green,mypy-final}.log.

Self-review added accounting and scheduling-source boundaries. A changed charge
was accepted (red 1 failed in 3.61s); the verifier now reconstructs charge,
unknown-usage status and policy violations from the actual reservation/telemetry.
All 14 initial stopped-source/receipt/final-lease cases passed in 37.57s, and an
actual delegated child plus queued sibling passed in 3.87s. A changed scheduler
repair limit was then accepted (red 1 failed in 3.72s); live resumption now matches
the original logical scope, route, dependencies, read-only status and repair
limit. The final 18-case stopped batch passed in 47.59s; session 28468 is terminal
and consumed, logged in root-subscription-stopped-resume-final.log. The eight-path
scope is root-subscription-stopped-resume-scope.json.
Full affected resume/teardown/publication evidence, remaining stopped/paused
controller paths, controlled base adoption and independent integrated scrutiny
remain required. No live model call, full CI, commit, PR or merge occurred.
Current independent review and remaining full v0.2 acceptance/release work are
not waived.

The controlled base-adoption red case reached the intended behavioral failure:
the existing service moved to VALIDATING instead of reopening the subscription
primary for fresh acceptance (1 failed in 9.43s,
root-subscription-base-adoption-red.log). Its process is terminal; the lost final
tool response was checked against the completed log and process inventory.
Resume/teardown/publication affected checks are the next fixed-input batch,
root-subscription-resume-run.py. Root remains the sole guarded PostgreSQL owner.

### 11 September 2026 — subscription base adoption

The 40-module affected resume/teardown/publication batch passed all 350 tests in
809.23s with five Google-library deprecation warnings and matching scoped hashes.
Session 50138 is terminal and consumed. Candidate HEAD remains 6d87fe0 plus the
scoped uncommitted changes. root-subscription-resume-affected.json/log retain the
exact argv, full status and before/after scoped identity. This evidence predates
the following base integration changes and is not a full CI run.

The next bounded scope is root-subscription-base-adoption-contract.md. The new
reservation keeps the primary blocked and its candidate closed until the actual
remote update and local adoption have succeeded. A separate closed admission
binds the existing repair debit to the original monitor delivery and accepted
source. Adoption then records a distinct receipt, requeues the same primary and
stays REMEDIATING without rewriting the run/worktree baseline or old acceptance.
The original wrong-state red (1 failed in 9.43s) now passes (1 in 10.41s,
root-subscription-base-adoption-first.log); session 67711 is terminal and consumed.
Mypy passes 380 production files (root-subscription-base-adoption-mypy-first.log).

Fresh changed-candidate publication and recovery tests are being integrated;
base resume/effect/budget boundaries and current independent review remain due.
No live model call, full CI, real remote write, commit, PR or merge occurred.

Fresh changed-candidate publication reached a closed-command shape mismatch in
release resume (1 failed in 14.97s, root-subscription-base-publication-red.log).
The exact base-update/adoption pair is now carried through subscription push
payloads, current publication preflight, historical replay and resumed delivery.
Historical source reconstruction reads the persisted command authority so later
acknowledgement does not invalidate the original source. Adoption replay plus
fresh acceptance/publication/recovered push/human merge gate passed both tests
in 33.59s (root-subscription-base-publication-first.log), session 45238 consumed.

Lost remote/local effect responses, pause before/after adoption, and lease expiry
at reservation/final settlement passed all seven checks in 83.20s
(root-subscription-base-recovery-first.log); session 32920 is terminal and consumed.
The integrated mypy check again passes 380 production files. Ruff currently
reports seven fixable import-order findings; formatting will follow the active
source-boundary batch. No independent review is current.

All 11 source/budget/history boundaries passed in 108.16s
(root-subscription-base-boundaries-first.log); session 50652 is consumed. This
includes a real three-attempt primary contract exhausted by its actual planning,
selection and acceptance usage, with no base effect or repair debit. Formatting
is complete and all 20 scoped paths pass Ruff and diff checks. Current mypy passes
380 production files (root-subscription-base-adoption-mypy-final.log).

The repeated-publication and paused-startup-recovery batch passed all five in
118.81s (root-subscription-base-followup-first.log); session 2912 is terminal and
consumed. It covers a second remote failure, a second controlled base advance,
and an actual startup-adapter reconciliation while paused before redelivery.
Both retained acceptance and the first base adoption remain verifiable after
later repairs. Production composition/delivery runtime passes 28 in 5.09s with
five library warnings (root-subscription-base-composition.log).

Root now owns the 32-module affected base/release/resume batch, session 61422,
root-subscription-base-run.py. Inputs in root-subscription-base-affected.json are
fixed until completion; the paired log retains actual output. This is an affected
suite, not full CI. The initial call yielded at 22:48:18 UTC, with the first check
due at 22:49:18. Remaining paused decision/controller paths, production final
acceptance wiring and current independent review are still required. No live model
call, full CI, real remote write, commit, PR or merge occurred.

Base affected batch completed: all 240 tests passed in 1404.79s with five existing
Google-library deprecation warnings and matching before/after scoped hashes.
Session 61422 is terminal and consumed (23:16:20 UTC). Candidate HEAD remains
6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db plus preserved scoped uncommitted changes;
root-subscription-base-affected.json/log retain exact argv, Python/environment,
status and input identity. This is affected evidence, not full CI. Current
independent review/correctness scrutiny remains outstanding under the recorded
provider and host thread limitations.

The next bounded outcome is root-subscription-failed-repair-contract.md. Isolated
acknowledgment proof has an observed missing-module red and 11 pure cases passing
in 0.71s; scoped Ruff/mypy pass. The actual PG fail-after-commit regression is now
running before controller/handler integration. Raw pending selection phase tests
are separately prepared and unrun. No live call, full CI, remote write, commit,
PR or merge occurred.

Failed repair delivery increment: root-subscription-committed-repair-red.log
reproduced the unnecessary remote continuation (1 failed, 1 deselected in11.71s).
Resume now binds a deeply proved repair receipt and immutable failed-command
identity inside run.resumed. It excludes only that acknowledged source from
failed-before-admission recovery. Actual remote/base resume and replay passed
both cases in24.21s (root-subscription-committed-repair-first.log), session37640
consumed. Production mypy passed385 sources before the subsequent push integration.

The fresh base-acceptance publication regression then reproduced the separate
COMPLETED-only source rejection (1 failed,2 deselected in17.81s,
root-subscription-failed-repair-publication-red.log), session58480 consumed.
Remote/base reviewed push now additionally accepts exact failed-delivery resume
provenance after existing deep controller proof. Failed status remains unchanged.
Current sole PG session10072 exercises three publication paths and eight receipt,
debit and lease boundaries; source inputs are fixed until it completes. Separate
post-validation acknowledgment-tampering tests are prepared and unrun. No current
independent review, live call, full CI, commit, PR or merge is claimed.

## 12 September — quota addition and recovery checkpoint

The latest requested addition is durable quota awareness. PostgreSQL must own
provider/account/pool exhaustion evidence, launch admission and one recovery probe;
expiry permits an attempt and does not assert a remaining allowance. The selected
primary, approved specialist routes, billing and ownership/effect recovery remain
binding. No development-provider adapter or local JSON cache is part of Forge.

Recovery checkpoint before this addition: the base adoption affected batch passed
240 tests in 1404.79s. Failed-delivery history/pending-decision rerun passed 8 and
failed the raw `pending` variant at the existing quiescence guard (122.25s,
`.llm-output/root-subscription-repair-history-pending-second.log`). Prepared and
observed pending decisions passed. The six history-test assertions were corrected
to inspect durable publication rejection rather than expect an exception. The raw
pending-decision production repair remains outstanding, with its tests preserved.

Development routing: the installed Gemini local preflight reports confirmed
exhaustion and `fallback_required` through 2026-09-13T15:02:39Z (the command returned
exit 1). A fresh Luna fallback completed the initial adapter patch and reports 42
focused checks; integrated acceptance is pending. The reused Terra runtime worker
returned twice after scaffolding without implementation/check completion. Root
reclaimed its files and PostgreSQL test ownership, preserving the scaffolds. Luna
now owns the separate quota operator API/CLI/task-inspector outcome. No live
application model calls, full CI, remote writes or commits occurred.

## 12 September — durable quota implementation and verification

The integrated addition uses migration `20260912_0019` and Forge-owned domain,
repository and application interfaces. Exhaustion is shared across runs/workers
by provider and opaque account/pool identity. Atomic scheduler admission skips
blocked pools before attempts, budget reservations or execution capacity; an
already approved specialist fallback preserves the task, worktree ownership,
frozen profile, requested route and auth/billing boundaries. The selected primary
never changes. Existing uncertain-client and effect reconciliation fences remain.

Known resets and bounded unknown-reset cooldowns are separate fields. One
recovery probe owns an expired pool; its admission and evidence revisions survive
restart. Older successes cannot clear newer evidence, and concurrent exhaustion
updates retain the longer active horizon. Operator reports are authenticated,
idempotent and audited. API, CLI and the refreshing task inspector show the
effective fallback and blocked/eligible/unknown states without allowance estimates.
`docs/v0.2-upgrade.md` and `.env.example` describe the shared mapping/configuration
and retained-evidence downgrade guard. The version-2 acceptance fixture bytes are
unchanged; the quota assertions extend A6/A7/A8.

Root observed and repaired these material findings in focused red/green checks:

- QCORE-1: stale probe settlement extended a newer known reset with the generic
  cooldown. Both stopped settlement and abandoned-probe recovery now preserve
  newer observation revisions.
- QCORE-2: abandoned-probe recovery and late settlement acquired pool/admission
  locks in opposite orders. A concurrent PostgreSQL test reproduced a deadlock;
  both paths now acquire the pool first.
- QCORE-3: the task projection treated expired blocks as blocked and successful
  recovery as measured availability. It now reuses the repository's eligibility
  status and resolves a task's pool even beyond the first global status page.
- QCORE-4: adapters conflated limit categories and missed supported error
  variants. Fake-client/classifier tests now separate actual account exhaustion,
  throttling, local budgets, authentication, outages, policy and unsupported
  routes, including the installed Codex schema's structured HTTP failures.
- QCORE-5: new quota tests initially reused the same basename in several test
  directories, preventing combined collection. Domain/API/CLI files now have
  unique names; no pytest policy was changed.

The affected PostgreSQL scheduler/admission/result/usage/runner/effect/decision
recovery batch passed **110 tests in 192.11s** with five existing library
deprecation warnings (`.llm-output/root-quota-affected-pg.log`). The separate quota,
operator and projection batch passed **16 tests in 27.96s** before the final
adapter-only extension and import sorting. It covers multiple runs/new UOWs,
single probes, uncertain live clients, current/stale responses, cooldowns,
account remapping, budgets, primary/paid-fallback denial, partial work/effects,
audit replay, status expiry and downgrade retention. Subsequent final-candidate
reruns are recorded below; no stale failing run is presented as current evidence.

The initial affected unit/operator/worker batch passed **160 tests in 12.93s**.
After adding documented Claude error cases and installed Codex schema variants,
the adapter subset passed **80 tests in 3.54s**. Frontend component tests passed
**4 tests**; typecheck, scoped ESLint and generated API contract checks passed.
Scoped mypy passed 19 production files, followed by the two final changed inputs.
Ruff's remaining two import-order findings were repaired and checked. Raw red and
green logs remain under `.llm-output/root-quota-*`.

Independent quota review is **not complete**: the installed Claude preflight
reported `fallback_required` for confirmed exhaustion through
2026-09-13T01:00:00Z; the permitted fresh Astra reviewer could not spawn because
of the host thread limit. No replacement model was silently substituted and no
review/correctness gate is claimed. The primary's checks are execution evidence,
not independent review. Full v0.2 acceptance remains blocked by the broader
implementation/capability/release work described above. No live application
provider calls, full CI dispatch, commit, PR, merge or production migration ran.

Final candidate checks completed at 00:49 UTC: **172 unit/operator/worker tests
passed in 12.13s**, and **16 quota PostgreSQL tests passed in 28.66s**
(`root-quota-final-unit.log`, `root-quota-final-pg.log`). Together with the 110
affected PostgreSQL checks above, these are 298 distinct backend checks; the
**five** final frontend checks are separate. The additional classifier regression
proves an explicit exhaustion code takes precedence over generic rate-limit
wording. The final UI regression proves a task's reason/retry time remains visible
beyond the global status page by reusing the shared quota card. The broader PG evidence preceded only
non-behavioral UOW import sorting and additional adapter classifier cases;
the final quota/worker/adapter runs cover those changed inputs. No other core
scheduler or settlement change followed that affected batch. Mypy, repaired Ruff,
frontend ESLint/typecheck and OpenAPI export/type consistency checks pass.
The final full-package mypy check passed **394 production modules**, and Ruff
passed all scoped quota Python source/test files (`root-quota-full-mypy.log`,
`root-quota-final-ruff-all.log`).
All root test processes are terminal and consumed; root retains PostgreSQL
ownership for the next integration task. `.llm-output/root-quota-verification.json`
records HEAD, scoped input hashes, commands, results and review limitations.

## 12 September — quota fallback application and independent review

Root found QCORE-6: scheduler admission allowed a frozen specialist fallback but
handoff application and later acceptance receipt verification still required the
preferred route. `ExecutionEnvelope.permits_route` now centralizes the exact
preferred binding or approved specialist fallback, preserving requested route,
primary identity and auth/billing boundaries. Invocation, application and receipt
proof use the same check. Two actual regressions reproduced handoff rejection
and then acceptance receipt rejection separately; their logs are
`root-quota-fallback-handoff-red.log` and `root-quota-fallback-receipt-red.log`.
The repaired broker/Git/handoff/acceptance path and domain boundaries passed six
tests in 16.83s (`root-quota-fallback-first-green.log`).

The reused implementation worker returned incomplete coverage twice. Root
interrupted it, confirmed ownership release and completed the regression directly;
the abandoned admission-only test is not counted as proof. Its missing cleanup
fixture was a test issue, not a reason to weaken retained-evidence migrations.
Root now owns all quota changes and the sole PostgreSQL test process.

The expanded unit suite passed 326 tests in 12.91s with five existing library
warnings and matching input hashes (`root-quota-fallback-unit.json/log`). Source
Ruff and full-package mypy passed (390 reported modules). The corresponding
PostgreSQL batch is still running at this checkpoint; its scoped inputs remain
fixed. These checks precede the review repairs below and are not their evidence.

The local Claude preflight again reported confirmed exhaustion through
2026-09-13T01:00Z, `fallback_required` with exit1. The permitted fresh Astra-low
reviewer successfully started this time and completed read-only review of
`root-quota-review.json/diff`. It found:

- QREV-1 (P2, fixed): after switching A to approved fallback B, exhaustion of B
  did not reconsider now-eligible frozen preferred A. Preserve its exact original
  binding and add an A/B/A regression with unchanged scope and repair budgets.
- QREV-2 (P2, fixed): the CLI quota listing exposed only its first 100 pools.
  Add bounded offset/limit options and a later-page regression. The new CLI test
  observed the missing option failure (`root-quota-pagination-red.log`).

The review found no further material issues in inspected transitions/classifiers
and recommended distinct correctness scrutiny of launch/probe reclamation races.
Both repairs have observed red and focused green evidence: the preferred-return
regression and fallback acceptance passed 2 tests in 11.64s; CLI/service passed 8
in 2.03s (`root-quota-review-repair-pg-green.log`,
`root-quota-review-repair-cli-green.log`). A fresh Sol-high correctness gate is
assessing the pool/lease/launch interleavings after these repairs; it is not yet
claimed complete.

The broader pre-repair PostgreSQL batch completed with **184 passed in 388.34s**
and five existing warnings, with matching scoped inputs. After the two review
repairs, the final unit batch passed **329 in 14.47s** and the affected quota/
scheduler/admission batch passed **58 in 112.84s**, both with matching scoped
hashes (`root-quota-review-final-unit.json/log`,
`root-quota-review-final-pg.json/log`). The earlier broader handoff/application
checks remain relevant: their code is unchanged; the new preferred-return path
and actual fallback acceptance were rerun. Repaired source Ruff/mypy pass.

Root also repaired QCORE-7 in the read-only task projection: an explicitly mapped
preferred model was incorrectly labelled a quota fallback by comparing requested
and effective models. It now compares against the frozen preferred effective
route. A corrected fixture distinguishes approved preferred mappings from actual
fallback selection. The focused assertion failed as expected, then all **7
projection/API checks passed in 7.87s** (`root-quota-mapped-status-assertion-red.log`,
`root-quota-mapped-status-green.log`). This later display change does not alter
quota admission or settlement; those sources remain stable for the gate. The
prior five frontend component checks, typecheck/lint and generated API contract
evidence remain applicable because the component/schema inputs are unchanged.

All root test sessions are terminal. The broader v0.2
pending-decision resume regression remains preserved and failing; no related
production change has been made during this quota increment. No live provider,
full CI, production migration, commit, PR or merge occurred.

## 12 September — quota probe recovery transaction fence

The independent Sol-high gate found QGATE-1 (P1): recovery read committed lease
and launch rows while a real renewal or launch intent could remain uncommitted.
After the old expiry it could clear that probe and later admit a second one.
Root reproduced both interleavings in two-session PostgreSQL tests with controlled
time (`root-quota-probe-races-red.log`, two expected assertion failures).

Recovery now obtains the existing run lock with `FOR UPDATE SKIP LOCKED` before
refreshing lease, attempt and launch evidence. Renewal and launch already share
that lock. A busy run retains its probe; skipping avoids the pool-to-run wait
that would invert settlement's lock order. No new lease state or launch path was
introduced. Both new cases plus the prior settlement lock-order and client-stop
recovery cases pass **4 tests in 8.46s**
(`root-quota-probe-races-first-green.log`). Scoped mypy/Ruff pass; formatting was
normalized before the expanded affected run. The final quota/scheduler/admission/
launch/preparation PostgreSQL batch passed **87 tests in 177.19s**, with identical
before/after scoped input hashes (`root-quota-postgate-pg.json/log`). All root
test processes are terminal; root retains sole database ownership.

The independent Sol-high gate passed the repaired candidate, verified its source
hashes against `root-quota-postgate-review.json`, and closed QGATE-1 with no
remaining material correctness findings. It confirmed the frozen preferred-route
return, fallback proof boundaries, stale-revision protection and settlement/probe
lock order. The short scheduler/admission transaction remains the launch
authorization point: later exhaustion observations do not revoke permits already
issued. The gate performed read-only review, not test execution.

The bounded quota addition is accepted on the preserved uncommitted candidate at
HEAD `6d87fe03c4a3794c97ebaa3ba0d82cb0d350c6db`. Consolidated evidence is in
`.llm-output/root-quota-acceptance.json`: 329 unit/operator/adapter/worker checks,
87 final affected PostgreSQL checks, five frontend component checks, and the
relevant source/schema/type/lint checks pass. Earlier broader 184-case PostgreSQL
evidence is retained with its input and reuse limits; overlapping runs are not
added into a new total. No live provider calls, full CI, production migration,
commit, PR or merge occurred. The broader v0.2 implementation, acceptance and
release gaps remain open; this quota acceptance does not complete a delivery
slice or A1–A9 release scenario.

## 12 September — resume completion and production acceptance dispatch

The previous goal turn made verified progress by completing the bounded quota
addition. The next recovery outcome is assigned to a fresh Terra-low complex
implementer, with ownership of the three resume/quiescence source files, their
three focused test files and sole guarded PostgreSQL execution. Gemini's local
preflight returned confirmed `fallback_required`, exit20, through
2026-09-13T15:02:39Z; no provider call was made. The task uses the complex route
because it spans retained state, control and source proofs.

Root separately fixed production final-acceptance composition. The existing
dispatcher was implemented but omitted from `SubscriptionDecisionRecovery` in the
worker factory, leaving final-acceptance decisions classified as unsupported.
The factory now supplies that dispatcher with the existing receipt verifier,
terminal-effect verifier and shared Git snapshot function. The focused regression
observed `(applied=0, unsupported=1)` before repair and then passed. All **63 affected
unit checks pass in 5.56s**, with matching source/test hashes
(`root-acceptance-composition-unit.json/log`); scoped Ruff/mypy pass. This is a
localized wiring repair and reuses the existing guarded acceptance implementation.
No PostgreSQL run was duplicated while the recovery worker owned that resource.

The recovery worker's first handoff is not accepted. Root identified PRSM-1
(missing decision-identity correlation), PRSM-2 (quota evidence not excluded),
PRSM-3 (only two primary decision shapes supported instead of the existing
retained-decision/phase cases), and PRSM-4 (the required affected batch and input
records missing). These returned to the same owner for complete repair and checks.
Its assertion that default imports resolve to the canonical checkout is not
established: root's current executable/package/module probes resolve into this
managed worktree. The worker must record actual command/import evidence.

The attempted read-only Google documentation agent and later separate protocol
assessment could not start because of the host thread limit. Root instead found
and exercised a supported result-echo channel in the installed Gemini0.59.0 source.
A harmless prepared-operation nonce returns in the ACP completion with native
call ID, allowing a two-stage Forge operation protocol before any effect. The
offline source-path experiment passes concurrent identical calls, reversed
completion order, replay, conflicting arguments, foreign tokens and stopped
attempt cases (`gemini-identity-prototype/probe.mjs`, `result.log`, `README.md`).
It performs no provider calls or real effects and is not a production adapter or
PostgreSQL proof. This changes the next capability task from assuming correlation
is impossible to implementing/verifying a bounded staged-operation protocol.
Google model/auth/configuration/billing/live proof remains unestablished.

The full v0.2 goal remains active. No live application model call, full CI,
production migration, commit, PR or merge was performed in this continuation.

## 12 September — retained-decision and validation resume integration

The resume worker supplied an accessible raw affected-suite log with **130 passed
in 376.90s**, but its next two handoffs did not finish the agreed source-role,
interrupted-validation and final verification boundaries. Root reassessed after
those incomplete repair rounds and reclaimed the nine owned paths and sole
PostgreSQL execution after the worker stopped. The 130-case result remains
historical evidence, not acceptance of the later source changes. The worker's
import claim and terse validation.md were not promoted to verified input records.

Root reproduced five source-role violations in a focused 12-case decision matrix:
worker wait/accept/scope-response and primary handoff/scope-request were wrongly
eligible for the pending-decision count exemption. The correction applies the
existing dispatcher's primary/worker parent and role constraints; all **12 cases
pass** after the five expected assertion failures. Unknown excluded command IDs
now give no exemption. Existing downstream application checks remain authoritative.

The actual final-acceptance fixture demonstrated that unadmitted validation could
resume, while completed schema-2 validation initially failed the legacy manifest
version guard. The first worker repair passed that guard but stopped at a test
fixture issue; a fresh terminal timestamp in the already-exposed local fake runner
resolved the repeated-result artifact identity, without changing the shared fixture.
The next controlled interruption test exposed the candidate-bearing admission event
mismatch (one failure, two passes in 21.56s). Root now routes completed subscription
validation through the existing full SubscriptionPublicationEvidence verifier and
binds interrupted validation's candidate to its retained acceptance and exact
original/resume command. Legacy schema-1 manifest checks remain unchanged.

The final focused recovery and tampered-source checks are running under root's
sole PostgreSQL ownership, with exact command/executable/import paths and input
hashes recorded by root-resume-check.py. Scoped five-module mypy passes. One test
import order correction and final affected-suite/review evidence are still due.
Claude local preflight again returned confirmed exhaustion through 13 September
01:00 UTC; no Claude or application model call was made. Independent integrated
resume review and the scoped correctness question remain open. The accepted quota
addition is unchanged by these recovery repairs; full v0.2 remains incomplete.

The final root affected batch now passes **201 tests in 594.86s**, with all 39
recorded source/test/configuration inputs unchanged. It covers stopped and pending
decision resume, causal controls/quiescence, plan/review/candidate application,
final-validation admission/publication/dispatch recovery, committed repair and
legacy delivery resume. The two failing negative-test fixtures from the initial
focused batch were repaired and both pass in the final batch; they were fixture
issues, not waived production failures. Root's process is terminal, and root
retains sole PostgreSQL ownership. The 63 composition/unit checks retain matching
inputs; their scope is wiring and shared recovery behavior, not live conformance.
All 12 integrated review paths pass Ruff/format checks and the five resume source
modules pass mypy (composition passed its separate scoped mypy check).

The independent Astra-low integrated review reported no material findings and
verified all 12 candidate hashes. The separate Sol-high correctness gate remains
pending on concurrent lease/control authority and full validation receipt binding.
`.llm-output/root-resume-acceptance.json` records this distinction and the exact
evidence; current tests do not substitute for the outstanding gate verdict.
The plan and A7 fixture requirements now include retained-decision and both final-
validation resume interruption points. No live provider, full CI, production
migration, commit, PR or merge occurred.

The separate Sol-high gate has now returned **RGATE PASS**, with all 12 hashes and
the saved integrated diff verified. Fresh command UoWs and repeated run/lease
fences prevent the suspected stale ORM authority through supported production
flows. The count exemption leaves unrelated effects and controls blocking; full
validation recovery binds the retained acceptance, exact command, candidate,
manifest, artifacts and receipts. No test or PostgreSQL process was started by
the reviewer. Deliberately synchronized deadlock-victim selection remains untested;
the reviewer found no stale-authority or partial-commit path. This bounded recovery
increment is accepted with the matching 201-case affected batch; full v0.2 remains
incomplete.

## 12 September — subscription measurement views

Root completed the bounded subscription usage query/API and shared run/global
web view after reclaiming an incomplete delegated draft. The draft loaded all
attempts and performed lookups per group; the final query selects complete groups
in PostgreSQL and streams their source rows through the existing strict decoder.
Durable consumption wins over provisional telemetry, including explicit unknowns.
Every subtotal reports measurement coverage; measured zero remains zero, unknown
remains null, and currencyless amounts have no monetary subtotal. Actual attempt
routes and currencies remain separate after a task changes route. Result counts
include failed and unfinished attempts without implying task acceptance.

TDD observed the missing-consumption assertion failure, 13 invalid-schema reds
and the missing web region. Two pagination fixture errors were corrected to use
valid approved route bindings. Final root PostgreSQL/API checks pass **35 tests
in 23.64s**, with matching inputs, covering nine real database cases, response
invariants, authentication/bounds, legacy projections and the generated contract.
The shared run/global UI checks pass **10 tests**. Existing hook checks pass **2
tests** for late-response cancellation and controlled-time, nonoverlapping refresh.
All processes are terminal. Scoped Ruff (six files), mypy (four sources), web
type checking, five-file ESLint and generated API type verification pass.

Claude local status again reported confirmed exhaustion, so the independent
integrated review used fresh Astra-low. All 13 candidate hashes and the saved
diff matched; no material finding. The final 35-case batch was pending at review
time and root subsequently verified its unchanged inputs. No extra correctness
gate was requested for this read-only increment. Root-usage-acceptance.json records
exact commands, hashes, tool/log evidence and limits. Production-scale query
performance and browser acceptance remain unverified. Primary-share, turn/wait,
repair/fallback and task-outcome assessment remain outstanding; this does not
complete slice 7 or A8. Existing records that already stored defaults as zero
cannot be reconstructed as historical unknowns by the projection.

The Google protocol worker's earlier draft used invented ACP frames; its later
five-test handoff still lacked bounded start tracking, admission of concurrent
MCP replay identities before awaits, and the normal execute-result ACP exchange.
Root reassessed and transferred the same three disjoint source/test paths to a
fresh Terra-low complex implementer after the previous writer stopped. GEM7–10
and the complete actual-source-shaped protocol contract are in
root-gemini-protocol-repair.md. That component remains unaccepted and unregistered;
Google model/auth/billing/live conformance and the production gateway are separate
unproved work. Root remains sole PostgreSQL owner. No live model, full CI,
production migration, commit, remote write, PR or merge was performed.

The fresh Google worker's first repair reported eight focused and 72 affected
checks, but root found GEM11: receipt completion still required the preparation
call's native ID, and its test reused that ID instead of the distinct execution
call ID emitted by the client. GEM12 adds outgoing result bounds; GEM13 requires
the missing cancellation/exception/restart/input-adversarial cases and exact
before/after evidence. These returned to the same sole writer; no Google protocol
acceptance is claimed. Root's independent read-only task-control map found that
the existing stop primitive terminalizes queued pause and does not update logical
task outcome flags. The future operator service must preserve paused dependencies
and use task-specific resume evidence. No task-control source was edited.

## 12 September — Google codec completion and queued-task controls

Root reclaimed the Google codec after the fresh worker's second handoff still
omitted the explicit adverse cases. Numeric progress tokens, separate native
prepare/execute IDs, early auxiliary completion, cancellation, uncertain broker
results, restart, invocation limits and malformed inputs now have focused tests.
New failing cases exposed native-ID reuse across proposal/receipt kinds, complete
MCP output exceeding the frame bound after escaping, and invalid Unicode escaping
the protocol error boundary. Root repaired them and matched numeric identifiers
to the pinned client's integer schemas. The actual prepare/execute bridge remains
an in-process codec preceding existing durable broker authority.

Final **113 combined protocol tests pass in 0.54s**, including 49 Google cases;
Ruff, format and source mypy pass with matching 11 relevant inputs. Exact evidence
and the two-file candidate are in root-gemini-final-*.json/log and
root-gemini-review.json/integrated.diff. Claude local status returned exit 20,
confirmed exhausted until 13 September 01:00 UTC. The fresh Astra-low fallback
review could not start because of the host's agent thread limit. Independent
review and the applicable correctness gate remain pending. Google is unregistered;
no provider process, capability acceptance or live model call occurred.

Three task-control worker handoffs returned no implementation, including the
reduced never-attempted scope. After a fresh-worker allocation also failed, root
implemented the first backend increment directly. It uses existing mutation and
audit records, run locks, scheduler flags and guarded cancellation wake. Paused
tasks remain pending without capacity or budget use. Resume proves the exact
receipt, current versions and unchanged contract/envelope/candidate/scheduling
binding. An additional red exposed modified receipt acceptance; audit response
digests now bind resume and replay to the original event. The first two real
PostgreSQL cases pass; the broader focused batch is running. Static mypy passes;
two import-order fixes remain. Running/pending-decision controls and operator
API/CLI/UI integration are still due, and this increment is not accepted yet.

The final affected PostgreSQL batch now passes **112 tests in 217.20s** with all
30 recorded inputs unchanged: 25 queued-task control cases and the existing 87
quota/scheduler cases. All root processes are terminal. Current Ruff/format and
mypy pass. Two handoff-property/admission-path test mistakes were corrected; the
request-digest repair retains only a digest and redacted reason. Self-review also
found that pausing a task while its whole run was already paused could issue an
unusable resume receipt. The first two fixtures had incomplete suspension state;
root replaced them with the production run-pause method, observed the expected
behavioral failure and verified the guard. TC1–3 and exact evidence are recorded
in root-task-controls-review.json; no remaining local test failure is known.

This stage uses existing tables and preserves current quota/recovery implementations.
It accepts only never-attempted specialist tasks; running and retained-decision
recovery plus API/CLI/UI remain due. The selected primary still uses run controls.
Independent review and the applicable correctness gate remain pending because
Claude is exhausted and a fresh native reviewer could not be allocated. These
new components are not independently accepted. The quota addition's earlier
reviews remain accepted, and all 87 quota regressions have now passed again with
the added UoW member. No full v0.2 release scenario, live provider, full CI,
production migration, commit, PR or merge is claimed.

## Individual stopped-task controls and quota regression — 12 September 2026

Candidate remains `forge/v0-2` at `6d87fe0` plus preserved uncommitted changes.
Root owns the implementation and sole PostgreSQL verification; all workers and
root command processes are stopped at this checkpoint. Migration 0020 adds a
durable lifecycle for audited individual task stops. Original mutation responses,
provider results, task contracts and ownership evidence remain retained.

Active and expired-lease attempts accept pause/cancel requests. Reconciliation
holds capacity until client/effect termination is proved, then settles the stop
once. Settled pauses retain path/candidate conflicts while releasing execution
capacity. Exact pre-pause pending decisions can resume application without a new
attempt/repair; late output remains unaccepted and requires the existing fresh
attempt path. Cancellation can supersede an active or settled pause directly.
Whole-run quiescence uses a separate physical-stop proof and cannot resume an
individually paused task. Locked-run recovery skips ahead so other runs progress.

The authenticated, CSRF-protected API, local operator CLI and TaskInspector share
the same audited, versioned service. The inspector distinguishes requested and
settled stops, obtains causal pause receipts after restart, retains the exact
request/key after an unknown response, and refreshes stale-version conflicts.
It uses normal scheduler quota admission when resumed work becomes eligible.
The read projection does not grant authority or fabricate available allowance.

Evidence is retained in `.llm-output/root-task-control-review.json` and its
integrated diff, with command-level candidate hashes in the referenced records:

- `root-task-stop-focused`: 22 PostgreSQL cases passed in 72.32s. Earlier queued
  coverage has 25 cases. The real failing cases included retained-decision
  admission, whole-run interference, cancelling a paused task, locked-run
  starvation, and expired-lease rejection; repairs passed their focused checks.
- `root-task-stop-affected`: 432 PostgreSQL cases passed in 1193.40s, including
  quota admission/probes/fallback, whole-run recovery and task/effect integration.
  All recorded inputs matched during the run and at the final checkpoint.
- `root-task-control-projection-affected`: eight PostgreSQL projection/quota
  inspection cases passed in 19.20s after adding the operator control projection.
- `root-task-control-operator-final`: 20 API/CLI cases passed in 3.36s, with five
  existing dependency warnings. Anonymous and CSRF denial, strict request types,
  exact causal UUIDs and safe errors are covered.
- `root-task-control-web-final`: 12 component cases passed in 6.05s. Scoped
  Python Ruff/mypy, ESLint, TypeScript, OpenAPI export verification and generated
  API type checks pass. The 43 production-composition unit cases also passed.

These counts overlap and are not a whole-suite total. No provider invocation,
full CI, production migration, commit, PR or merge occurred. The earlier quota
acceptance remains recorded, with current regression evidence for shared changes;
it does not establish acceptance of this new task-control batch.

Independent task-control review and the scoped correctness gate are pending.
Claude's local status returned `fallback_required` (exit 20), with a known reset
of 13 September 01:00 UTC. The host rejected fresh Astra-low reviewer allocation
because the agent thread limit was reached. No independent pass is claimed.
Controls for queued/blocked work with a prior terminal attempt, late quota/stop
budget interactions, complete browser acceptance and the remaining v0.2 flows
remain due. The full goal and all incomplete A1–A9 release gates remain active.


## Quota-preserving settled controls — 12 September 2026

Candidate remains `forge/v0-2` at `6d87fe0` plus preserved uncommitted work.
Forge's PostgreSQL quota state remains independent of the development framework.
No provider is launched by these checks, and unknown allowance is never reported
as zero or confirmed availability.

Settled quota/repair histories can pause, cancel or resume with their original
queued/blocked state, frozen route, ownership and budgets. Scope-response replay
binds a contiguous audited idle-resume chain. A confirmed exhaustion result that
arrives during a task or whole-run pause retains its durable quota observation
and queues future work without another repair debit. Cancelling a pending task
while its run is paused no longer requires resuming execution first.

Independent Astra-low review found TC-001 (mixed-case Windows path comparisons)
and TC-002 (capacity exemption trusting a lifecycle row). Both had failing real
PostgreSQL regressions, were repaired and explicitly closed in the re-review.
Capacity now requires audited settlement and physical stop/effect proof, using
only nonlocking reads of other runs. Valid stopped tasks retain ownership while
releasing execution capacity; invalid evidence remains counted.

Verification, with exact command/input hashes and logs:

- `root-idle-controls-final-pg`: **218 passed in 646.49s (0:10:46)**. Includes quota pools/probes/fallback,
  scheduler admission, task controls, source/replay guards and whole-run recovery.
- `root-idle-controls-final-unit`: **122 passed, 5 warnings in 7.40s**; provider classification and
  quota/task-control API/CLI/service checks using fake clients.
- `root-idle-controls-web`: **13 component tests passed**. TypeScript, generated
  OpenAPI/type verification, scoped ESLint, mypy and Ruff/format checks passed.
- `root-task-control-review-repair`: **8 passed, 22 deselected in 28.29s**,
  included in the final affected run. The earlier 176-case run's two failures
  were existing conflict-message expectations; the rejection behavior remained
  intact, its wording was restored, and the final rerun passed.

These counts overlap; they are not a full-suite total. The independent re-review
verified all 46 scoped hashes at diff
`5f96adf4aa31463e31be411e5bf80587d980cb428976ce50eea61a9332536048`.
Subsequent documentation changes preserve all 41 reviewed code/test inputs.
`.llm-output/root-idle-controls-verification.json` records current evidence and
the review closures. The original quota acceptance remains historical evidence;
the current shared integration is verified by the newer records above.

The separate Sol-high correctness gate is **unmet**: the host rejected both
fresh allocation and reuse of the earlier gate with `agent thread limit reached`.
Its concrete read-only contract is retained in
`.llm-output/root-task-control-correctness-contract.md`. Astra's re-review is not
a substitute. Claude's installed-framework quota preflight reports exhaustion
until 13 September 01:00 UTC; no Claude call was attempted.

The plan, A6/A7 acceptance scenarios and inspector guide now describe these
behaviors. Full browser acceptance, other v0.2 implementation, authorized live
application verification/full CI and release gates remain incomplete. The Google
gateway/MCP bridge draft is preserved but unaccepted: its worker did not finish
supervised fake-peer or child-lifecycle validation. All workers and root-owned
checks for this increment are stopped. No commit, PR, merge, production migration
or live provider call was made.
