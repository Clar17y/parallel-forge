# Forge v0.1 implementation ledger

## Current verified state (7 September 2026)

Task14's controlled-tool completion batch is **accepted and pushed** at
`c77669db8c6f5558282b05de77fc27b288a2c62e` on `origin/forge/v0-1`:
Git commit, named checks, eligible evidence reads and orphan tool settlement are
implemented. The integrated review and finding-specific rechecks have closed
C01-C03 and G01; the separate Sol-high correctness recheck passed. No PR merge
is authorized. Baseline failures were resolved in earlier verified checkpoints.

Evidence includes a fresh independent pre-repair full run (2021 passed, 57
skipped), then 84 affected tests and seven independent cancellation-boundary
checks after the G01 repair, with Ruff and strict mypy (144 sources) passing.
Subsequent formatter-only edits preserve exact Python ASTs. The full run is
historical for the pre-repair candidate, not mislabeled as a fresh post-repair run.

Tasks15-16 now have recovered contract, context-selection and offline ADK tests.
The frozen pre-repair candidate passed 251 agent/context/PostgreSQL tests (six
warnings, 4.97 seconds), including real prompts. Primary regressions corrected
cached-token accounting and lost usage on invalid output. The actual offline
SDK cost probe now stops before the next tool call after exceeding the cost
limit (zero effects, previously one), while retaining measured usage.

The gateway now retains each attempt's provider request ID beside aggregate
usage. Real PostgreSQL tests now verify success, invalid-output failure and
atomic finalization rollback with fail-closed retry. An independent full run on
that same frozen candidate passed 2261 tests, with 57 skipped, one live test
deselected and six warnings (487.13 seconds). Skip reasons were not printed.
Ruff, formatting and strict mypy passed. This is pre-repair evidence, identified
by HEAD `0ce4b06867a4c8838ea5a2916915ec6d31a193f7` and integrated diff SHA256
`2c7fae1890d6e7e5f6152fbc30031a109cba1948cf04c779898f4577accd1230`;
the verifier's before/after manifests were identical.

The sole integrated Claude Opus 5 medium review found A16-C01 (major): a
post-provider pricing failure could discard measured usage. Its repair now
retains measured-but-unpriced aggregate and attempt metadata, with PostgreSQL
evidence. A16-C02 clarifies duration-budget exhaustion versus provider timeout;
A16-C03 corrects a passing regression's stale name. The targeted recheck closed
all three findings. Fresh affected checks passed 1230 tests, with two existing
host link-creation skips and six warnings (61.28 seconds); Ruff, formatting and
strict mypy passed on the identical before/after candidate.

The separate Sol-high correctness gate found four major defects: unbounded
stream cleanup (A16-G01), known-price catalog output replacing measured identity
(G02), aggregate credential checks bypassed when attempt metadata is empty
(G03), and measured usage lost on prompt drift after the first response (G04).
All four findings are now repaired and CLOSED by the same Sol-high gate's
finding-specific source recheck. Tasks15-16 are **accepted and pushed** at
`fe1a237ffd109daed2e0c08da755f4aa347da11c`. The accepted Git tree
`825e73411711bdeb3ae996a64ac7dbac6b018335` exactly matches the frozen tested
candidate at HEAD `d555a78dc47023428a08b6115fd3be9f78eeb63d` plus the recorded
uncommitted inputs; this is a tree mapping, not a claim tests ran on a later HEAD.

Current affected verification: **1246 passed, 2 skipped, 6 warnings (61.59s)**;
Ruff, formatting and strict mypy (144 sources) passed. Before/after identities
were identical. The skips are host-unavailable symlink/file-link creation in
`test_repository_inspection.py:148` and `test_untrusted_instructions.py:142`.
The recheck ran no additional tests; its independent contribution is source
inspection of G01-G04. The primary recorded its final handoff in
`.llm-output/runs/tasks15-16/correctness-gate/recheck/report.md` after interrupting
a stalled recheck and obtaining a bounded result. No unresolved major finding
remains in this batch. Uncooperative Python cleanup cannot be forcibly killed;
owner settlement is bounded and detached task completion is observed.

Task17 is now active: exact plan approval, revision feedback, semantic planning
attempts and production worker composition. This is one high-risk integrated
batch because authorization, state races and durable admission are coupled.
Core implementation uses Terra-low; routine composition will use Gemini first.
Individual tests and documentation edits do not trigger separate reviews.
Task17's tested implementation checkpoint is now pushed at
`451e39b121b7dcf8fb81a9243767ceff12034abd`, but acceptance still awaits the separate
Sol-high correctness repairs/rechecks for A17-G01/G02. Both integrated authority
findings are closed.
Tasks17-29 and final cross-platform/end-to-end acceptance remain incomplete.

The entries below retain the chronological evidence and earlier checkpoints.

## Active continuation â€” 7 September 2026

Goal: complete all 29 tasks and deterministic acceptance criteria in
`docs/superpowers/plans/2026-08-21-forge-v0-1.md`. No task is complete merely
because source exists or an old checkbox says so.

The user subsequently directed work to the existing `C:\dev\parallel-forge`
checkout on `forge/v0-1`, overriding the initial fresh-worktree instruction.
This exception applies to development assistants in this continuation, not to
Forge runtime agent confinement. Future PR merges still need fresh explicit
authorization. Verified checkpoints may be pushed without force.

Starting HEAD and fetched remote: `072b5416f49d08aa6cabef10c8c2eda972c1f93b`.
The checkout was clean. Python 3.14.5 and Node 24.20.0 are available;
`uv sync --frozen --extra dev --python 3.14` installed the locked environment.
The preserved bootstrap runtime remains outside this checkout.

Before the location correction, the repository setup script successfully
created `codex/complete-v01` with an ACTIVE isolated database and checkpoints
`manifest.created`, `worktree.created`, `database.active`, `environment.staged`,
and `setup.complete`. That unused worktree and its branch/resources are retained;
no teardown was requested. The control database remains distinct from it.

### Current checkpoint and next actions

Latest recovery update: exact write operation kind, key, request digest,
run/execution/step/project/policy/resource bindings and outcome schemas are now
checked before settlement. Six PostgreSQL cross-scope counterexamples failed
before the repair. Recovery verifies canonical descriptor and blob bytes, keeps
terminal history immutable, and uses the existing receipt/audit format.
Twenty-six recovery tests now cover malformed proof, concurrent settlement,
cancelled runs, transaction interruption/retry, replay and artifact substitution.
The affected recovery/scan/write/binding suites passed 59 tests in 46.03 seconds;
owned-file Ruff and strict mypy passed. Full before/after identities differ in
two files being edited by the named-check worker, so these results are development
evidence and will be rerun for the frozen integrated candidate. Logs are in
`task14-completion-20260907/orphan-write/` under `.llm-output/runs/`.

The named-check prelaunch cancellation worker owns its service/adapter and tests.
Git/named orphan integration and the complete batch acceptance remain open.
No additional review, checkpoint commit, push or merge has been performed.

Current development update: the updated repository/personal AGENTS.md and
framework v4 were reread. This high-risk Task 14 completion batch will receive
one integrated review and the required scoped correctness gate; no new review
was launched for these small repairs.

Caller cancellation now reaches only the named check's terminal runner child;
the owner retains receipt and audit settlement through repeated cancellation.
The PostgreSQL regression failed before this repair and passes afterwards.
The first orphan write recovery behavior test also exposed an audit missing its
artifact digest; the repair preserves that pointer and verifies idempotent
recovery/replay without another write. Recovery's bounded page iteration now
passes type checking. The recovery implementation remains provisional: strict
cross-scope validation, concurrency, cancelled history, and crash cases still
need completion. Named-check cancellation before launch and operator
cancellation also remain open; running-command cancellation is not evidence
that those distinct cases are handled.

Current affected evidence: 54 tests passed in 39.82 seconds, full source/test
Ruff passed, and strict mypy passed for 144 source files. Records are under
`.llm-output/runs/task14-completion-20260907/cancellation-recovery-*`; complete
before/after source identities are unchanged. These are development checks,
not Task 14 acceptance or a whole-branch verification. No new commit, push,
or merge was made for this unfinished completion batch.

Verified implementation checkpoint `826ad544aa979784cdd0815898ed419e5d4035ef`
is preserved on `origin/forge/v0-1`.

The integrated Task 14 foundation batch is accepted at candidate HEAD
`c1298b2f8bdf12e7a79194b266c9625c43a11cb4`, complete diff SHA256
`d383699625b30d6102efd355293b85a6acce86586013bd369357895a5876b329`.
Its final affected suites passed **146 tests**, with full source/test Ruff,
strict mypy across 140 source files, and diff checks passing. Complete candidate
identity stayed unchanged during verification. The final ledger update is a
separately inspected documentation-only delta.

Task 14 remains open: Git commit and named-check service dispatch, evidence
reader tools, and orphan tool-call settlement remain. No later task or
whole-branch acceptance is claimed. This goal has not merged a PR. The base
`c1298b2` was an externally supplied workflow-documentation update; it does not
authorize future PR merges.

| Pending slice | Frozen candidate | Actual status and next action |
| --- | --- | --- |
| Write cancellation, audited tests, immutable request/resource binding | `task14-write-binding-repaired-candidate.json`, diff `9d12f53e8190c45bfe806aee9665c84aa24c97aba04c69fba7c36d27cd188342` | Accepted for this checkpoint: fresh independent review, Sol-high gate, and fresh verifier PASS (206 tests, Ruff, strict source mypy, stable hashes). Service integration is covered below; orphan recovery remains. |
| SDK invocation identity foundation | `task14-identity-checkpoint-candidate.json`, diff `2c9aef559ad0e59b02ec8807dc2d0acc67ab2fb21b82ce603289f0e5b9618f5e` | Accepted for checkpoint: independent Astra ACCEPT, Sol-high PASS, fresh verifier 163 passed/one file-link capability skip, Ruff/strict mypy/diff and stable hashes PASS. Durable service admission/replay is covered below. |
| Evidence codec | `task14-codec-fold-candidate-candidate.json`, diff `2456d255be0fcd525b85fdfb7292cd3a9657d4f095eee44c805bc1b8e8ebdb9c` | Accepted for checkpoint: fresh Astra ACCEPT, Sol-high PASS, fresh verifier 105 domain tests/zero skips, Ruff and configured-strict source mypy PASS. Recursive model validation, unknown-field rejection, safe malformed-input errors, immutable nested data, fixed golden bytes/digest and UTC-instant chronology are covered. Persistence is covered below; runtime reader tools remain. |
| Durable write identity service | `runs/task14-foundations-20260907/cg01/candidate.json` | Accepted: trusted invocation binding, exact canonical replay and strict descriptor checks; positive and negative service coverage. |
| Evidence persistence | Same integrated candidate | Accepted: immutable schema/model/UoW/repository, causal projections, replay, rollback, snapshot isolation, and successful/denied bound reads. Runtime reader-tool dispatch remains next. |
| Git effect adapters and atomic phase preparation | Same integrated candidate | Accepted: exact preparation/publication authority, publication and no-repeat reconciliation tests, atomic phase transition support, and durable pre-effect lease confirmation. `git.commit` service integration remains next. |

All candidate paths above are under `.llm-output/`; each manifest records its base.
Next action: execute `task14-completion-batch.md` using the existing detailed
Git/check/evidence/recovery contracts, with one integrated review after the
combined implementation rather than per-adapter review cycles. Staged contracts
are not implementation evidence.

Completion batch in progress at base `94689ec`: Git service integration and
canonical evidence reads have assigned writers; named-check receipt/recovery
adapter implementation is assigned separately with non-overlapping ownership.
Gemini run `e02a1600-3416-4936-8818-e3638c6ae507` completed the pure command
result/output codec. Primary corrected a vacuous unsorted-JSON test and reran
the focused suite: **46 passed**, with identical complete before/after
candidate records under `runs/task14-completion-20260907/named-check-codec/`.
This is component test evidence, not acceptance of the unfinished batch. No
component-only review was launched. Git duplicate observation, canonical replay,
cancellation and failure settlement still require completion and validation.

Further unaccepted integration work now includes real managed-Git commit/replay,
publication operation settlement, and durable cancellation before publication
with a canonical cancelled receipt. Focused tests exposed and repaired pending
publication state and cancelled replay; a phase-rollback exception-boundary test
remains in repair. Named-check adapter invoke/reconcile, strict authority and
receipt-pointer checks, canonical evidence reads, and bounded orphan discovery
have component coverage. The combined component run passed 82 tests; a new
unselected service test was created during it, so final verification must be
repeated on a stable candidate. Initial named-check service admission, terminal
receipt/audit, and no-rerun replay are now implemented: real PostgreSQL tests
pass for success, nonzero exit and timeout. The broader affected run found one
Planner denial regression (204 passed/one failed); the repaired role dispatch
and typed stream iteration pass 55 focused checks, full Ruff and strict mypy
across 143 source files. Named-check cancellation, crash settlement and further
adversarial service tests remain, as does orphan settlement. No new independent
review cycle or acceptance is claimed.

The repaired initial-service candidate passed **205 affected tests** in 64.29s;
complete before/after records are identical at
`runs/task14-completion-20260907/repaired-initial-service-{before,after}.json`.
Full Ruff, strict mypy over 143 source files and diff checks pass. This ledger
addition is documentation-only after that check. It is a tested development
checkpoint, not Task 14 completion or review acceptance; cancellation and orphan
settlement remain the next required work. Last accepted remote implementation
remains `826ad54` (branch/documentation HEAD `94689ec`).

Foundation review: Claude Opus 5 run `291ac165` identified B01-B05; targeted
recheck `c31047e6` accepted their repairs and V01. The scoped Sol-high gate found
CG01 (stale lease snapshot before effect); its finding-specific recheck passed
after exact-owner renewal and refreshed-expiry validation were added. No
material findings remain in this batch. The earlier Astra attempt failed its
usage limit and produced no review. Gemini's historical individual-quota error
was classified as provider_error rather than fallback_required; no Luna
implementation fallback was invoked for that error.

Prepared Git acceptance is preserved in `.llm-output/task14-git-ancestry-final-candidate.json`,
`task14-git-final-review.md`, `task14-git-ancestry-correctness.md`, and
`task14-git-ancestry-verification/report.json` with raw logs. Its fresh verifier
reported158 passed, five symlink-capability skips, Ruff/source mypy/diff checks
passed. This does not replace final whole-branch and cross-platform acceptance.

### Integrated foundation review â€” 7 September 2026

Fresh full deterministic verification at HEAD `c1298b2`, complete diff SHA256
`de54239f250cd4280ca6fef1a5756717110ebbb0660d2ed464b4c406395b5bcc`,
reported **1,862 passed, 57 skipped, one failed**. Before/after complete identities
matched. Ruff, strict mypy and diff checks passed. The failure was the historical
Task10 migration-head assertion still expecting `20260822_0002` after migration
`20260906_0003` was added. Evidence: `.llm-output/runs/task14-foundations-20260907/verification/`.

One integrated Claude Opus5 review completed in run `291ac165-7ec5-4ad2-9424-9beb908ee32d`.
It found B01 (ORM string compared by enum identity, rejecting every prior-review
read), B02 (missing successful reader tests), B03 (missing publish/reconcile
success tests), B04 (retention FK metadata inconsistent with immutable triggers),
and B05 (missing pre-read descriptor size guard). A consolidated repair pass addressed these findings. Earlier E07 negative-test GREEN did not prove reader correctness; the
positive path must also pass. No foundation acceptance is claimed yet.

The primary reread the current personal/repository AGENTS.md and framework
workflow after the user's usage reminder. Small changes use direct checks;
this high-risk batch uses one integrated review, finding-specific repair
rechecks, and one scoped correctness gate. No automatic per-fix reviews or
repeated full-branch reviews. The failed Astra invocation produced no review.

## Baseline evidence

Historical recovery evidence: 1,530 passed, 57 skipped, 92 failures (88 state
engine, 3 run service, 1 schema). This is not current acceptance evidence.

Fresh verification at the starting HEAD, with empty tracked diff
(`git hash-object`: `e69de29bb2d1d6434b8b29ae775ad8c2e48c5391`):

| Check | Result | Evidence |
| --- | --- | --- |
| `python -m pytest apps/orchestrator/tests/application/test_state_engine.py -q` | 478 passed, 88 failed | `.llm-output/baseline-initial-worktree/01-state-engine.log` |
| `python -m pytest apps/orchestrator/tests/application/test_run_services.py -q` | 16 passed, 3 failed | `.llm-output/baseline-initial-worktree/02-run-services.log` |
| Schema suite | Initial invocation interrupted; full suite reproduces the schema failure | `.llm-output/baseline-canonical/full-deterministic.log` |
| Full deterministic suite | 1,529 passed, 57 skipped, 93 failed in 333.11 seconds | `.llm-output/baseline-canonical/full-deterministic.log` |
| Ruff | Passed | `.llm-output/baseline-canonical/ruff.log` |
| Strict mypy | Passed, 136 source files | `.llm-output/baseline-canonical/mypy.log` |

The first two checks ran in the identical-HEAD managed worktree before the
location correction. Logs were copied intact to the active checkout. Initial
failures show approval-state fixtures missing gate/digest metadata and ordinary
transition calls attempting evidence-bound approval transitions. Contract
diagnosis supports updating those fixtures without relaxing approval guards.
The fresh full run additionally failed
`test_prepared_live_context_releases_all_retained_handles[exception]`; the
Windows handle-closure assertion is under separate investigation. The 57 skips
are recorded individually in the full log and include POSIX-only behavior,
unavailable symlink privileges, and Bash wrapper coverage on this Windows host.
They remain cross-platform verification requirements, not waived coverage.

## Task status

| Tasks | Current evidence-backed status |
| --- | --- |
| 1â€“13 | Baseline repairs accepted and pushed through `95cc66e`; full deterministic Windows regression passed (1,660 tests, 57 skips), followed by focused test-strengthening gates. Final cross-platform and integrated acceptance remain. |
| 14 | Planner canonical reads accepted in `8199a4c`. Invocation/audit tests recovered and under review; prepared Git port/history repair and admitted-write cancellation are in progress. Commit, named check, validation and review evidence service tools remain unavailable. |
| 15â€“16 | Agent contracts, prompts, fake gateway, ADK runtime/gateway exist. Two Task 14 agent tests are in progress; the planned contract/runtime suites, context-security tests and opt-in live smoke coverage still need recovery. Implementation is not accepted without tests/reviews. |
| 17 | Planning service/handler exist. Plan approval/revision handlers and workflow tests are absent. `worker/main.py` defaults to an empty handler registry, so production planning dispatch is not wired. |
| 18 | Delivery, validation/review orchestration and remediation workflow remain to implement. |
| 19 | Dashboard projections, artifact API, resumable SSE and OpenAPI export remain to implement. |
| 20â€“22 | Web workspace, authentication shell, project/policy controls and run cockpit remain to implement. |
| 23â€“25 | GitHub read adapter and deterministic publication/monitoring/merge controllers remain to implement. |
| 26 | Evidence, approval, audit, usage and security dashboard views remain to implement. |
| 27 | Versioned deterministic evaluation harness remains to implement. |
| 28 | Foundational recovery/lifecycle code exists; full workflow reconciliation and security hardening remain to implement. |
| 29 | Process/UI acceptance, cross-platform CI, development launcher and final operator documentation remain to implement. |

## Gates and next action

Use the installed multi-provider framework: Gemini first for routine changes,
confirmed quota fallback only; independent Opus 5 review; separate Sol-high
correctness gate for high-risk changes; fresh verifier command evidence. Keep
provider contracts and logs under `.llm-output/` and publish concise evidence
summaries here without credentials or raw provider output.

Current work is Task 14 audit-test strengthening, prepared Git history proof,
and admitted-write cancellation. Baseline checkpoints `365deaa`, `35da627`,
`95cc66e` and Planner checkpoint `8199a4c` are accepted and preserved remotely.
Detailed evidence and outstanding work follow below.

## Checkpoint history

Current slice: Gemini completed approval fixture repairs and two focused
intervention guards: reject resolution that discards its own suspension context,
and require renewed monitoring/approval rather than entering MERGING after an
intervention discarded merge-gate evidence. Contract:
`.llm-output/baseline-repair.json`. Provider run:
`.llm-output/agent-framework/05bbb890-7f65-4f1d-b195-457e919487be`.
The provider reports 606 application tests and 19 schema tests passing; fresh
independent verification is underway. Saved four-file diff SHA-256:
`7ec9e72bd5d01f9efd082dd5c1c4c4f9779f3d991c39264270e07ac4f320f8b2`.
Per-file identities are in `.llm-output/approval-repair-candidate.json`.

Opus 5 review attempt returned provider error 429, session limit (reset reported
as 21:00 Europe/London), with no review performed. Evidence:
`.llm-output/agent-framework/1dc75842-a13d-48fe-82c5-dc1648691cdb`.
The framework-authorized fresh Astra-low review fallback found no material
findings and verified all candidate identities. The separate Sol-high gate
passed and independently ran 606 application tests, 19 PostgreSQL schema tests
and Ruff. Notes: `.llm-output/approval-fallback-review.md` and
`.llm-output/approval-correctness.md`. The fresh verifier recorded 625 focused
tests and 851 affected application/domain/persistence tests passing, with one
platform skip; Ruff and relevant mypy passed. Automatic file identities stayed
unchanged. Evidence: `.llm-output/approval-verification/manifest.json`.
These are bounded repair gates, not final whole-branch acceptance.

Windows path diagnosis passed the focused cases and full path suite (132 passed,
22 skipped), while instrumentation demonstrated raw HANDLE integer reuse. This
does not prove the original failure was harmless. Gemini is implementing a
bounded lifetime-aware test-instrumentation repair under
`.llm-output/path-test-repair.json`; no path runtime changes are authorized in
that slice. Its regression must still detect missing/double closes.

The first path-test repair passed a quality review but failed the separate
Sol-high gate: it recorded a successful release before the native close
succeeded. `.llm-output/path-correctness.md` documents a live-handle leak that
passed the matcher. A Terra-low complex implementer is repairing attempt versus
success tracking and the Windows native-close return check under
`.llm-output/path-correctness-repair-contract.md`. This slice is not accepted.

The next full run at integrated diff
`6fcd1bd69fbc947c2e45edda728f2e3ec69fbbef9378a939c834dfa020fd4ad9`
recorded 1,636 passed, 57 skipped, 10 failed; Ruff/mypy passed and file identities
were unchanged. Evidence: `.llm-output/baseline-final-verification/manifest.json`.
All documented approval/schema failures were resolved; the ten newly exposed
failures were six developer-lifecycle and four manifest tests under a longer
temporary root. A controlled Windows probe demonstrated native temporary-file
creation failing at 274 characters while an equivalent shorter temporary path
succeeded. Manifest staging unnecessarily repeats the entire final filename.
Gemini is repairing only the staging filename, retaining random UUID entropy,
full final identity and atomic/security rules, under
`.llm-output/manifest-temp-repair.json`. Do not dismiss these as passing merely
because a shorter test root avoids the bug.

The approval/intervention slice is accepted as a bounded verified checkpoint:
`365deaa717a459d761fffc0ed53a41211f9114c0`, pushed to `origin/forge/v0-1`.
Its commit contains this ledger.

The path-close repair is accepted in checkpoint
`35da627342df0e26b36c59fb8647ecf7fe37e13e`, pushed to `origin/forge/v0-1`.
Terra corrected the
native `CloseHandle` false-result handling and the test recorder separates
attempts from successful releases. Deterministic missing/failed/double-close
regressions retain actual cleanup evidence. Fresh Astra review and the separate
Sol-high gate found no remaining path-close defects. Evidence:
`.llm-output/windows-review-fresh.md`,
`.llm-output/windows-correctness-fresh.md`, and
`.llm-output/path-complex-repair/`. The Sol report fails the combined candidate
for separate manifest findings; it explicitly passes the handle-close repair.

Fresh full verification at `365deaa` plus the recorded candidate passed 1,649
tests with 57 platform skips in 327.24 seconds. Ruff and strict mypy passed;
automatic before/after HEAD, tracked diff and file identities matched.
Evidence: `.llm-output/windows-full-verification/manifest.json` (diff SHA-256
`2ddf214dcc4010e237c704bed40844c50cb0118948b92e080bab3d37fef40381`).
This resolves the observed baseline failures, but does not override independent
review findings or establish final acceptance.
The verifier's aggregate verdict remained FAIL because `git diff --check`
also found an extra EOF blank line in the unaccepted manifest test; the current
manifest owner will remove it. The accepted path files had no hygiene finding.

Manifest publication remains unaccepted. Independent Astra review reproduced a
test-fixture defect: a 150-character pytest base makes the intended supported
final path 265 characters. The separate Sol-high gate also proved a preexisting
concurrent-create race: two callers passed absence checks, both reported success,
and `os.replace` overwrote the first manifest with a different immutable identity.
The exact deterministic probe is in `.llm-output/windows-correctness-fresh.md`.
Terra is repairing exclusive publication and the fixture under
`.llm-output/manifest-publication-complex-contract.md`. The routine fixture-only
follow-up contract was not dispatched because this requires a concurrency repair.

A fresh read-only Tasks 1â€“13 source/test map is saved in
`.llm-output/early-task-acceptance-map.md`. Each has corresponding implementation
and focused coverage; historical Task 13 E2b incompleteness is superseded by
current lifecycle orchestration. Concrete production worker adapter registration
remains workflow integration work. Windows platform skips remain required
cross-platform evidence, not waived behavior.

Terra completed exclusive creation, exact winning-caller coverage, a private
OS-temp native-limit fixture, and post-publication cleanup-failure coverage.
The first Terra worker stopped responding; its partial edits/logs were preserved
and a fresh Terra worker completed the bounded checks. Details:
`.llm-output/manifest-worker-handoff.md`. Failed staging unlink now has explicit
coverage: creation returns a redacted error, exact payload remains with two
links, and loading succeeds after the retained staging link is removed.

The next independent verifier passed 1,653 tests with 57 skips in 323.26 seconds,
Ruff, mypy and diff checks. Before/after identities matched at `35da627` plus
tracked diff `b5ad8416737a1c9659677bc24273458b29144fac6392d1292aafb008080c77be`.
Evidence: `.llm-output/manifest-final-verification/manifest.json`.
The separate fresh Sol-high gate accepted the concurrent-create/fixture fixes
but found a further preexisting POSIX defect: `_publish` ignores a short
`os.write` return count and can report success with incomplete JSON. Its probe
published 1,086 of 2,172 bytes. Evidence:
`.llm-output/manifest-final-correctness.md`. Gemini is repairing this bounded
write loop and adding short-write/zero-progress coverage under
`.llm-output/manifest-short-write-repair.json`. Manifest acceptance remains
blocked until this repair passes current review and verification.

Opus 5 review completed after the reported reset, on saved two-file candidate
`.llm-output/manifest-repaired-candidate.json`. Run:
`.llm-output/agent-framework/607dad1c-c4b5-45ff-a057-24119a575e2e`.
It confirmed the no-clobber and fixture corrections and found a MEDIUM Windows
cleanup/recovery gap: closing the staging handle before by-name deletion permits
transient sharing failures, leaving an unreadable two-link manifest. Parent
accepted retained-handle cleanup with bounded retries, and rejected the suggested
orphan sweep because a two-link staging file can still be an active publication.
Assessment: `.llm-output/manifest-opus-assessment.md`. Two LOW observations remain
fail-closed transient reader behavior and preexisting POSIX by-name publication;
single-link/security checks must not be weakened.

Gemini short-write repair completed in run
`.llm-output/agent-framework/1f3d8864-fa77-4bab-8d50-7346029e90a2`.
Four focused short-write/error tests passed after observed RED; the affected
manifest/developer suites passed 21 tests with three platform skips, with Ruff
and mypy clean. Raw logs: `.llm-output/manifest-short-write-repair/`.
Terra completed the retained Windows staging-handle cleanup under
`.llm-output/manifest-handle-cleanup-contract.md`. It retains the handle through
exclusive publication, retries native disposition and close within bounds, and
reacquires/verifies the exact staging identity after failed save replacement.
Focused manifest/developer tests passed 24 with three platform skips; focused
secret cleanup tests passed five with four skips; Ruff/mypy/diff checks passed.
The worker did not capture a complete full secret-store run, so it is not claimed.
Evidence: `.llm-output/manifest-handle-cleanup/red-green.md`.
A fresh review and full verification of the combined repaired candidate are
required; the earlier candidate is historical evidence.

Additional Task 14 inspection found no committed tests referencing
`ControlledToolService`; current tool tests cover the authorization matrix and
argument boundary only. Recover direct invocation/audit and durable repository
write tests before accepting Task 14. Read-only design work for durable commit
is saved in `.llm-output/task14-commit-design.md`.

The combined manifest production candidate passed the fresh Sol-high gate:
`.llm-output/manifest-combined-correctness.md`. Full verification passed 1,660
tests with 57 platform skips in 317.55 seconds, Ruff, mypy and diff checks;
before/after identities matched. Evidence:
`.llm-output/manifest-combined-verification/manifest.json`, tracked diff
`87bc1caf45e09830717b8834af677679b03ab6ed9a63fdf4dbabe4065d4317f1`.
Opus review run `.llm-output/agent-framework/038ad97b-6b6a-48c0-bc52-2d22688af41a`
accepted production criteria but found a HIGH test gap: failed-save setup raises
before reaching rename/cleanup, so its assertions were vacuous. It also required
foreign-staging-identity coverage. The originating Terra worker is fixing only
the test module under `.llm-output/manifest-test-gap-contract.md`. Production
is frozen. The permanent native-cleanup failure state and manual recovery are
now documented in `docs/recovery-environment.md`; ambiguous entries are never
automatically swept. Current tests must pass independent review before acceptance.

The final manifest checkpoint is accepted. Opus 5 run
`.llm-output/agent-framework/493ea2f8-b981-4676-9ac7-86567ae038d6` accepted the
bounded repair and recovery documentation with no blocking findings. Residual
test-strength observations are nonblocking; the separate fresh Sol-high gate
directly checked handle balance and identity-guard behavior and passed:
`.llm-output/manifest-checkpoint-correctness.md`.

Fresh verification passed 64 tests with 18 platform skips, Ruff, mypy (three
source files), and diff checks. Before/after HEAD was `35da627`; tracked diff
SHA256 was `8d5a7c740d76c5eecb38733bd849941700d66acdd9d640a1dbdbe39fba81f693`.
All four candidate file hashes matched. Evidence:
`.llm-output/manifest-checkpoint-verification/manifest.json`.
Production SHA256 remained
`3f24a1959e8e926651f1ff26bd95180fadb73352307b09070b7e219b6a1de898`:
the 1,660-pass full run above validates this production code, while the final
focused run validates the strengthened tests and documentation candidate.
The final test follow-up retained a static diagnosis of the old vacuous fixture,
not a raw observed RED run; it must not be represented as such. Only this ledger
acceptance summary was added after the final frozen-candidate gates.

Checkpoint `95cc66e` was pushed to `origin/forge/v0-1`. Task 14 invocation/audit
coverage is now assigned to Gemini under `.llm-output/task14-audit-recovery.json`;
pre-provisioning Planner reads are assigned to Terra under
`.llm-output/task14-planner-read-contract.md`, with non-overlapping ownership.
Both slices require independent review and fresh verification before acceptance.
The first two Gemini starts returned `setup_error`, shared invocation lock busy
(runs `159b02c7-7d89-48cc-aabf-8ec6304eeede` and
`f35bbde8-a32b-443f-9716-4d2935e0a4b4`). No writer started and no quota fallback is
authorized by this error. The test assignment waits for a bounded later retry;
the Terra Planner slice continues independently.

Planner-read candidate is frozen in `.llm-output/task14-planner-read-candidate.json`
and `.diff`, based on `95cc66e`, scoped diff SHA256
`3f8a1aeb6421b1c77bc4f59180467972a0d347c49477a8aceede83e6a326808d`.
Author checks passed 131 tests with one skip, Ruff and strict mypy. Independent
Opus review and fresh scoped verification are running; a separate Sol-high gate
is required before acceptance. Existing managed-worktree Planner reads have an
explicit compatibility regression. Audit coverage remains separately queued.

The independent Git port slice is being implemented under
`.llm-output/task14-git-prepared-contract.md`: preparation stages only after the
future service admits a durable intent; publication verifies the exact prepared
HEAD/index tree and does not restage. Service wiring is not yet implemented.
The invocation-identity design is under separate correctness review; current Git
state alone cannot distinguish a retry from a new same-message invocation.

Identity design review completed conditionally:
`.llm-output/task14-identity-design-gate.md`. A versioned Forge UUID scoped to
trusted execution and bounded SDK correlation is viable, but admission must bind
a pre-redaction argument digest, lookup durable calls before volatile Git inputs,
and settle orphaned RUNNING tool calls. Current ephemeral ADK sessions do not
support process-crash replay of provider call IDs; persisted Forge calls/intents
remain recovery authority. These are implementation requirements, not completed
behavior. The admitted-write cancellation repair contract is queued in
`.llm-output/task14-write-cancellation-contract.md`.

Git port author verification passed 103 tests with five platform skips, Ruff and
mypy. Frozen candidate `.llm-output/task14-git-prepared-candidate.json` has scoped
diff SHA256 `d204e3c6fa85e0b75e335620d0af6919e7335732929e32bb572fdca286399b84`.
Fresh verification and Sol-high correctness review are running; Opus review is
queued after the Planner review. This is not an accepted checkpoint yet. The
third Gemini audit assignment attempt (run
`d49f0aed-de93-422d-8fd6-75c8cc9522ef`) is active after the earlier lock failures.

Planner Opus review `444b16a7-7a64-4aa4-bbf2-710cbe16db71` accepts source logic but
rejects acceptance evidence: distinct canonical/worktree paths and non-Planner
no-worktree denials are missing. Parent also requires canonical policy-secret
exclusion proof, explicit PLANNING state, and event resource identity. Repair
contract: `.llm-output/task14-planner-repair-contract.md`. Earlier scoped
verification (131 passed, one skip) remains historical for that superseded
candidate. No Planner checkpoint has been accepted.

Git fresh verifier passed 103 tests with five skips, Ruff, mypy and scoped diff
checks; candidate hashes remained stable. Evidence:
`.llm-output/task14-git-verification/report.json`. Opus Git review attempt
`70c642a0-5099-413d-aa88-297526107645` returned session-limit error (reported reset
02:00 Europe/London), no review. A fresh Astra-low fallback review is running,
alongside the independent Sol-high gate. No Git checkpoint is accepted yet.

Astra Git review rejected the candidate with P1: replacement refs can substitute
commit contents during exact-object proof. Report:
`.llm-output/task14-git-review-fallback.md`. Original Terra is repairing this
under `.llm-output/task14-git-replacement-repair.md`; prior Git verification is
historical. The old Planner Sol gate was interrupted after candidate rejection,
not accepted; it also identified the overly broad AWAITING_PLAN_APPROVAL state.
Fresh Sol gates and verifier evidence are required for both repaired candidates.

Final Planner test-only repair passed fresh Astra review without material
findings: `.llm-output/task14-planner-final-review.md`. Frozen five-file candidate
is `.llm-output/task14-planner-final-candidate.json`, scoped diff
`8045b19dfb5e65cc93160c7723035b82dd14545b5bdabba7e1fc4bc82671a2ed`.
Production hashes remained unchanged while secret-canary and exact-denial tests
were strengthened. Fresh Sol gate and verification remain required.

Gemini audit recovery completed with 23 tests passing at its validation time;
later canonical-reader binding changes require a test-wrapper integration update.
That update is running under `.llm-output/task14-audit-integration.json`; old
23-pass evidence must not be presented as verification of the integrated state.
Git replacement and truncated-ancestry repairs passed author checks (105 passed,
five skipped), with fresh review/gate underway on
`.llm-output/task14-git-repaired-candidate.json`, scoped diff
`3dfda63ee4ac5ba2c35e5a93557283fc99531ed7480c5d1454cfd2bb76b389c9`.

Final Planner fresh verification passed 187 tests with one platform skip, Ruff,
strict mypy for four source files, and scoped diff checks. Before/after HEAD and
all five candidate hashes matched. Evidence:
`.llm-output/task14-planner-final-verification/verification.md`. Separate Sol
verdict remains pending; no checkpoint accepted yet.

Gemini integration run `245ba9c6-538f-4715-8a5c-3d07abfe3c23` completed: 23 owned
tests and 102 authorization/security tests passed, Ruff clean. Audit tests are
frozen under `.llm-output/task14-audit-final-candidate.json`, scoped diff
`f57cc8227b1a583e1c74749f2314beecd9b808d8e5b259aac1cb80b313c67477`, and independent
review is running. Cross-context helper checks must not be mislabeled as full
service integration. Write cancellation remains explicitly deferred.

Git shallow/graft history masking is an additional unresolved review finding;
`.llm-output/task14-git-repaired-review.md` preserves the rejected candidate
review. The writer is adding complete-history guards and real graph regressions.
The Sol gate rejected that snapshot and will resume on refreshed evidence.

Planner canonical-read slice ACCEPTED: fresh Sol-high gate
`.llm-output/task14-planner-repaired-gate.md` passed final candidate, verified all
five hashes, and ran 34 Planner tests, 102 authorization/security tests, three
repository-exclusion tests and a parent-exclusion probe. Combined with fresh
Astra acceptance and the 187-pass/one-skip verifier above, this supports the
bounded checkpoint. Production composition and final full/cross-platform checks
remain later acceptance work. Only ledger summaries were added after the gates;
the five source/test candidate files stayed unchanged. Task 14 remains open.

Planner checkpoint `8199a4c` was pushed to `origin/forge/v0-1`. The separate
cancellation slice is now assigned under
`.llm-output/task14-write-cancellation-contract.md`, owning only the service and
a new cancellation test module. Gemini owns audit-test overlap/redaction repairs;
Terra owns Git history guards. These scopes are not yet accepted and must pass
their own current review and verification before checkpointing.

Next: integrate these Task 14 slices, then implement durable commit/check tools
and evidence readers. No numbered task or final acceptance gate has yet been
marked complete.

Continuation update: Gemini audit review repair
`d4e15a38-2cc4-4187-9896-3a3e26b0fe36` completed with27 owned tests and102
security/authorization tests passing. It adds deterministic overlapping admission
and stored digest-only evidence checks. Parent found trailing-newline canary
searches vacuous against JSON serialization; a test-only followup is active under
`.llm-output/task14-audit-canary-repair.json`. No audit acceptance yet.

Cancellation initial author candidate passed30 related tests, but parent requires
missing original-contract cases (lease renewal while blocked, genuinely repeated
cancellation, pre-admission cancellation, artifact/audit failure and bounded test
cleanup). The same writer is completing those before independent review.

The previous Git writer was interrupted after no progress response; it confirmed
no pending tool session and no shallow/graft changes. A fresh Terra writer owns
the exact Git/test repair under `.llm-output/task14-git-history-repair/`. Earlier
Git105-pass evidence remains superseded, with no accepted Git checkpoint.

The evidence-reader design now specifies immutable execution input bindings and
causal prior-review selection; it remains future design, not implementation.
A separately owned invocation-correlation foundation is active under
`.llm-output/task14-identity-foundation-contract.md`. Durable admission and crash
settlement are still subsequent work; Task14 remains open.

Latest audit provider `0ce387ad-6afe-4200-ba66-2f1d3fbf7a7c` completed27 owned
tests and102 authorization/security tests, Ruff clean. Bare ASCII canary positive
controls now prove JSON leak assertions detect plaintext. The cancellation writer
subsequently made an authorized bounded cleanup-join change to the persistence
audit module, so final joint verification must use its updated hash.

Git history repair observed2 focused RED failures, then2 GREEN; full Git suite
passed107 tests with5 platform skips in96.59s. Independent gates remain pending.
Evidence `.llm-output/task14-git-history-repair/`.

Cancellation source remains unaccepted. Original writer stopped with partial test
repairs and no running command. A fresh Sol-low adversarial test specialist owns
final test repair under `.llm-output/task14-cancellation-test-finish/`: real
cancellation inside admission, successful post-cancellation lease renewal,
bounded blocked-thread cleanup, and artifact/audit failure evidence. Passing
counts from the earlier weaker tests are historical, not final acceptance.

Frozen candidates now in independent fresh Astra review and separate Sol-high
correctness gate (Claude Opus5 remains unavailable until reported02:00 reset):
- Git3files: `.llm-output/task14-git-history-final-candidate.json`, diffSHA256
  `def905c52e33e1f3a5df734e07b43628a50585739ab95eaeb233619e7f03bc34`.
- Write/audit4files: `.llm-output/task14-write-audit-reviewed-candidate.json`, diffSHA256
  `d5001ab4e45940a13288b0662a791522eef9736b868eea1787b146d4105707ab`.
Both use HEAD8199a4cfe8abdda93362fd1695f5013015a154c0 and exclude active ADK
foundation changes. Final cancellation test specialist ran7 focused/54 affected
tests and Ruff successfully, with production source unchanged. No production RED
was claimed for that test-strength pass; logs are in
`.llm-output/task14-cancellation-test-finish/`. Fresh verifier remains required
after review; no new accepted checkpoint yet.

FreshAstra review `.llm-output/task14-git-write-review.md` accepts the frozenGit
candidate with no materialfindings, but rejects write/audit with P2 postcommit
handoffgap: cancellation in admissionUoW cleanup can precede completion-task
creation. Parent acceptedfinding and dispatched exact two-file repair under
`.llm-output/task14-postcommit-handoff/`. The prior write/audit candidate and54-pass
evidence are superseded for sourceacceptance; new review/gate/verifier required.
Git candidate stays frozen pendingSolgate and freshverifier.

SeparateSol-high gate rejects BOTH frozen candidates. In addition to the known
write postcommit handoffgap, Git guard currently protects only inspection:
prepare/publish still trust merge-base under common graft metadata. An unrelated
retained HEAD grafted to authorizedbase can forge ancestry and permit mutations.
Requires guards for all ancestry-dependent prepare/publish proofs underretained
binding, plus real unrelatedHEAD/graft and shallow tests. Parentacceptedfinding;
Git sourcecandidate is superseded and NOTaccepted despite Astra scopedacceptance
and107passingtests. No checkpoint untilfreshrepair/review/gate/verifier.

Git ancestry repair refreshedcandidate:
`.llm-output/task14-git-ancestry-final-candidate.json`, scoped diffSHA256
`93c1b91fc6974a93b5c9d032722702811c70bf51c4c35a5449056aa7f17e1567` atHEAD8199a4c.
Real prepare and publish grafted-unrelatedHEAD cases failed beforefix, then full
Git111passed5skipped; sourceRuff/mypy passed. The priorSolgate is reviewing this
exact repairedGitcandidate. FreshAstrareview/verifier stillrequired. Writer's
extraGeminiattempt f740697b-1e59-4e55-84c1-d47557e60b62 returnedserverbusyERROR,
notquota; assignedTerra finished, noLunafallback. No pendingGitprovider session.

Invocationfoundation originalTerra stopped with partialsource and no livecommand.
Its unloggedchecks are notacceptance evidence. FreshSol test_engineer owns two
agenttestmodules to expose mixed-invalidID batch/safeproperty-access gaps and
record actualofflineRunner proof; sourcefix follows afterthatRED contract.

Gemini evidencecodec foundation active under `.llm-output/task14-evidence-codec.json`,
ownednewdomain/evidence.py andtests/domain/test_evidence.py only. It implements
shared immutablemanifest/codec, noDB/serviceauthority. Task14 remainsopen.

### Task 14 continuation after prepared Git checkpoint

Prepared Git checkpoint `77784c641a049a9bd79e4348a643a2099e7e5e48` was pushed to
`origin/forge/v0-1`; only its three Git files and this ledger were committed.

The next cancellation repair has a preserved real PostgreSQL RED regression:
cancel after the database commit succeeds but before its coroutine returns.
The revised owned task spans commit settlement and write completion inside the
admission context. Author evidence currently shows ten cancellation tests and
37 audit/persistence/cancellation tests passing. This remains unaccepted until
fresh independent review, Sol-high review and command verification finish.
Raw logs: `.llm-output/task14-commit-settlement-repair/`.

Gemini codec run `ba6d3f74-a7ce-4b78-8cf2-c85f1f0c319a` completed, but independent
Astra review rejected candidate diff `276423d7f85caff26760d2515a9bc93c6993d8a06bb8c029b415c3c8401c7072`.
The encoder did not revalidate modified model instances, nested copied review
collections could remain mutable, several malformed-input errors escaped the
safe codec boundary, and the golden test did not pin bytes or a digest.
A bounded Terra repair now owns the two codec files. Review evidence is saved
in `.llm-output/task14-codec-review.md`; passing author tests are not acceptance.

Invocation identity remains in progress. A test specialist is establishing
actual installed ADK event ordering and malformed-batch behavior using an
offline model. Gemini separately owns optional immutable request/resource
bindings in tool-call persistence; it cannot edit the active service repair.
These foundations do not yet provide durable invocation replay or orphan-call
recovery, and Task 14 remains open.

Codec repair handoff: the first Terra repair attempt ended after inspection with
no edits or live command. Its four findings remain unresolved; the unchanged
candidate is not accepted. Concrete next steps are preserved in
`.llm-output/task14-codec-repair-handoff.md`.

The cancellation candidate is now frozen at HEAD `77784c6`, scoped diff SHA256
`26ef1a1590b0cda1920b9ba2777fc8ba3be8bd4f945a869471c3de9579bf33c7`.
After the writer stopped, primary integration limited the cleanup test barrier
to the first admission UoW. The focused ten tests and Ruff passed again.
Fresh independent review and a separate Sol-high gate are running; a verifier
has not yet accepted this candidate. Candidate and hashes are recorded in
`.llm-output/task14-write-settlement-candidate.json`.

Integrated write/binding candidate (seven files) was rejected by independent
review for a strict-type mismatch: metadata schema `true` or `1.0` compared equal
to integer `1`, permitting an accepted row that the strict decoder could not
read. The fresh verifier recorded matching identities but correctly did not run
acceptance commands for this rejected candidate. A bounded Terra repair owns the
three binding files; cancellation files remain frozen. Evidence:
`.llm-output/task14-write-binding-review.md` and
`.llm-output/task14-write-binding-verification/report.md`.

The identity source candidate now uses the public ADK after-model callback to
reject missing provider IDs before SDK normalization, in addition to finalized
event validation. The actual offline tests pass: 18 focused, 120 affected;
Ruff and source mypy pass in author evidence. Primary corrected an unsupported
comment after the writer stopped. The final five-file candidate is
`.llm-output/task14-identity-final-candidate.json`, diff SHA256
`2c9aef559ad0e59b02ec8807dc2d0acc67ab2fb21b82ce603289f0e5b9618f5e`.
Fresh independent review is running. It is not yet accepted and does not switch
service admission to invocation IDs.

Latest frozen candidates on HEAD77784c6:
- Write/binding: `task14-write-binding-repaired-candidate.json`, scoped diff
  `9d12f53e8190c45bfe806aee9665c84aa24c97aba04c69fba7c36d27cd188342`.
  Fresh independent review ACCEPT (part A of
  `.llm-output/task14-repaired-slices-review.md`). Fresh verifier has passed
  206 selected tests; remaining checks/final identities and Sol gate pending.
- Identity: independent review ACCEPT in `.llm-output/task14-identity-review.md`;
  Sol correctness gate running, fresh verifier still required.
- Codec: Gemini retry `96591507-2a99-45dc-9a07-41a0bdb63978` completed with
  8 preserved RED failures, 32 focused and 100 domain tests passing. Fresh
  independent review still REJECTS two P2s: incomplete constructed models leak
  AttributeError, and manual nested rebuilding silently drops unknown fields.
  No codec writer is active. Exact repaired-but-rejected candidate is
  `.llm-output/task14-codec-repaired-candidate.json`, diff
  `852419ee9423c537c7a0375a2dfa3d56cba17ebba23a7cdd1493ad5dcfa51efb`.
  Next repair contract: `.llm-output/task14-codec-final-repair-contract.md`.

No new checkpoint has been committed since77784c6. None of these passing
subsets replaces final whole-branch, cross-platform or Task29 acceptance.


### Accepted write cancellation and immutable binding checkpoint

Candidate HEAD77784c6, seven-file diff SHA256
`9d12f53e8190c45bfe806aee9665c84aa24c97aba04c69fba7c36d27cd188342`:
independent Astra review ACCEPT (part A, `task14-repaired-slices-review.md`),
separate Sol-high gate PASS (`task14-write-settlement-gate/final-report.md`),
and fresh verifier PASS (`task14-write-binding-final-verification/`).
The verifier ran206 selected tests, Ruff on seven files, strict mypy on three
sources, and scoped diff checks. HEAD, scoped diff, seven file hashes and the
recorded domain/tool.py dependency hash stayed unchanged. This checkpoint adds
optional persistence fields; it does not switch service admission to SDK IDs.

The gate's real PostgreSQL expired-commit-lease probe confirmed correct fencing:
zero writes, operation NEEDS_RECONCILIATION with owner/lease cleared, linked
RUNNING tool call, no artifacts/completed events, both callers returned the
stable ToolInvocationError, and no deadlock. Uncertainty is retained explicitly;
this is not a promise of a successful effect after lease authority expires.
Later orphan-call/reconciliation integration remains required for Task14.
Process death and ambiguous network failure during commit were not injected.
Probe source/output are preserved beside the gate report.

Only the seven accepted files plus the ledger belong to this checkpoint.
Identity and codec candidates remain separately unaccepted. No whole-branch or
Task14 completion is claimed.

### Accepted identity and codec checkpoints

Identity checkpoint `a55ed19845896992cc73d341ad88132eec98a72e` is pushed.
Five-file candidate diff2c9aef559ad0e59b02ec8807dc2d0acc67ab2fb21b82ce603289f0e5b9618f5e
passed fresh Astra review (`task14-identity-review.md`), separate Sol-high gate
(`task14-identity-correctness.md`) and fresh verifier163 passed/one host file-link
skip, Ruff and strict mypy (`task14-identity-final-verification/`). The SDK2.5
raw-call validation occurs before ID synthesis, context injection excludes model
authority, duplicate/malformed batches cannot execute effects, and cleanup is
covered offline. Live provider ID compatibility remains opt-in/unverified.

Codec checkpoint `ce1374adc3a867423a5c926491ace1d09af77ac6` is pushed. Two-file
candidate diff2456d255be0fcd525b85fdfb7292cd3a9657d4f095eee44c805bc1b8e8ebdb9c
passed fresh Astra (`task14-codec-fold-review.md`), separate Sol-high
(`task14-codec-correctness.md`) and fresh verifier105 domain tests/zero skips,
Ruff and configured-strict mypy (`task14-codec-fold-verification/`). Codec
content hashes stayed fixed while the unrelated identity checkpoint advanced
HEAD. The final provider call ended with quota error after partial edits and raw
RED capture; fresh verification supplies GREEN evidence, not provider success.
No Luna fallback or installed framework/cache modification occurred.

Durable write service integration is active. Evidence persistence and Git effect
plans are staged separately. These checkpoints do not complete Task14 or the
whole branch; immutable producer/reader wiring and recovery still remain.

### Task14 completion review and current evidence (2026-09-07)

The integrated completion candidate remains uncommitted on `forge/v0-1` at
`94689ec`; Task14 is not yet accepted. Full Python verification of the frozen
pre-repair candidate passed **2016 tests, 57 skips**, with Ruff and strict mypy
(144 source files) passing. A later receipt type-confusion regression was
reproduced and repaired; **83 affected tests passed** against identical before/
after candidate manifests, with Ruff/mypy passing again. The full result is
historical evidence for the pre-repair candidate, not a fresh final acceptance.

One independent **Claude Opus5, medium** integrated review completed. Findings
T14-C01 (historical replay admission semantics), T14-C02 (registered redactor in
recovery), and T14-C03 (duplicate polling rate) are tracked in the completion
run's `findings.md`, with original review in `review.md`. C02's focused regression
is green; C03 repair is underway. C01's proposed live-state gate conflicts with
explicit exact-invocation historical replay after cancellation and is under
contract-based disposition. Targeted finding recheck and the scoped Sol-high
correctness gate remain required. Small edits do not trigger additional whole-
batch reviews. No new commit, push, merge, or Task14 completion is claimed.

Evidence directory: `.llm-output/runs/task14-completion-20260907/` including
`integrated-before.json`, `integrated-after.json`, `integrated-full-tests.log`,
`review-candidate-before.json`, `review-candidate-after.json`,
`receipt-type-affected.log`, and `orphan-write/C02-green.log`.

### Verified formatting checkpoint and pending Task14 gate (2026-09-07)

Checkpoint `249862adc0104105f3ab0df01457d89535b6404b` is pushed to
`origin/forge/v0-1`. It contains only 17 previously accepted files whose pre-format
contents matched HEAD and whose staged Python ASTs remained identical to HEAD.
Risk is low: deterministic formatting. No additional agent review was required.

The complete formatting pass covered 27 files; the other 10 remain with the
unaccepted completion batch. All 27 before/after Python ASTs are identical.
Current checks pass: 232 files formatted, full Ruff, strict mypy (144 sources),
and `git diff --check`. Before/after check manifests match. Evidence is under
`.llm-output/runs/task14-completion-20260907/`: `format-validation.json`,
`format-baseline-paths.json`, `postformat-before.json`, `postformat-after.json`,
and `postformat-*.log`. Exact pre-format files are preserved in `preformat-source/`.

Fresh independent full-suite verification of the pre-format candidate passed
**2021 tests, 57 skipped, 6 warnings**, exit 0, in 453.66 seconds. The raw output
is retained in `final-verification/full-pytest.log`, with command results and
before/after identities alongside it. The `-q` output proves the skip count,
not individual skip reasons; the earlier generic live-provider/GitHub attribution
was corrected. Post-format checks and AST identity establish the formatting
change separately; the full-suite run is not mislabeled as a post-format run.

The single integrated Claude Opus5 medium review and targeted recheck are
complete: T14-C01, C02 and C03 are closed. The scoped Sol-high correctness gate
is still active. Task14 is not yet accepted, and no implementation checkpoint,
merge, or completion of Tasks15-29 is claimed by this formatting checkpoint.

The scoped correctness gate subsequently completed with **T14-G01 (Medium)**:
a named-check cancellation can arrive between preflight and concrete runner
ownership, leaving a definitely unlaunched call without terminal evidence.
The finding is accepted and a bounded launch-ownership repair with deterministic
PostgreSQL regressions is underway. The full-suite pass did not cover this
interleaving; Task14 remains unaccepted. Evidence and repair logs are in the
completion run's `correctness-gate.md`, `findings.md`, and `G01/`.


### Accepted Task14 completion batch (7 September 2026)

The completion checkpoint adds Developer Git commit and policy-named command
execution, Reviewer validation/prior-review evidence reads, and bounded orphan
settlement for proven write/Git/named effects. Trusted invocation identities,
immutable receipts and lineage, admission-before-effect, once-only terminal
audit, controlled worktree/environment boundaries, and read-only recovery are
covered by the new service, adapter and PostgreSQL suites. The Task14 acceptance
map is preserved locally in the completion run's `acceptance-map.md`.

Independent review evidence: one integrated Claude Opus5 medium review, then
targeted rechecks of the existing finding IDs. C01 is closed as intentional
same-identity historical replay without new effects or budget; C02 threads the
registered redactor through recovery; C03 bounds duplicate polling. The separate
Sol-high gate found G01; its repair adds explicit launch ownership so cancellation
before launch yields canonical no-effect evidence and cancellation after launch
cannot fabricate it. Both targeted Claude and Sol rechecks close G01 with no
material remaining defects. No full-batch review was restarted for these repairs.

Verification evidence under `.llm-output/runs/task14-completion-20260907/`:

- `final-verification/full-pytest.log`: 2021 passed, 57 skipped, exit 0, before G01.
- `task14-plan-gate.log`: 122 authorization/audit/capability tests passed.
- `G01/primary-affected.log`: 84 affected named/runner/recovery tests passed;
  `G01/primary-before.json` and `primary-after.json` match exactly.
- `G01/correctness-recheck.md`: independent seven-boundary-test pass and G01 closure.
- `G01/review.md`: targeted Claude closure against the exact 499-line repair diff.
- `G01/final-format-proof.json`: two final formatted files retain identical ASTs;
  current source Ruff and strict mypy pass. Formatting is low risk and used
  direct checks without another agent review.

Production composition in Tasks17-18/28 must use session-factory-backed operation
repositories for concurrent heartbeats and the same registered redactor for live
and recovered writes. Every terminal runner receiving launch ownership must
accept it before any effect; the shipped host/Docker runners comply. Missing or
ambiguous effect proof stays unresolved and never authorizes repetition. Later
worker/recovery scheduling, live providers/GitHub, cross-platform CI and the
complete delivery workflow remain outside this component checkpoint.


## Tasks15-16 accepted checkpoint — 7 September 2026

Source commit `fe1a237ffd109daed2e0c08da755f4aa347da11c` was pushed without
force and verified by exact `git ls-remote origin refs/heads/forge/v0-1`.
No PR merge, deployment, live provider/GitHub test or bootstrap teardown occurred.

The final affected command was `rtk proxy ./.venv/Scripts/python.exe -m pytest
apps/orchestrator/tests/agents apps/orchestrator/tests/application
apps/orchestrator/tests/security tests/integration/test_planning_failed_usage.py
-q -ra -m "not live_provider and not live_github"`. Full source/test Ruff check
and format check, plus strict mypy on `apps/orchestrator/src/forge`, also passed.
Exact commands, exit statuses and complete candidate/environment manifests are
in `.llm-output/runs/tasks15-16/gate-repair-checks-final/`.
Repair diff SHA256: `1d7736ef4e9f1f3994255ea2791ef9b183526b00be0152b507f881db08b88a62`.
The earlier 2261-test full run remains historical pre-repair evidence.

| Finding | Original failure | Repair and disposition |
| --- | --- | --- |
| A16-C01 | Pricing failure lost measured usage | Preserve unknown-priced aggregate/attempts; cached pricing preflight; Opus recheck closed |
| A16-C02 | Duration/provider timeout meaning unclear | Outcome contract clarified; Opus recheck closed |
| A16-C03 | Passing regression named as defect | Test wording corrected; Opus recheck closed |
| A16-G01 | Stream close retained owner indefinitely | Fixed close/drain bounds, owner/child cancellation distinction, repeated-cancel regression; Sol recheck closed |
| A16-G02 | Catalog replaced measured identity | Preserve counters, request/record/execution identity and timestamp; safe catalog metadata; Sol recheck closed |
| A16-G03 | Empty attempts bypassed credential checks | Aggregate validation in carrier and service; real PostgreSQL proof; Sol recheck closed |
| A16-G04 | Post-response prompt drift discarded usage | Typed repair failures preserve first billed attempt and reason; real PostgreSQL proof; Sol recheck closed |

Review metrics for this integrated batch: one Opus5-medium integrated review
(run `37125534-ef7c-4f73-8efb-903208ab7d99`), one C01-C03 targeted recheck
(run `1d66ce97-60e9-4434-a46d-ce267b014ba1`), one scoped Sol-high gate and one
G01-G04 repair recheck. Seven actionable findings closed; no whole-batch restart.
Opus review usage: input66, cache-create182505, cache-read3953788, output32732;
recheck: input60, cache-create81016, cache-read1620887, output20434. Native token
counts are unavailable. Provider usage JSON retains reported auxiliary CLI usage.
Supervisor recovery required interrupted C01 and G01 writer handoffs and the
stalled gate handoff; none triggered an extra integrated review. Finding-specific
rechecks were required by concrete pricing and correctness defects, not by small
edits or diff size. Current review evidence is separate from executed test proof.

Next action: implement Task17 under `.llm-output/runs/task17-core/contract.json`,
then wire production worker/settings through the Gemini-first route, integrate
real API-to-queue-to-worker planning/approval evidence, and run one integrated
review plus scoped high-risk gate. Task18 will consume prepare_worktree; Task17
must not simulate delivery effects. The existing v0.1 worktree remains authorized.


## Task17 development update — 7 September 2026

Task17 is one high-risk integrated batch, not yet accepted or reviewed. The first
core assignment produced only its missing-handler RED after about 18 minutes.
The primary interrupted it, recovered a stopped handoff, and resolved its concrete
task-digest design gap before narrowing the implementation contract.

Core slice A adds required immutable task_digest to approval evidence, freezes it
in planning, wires shared authoritative validation into default API approval,
and exposes a typed running-execution lookup. Its 24 targeted domain/auth/planning
usage tests and targeted static checks pass. The new PostgreSQL authorization
drift matrix was not delivered in that handoff; it remains required. Legacy
missing-digest evidence fails closed; no migration or synthetic task version.

Gemini-first composition run b08ac653-019f-46ef-b2b2-c245b13410a4 timed out after
900 seconds with partial edits. This was not quota exhaustion; no Luna fallback
ran. The primary preserved edits/logs, confirmed the exact local cascade backend
process and both listeners stopped, and recorded timeout-recovery.json under
.llm-output/runs/task17-composition/ before taking over integration. Interrupted
provider generation is not claimed completed.

Primary integration replaced raw session access with the execution port and
reproduced six missing request-binding guards before repairing them. Composition,
settings and startup checks passed 22 tests/5 SDK warnings (1.61s). Full static
checking awaits removal of temporary missing-handler scaffolding. Two real-PG
checks passed (3.77s): Planner read callbacks use persisted run/execution/step
identity, record tool evidence, and keep secret-file contents out of results.
The protected read retains the previously accepted adapter-error status; initial
test assumptions about that status and the step column were fixture corrections.
These are development results while another writer is active, not frozen proof.

Current core contract: .llm-output/runs/task17-core/handlers-contract.json covers
approval/revision handlers, semantic planning attempts, stale settlement and
narrow persistence support. The primary owns composition and
 tests/integration/test_worker_planning_e2e.py: four actual HTTP/PG scenarios
(exact approval, revision, pre-API base drift, post-API base drift) await those
handlers. The combined completed batch receives one integrated review and scoped
Sol-high gate; no Task17 review has run. No merge/live acceptance occurred.


## Task17 integration and Gemini recovery — 7 September 2026

Gemini retry `2f8da4e5-c104-4cb4-8c10-274b173ba451` completed successfully
through the configured Flash-medium route (exit 0, approximately 356 seconds).
It removed temporary composition class overrides and added real production
construction and invalid-price/settings checks. The primary independently ran
the three composition/settings/entrypoint test files: 31 passed, 5 SDK warnings
in 1.61s. Its handoff is in the provider run directory, not the requested task
directory. Provider test claims lack separate raw command logs; the primary's
executed check is the development evidence. No fallback or review was invoked.

The primary's real HTTP/PostgreSQL authorization matrix found two actual defects:
current project policy drift and persisted run base drift were accepted against
old evidence. The core writer repaired the shared validator; all six matrix
cases now pass (9.29s), including unchanged evidence, actual Git base change,
run-base change, incorrect frozen task digest, missing legacy task digest, and
current policy change. The task row remains append-only: an initial test fixture
attempted to mutate it and was correctly rejected by its database trigger.

Three additional actual API/queue/worker cases pass (8.38s): exact approval
queues one preparation command; pre-API base drift rejects authorization;
a forged worker command actor cannot consume or invalidate the legitimate gate.
The initial approval test's lowercase database command status was a fixture
error, corrected to the existing uppercase persisted contract. Revision and
post-authorization drift remain red and unaccepted. Their semantic admission,
feedback linkage and atomic stale settlement are still under implementation.

These results are development checks on changing inputs at HEAD 895f9bf, not
frozen acceptance. Exact commands, exit statuses and distinctions between real
defects and fixture errors are in
`.llm-output/runs/task17-integration/development-results.json`; raw logs are
adjacent. Task17 still requires completed revision/replay/concurrency evidence,
affected checks on a frozen candidate, one integrated Opus review, and the
scoped Sol-high correctness gate. No Task17 review has run. Tasks18-29 remain
open; the accepted source checkpoint remains fe1a237.


## Task17 revision integration — 7 September 2026

Task17 remains unaccepted; no integrated review has run. The primary took over
core integration after the writer's source handoff. Real workflow tests exposed
unsupported feedback bounding, identical plan/feedback provenance collisions,
missing challenge invalidation, and a semantic-attempt bypass of an ambiguous
RUNNING execution. These are repaired with focused failing/passing evidence in
`.llm-output/runs/task17-integration/`.

Revision and stale-base workflows now pass through a second model execution and
fresh approval gate. Plan evidence includes its semantic attempt. Identical
plan/context content retains its original provenance; each execution references
its input/output artifacts independently. Repeated identical feedback has a
command-bound immutable JSON envelope, verified against the originating completed
revision command and run/version/actor before becoming untrusted Planner input.
Old gate challenges expire without being consumed, and owned approvals become
invalid. A new semantic attempt cannot bypass an unsettled prior execution.

The primary's PostgreSQL repository tests also reproduced and repaired stale
returned base bindings and partial restart state remaining committable after an
event failure. Concurrent restart requests now have a test proving exactly one
new version. The focused repository and failed-usage tests pass 9 cases after
repairs. Full source mypy passed before the final fixture-only changes.

Gemini test-only run `542936df-b49e-4301-b1c6-9bee489a52c1` completed exit0 and
produced the three missing plan-named behavioral/security files. The primary
independently confirmed 61 tests passing in 0.83s. Unlike the first composition
handoff, this run includes actual command/exit/output evidence in
`.llm-output/runs/task17-tests/check_evidence.json`. No per-writer review occurred.

The first frozen affected candidate at HEAD76f6dc8 was unchanged before/after:
1541 passed, 2 failed, 2 host-link skips, 6 warnings, 295.28s. The failures were
an existing failed-usage harness overriding the old internal context signature;
the fixture now accepts the optional feedback and asserts none for that initial
planning scenario. Two earlier direct context tests were restored by separating
feedback persistence reads from repository context selection. Ruff/style issues
were also corrected. This failed broad run is retained under frozen-affected;
it is not passing acceptance evidence. A fresh combined run is required.

Next: freeze the integrated source and all relevant inputs, rerun affected
agent/application/security/persistence and planning PostgreSQL checks, then
perform the single Opus5-medium integrated review and scoped Sol-high gate.
No Task17 source checkpoint, PR merge, live provider/GitHub acceptance or bootstrap
runtime modification is claimed. Tasks18-29 remain open.

## Task17 integrated review and authority repairs — 7 September 2026

Task17 is still unaccepted. The frozen crash-recovery candidate at HEAD edb0db8
passed 1546 affected agent/application/security/persistence and real PostgreSQL
workflow tests, with 2 host-link skips and 6 dependency warnings (256.14s pytest,
259.07s command wall time). Full-source/test Ruff and format checks and source
mypy passed. Complete before/after identity was unchanged. Evidence:
`.llm-output/runs/task17-integration/frozen-crash-repair/`. This is the reviewed
candidate, before the following authority repairs, not final acceptance.

One integrated review was attempted with Opus5 medium. Run
`a4660db3-9a78-4872-80be-17b663618bd8` hit its session limit before any review
(exit1, 429, zero API tokens). The disclosed, authorized fresh Astra-low fallback
reviewed the complete batch and found two high-priority defects:

- A17-C01: stripping queued revision feedback bypasses source lineage, including
  after failed crash recovery. The primary reproduced an unauthorized second
  Planner call in PostgreSQL and is binding every restart to its immutable
  event, exact queued command and completed source.
- A17-C02: internally consistent evidence can name a plan that the latest
  completed execution did not produce. A bounded Terra-low repair is adding
  execution output and evidence producer/parent validation with adversarial
  PostgreSQL coverage, retaining legitimate identical-content reuse.

Both findings remain open until a finding-specific independent recheck; the
separate scoped Sol-high correctness gate follows repairs. Findings, repair
ownership, original candidate identity and focused evidence are recorded under
`.llm-output/runs/task17-review/`. No per-writer or repeated whole-branch review
was launched. Risk remains high because authority and crash recovery are coupled.
Tasks18-29 remain open and accepted implementation remains fe1a237.

## Task17 tested implementation checkpoint — 7 September 2026

Pushed implementation checkpoint `451e39b121b7dcf8fb81a9243767ceff12034abd`
preserves the complete Task17 planning, approval/revision and production worker
composition batch. It is **not yet Task17 acceptance**: the separate Sol-high
correctness gate has not returned its final disposition. The commit explicitly
records this limitation. No PR merge or live-provider/GitHub acceptance occurred.

The one finding-specific Astra-low recheck closed A17-C01 and A17-C02, with no
material repair-caused regressions. C01 now binds every semantic restart to the
immutable causal event, exact queued ID/payload/actor/version, and source command;
crash replay requires the exact leased revision source and model admission its
completed source. C02 resolves the exact successful planning execution, requires
the gate artifact's exact execution producer and plan parent, and verifies the
plan artifact against the execution output FK. Identical plan bytes remain
reusable without rewriting their original producer.

Focused PostgreSQL checks passed 11 restart cases and 3 independently isolated
provenance cases after observed RED failures. The first frozen repaired suite
passed 1555 tests, then API/settings/approval-model coverage exposed two older
fixtures that expected fabricated evidence to be accepted. The route-only tests
now explicitly inject their authorization-service boundary. The PostgreSQL
default-app test verifies incomplete plan evidence is rejected with no approval,
queue entry, state change or challenge consumption; the actual authenticated
Planner-to-approved-workflow integration retains success and replay coverage.
An async fixture also closes its engine on the owning event loop. These are
test-only changes, checked directly without another integrated review.

Final frozen verification at HEAD `f6dd2df38fb184e6186e53a16057d4dba6d169f0`
passed **1616 tests, 2 host-link skips, 6 dependency warnings (266.83s)**. The
pytest command took 270.08s; full-source/test Ruff and formatting and strict
mypy (149 sources) passed. Before/after complete identities were identical.
The tested staged tree `0f9cad50c9717beee6c125e5a992bca917fb5088` exactly matches
the pushed implementation commit's tree; this maps the candidate to the commit,
not a claim that tests executed on the later HEAD. Commands, raw results and
complete input manifests are in
`.llm-output/runs/task17-integration/frozen-final-candidate/`.

The test selection includes API, approval model and settings tests plus all
agent/application/security/persistence tests and four planning PostgreSQL files.
Every invocation excludes `live_provider` and `live_github`. Skips remain the
host's unavailable symlink/file-link creation. Earlier 1555/1546 results and the
59-pass/2-failure API supplement are retained as historical development evidence.

The original Opus5 request failed before review due to its session limit; one
native Astra integrated review and one finding-specific recheck actually ran.
Two recheck start requests hit the host thread limit before a completed writer
released capacity. A single separate Sol-high correctness gate was launched;
the primary requested a bounded final handoff after its inspection. Do not infer
PASS from silence or restart broad reviews. The next action is to collect that
disposition, repair any concrete findings by ID with affected verification, and
only then accept Task17 and activate Task18. Task18 interfaces and dependency
outline are prepared read-only under `.llm-output/runs/task18-map/`; Tasks18-29
remain open. Bootstrap runtime and unrelated work are preserved.

## Task17 correctness findings and repairs — 7 September 2026

The scoped Sol-high gate returned two high-priority findings. Task17 remains
unaccepted; checkpoint451e39b and its1616-test result are preserved but do not
prove these newly identified recovery cases. The previous goal turn made
implementation and verification progress; the subsequent wait used the live
`task17_correctness` agent handle rather than inferring a verdict from silence.

- A17-G01: after stale approval invalidation/restart commits, a crash before queue
  acknowledgement leaves the reclaimed approval command unable to replay. It
  fails terminally and prevents the dependent planning command from starting.
  The primary reproduced this in PostgreSQL, then added exact committed-stale
  replay proof against the leased source, invalidated approval, immutable event
  evidence/policy, run/version/actor and queued command. Nine focused PostgreSQL
  and approval-unit cases pass (15.79s), including queue-ID, approval-digest and
  source-actor tampering. Evidence: `task17-review/g01-red.log`, `g01-green.log`
  and `g01-matrix.log` under `.llm-output/runs/`. Recheck remains pending.
- A17-G02: failed lease renewal does not stop the active Planner, and settlement
  lacks a current command lease-owner/generation fence. Reclaim may terminally
  fail its command while the original provider later commits a successful plan.
  A bounded Terra-low writer owns worker/planning/command-boundary repairs and
  dedicated two-worker PostgreSQL coverage. Command/run lock order, cancellation,
  measured usage and recovery-required delivery semantics must remain correct.
  Implementation and verification are in progress; no repair is yet accepted.

Original findings and owned paths are retained in
`.llm-output/runs/task17-review/correctness-findings.md`. Once both repairs and
affected checks are integrated, request a finding-specific recheck of G01/G02
from the same Sol gate. A17-C01/C02 remain closed; do not start another broad
integrated review absent a material design/scope change. Tasks18-29 remain open.

### G01/G02 repair development evidence

The installed personal AGENTS and provider skills were reread after another
framework update. They now allow direct small fixes, proportionate review and
scoped input evidence. This repair still warrants the existing G01/G02-specific
correctness recheck because it changes lease ownership and crash recovery. No
new broad review or per-fixture review has been started.

G02 now fences provider admission and success/failure settlement against the
current PostgreSQL lease owner, expiry and delivery generation. Renewal loss
cancels the handler; recovery-required delivery remains leased instead of
terminally failing the original admitted execution. Real two-worker cases cover
late success and invalid output, including reuse of the same worker ID. A
context-selection reclaim case proves the old delivery cannot dispatch a model.
Late measured usage is retained in a PostgreSQL-indexed immutable artifact and
causal observation event without changing the execution, command or run state.
Eight lease integration cases pass (9.28s). Focused RED evidence reproduced both
the stale-worker error path and missing late usage before their repairs.

An affected development run passed 92 cases and exposed ten HTTP fixture count
assertions: fixtures now create a real initial planning lease, but assertions
counted every command as a new approval. They now select approve_plan commands;
all ten cases pass (14.76s). These are direct fixture checks, not acceptance.
Logs are under `.llm-output/runs/task17-review/` (`gate-repairs-development.log`,
`gate-fixture-green.log`, `combined-primary-green.log`, `late-usage-red.log`,
`late-usage-green.log`). The bounded worker cancellation refinement remains in
progress. After integration, run the affected suite and recheck G01/G02 by ID.
Task17 remains unaccepted; Tasks18-29 remain open. Remote branch now preserves
ledger checkpoint `0adae72`; current source repairs are still local uncommitted
work. No merge, live acceptance or bootstrap runtime change occurred.

### Integrated G01/G02 verification

The bounded worker repair now keeps the UoW inside a retained processing task.
After lease loss, tick returns within the cancellation-start bound even when a
handler suppresses cancellation. The processing task owns rollback/session close
until that handler finishes; `Worker.drain()` awaits retained cleanup and late
exceptions are observed. PostgreSQL assertions await actual drain before checking
the late-usage receipt and unchanged recovery state.

At HEAD `c8bc57ef0c493687f21151bf9629891d2ce6726f` plus the integrated repair,
the affected suite passed **1629 tests, 2 host-link skips, 6 dependency warnings
(291.47s; 294.86s command duration)**. Full source/test Ruff, formatting (258
files) and strict mypy passed. Relevant tracked/untracked source, test and project
input hashes were identical before/after; no exhaustive scratch/ignored inventory
was needed. Commands and evidence are in
`.llm-output/runs/task17-integration/gate-repairs/`. All pytest selections exclude
`live_provider` and `live_github`. The previous goal turn made concrete progress
through late-usage and fixture repairs; this turn integrated the bounded worker
repair and verified the affected batch.

Saved repair diff SHA256:
`ae94db6b2a8a61a89a8dbf56f631cbb07b3d2e4781f2cf8bf2ccc8cd4d136e3b`,
including the new lease integration file. The same Sol correctness agent has
received one finding-specific G01/G02 recheck request (repair round 1), with the
original findings and this evidence. A17-C01/C02 remain closed. No new integrated
review was launched. This is a tested repair checkpoint, **not Task17 acceptance**;
collect the concrete recheck disposition before activating Task18. Tasks18-29
remain open, and future PR merges still require fresh human authorization.
