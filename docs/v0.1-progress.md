# Forge v0.1 implementation ledger

## Active continuation — 6 September 2026

Goal: complete all 29 tasks and deterministic acceptance criteria in
`docs/superpowers/plans/2026-08-21-forge-v0-1.md`. No task is complete merely
because source exists or an old checkbox says so.

The user subsequently directed work to the existing `C:\dev\parallel-forge`
checkout on `forge/v0-1`, overriding the initial fresh-worktree instruction.
This exception applies to development assistants in this continuation, not to
Forge runtime agent confinement. Future PR merges still need fresh explicit
authorization. Verified checkpoints may be pushed without force.

Starting HEAD and fetched remote: `072b5416f49d08aa6cabef10c8c2eda972c1f93b`.
The checkout was clean. Python 3.14.5 and Node 24.20.0 are available;
`uv sync --frozen --extra dev --python 3.14` installed the locked environment.
The preserved bootstrap runtime remains outside this checkout.

Before the location correction, the repository setup script successfully
created `codex/complete-v01` with an ACTIVE isolated database and checkpoints
`manifest.created`, `worktree.created`, `database.active`, `environment.staged`,
and `setup.complete`. That unused worktree and its branch/resources are retained;
no teardown was requested. The control database remains distinct from it.

## Baseline evidence

Historical recovery evidence: 1,530 passed, 57 skipped, 92 failures (88 state
engine, 3 run service, 1 schema). This is not current acceptance evidence.

Fresh verification at the starting HEAD, with empty tracked diff
(`git hash-object`: `e69de29bb2d1d6434b8b29ae775ad8c2e48c5391`):

| Check | Result | Evidence |
| --- | --- | --- |
| `python -m pytest apps/orchestrator/tests/application/test_state_engine.py -q` | 478 passed, 88 failed | `.llm-output/baseline-initial-worktree/01-state-engine.log` |
| `python -m pytest apps/orchestrator/tests/application/test_run_services.py -q` | 16 passed, 3 failed | `.llm-output/baseline-initial-worktree/02-run-services.log` |
| Schema suite | Initial invocation interrupted; full suite reproduces the schema failure | `.llm-output/baseline-canonical/full-deterministic.log` |
| Full deterministic suite | 1,529 passed, 57 skipped, 93 failed in 333.11 seconds | `.llm-output/baseline-canonical/full-deterministic.log` |
| Ruff | Passed | `.llm-output/baseline-canonical/ruff.log` |
| Strict mypy | Passed, 136 source files | `.llm-output/baseline-canonical/mypy.log` |

The first two checks ran in the identical-HEAD managed worktree before the
location correction. Logs were copied intact to the active checkout. Initial
failures show approval-state fixtures missing gate/digest metadata and ordinary
transition calls attempting evidence-bound approval transitions. Contract
diagnosis supports updating those fixtures without relaxing approval guards.
The fresh full run additionally failed
`test_prepared_live_context_releases_all_retained_handles[exception]`; the
Windows handle-closure assertion is under separate investigation. The 57 skips
are recorded individually in the full log and include POSIX-only behavior,
unavailable symlink privileges, and Bash wrapper coverage on this Windows host.
They remain cross-platform verification requirements, not waived coverage.

## Task status

| Tasks | Current evidence-backed status |
| --- | --- |
| 1–13 | Substantial existing implementation; baseline repair, regression verification, and review evidence still required |
| 14 | `services/tools.py` explicitly leaves commit, named check, validation and review evidence tools unavailable; pre-worktree Planner reads need an authorization path. Planned audit test module is absent. |
| 15–16 | Agent contracts, prompts, fake gateway, ADK runtime/gateway exist. Planned `tests/agents`, context-security tests and opt-in live smoke test are absent. Implementation is not accepted without recovered tests/reviews. |
| 17 | Planning service/handler exist. Plan approval/revision handlers and workflow tests are absent. `worker/main.py` defaults to an empty handler registry, so production planning dispatch is not wired. |
| 18 | Delivery, validation/review orchestration and remediation workflow remain to implement. |
| 19 | Dashboard projections, artifact API, resumable SSE and OpenAPI export remain to implement. |
| 20–22 | Web workspace, authentication shell, project/policy controls and run cockpit remain to implement. |
| 23–25 | GitHub read adapter and deterministic publication/monitoring/merge controllers remain to implement. |
| 26 | Evidence, approval, audit, usage and security dashboard views remain to implement. |
| 27 | Versioned deterministic evaluation harness remains to implement. |
| 28 | Foundational recovery/lifecycle code exists; full workflow reconciliation and security hardening remain to implement. |
| 29 | Process/UI acceptance, cross-platform CI, development launcher and final operator documentation remain to implement. |

## Gates and next action

Use the installed multi-provider framework: Gemini first for routine changes,
confirmed quota fallback only; independent Opus 5 review; separate Sol-high
correctness gate for high-risk changes; fresh verifier command evidence. Keep
provider contracts and logs under `.llm-output/` and publish concise evidence
summaries here without credentials or raw provider output.

Current slice: Gemini completed approval fixture repairs and two focused
intervention guards: reject resolution that discards its own suspension context,
and require renewed monitoring/approval rather than entering MERGING after an
intervention discarded merge-gate evidence. Contract:
`.llm-output/baseline-repair.json`. Provider run:
`.llm-output/agent-framework/05bbb890-7f65-4f1d-b195-457e919487be`.
The provider reports 606 application tests and 19 schema tests passing; fresh
independent verification is underway. Saved four-file diff SHA-256:
`7ec9e72bd5d01f9efd082dd5c1c4c4f9779f3d991c39264270e07ac4f320f8b2`.
Per-file identities are in `.llm-output/approval-repair-candidate.json`.

Opus 5 review attempt returned provider error 429, session limit (reset reported
as 21:00 Europe/London), with no review performed. Evidence:
`.llm-output/agent-framework/1dc75842-a13d-48fe-82c5-dc1648691cdb`.
The framework-authorized fresh Astra-low review fallback found no material
findings and verified all candidate identities. The separate Sol-high gate
passed and independently ran 606 application tests, 19 PostgreSQL schema tests
and Ruff. Notes: `.llm-output/approval-fallback-review.md` and
`.llm-output/approval-correctness.md`. The fresh verifier recorded 625 focused
tests and 851 affected application/domain/persistence tests passing, with one
platform skip; Ruff and relevant mypy passed. Automatic file identities stayed
unchanged. Evidence: `.llm-output/approval-verification/manifest.json`.
These are bounded repair gates, not final whole-branch acceptance.

Windows path diagnosis passed the focused cases and full path suite (132 passed,
22 skipped), while instrumentation demonstrated raw HANDLE integer reuse. This
does not prove the original failure was harmless. Gemini is implementing a
bounded lifetime-aware test-instrumentation repair under
`.llm-output/path-test-repair.json`; no path runtime changes are authorized in
that slice. Its regression must still detect missing/double closes.

The first path-test repair passed a quality review but failed the separate
Sol-high gate: it recorded a successful release before the native close
succeeded. `.llm-output/path-correctness.md` documents a live-handle leak that
passed the matcher. A Terra-low complex implementer is repairing attempt versus
success tracking and the Windows native-close return check under
`.llm-output/path-correctness-repair-contract.md`. This slice is not accepted.

The next full run at integrated diff
`6fcd1bd69fbc947c2e45edda728f2e3ec69fbbef9378a939c834dfa020fd4ad9`
recorded 1,636 passed, 57 skipped, 10 failed; Ruff/mypy passed and file identities
were unchanged. Evidence: `.llm-output/baseline-final-verification/manifest.json`.
All documented approval/schema failures were resolved; the ten newly exposed
failures were six developer-lifecycle and four manifest tests under a longer
temporary root. A controlled Windows probe demonstrated native temporary-file
creation failing at 274 characters while an equivalent shorter temporary path
succeeded. Manifest staging unnecessarily repeats the entire final filename.
Gemini is repairing only the staging filename, retaining random UUID entropy,
full final identity and atomic/security rules, under
`.llm-output/manifest-temp-repair.json`. Do not dismiss these as passing merely
because a shorter test root avoids the bug.

The approval/intervention slice is accepted as a bounded verified checkpoint:
`365deaa717a459d761fffc0ed53a41211f9114c0`, pushed to `origin/forge/v0-1`.
Its commit contains this ledger.

The path-close repair is now accepted as a bounded slice. Terra corrected the
native `CloseHandle` false-result handling and the test recorder separates
attempts from successful releases. Deterministic missing/failed/double-close
regressions retain actual cleanup evidence. Fresh Astra review and the separate
Sol-high gate found no remaining path-close defects. Evidence:
`.llm-output/windows-review-fresh.md`,
`.llm-output/windows-correctness-fresh.md`, and
`.llm-output/path-complex-repair/`. The Sol report fails the combined candidate
for separate manifest findings; it explicitly passes the handle-close repair.

Fresh full verification at `365deaa` plus the recorded candidate passed 1,649
tests with 57 platform skips in 327.24 seconds. Ruff and strict mypy passed;
automatic before/after HEAD, tracked diff and file identities matched.
Evidence: `.llm-output/windows-full-verification/manifest.json` (diff SHA-256
`2ddf214dcc4010e237c704bed40844c50cb0118948b92e080bab3d37fef40381`).
This resolves the observed baseline failures, but does not override independent
review findings or establish final acceptance.

Manifest publication remains unaccepted. Independent Astra review reproduced a
test-fixture defect: a 150-character pytest base makes the intended supported
final path 265 characters. The separate Sol-high gate also proved a preexisting
concurrent-create race: two callers passed absence checks, both reported success,
and `os.replace` overwrote the first manifest with a different immutable identity.
The exact deterministic probe is in `.llm-output/windows-correctness-fresh.md`.
Terra is repairing exclusive publication and the fixture under
`.llm-output/manifest-publication-complex-contract.md`. The routine fixture-only
follow-up contract was not dispatched because this requires a concurrency repair.

A fresh read-only Tasks 1–13 source/test map is saved in
`.llm-output/early-task-acceptance-map.md`. Each has corresponding implementation
and focused coverage; historical Task 13 E2b incompleteness is superseded by
current lifecycle orchestration. Concrete production worker adapter registration
remains workflow integration work. Windows platform skips remain required
cross-platform evidence, not waived behavior.

Next: finish manifest repairs, fresh independent review and Sol-high gate, and
current verification. Then proceed to the saved Task 14 Planner-read contract,
followed by durable commit/check tools and evidence readers. No numbered task
or final acceptance gate has yet been marked complete.
